name: Instagram Daily Card News (Closing)

# 평일 17:30 KST 자동 실행 — 마감 시황 카드뉴스. daily-market-sync가 그 전에 끝나 있어야 최신 데이터로 카드가 만들어진다.
on:
  schedule:
    - cron: "30 8 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: instagram-card-post-closing
  cancel-in-progress: true

env:
  TZ: Asia/Seoul
  PROMO_CARD_THEME: light # 사이트 기본 테마와 통일 (다크로 바꾸려면 'dark')
  SLOT: closing
  PROMO_FORCE_DATE: "2026-07-16" # TEMP TEST: 7/16 마감데이터 강제 지정, 검증 후 제거 예정

jobs:
  render-and-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # 주말/중복 발행/휴장일(데이터 미준비) 세 가지를 모두 걸러서 should_publish를 결정한다.
      # - 주말: cron이 평일에만 돌긴 하지만 workflow_dispatch 수동 실행 대비 방어.
      # - 중복 발행: 오늘 이미 발행했으면 재실행(재시도, 수동 dispatch 중복 등) 스킵.
      # - 휴장일: 공휴일에는 daily-market-sync가 돌아도 지수/총평(analysis)이 비어있거나
      #   더미(0%) 데이터만 들어올 수 있어서, check-promo-data-ready.mjs로 실데이터 여부를 확인한다.
      - name: Check if should publish (weekend / dedup / data-ready)
        id: dupcheck
        run: |
          set -euo pipefail
          TODAY=$(TZ=Asia/Seoul date '+%Y-%m-%d')
          DOW=$(TZ=Asia/Seoul date '+%u')  # 1=Mon ... 6=Sat, 7=Sun

          if [ -z "${PROMO_FORCE_DATE:-}" ] && [ "$DOW" -ge 6 ]; then
            echo "::notice::주말(요일=${DOW}) — 발행 스킵"
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          STAMP_FILE="generated/${SLOT}/last-published-date.txt"
          if [ -z "${PROMO_FORCE_DATE:-}" ] && [ -f "$STAMP_FILE" ] && [ "$(cat "$STAMP_FILE")" = "$TODAY" ]; then
            echo "::notice::오늘(${TODAY}) 이미 ${SLOT} 발행 완료 — 중복 실행 스킵"
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if ! node scripts/check-promo-data-ready.mjs --slot="${SLOT}"; then
            echo "::notice::오늘(${TODAY}) ${SLOT} 데이터 미준비(휴장일 등) — 발행 스킵"
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "should_publish=true" >> "$GITHUB_OUTPUT"

      - name: Install dependencies
        if: steps.dupcheck.outputs.should_publish == 'true'
        run: npm ci

      # 한글(특히 영문+한글이 붙어있는 굵은 글씨) 렌더링 깨짐 방지용 Noto Sans CJK 설치.
      # ubuntu-latest 러너 기본 폰트만으로는 일부 글자가 대체 폰트로 넘어가며 네모(tofu)로 깨지는 현상이 있었음.
      - name: Install Korean + emoji fonts
        if: steps.dupcheck.outputs.should_publish == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk fonts-noto-cjk-extra fonts-noto-color-emoji
          fc-cache -f

      - name: Render 5 card images
        if: steps.dupcheck.outputs.should_publish == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node scripts/instagram-card-post.mjs --slot=closing --render

      - name: Commit and push generated images
        if: steps.dupcheck.outputs.should_publish == 'true'
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add generated/
          if git diff --staged --quiet; then
            echo "::notice::No image change, skipping commit"
            exit 0
          fi
          STAMP=$(TZ=Asia/Seoul date '+%Y-%m-%d %H:%M %Z')
          git commit -m "chore(promo): closing instagram cards ${STAMP}"
          git push origin HEAD:${GITHUB_REF_NAME}

      # 방금 커밋한 이미지가 raw.githubusercontent.com CDN에 반영될 시간 대기
      - name: Wait for CDN
        if: steps.dupcheck.outputs.should_publish == 'true'
        run: sleep 20

      - name: Publish carousel via Meta Graph API
        if: false # TEMP TEST: disabled until rendered PNG is verified
        run: node scripts/instagram-card-post.mjs --slot=closing --publish
        env:
          META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
          INSTAGRAM_ACCOUNT_ID: ${{ secrets.INSTAGRAM_ACCOUNT_ID }}

      - name: Record published date (dedup guard)
        if: false # TEMP TEST: disabled until rendered PNG is verified
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          mkdir -p "generated/${SLOT}"
          TZ=Asia/Seoul date '+%Y-%m-%d' > "generated/${SLOT}/last-published-date.txt"
          git add "generated/${SLOT}/last-published-date.txt"
          if git diff --staged --quiet; then
            exit 0
          fi
          git commit -m "chore(promo): mark ${SLOT} published $(TZ=Asia/Seoul date '+%Y-%m-%d')"
          git pull --rebase origin main
          git push
