# TOTALMONEY_STATUS.md — 프로젝트 현황 통합본

> D:\vibe\docs 안의 세션별 인수인계 md 18개(2026-06-19 ~ 2026-08-08)를 시간순으로 전부 읽고 통합·중복제거한 문서입니다.
> 이 파일 하나가 프로젝트의 "지금 상태"를 대표합니다. 개별 세션 로그 원본은 `/archive`에 보관되어 있습니다.
> **추가 갱신: 2026-09-26 저녁 (세션 11, 프로젝트 창)** — **AI 종목분석 "OpenAI returned non-JSON" 3연속 실패 수정**(여분 닫는 괄호 제거·AI 문법 교정 1회), 실패 시 내부 오류문구·빈 리포트 노출 금지·저장 안 함·무료 횟수 환원. **시세 라이선스 추가 조사**: KRX Data Marketplace 가격표 확보(주식 일별 연 99만원 등), 코스콤 FAQ상 개인도 실시간 계약 가능 → **코스콤 문의 접수(9/26, 시우)**.
> **추가 갱신: 2026-09-26 오후 (세션 10, 프로젝트 창)** — **Claude Opus 재채점(65→75점) 반영**: 시장 대비 위치 PER을 최근 4개 분기 기준으로(SK하이닉스 31.6→8.4배), 확률 조정폭 ±10%p·순환 근거 삭제·A안 근거에 20거래일 통계 문장 코드 부착, 손절폭 최소 ATR 1배, 역배열이면 추세 점수 +1 상한, 재료·업황 근거 규칙(자사주 재료 복귀), 면책·확률 표기 보강(유사투자자문 검토), B안 중복·종합의견 뜻 뒤틀림·현재가 오기 교정, **OpenAI 응답 JSON 자동 복구**. 커밋 `3f08c490`·`7257d5bc`·`17d6801f`·`efff10a2`·`8e581a8d`·`994fd893`·`07b26f54`.
> **추가 갱신: 2026-09-26 점심 (세션 9, 프로젝트 창)** — 종가시그널 두 카드 하단 맞춤·요약 수익률 색. **AI 종목분석 외부 리뷰 4종(Kimi·GPT·Gemini·Opus) 반영**: 보조지표(MACD·볼린저·거래량·ATR) 코드 계산, **시나리오 기준 확률 코드 계산 + ±20%p 제한**, 목표가 최소 거리 강제, **최근 4개 분기(TTM) PER**, 추세 사실 코드 확정, 종합의견 첫 문장 = 기준 계획+대안, **초보자용 한눈에 보기**(좋은 점·주의할 점·볼 가격 3개), '상승 확률' → '현재 판단', 채점 기준 한 줄. 커밋 `27375bcd`·`fe80325d`·`32886bfa`·`7e315ed1`·`6bd6f846`·`019bb0c2`·`61961c69`·`3874d5b3`·`e7a9a88a`. 상세는 6장 맨 위.
> **추가 갱신: 2026-09-26 낮 (세션 8, 프로젝트 창)** — 국내주식 모바일 차트 빈칸 제거, **암호화폐 크립토 뉴스 폐지·오른쪽 코인 상세 확장**(공급량·링크 등), **AI 종목분석 4종 보강**(진입가 하나로 통일 · 실적 발표 이벤트 · 과거 유사 국면 통계 · **예측 자동 채점 + 성적표 달력**), 1년 수익률 기준을 52주 창으로, **종가시그널 "직전 선정 결과"를 왼쪽과 같은 카드 체계로**. 커밋 `d2380c03`·`f3d47157`·`34c029a6`·`fa0b443a`. 상세는 6장 맨 위.
> **추가 갱신: 2026-09-26 오전 (세션 7, 프로젝트 창)** — 히트맵 IT 쏠림 완화·라이트 모드, **읽는 페이지 배치 개편**(브리핑·마감시황·일정: 왼쪽 숫자판 고정+오른쪽 본문, 폭 1640px·배율 1.14 / 매매·종가시그널·AI종목분석 같은 폭), **국내·미국·암호화폐 페이지 넘겨도 선택 유지**, **상세 시세 CDN 10초 캐시**(국내 첫 상세 6.7→2.7초). 커밋 `72f3baac`~`a1479255`. 상세는 6장 맨 위.
> **추가 갱신: 2026-09-26 새벽 (세션 5·6)** — 홈 히트맵 지수 8개·모바일 맞춤(`eae95a25`·`29d98b81`) + 홈 리포트 줄 개편: **AI 종목분석 카드 삭제 → "일정" 카드(경제지표) 이동, 오늘의 특징주 3→8개**(`226f73f9`). 상세는 6장 맨 위.

> 통합 기준일: 2026-08-09 · **최종 갱신: 2026-09-25 저녁 (Cowork 세션 4)** — 🔴 **사이트 비공개 전환(회원 승인제)** + 넓은 화면 전면 정리 + **홈 섹터 히트맵**. ①`middleware.js`로 승인 회원만 출입(가입→대기→마이페이지에서 운영자 승인, 요금제 FREE/PRO/PREMIUM 바로 부여), 인스타 자동발행·텔레그램 공개채널 일시중지 ②국내주식(구 실시간시세)·미국주식 6탭+좌목록·우상세 분할, 전 페이지 전체 폭, 상단 메뉴 11개 펼침·페이지 제목 배너 제거 ③`assets/wide.css`(넓은 화면 규칙 모음)·`assets/tm-chart-style.js`(차트 공용 규칙: 격자 제거·콤마/$ 눈금·음수 눈금 제거·최근 구간만) ④차트 API 실패 구간 재시도(엔비디아 차트 빈 구간 원인) ⑤홈 첫 화면 = 섹터 히트맵(코스피·코스닥·미국·암호화폐, WICS/GICS 섹터) + 지수 4개, 실시간 AI 분석 칸 삭제 ⑥**매일 AI 자동 분석(시드) 폐기** — 로컬 예약작업 `totalmoney-ai-feed-seed`는 시우가 앱에서 직접 삭제 필요. 커밋 `a924476a`~`e7ab2210`. 이전 갱신 2026-09-25 새벽 (Cowork 세션 3) — 🔴 **사업 방향 재편: "개인 도구 vs 공개 콘텐츠"**. 시세 라이선스 조사 결과 공식 무료 소스(KIS·KRX OPEN API·data.go.kr)는 전부 비상업·제3자 제공 금지 → 가격 숫자는 시우 개인 도구에서만 쓰고, 공개 쪽은 가격 없는 AI 분석 콘텐츠로 간다(유료화는 사업자등록 후 네이버 프리미엄콘텐츠 우선). 개인 대시보드 1단계 배포(운영자 전용 잠금·1분 수집·관심종목). 그 전: 구글 로그인 오픈, 상단 메뉴 재구성([시장▾]+아이콘 타일+PRO 3종, 홈버튼 제거), 종가시그널 결과표 정리, **전체 리뉴얼 착수**(전체 폭 프로토타입 `lab-realtime.html` + 국내 전 종목 데이터), 🔴 **KIS 약관 제5조③(시세 제3자 제공 금지) 이슈 발견**. 그 전(같은 날 밤): 사업자등록 2027 연기·소셜 로그인 선진행 결정, 카드뉴스 v3(마감·아침, 사이트 색 통일), KRX 휴장일 달력·점검봇 재정리, 종가시그널 모바일 목록·순위 배지·자기학습. 다음 세션 할 일은 3장 맨 위 "다음 세션 체크리스트". 그 전: — 삼성 실적 의심 건 종결(공시와 일치)·로그인 버튼 초록 유지 확정·인스타 카드 로고 스텝 마크로 교체. 이전 갱신 2026-09-23 — **전 페이지 디자인 마감 + 로고 재교체 + 일정 페이지 재구성.** ①**`premium.css` 마감 레이어 신설**(모든 페이지 맨 마지막 로드, 지우면 원복) — 바탕 한 색(다크 #000/라이트 #fff)+카드 한 겹(#111113·테두리 #232326·라운드 16), 다크에서 본문 띠가 카드보다 밝던 명암 역전 해소, 3겹 중첩 카드·색 상단바·왼쪽 굵은 띠 제거, 모노 서체→Pretendard+tabular-nums, 다크 초록 버튼 글씨를 짙은색으로(ui-audit 대비 미달 41→7건, 남은 4건은 네이버 브랜드 버튼) ②**로고를 "스텝 마크"로 재교체**(기울기·폭 같은 평행사변형 2개, 판 제거, 워드마크 CSS 소문자 "totalmoney"+초록 AI) + 파비콘 전 세트 재생성(v8) ③**일정 페이지 "하루 = 카드 한 장"** 재구성(그날 일정분석을 카드 안에, 소제목 제거, 신규상장 앰버 강조+맥박 점, 모바일은 사이드 대신 "다음 핵심 지표"+가로 날짜 띠) ④실시간시세·미국주식·암호화폐·AI분석 **차트를 매매시그널 스타일로 통일**(실시간시세는 캔들/거래량 2패널→1패널), 순위표 줄무늬 제거, 상세를 열면 아래 행이 한 칸씩 밀리던 버그 수정 ⑤버그: 종가시그널→AI분석 이동 시 종목명 대신 코드 표시 / 다크모드가 가끔 라이트로 강제 전환(site-shell 폴백 "light" 잔재) / 브리핑·마감시황 등락률 <em>이 전부 초록 / 마이페이지 로그아웃 시 계정 카드 노출. 커밋 `6c250492`·`9555af28`·`798792e6`·`071ffcd4`·`6676d414`·`b2e73e3f`·`2b4d72e3`·`f7dfa0e6`, 전부 배포·라이브 확인.

> 이전 갱신: 2026-09-22 (Cowork 세션) — **종가시그널 문구 정비(유사투자자문) + 인스타 성적 카드뉴스 + 브랜드 전면 교체.** ①금감원 제재 사례와 대조해 승부·적중 표현을 걷어내고 **필수 고지 3종**(원금 손실 가능성 / 개별 투자상담 불가 / 정식 금융투자업자 아님)을 넣었다 ②종가시그널 성적 카드뉴스를 토 11:00 주 1회 자동 발행으로 신설(이 덱만 AI를 안 부른다 — 발행당 토큰 0) ③**기본 테마를 다크로** ④모바일 헤더 위 회색 띠의 정체는 **안 지운 옛 네이비 `theme-color`**였고 세 곳(HTML 20개·theme.js·webmanifest)을 검정으로 통일 ⑤골드 `#ffd84d` 11곳 전멸 — 마이페이지 노란 밑줄 재발의 뿌리였다 ⑥**로고를 M 모노그램으로 재디자인**하고 파비콘 전 세트·워드마크 한 줄까지 교체(시안 3종 중 시우 선택). 추적 파이프라인 첫 실전도 확인 — 9/21 선정 5종목이 익일 시가 +1.96%(4/5 수익)·종가 +0.27%로 기록됐다. 커밋 `f68edad3`·`df6627b6`·`0769daf8`·`23d07555`, 전부 배포·라이브 확인 완료.

> 이전 갱신: 2026-09-21 (Cowork 세션) — 종가시그널 v2 전면 개편(기법 합의 채점·상위 5종목·성과 추적·달력).

> 이전 갱신: 2026-09-18 (2차, Cowork 세션) — AI 종목분석 수급 방향 오류 수정 + 세션 작업경로 정정.
>
> 이전 갱신: 2026-09-18 (1차) — 재벌 그룹주 로고 통일 + '종가베팅'→'종가시그널' 개명 + DART 재료 팩터 + AI 어투 정중체 전환 세션. ①**재벌 그룹 계열사 로고를 그룹 대표 마크로 통일**(삼성·SK·HD·현대차·LG·카카오·LS — 계열사가 각자 자기 파비콘을 쓰고 있었다), 국내 manifest 484 → **503종목** ②**'종가베팅' → '종가시그널' 개명**(유사투자자문업 규정상 '베팅' 회피) — 문구 전 페이지 + URL `close-signal.html` + 301 리디렉션. **코드·인프라 이름은 일부러 그대로 뒀다**(pg_cron·ALLOWED_WORKFLOWS 연쇄 실패 방지 — 5장에 규칙 신설) ③**비어 있던 재료(D) 팩터 20점을 DART 당일 공시로 채움** — AI 판정 없이 공시 제목을 정규식으로 분류, 하드필터 통과 후보 전원에게 붙여 채점 분모를 균일하게 유지. 첫 실전 반영은 9/21(월) ④**AI 종목분석 어투를 정중체 '-습니다'로 전면 전환** + 독자 대화체 + 전문용어 괄호 풀이 — 실제 리포트 문장을 좋은 예/나쁜 예 쌍으로 프롬프트에 앵커로 박은 것이 결정적이었다 ⑤헬스체크 인스타 감시를 현행 캐러셀 3종으로 교체(이미 없어진 슬롯을 보고 거짓 경보를 내고 있었다). ⚠️ **이 작업들은 Cowork가 아닌 일반 채팅 세션에서 패치 파일로 진행돼 STATUS.md 갱신이 누락됐고, 뒤이은 Cowork 세션에서 git 로그·Supabase를 역추적해 반영했다.**
>
> 이전 갱신: 2026-09-13 — 종목 아이콘 전면 개편 + PC 레이아웃·표 밀도 정리 세션(하루에 v15~v23). ①**홈·실시간시세 PC 표가 깨져 있던 것 수정** — 아이콘을 넣으면서 종목명이 세로로 깨지고(홈) 표가 카드 안에서 좌우로 떠 있었다(실시간시세). 원인 3종은 6장 ②**국내 종목 로고 346 → 484종목** — FMP 로고는 13%만 쓸 만해 삼성전자·NAVER·카카오·현대차가 전부 이니셜 배지였다. **회사 공식 홈페이지 파비콘**(구글 파비콘 서비스 경유, 우리 저장소에 저장)으로 메웠다 ③**미국·해외 로고 107종목 자체 호스팅** — `home.js`가 financialmodelingprep.com을 화면에서 직접 링크하던 것(핫링크) 제거, 미국주식 페이지에는 로고 신규 추가 ④**이니셜 배지 글자 규칙**(한글 2자·영문 4자) — 한 글자라 삼성·삼양이 같고 KT&G가 KT로 잘리던 것 해결. 삼성 계열은 시우 판단으로 파란 S 유지 ⑤🔴 **manifest를 `fetch(..., {cache:'force-cache'})`로 받고 있어 배포해도 화면이 안 바뀌던 버그** — 로고 127종목 추가가 통째로 안 보였다 ⑥**매매시그널 계열 메뉴 스크롤 튐** — 선택된 줄만 25px 높았던 게 원인 ⑦**표 밀도** — PC 한 화면 14줄 → 17줄, 탭바 61→49px(실시간시세·마감시황 종목순위·미국주식 공통). **토스·트레이딩뷰 로고 CDN은 검토 후 불채택**(사유는 6장). ⚠️ **v21·v22·v23 배포 스크립트가 `D:\vibe\_incoming-v21~23\`에 대기 중 — 3장 최상단 참고.**
>
> 이전 갱신: 2026-09-08 — 마감시황 무음 실패 사고 복구 + 인스타 중복 발행 정리 세션. ①16:15 마감시황 예약작업이 **실행은 됐는데 웹서치 도중 커밋 없이 조용히 끝나** 17시가 넘도록 미발행 — 잡아야 할 `daily-report-watchdog.yml`은 이미 삭제돼 있었고 health-check는 "분석 자체가 없음"을 안 봤다. 연쇄로 17:30 인스타 마감 카드까지 스킵 ②실데이터로 직접 복구 발행(코스피 6,954.52 −0.58%, 특징주 10종목, 텔레그램 success 확인) ③**재발방지 3종** — health-check에 `checkDailyClosingAnalysis` 추가 / 재시도 예약작업 `totalmoney-daily-closing-report-retry`(평일 16:50, 데드라인 17:20) 신설 / 원 예약작업에 검색 8회 상한·16:45 착수 데드라인·"커밋 없이 끝내지 말 것" 규칙 ④**인스타 릴스·캐러셀이 하루 2건씩 중복 발행되고 있던 것을 발견** — 9/7 전환 때 워크플로 `schedule:`만 끄고 **Supabase pg_cron 호출 대상은 구 릴스로 남겨둔 게 원인**, 스탬프 파일도 서로 달라 dedup이 안 됐다 ⑤pg_cron 3개를 캐러셀로 이관하고 **토요일 랭킹 job 신설**(그동안 GitHub cron으로 한 번도 안 돌았다), Edge Function allowlist v8 배포, `event_name=='schedule'`일 때만 걸려 있던 **가드 구멍**을 자동 트리거 전체로 확대. **발행 시각표는 5장 참고**(아침 08:30 · 마감 17:30 · 토 랭킹 10:30, 전부 pg_cron 정시). 6장 최상단 참고
>
> 이전 갱신: 2026-09-07 — 인스타그램 카드뉴스 v2 전면 재편 세션. ①**정지 릴스 → 캐러셀 전환**(시청유지율로 판정하는 릴스에 정지 이미지를 올려 도달이 안 나오던 구조적 문제) ②**콘텐츠 축을 숫자 나열 → "AI 한 줄 판단"으로 이동** — 이미 매일 생성되던 7,000자 분석이 카드에 한 글자도 안 쓰이고 있었다 ③**덱 자동 생성 파이프라인 신설**(`promo-build-deck*.mjs`) — 숫자는 코드가 계산하고 문장만 AI가 쓴다, Claude→OpenAI→코드 폴백 3단 ④**훅 유형을 데이터가 판정**(마감 A/D/E/C · 아침 EWY/SPLIT/SAFE/SYNC · 랭킹 ABSENT/PRESENT/GAP)하고 그 답이 반드시 2번 카드에 오게 설계 ⑤**아이폰 실기기 기준 타이포 재설계**(본문 24px→34~36px, 배율 0.36 역산) + 인스타 페이지 배지와 겹치던 우상단 비움 ⑥마감·아침·주말 랭킹 **3개 워크플로 모두 캐러셀 자동 발행으로 교체**(릴스는 수동 롤백용으로 보존) ⑦AI 종목분석 '리포트 열기' 스크롤 버그 수정. **⚠️ Anthropic API 크레딧 소진 확인 — 현재 OpenAI 폴백으로 가동 중**(3장 참고). 6장 최상단 참고
>
> 이전 갱신: 2026-09-04 — 회원가입·로그인 전면 강화 + 증시 일정 정리 세션. ①**소셜 로그인 3종**(네이버 연동 완료·가동 중 / 구글·카카오는 콘솔 설정 대기, 스위치로 꺼둠) — 네이버는 Supabase 미지원 provider라 **Supabase Edge Function**으로 구현(Vercel Hobby 함수가 12/12로 꽉 차 있어 Vercel에 못 만듦) ②비밀번호 8자+영문숫자, **비밀번호 찾기/재설정 신설**(기존엔 복구 수단이 아예 없어 비번 분실 = 계정 영구 잠김) ③약관·개인정보·만14세 필수 동의 + 마케팅 선택 동의 ④**핵심지표 판정을 `assets/key-indicators.js` 공용 모듈로 분리** — 데이터 408건이 전부 `impact:"high"`라 텔레그램 필터가 사실상 무력했던 문제 해결(하루 알림 16건 → 7건) ⑤일정 분석 카드 레이아웃 정상화. 6장 최상단 참고
>
> 이전 갱신: 2026-09-03 (오후) — 매매시그널 강화 세션. ①검색 결과에 **"왜 걸렸는지" 근거 칩** 신설(조건에 통과한 실제 값을 종목마다 표시) ②**재무비율 조건검색 신설**(부채비율·영업이익률·순이익률·ROE·유동비율) — DART 사업보고서에서 2,858종목을 수집하는 주 1회 배치 포함 ③**미지원 조건을 조용히 버리던 구멍 차단**(즉시검색·감시저장 양쪽) ④검증 중 발견한 데이터 버그 4건 수정(PER/PBR "0=값없음" 오염 · 갭 판정 기준 불일치 · 적자기업 음수 PER · HTS "십억" 단위 오독). 6장 최상단 참고
>
> ⚠️ **날짜 라벨 정정**: 아래 "2026-09-04 (새벽)"으로 적힌 세션은 실제로는 **2026-09-03 오전~13:32 KST**에 이뤄진 작업이다(마지막 커밋 `bfb6390`이 09-03 13:32). 기록 보존을 위해 본문 라벨은 그대로 두되, 시간순으로 읽을 때 혼동하지 말 것.
>
> 이전 갱신: 2026-09-04 (새벽 — 실제로는 09-03 오전) — 유료 핵심 기능 퀄리티 향상 세션 2차. **로드맵 D·E·F를 한 세션에 처리**: ①DART 실적 추이 카드 신설(+선행과제 corp_code 전체 매핑 3,988건 — 과거 "타임아웃" 실패의 진짜 원인이 Node fetch였음을 규명하고 curl로 해결) ②리포트 저장·사후 추적 + 원문 보관(AI 호출 없이 다시 열기) ③백테스트용 일자별 스냅샷 축적 시작(`data-archive` 브랜치) ④"중앙값"→"종목 중앙값" 표기 명확화 ⑤일목균형표 제거 ⑥재료 분석 라이브 검증 후 2건 보강. 6장 최상단 참고
>
> 이전 갱신: 2026-09-03 (밤) — 유료 핵심 기능 퀄리티 향상 세션 1차. **AI 종목분석에 "시장 대비 위치" 카드 신설**(전종목 지표 캐시로 백분위를 코드가 직접 집계), 출처 배지 전면 제거, 도표 4종 추가, **재료 분석에 최신 뉴스가 안 들어가던 근본 원인 수정**. 같은 날 낮에는 사이트 감사 지적 3건 해결(증시 일정 리팩터 · 시세 4종 공통 요약 스트립 · Premium 상품 정의)과 **D:\vibe 로컬 저장소 GitHub 재동기화**(1,514 커밋 뒤처져 있었음) — 6장 참고

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | TotalMoney / TotalMoney AI |
| 성격 | 한국주식·미국주식·암호화폐 통합 대시보드 + AI 종목분석 + 매매시그널 스크리너 + 인스타그램/텔레그램 자동 콘텐츠 발행 |
| URL | https://www.totalmoney.kr (https://totalmoney.kr 별칭 포함) |
| 저장소 | GitHub `mandoo411/vibe` (public), main 브랜치 |
| 로컬 폴더 | `D:\vibe` |
| 호스팅 | Vercel, 프로젝트명 `bori-cal`, `projectId: prj_VnAs98x3Puq8qzzdylHyrGyYZHFJ`, `teamId: team_QubZdjmTuImLo6ClM59KHPrt`. main push 시 자동 배포 |
| 운영자 | 시우 (aaron20177@gmail.com), GitHub 계정 `mandoo411`. 40대, 야간 근무 + 주식/크립토 트레이딩, 비개발자. **AI(Claude/Cowork)의 힘으로 자동화 수익 모델(구독형 SaaS)을 만드는 것이 목표.** 미션을 주면 알아서 끝까지 실행해주는 방식을 선호하되 결과물 퀄리티는 까다롭게 검수함 |
| 공식 채널 | 텔레그램 `@totalmoney_ai`(t.me/totalmoney_ai), 인스타그램 `@totalmoney_ai`. X(트위터)·Threads는 초기 기획서에 있었으나 **계정 미생성으로 미가동** |

### 기술 스택

- **프론트엔드**: 정적 HTML/CSS/JS (프레임워크 없음), 페이지별 파일 직접 구성
- **차트**: TradingView Lightweight Charts — 페이지마다 독립 구현체 3종(`assets/lw-candle-chart.js`, `stock-analysis.js` 자체구현, `realtime-board.js`), 공유되지 않으므로 수정 시 항상 3곳 확인 필요
- **백엔드**: Vercel 서버리스 함수(`api/*.js`). **Vercel Hobby 플랜은 배포당 함수 12개 제한**이 있어 과거 이 한도를 넘겨 배포가 조용히 실패한 사고 이력 있음(아래 5장 참고) — 새 API 라우트 추가 시 항상 유의
- **데이터 서빙**: `api/repo-data.js`가 GitHub Contents API로 `data/*.json`을 직접 읽어 서빙(60초 캐시) → 데이터 파일만 바뀐 커밋은 Vercel 풀빌드 없이 ~1분 내 사이트에 반영됨. `vercel.json`의 `ignoreCommand`가 `data/`만 바뀐 커밋의 풀빌드를 의도적으로 스킵(버그 아님)
- **인증/구독**: Supabase (Auth + Postgres), `assets/auth.js`/`assets/auth-config.js`(클라이언트), `lib/supabase-server.js`(서버, service_role)
- **결제**: 포트원(PortOne) — **현재 결제 비활성 상태(테스트 채널만 구축됨).** 유사투자자문업 교육 수료 → 사업자등록 완료 후 결제서비스 정식 오픈 예정(2026-08-09 사용자 확인). `assets/auth-config.js`의 `SETUP_PENDING` 플래그로 온/오프 제어
- **시세 데이터**: KIS(한국투자증권) Open API(국내·미국), CoinMarketCap Pro API(암호화폐 시세/지표), Binance 공개 API(암호화폐 캔들), Yahoo Finance(환율 등 일부 보조 지표 — 단 `chartPreviousClose` 필드는 신뢰 불가, 5장 참고)
- **AI**: Anthropic Claude(주력) → OpenAI GPT-4o-mini(폴백, 크레딧 부족 시) 이중화. 이미지 생성은 Gemini → OpenAI → SVG/CSS 순 폴백. **⚠️ 2026-09-11 현재 Anthropic 잔액이 소진돼 실질적으로 OpenAI가 콘텐츠를 만들고 있다**(운영자 확인) — 폴백 경로를 새로 만들 때는 Anthropic 전용 기능(web_search 등)에 의존하지 말 것
- **전역 타이포 보정 레이어**: `typography.css`(2026-09-11 신설) — 모든 페이지에서 **가장 마지막에 로드**되며 색 대비·최소 글자크기·줄간격만 덮어쓴다. 색·크기가 11개 CSS에 하드코딩돼 흩어져 있어 개별 파일을 고치면 다른 페이지에서 회귀가 나기 때문에 분리했다. 값은 감이 아니라 **실제 렌더링 픽셀에서 측정한 WCAG 대비**로 정했다(6장 2026-09-09~11 참고). 새 UI를 만들 때 이 파일이 덮어쓸 수 있음을 유의
- **발행**: Meta Graph API(인스타그램 릴스+스토리), 텔레그램 Bot API
- **자동화**: GitHub Actions(저장소 내장 cron) + Cowork(Claude Desktop) 예약작업 두 축으로 분업 운영

---

## 2. 완료된 기능 / 페이지별 현황

| 영역 | 파일 | 현황 |
|---|---|---|
| 홈 | `index.html`, `home.js`, `site-shell.js` | 운영 중. 미니 지수 티커, 실시간시세/미국시장/암호화폐 요약 카드, 다크·라이트 모드, 데스크톱 GNB + 모바일 하단바(5탭: 홈/시세/매매시그널/AI분석/전체보기) 완료. **(2026-09-02) 첫 화면 전면 재설계** — ①하드코딩된 가짜 지표 4종(분석 종목수 2,847개 / 오늘 분석 1,203건 / **평균 정확도 73.2%** / 실시간 업데이트 30초, 모바일 히어로에만 노출돼 데스크톱 감사에서 안 보이던 것) 완전 제거. 근거 없는 수치이고 "평균 정확도"는 투자정보 서비스에서 표기하면 안 되는 항목이라 원칙 위반이었음 ②공허한 카피("실시간 시장을 AI로 분석하세요") 제거하고 히어로를 실데이터 블록으로 교체 — 날짜 + 장 상태 배지(장전/장중/장마감, KST 코드 판단) + **대표 지수(코스피)를 42px 모노 수치로 크게** + 코스닥·원달러·나스닥선물 보조 칩 + 오늘 리포트 한 줄(본문 15px) ③**"오늘의 AI 리포트" 3카드 스트립 신설**(장전 브리핑 / 마감시황 / AI 종목분석) — 무료 시세 사이트와 구분되는 유료 가치를 첫 화면에서 증명 ④사이드바 'AI 장 브리핑' 카드는 새 브리핑 카드와 같은 keyIssues라 중복이어서 제거하고 **'오늘의 특징주'**(선정 이유 포함)로 교체 ⑤세 영역이 같은 문장을 반복하지 않게 분담(히어로=핵심 한 줄, 브리핑 카드=핵심 이슈, 마감시황 카드=향후 전략). **모바일**은 리포트 3장을 옆으로 넘기는 스냅 카드로 전환 + 보조 지표 2개로 축소해 2,525px → 1,841px |
| 실시간시세 | `realtime.html`, `realtime-board.js/.css` | 운영 중. KIS 직접 호출 + 정적 폴백, 상승률/거래대금 TOP100, 아코디언 상세(52주 고저·상하한가·신용잔고), 자체 캔들+이평선(20/60/120/200일)+거래량 듀얼패널, 종목 검색 자동완성. **(2026-08-11)** 데스크톱 캔들/거래량 차트 하단 잘림 버그(CSS 높이값과 JS 렌더 높이 불일치, 전종목 공통) 및 5초 자동갱신으로 순위가 바뀔 때 열려있던 차트·버튼이 먹통이 되던 버그(outerHTML 재조립 시 이벤트 리스너 유실) 모두 원천 수정. **(2026-08-26)** 종목검색 별칭 테이블 신규 추가 — "현대차"로 검색해도 공식 상장명 "현대자동차"가 나오도록 수정(기아/네이버/포스코/하이닉스 등 자주 쓰는 약칭·구 사명 다수 포함, `STOCK_NAME_ALIASES`). **(같은 날 3차 후속 세션)** AI종목분석 검색 정렬 버그(아래 항목) 조사 중 실시간시세 검색 드롭다운도 8개로 하드컷하고 스크롤이 안 되게 막혀있던 걸 함께 발견 — CSS(`.rt-ac`)에 `max-height`+`overflow-y:auto` 추가, 표시상한 8→50으로 통일(`ec256579`). 관련성 점수 로직(`findStockMatches`) 자체는 원래 정상이었고, 오히려 이 로직을 AI종목분석 수정의 기준으로 삼음. **(같은 날 4차 후속 세션)** 우선주(영숫자 코드) 검색·시세조회 버그 수정에 실시간시세도 포함(`normalizeCode6ForQuote`를 `code6Maybe` 재사용으로 단순화, 상세보기/차트열기 경로의 숫자전용 검증을 영숫자 인식 함수 `isValidStockCode`로 교체) — 단 랭킹탭(상승률/거래대금/시가총액 등)의 숫자전용 필터와 WebSocket 구독 코드 정규화는 이번 범위에서 제외(4장 참고, 필요시 후속 요청). **(2026-09-03)** 표 위에 공통 시장 요약 스트립 추가(위 "시장 요약 스트립(공통)" 항목 참고) — 실사이트 실측: 코스피 6,562.72 −3.99% / 코스닥 803.98 −2.10% / 시총 상위 100종목 중 상승 6·하락 94 / 상승률 1위 한진칼 +7.16% |
| 마감시황 | `daily-market.html/.js/.css` | 운영 중. TOP30 탭, AI 종합분석(대시보드+분석+종목상세 3영역), 특징주 그룹A/B 교차보충 규칙, 수급(투자자별 매매동향) 실데이터 표기, 평일 16:15 KST Cowork 예약작업이 분석 작성 → push → 텔레그램 자동 발송. **(2026-09-01)** 사용자 지적("메모장에 대충 갈겨 쓴 느낌", "PPT처럼 색감·표·도표 써서 브리핑할 수준으로")으로 **AI 시황분석 탭을 브리핑 덱으로 전면 리디자인** — 원인은 `analysis`(이모지 소제목이 붙은 4,600자 한 덩어리 텍스트)를 `<div>` 하나에 `pre-wrap`으로 그대로 출력하던 것. `dmxParseAnalysis()`로 7개 섹션을 분해하고(헤더 2개 미만이면 `null` 반환 → 기존 줄글 렌더링 폴백, 5~6월 과거 데이터 보호) 컴포넌트 8종으로 재구성: 히어로 배너(핵심 한 줄+장 성격 태그), 지수 카드+장중 저점→시가→종가 레인지 바, 오늘의 시장 온도, 투자자 수급 다이버징 막대차트, 업종별 등락 랭킹 바, 기관·외국인 순매수 상위, 시장 흐름(문단 번호 배지+인용문 blockquote 분리), 특징주(급등/급락 분리+등락률 바+리스크 슬롯), 내일 변수 번호 카드. **시장 온도는 AI가 아니라 코드가 TOP30 실데이터에서 직접 집계**(상한가·+20%이상·하한가·거래대금 TOP10 중 상승·거래대금 1위)해 할루시네이션 여지가 없고 과거 전 날짜에 소급 적용됨. 수급 막대는 코스피·코스닥을 같은 스케일로 정규화(따로 하면 코스닥 2,535억이 코스피 1조5,000억과 같은 길이로 보임). 장중 고가가 없는 날은 지어내지 않고 오른쪽 끝 라벨을 "종"으로 표기. 생성 규칙(`scripts/daily-market-ai.mjs`)에는 `marketTone`/`indexDetail`/`investor_trend.etc`/`sectors`/`netBuyLeaders`/`featured_stocks[].risk`를 추가하되 전 항목에 "확인 안 되면 키 생략"을 명시, 향후 전략의 일반론 금지, 내일 변수 번호 줄 분리 강제. 8/31은 본문에 글자 그대로 있는 숫자만 구조화 백필(리스크 항목은 새로 써야 하는 분석이라 미백필 — 내일 리포트부터 적용). Playwright 3뷰포트 + 실사이트 렌더 검증 완료(6장 상세). **(2026-08-14)** 선행 GHA `daily-market-sync.yml`(15:40 KST, KIS시세 수집)이 KIS API `fetch failed`로 1회 실패해 마감시황 작업이 기초데이터 없이 STOP된 사고 발생 → 실제 뉴스로 수동 발행 복구, 재발방지로 `totalmoney-daily-closing-report` SKILL.md에 `repository_dispatch` 자동 재시도 로직 신설(아래 6장 상세). **(2026-08-18)** Cowork **클라우드** 세션에서 이 저장소로 `git push`가 막히는 Anthropic 측 알려진 버그(git-proxy 저장소 인가 문제, 미해결)를 처음 확인 → "클라우드에서 새 작업 실행" 토글을 꺼서 예약작업이 PC 로컬에서 실행되도록 전환해 우회. 같은 날 로컬 Cowork 워크스페이스 디스크 부족으로 예약작업이 한 차례 더 실패했다가 사용자가 C: 여유공간을 확보한 뒤 재실행해 정상 발행(GitHub 커밋 → Vercel 배포 → 텔레그램 발송까지 전부 확인 완료, 아래 6장 상세). **(2026-09-02) AI 탭 모바일 접이식 전환** — 6,318px(7.5화면) → 1,822px(2.2화면). 지수 마감·오늘의 시장 온도만 펼쳐 두고 시장 흐름·투자자별 매매 동향·향후 전략·오늘의 특징주·내일 주목할 변수는 제목만 남김(탭하면 펼침, 내용 미삭제). 덱 내부 `.dmx-block`과 페이지 레벨 `.dm-section` 두 종류를 함께 처리하고, 접힌 `.dm-section`에도 카드 테두리를 줘 목록처럼 읽히게 통일. 데스크톱은 무변화(검증: 접힘 0개, 전체 높이 3,428px 동일). **(2026-09-02 밤) 버그 2건 수정** — ①이모지 섹션 헤더가 없는 구형 리포트(7/1)의 본문 2,001자가 화면에서 통째로 사라지던 문제: 덱이 구조화 블록만으로도 "비어있지 않음"으로 판정돼 상위의 줄글 폴백이 영영 실행되지 않던 게 원인, `dmxRenderProseFallback()`으로 파싱 실패 시 원문을 덱 안에 넣도록 수정(`8bd8221`) ②모바일에서 스크롤 중 접이식 섹션이 튕기듯 닫히던 문제: iOS 사파리가 주소창을 접었다 펼 때마다 쏘는 `resize`로 패널이 재렌더되며 접이식이 리셋되던 게 원인, 뷰포트 **폭**이 바뀐 경우에만 재렌더 + 사용자가 펼친 상태를 기억하도록 수정(`c10c3cf`). **③(미해결→조치) 9/1에 추가한 구조화 필드 6종이 실제 발행에서 하나도 안 채워지고 있었음** — 규칙이 호출되지 않는 죽은 코드(`scripts/daily-market-ai.mjs`)에 들어가 있었던 게 원인이라 예약작업 SKILL.md에 STEP 3b로 옮겨 넣음, 9/3 발행분이 첫 검증(3장 최상단) **(2026-09-08) 예약작업이 실행은 됐는데 웹서치 도중 커밋 없이 조용히 끝나는 무음 실패 발생** — 그 여파로 17:30 인스타 마감 카드까지 "데이터 미준비"로 스킵됐다. 이걸 잡으라고 만들었던 `daily-report-watchdog.yml`은 이미 저장소에서 삭제돼 있었고 health-check는 지수 이상치만 보고 "분석 자체가 없음"은 안 봤다. 재발방지 3종 신설: ①health-check에 `checkDailyClosingAnalysis`(평일 17시 이후 `analysis`/`indexes` 결측 시 텔레그램 DM, 휴장일은 등락률 전부 0으로 판별해 오탐 제외) ②재시도 예약작업 `totalmoney-daily-closing-report-retry`(평일 16:50, 이미 발행됐으면 즉시 종료, 데드라인 17:20) ③원 예약작업 프롬프트에 검색 8회 상한·16:45 착수 데드라인·"커밋 없이 끝내지 말 것" 규칙 추가(6장 상세) |
| **마감 리포트 자동 생성** | `scripts/daily-closing-report.mjs`, `.github/workflows/daily-closing-report.yml` | **(2026-09-11 신설)** 운영 중. 그동안 마감 리포트 본문(지수·총평·수급·특징주)은 시우 PC의 Cowork 예약작업만 채울 수 있었고, Windows KB5124008이 로컬 샌드박스를 깨뜨리자 9/10·9/11 이틀 연속 누락됐다(3장 참조). 이제 GitHub Actions 러너 안에서 전부 만든다 — 지수 OHLC는 KIS `inquire-daily-indexchartprice`(FHKUP03500100), 수급은 `inquire-investor-daily-by-market`(FHPTJ04040000), 원/달러는 Yahoo `KRW=X` 일봉, 종목 재료 근거는 언론사 RSS + 네이버 종목뉴스, 본문은 **OpenAI**(기본 `gpt-5.1`, 사용 불가 시 `gpt-5-mini`로 자동 강등. 약 130원/일·월 3천원선. 저장소 variable `CLOSING_REPORT_MODEL`로 교체 가능). `daily-market-ai.mjs`의 기존 시스템 프롬프트를 그대로 재사용해 할루시네이션 가드(뉴스에 없는 재료 금지)를 유지한다. **세 API 모두 날짜 지정이 되므로 과거 날짜 백필도 같은 워크플로로 처리**한다(`date`, `force`, `skip_ai`, `news_file` 입력). 안전장치: 본문이 이미 있으면 건너뛰고(PC 결과 보호), 코스피 지수를 못 받으면 중단하며(부정확한 발행 방지), 실패 시 관리자 텔레그램으로 알린다. 자동 커밋 메시지에 `AI 마감시황 분석`을 넣어 `telegram-notify.yml` 발송 조건도 충족시킨다. 정시성은 pg_cron(평일 16:20 KST)이 담당하고 GitHub schedule은 백업. |
| 장전 브리핑 | `briefing.html` | 운영 중. 미국시장·거시지표 카드, EWY·SOX 등 추가 지표, 평일 08:03 KST 자동 생성/게시, Yahoo `chartPreviousClose` 버그 수정 완료(5개 파일 전면 수정). **(2026-08-12)** 8/7 이후 Cowork 예약작업이 `D:\vibe` 마운트 누락으로 며칠간 조용히 발행 실패하던 사고를 발견·복구(아래 6장 상세). 마감시황처럼 날짜 캘린더 + 좌우 화살표 내비게이션 신규 추가 — 클릭 시 `data/briefing/<YYYY-MM-DD>.json` 아카이브를 불러와 그날 시점 그대로 표시, URL 해시(`#YYYY-MM-DD`)로 직접 링크 가능. 오늘 날짜는 기존과 동일하게 실시간 데이터. **(2026-08-13)** 날짜 트리거 UI 폴리시 2건 — 아래화살표(chevron) 아이콘 제거(기능은 유지) + 호버 시 색상 강조 추가. 같은 날 **Cowork 마운트 실패가 재발**(재발방지책에도 불구, 아래 6장 상세) — 복구 과정에서 신규 버그도 발견: 수치 데이터는 별도 GHA(`morning-briefing.yml`, 07:55 KST 독립 실행)가 정상 갱신했지만 `aiAnalysis`(질적 분석 텍스트)는 전날(8/12) 내용이 그대로 남아있어 최신 수치와 내용이 불일치한 채 라이브 노출되고 있었음 — 실제 뉴스 리서치로 수동 복구. 재발방지책 강화: 평일 08:15 KST에 발행 여부를 확인해 1차가 실패했으면 08:30까지 대신 발행을 완료하는 2단계 안전망(`totalmoney-morning-briefing-retry` Cowork 예약작업) 신규 추가. **다음날(08-14) 또 재발** — 이번엔 재시도 작업도 절반만 완료(마운트·수치데이터 트리거는 성공, 분석 작성·발행은 미완료)하는 새 패턴 발견, health-check 알림도 자체 cron 공백으로 지연 발송됨. 두 차례 모두 실제 뉴스 리서치로 수동 복구·텔레그램 발송까지 확인(아래 6장 상세). **(2026-09-02) 마감시황과 동급의 브리핑 덱으로 전면 전환** — 3열 표 8개(글로벌해석/국내체크/환율원자재/미국뉴스/국내뉴스/업종전략/포트폴리오/리스크)를 카드·막대·배지로 전부 교체하고 표는 하나도 남기지 않음. 히어로 배너(핵심 한 줄 + 장 성격 태그는 **코드가 간밤 3대 지수 등락에서 직접 계산**), 지수·환율·원자재 수치 카드(없앤 '글로벌 시장 해석' 표의 AI 코멘트를 각 카드 하단에 흡수 → 중복 카드 2개 제거), '간밤 시장 온도' 스코어보드(지수/섹터/대형주 상승 개수·최강최약 섹터 전부 실데이터 집계), 섹터·대형주 다이버징 막대차트(기존엔 값이 "—"인 죽은 컬럼이었음), 포트폴리오 실비율 막대, 업종 의견 색상 배지, 리스크 경고 카드. 문장 분리기 신설(종결부호 뒤 공백일 때만 분리 → "2.80%" 소수점에서 잘리던 버그 해결). 실시간 지수 API가 일부 지수만 내려줄 때 다우 카드가 사라지던 버그도 id 기준 병합으로 수정. **모바일은 접이식 전환으로 14,728px(17.5화면) → 3,526px(4.2화면)** — 지수 마감·시장 온도·핵심 이슈 3개만 펼치고 나머지는 제목만(탭하면 펼침, 내용은 미삭제). 데이터 스키마·생성 스크립트는 무변경 |
| 증시 일정 | `weekly-market.html/.css/.js`, `assets/key-indicators.js` | **(2026-09-04) 분석 카드 레이아웃 + 핵심지표 판정 실질화 — 커밋 2건.** ①**레이아웃**: `.analysis-grid`에 예전 "3장 카드" 디자인의 `repeat(3, 1fr)`이 남아 있어, 지금 구조상 그 안에 들어가는 묶음 div 1~2개가 **왼쪽 1/3만 차지하고 오른쪽이 텅 비어** 보였다. 게다가 `event-analysis-*` 클래스는 **CSS가 하나도 없어** 브라우저 기본 `<ul>` 불릿으로 렌더링되고 있었다. 섹션은 전폭을 쓰게 하고 `.event-analysis-list`를 `repeat(auto-fit, minmax(268px,1fr))` 그리드로 만들어 지표 카드가 가로로 나열되게 함. **268px인 이유**: 본문 영역 실측 폭이 874px 고정(오른쪽 달력 사이드바 때문에 화면이 아무리 넓어도 908px를 넘지 않음)이라 300px로는 3열이 안 만들어진다. 실측 900px→3열 / 700px→2열 / 560px 이하→1열 ②**★ 핵심지표 판정이 사실상 작동하지 않고 있었다**: 텔레그램의 `isHighImpact()`는 `impact === "high"`를 봤는데 **데이터 408건이 전부 `"high"`**였다(소스가 애초에 high-impact만 수집). 그래서 원유재고·모기지금리·연준 위원 개별 연설까지 "주요 지표" 알림으로 나갔다. 웹은 2026-09-03에 자체 `isKeyIndicator`로 이미 대응했지만 **텔레그램과 기준이 갈려 있었다** → `assets/key-indicators.js` 신설(브라우저·Node 공용, `createRequire`로 `.mjs`에서 로드)로 **단일 기준점** 확보. 포함: FOMC(금리·의사록·점도표·기자회견·잭슨홀)·의장/부의장 발언·CPI·PCE·PPI·고용보고서·GDP·ISM·소매판매 + 운영자 선택분(주간 신규실업수당·EIA 원유재고) + **한국 지표 전반**. 제외: 연준 개별 위원 연설·지역 연은 지수·주택·무역/재고·내구재·ADP·JOLTs·미시간·API 원유·모기지금리. 실측 **408 → 154건**, 하루 알림 **16건 → 7건**(8/26 기준). 적용 범위는 **텔레그램 + 웹 상단 강조**(다음 핵심 지표·이번 달 핵심 지표·달력 강조점)까지고 **웹 전체 일정표는 정보량 유지를 위해 그대로 전부 노출**. `refresh-indicator-actuals.mjs`는 실제치 **수집** 범위라 `isHighImpact` 유지(좁히면 실제치가 안 채워짐) ③텔레그램 문구 `⚠️ 주요 지표 (HIGH)` → `⚠️ 오늘의 핵심 지표`("HIGH"는 데이터 소스 내부 용어였음). **기준을 바꾸려면 `assets/key-indicators.js` 한 파일만 고치면 웹·텔레그램에 동시 반영된다.** — 이하 이전 기록: 운영 중. 캘린더 위젯 + 오늘일정/AI분석/다가오는 일정 3섹션, 경제지표 한글 번역, 실적(krEarnings) DART 우선 병합, 핵심지표 실제수치 5분 간격 자동 업데이트+속보. **(2026-08-13)** "데이터가 YYYY-MM-DD 이후 갱신되지 않았습니다" 내부 사정 안내 배너를 고객 화면에서 제거(같은 상황은 health-check.mjs가 내부적으로 계속 감지). **(2026-08-26)** 신규상장(IPO) 공모가 표시 버그 수정 — 미확정 공모가가 "0원"으로 표시되던 문제를 공란 처리로 수정(`formatOfferingPrice`), 근본원인이었던 38.co.kr 파싱 버그(라벨·값이 분리된 `<td>` 셀 구조를 못 읽던 문제)도 `scripts/weekly-schedule.mjs`에서 함께 수정 — 기도산업(28,400원)·니어스랩(41,200원)·해치텍(23,000원) 실제 확정가로 라이브 반영 확인. **(2026-09-03) 감사에서 "사이트에서 가장 값싸 보이는 화면"으로 지적된 4건 일괄 수정**(커밋 `c5f3090`) — ①**중복**: `renderUpcoming`이 항상 '오늘'부터 그려서 위쪽 '오늘 일정' 섹션과 첫 그룹이 100% 겹쳤다. 선택 날짜의 **다음날**부터로 바꾸고 날짜 그룹 6개 상한 + D-day 칩 추가. 날짜를 옮길 때 호출부마다 `renderDateSchedule`만 부르거나 `renderUpcoming`을 '오늘' 기준으로 불러 세 섹션이 따로 놀던 것도 `renderSelectedDate()` 단일 진입점으로 통일 ②**중요도 배지 변별력 0**: 근본원인은 데이터 소스(ForexFactory/Investing)가 **애초에 high-impact 항목만 수집**해 `row.impact`가 403건 전부 `"high"`였던 것 — 그 값을 배지로 그대로 쓰면 안 된다. 지표 성격 기준 3단계로 재설계(`SECONDARY_INDICATOR_RE` 신설, `isKeyIndicator`→핵심 / `isMediumOnlyIndicator`+2군→주목 / 나머지→참고), 라벨도 높음·보통·낮음 → **핵심·주목·참고**로 변경. 실사이트 실측 분포 **핵심 11 / 주목 16 / 참고 11**. 중요도 컬럼이 '핵심'을 표시하게 되어 완전히 중복이 된 좌측 `key-indicator-badge`는 제거 ③**템플릿 문형**: 4건이 전부 "예상 상회 시 …할 가능성이 높습니다 / 예상 하회 시 …할 수 있습니다"로 읽히던 원인은 **프롬프트가 그 문형을 스키마에 그대로 못 박아둔 것**(`scripts/weekly-schedule.mjs`의 `buildAnalysisPrompt`). `analysis` 한 덩어리를 `why`/`bull`/`bear`/`watch`로 분해하고 "예상 상회 시"는 화면 라벨이 담당하게 함 + 작성 규칙 6개(애매한 종결 금지, 6개 항목 문형 중복 금지, bull/bear 단순 대칭 금지, 수치 창작 금지, 40~70자, watch는 실존 지표·종목만) 추가. 화면은 `analysisItemHtml()`로 카드화(관전 포인트 태그 포함)하고 **why/bull/bear가 없는 구버전 레코드는 `analysis` 원문을 그대로 렌더하는 폴백**을 넣음(9/2 실데이터로 폴백 동작 확인) ④**캘린더 아래 빈 공간**: 사이드바에 '다음 핵심 지표'(D-day + 시각 + 이전·예측 실값)와 '이번 달 일정'(핵심 지표/경제지표/국내 실적/신규상장 4셀) 카드 신설 — 둘 다 실데이터 집계, 해당 달에 수집된 일정이 없으면 지어내지 않고 빈 상태 표시 |
| AI 종목분석 | `stock-analysis.html/.js/.css` | 베타 운영 중. 국내·미국주식·암호화폐 전종목 지원, 7카드 UI(차트/이벤트/시나리오/전략/리스크), Claude JSON 스키마 + OpenAI 폴백, 자체 캔들차트(500개 캔들, ~2년치), 로그인+구독 기반 접근 제어(무료 월 3회, Pro/Premium 무제한). 과거 있던 `?betakey=` 우회 백도어는 완전 제거됨. **(2026-08-26)** 실시간시세와 완전히 별개인 자체 검색 구현이라 누락돼있던 종목검색 별칭 테이블(현대차→현대자동차 등)을 이 페이지에도 동일하게 추가 반영(배포가 Vercel 일일 배포한도에 걸려 지연됐다가 이번 세션에 라이브 반영 확인 완료). **(같은 날 3차 후속 세션)** 사용자 제보(스크린샷: "삼성전자" 검색해도 실제 삼성전자(005930)가 아니라 이름에 "삼성전자"가 포함된 채권혼합형 ETF들이 먼저 뜨고 정작 삼성전자는 목록 밖으로 밀려 선택조차 안 됨) — `filterStocksForAutocomplete`가 관련성 순위 없이 원본 리스트 순서 그대로 substring 필터만 하던 게 원인. 실시간시세(`findStockMatches`)가 이미 쓰던 점수체계(정확일치>시작일치>포함)로 통일(`ec256579`). "외 N개 더 있습니다"로 막혀있던 표시도 스크롤 가능한 목록(표시상한 8→50)으로 전환. 합성데이터로 "삼성전자" 검색 시 005930이 1위로 정렬됨을 오프라인 검증 완료 — **배포는 Vercel 일일 배포한도 소진으로 지연, 다음 세션에서 라이브 재확인 필요**(3·4장 반영). **(같은 날 4차 후속 세션)** 삼성전자우·삼성물산우 등 우선주(영숫자 6자리 코드)가 아예 검색이 안 되던 버그를 근본 수정 — KIS 종목마스터(.mst)에서 우선주 114종목을 추출해 `assets/stock-list.json`에 병합, 코드 정규화 함수들의 숫자전용 처리를 영숫자 허용으로 확장, "삼성물산우"처럼 보통주 이름을 포함하는 질의의 모호매칭도 startsWith 우선순위로 해결 — 실시간시세와 함께 라이브 curl로 정상 시세 반환까지 확인 완료(6장 상세). 이어서 AI분석 결과 텍스트가 세로로 한 글자씩 깨져 나오는 버그를 두 차례 수정 — 1차(`analysis.supply`/`story` 등, `formatProseText`)는 배포·라이브 확인 완료, 2차(한눈에 요약 카드의 `summary.description`, `sanitizeOneLineText`)는 커밋(`3e83e7da`)까지 마쳤으나 Vercel 배포한도 재소진으로 배포 대기 중(3장 최상단 항목 참고). 같은 세션에서 GPT의 AI분석 품질 2차 리포트(18개 항목)를 검토해 우선순위 5개(A/B/C 시나리오 상호배타화, 수급 1/5/20일 추세 코드 집계, 재료반영률 10%단위 버킷팅, 엘리어트 파동 격하, FACT/AI해석 시각 구분)와 부가 항목(종합점수 분모 표기, target2 장기잠재 라벨, story 서술 순서 등)을 반영, 커밋 `6a447f41`로 배포·라이브 확인 완료(6장 상세). **(같은 날 5차 후속 세션)** 4차 세션에서 배포 대기 중이던 `summary.description` 세로텍스트 2차 수정을 Claude-in-Chrome으로 실제 로그인 세션에서 삼성전자 분석을 직접 실행해 재현 안 됨을 최종 확인(배포한도 정상 회복). 이어서 사용자 요청으로 **UI 전면 리디자인**(카드1 "한눈에 요약"을 좌: 신호배지+상승확률 / 중앙: 요약문 / 우: 추세·모멘텀·거래량·수급 2x2 필그리드 3분할로 재구성, 종합점수+설명은 카드 하단 풋터로 이동, 카드2/3은 `ai-card`/`ai-card__body`에 flex 적용해 같은 행 높이 자동 통일, 카드4 "다가오는 이벤트"의 데스크톱 반쪽 폭 잘림 버그 수정 + 이벤트 리스트 카드형 배경/pill 뱃지 적용, 카드5~7은 padding·radius·배경 통일)을 구현. Playwright로 라이트/다크/모바일 오프라인 렌더링 검증 후 커밋(`8e94e90`)·푸시, Vercel 배포 READY 확인, Claude-in-Chrome으로 실제 로그인 세션에서 삼성전자 라이브 분석 전체 7카드(라이트+다크)까지 최종 검증 완료. **(2026-08-28)** "다가오는 이벤트" 카드의 국내종목 데이터 소스를 AI(Claude 생성/web_search)에서 **DART(전자공시) Open API 기반 일일 배치**로 완전 전환(미국주식은 기존 AI 방식 유지) — 기존 AI 생성 방식은 오늘 날짜 이벤트를 못 걸러내거나, 이미 끝난 실적발표를 "예정"으로 잘못 표기하거나, 기준금리 결정 같은 종목분석과 안 맞는 매크로 일정·"미래 일정으로 확인됩니다" 같은 필러 문구가 섞이는 등 구조적 한계가 있었음(사용자 제보로 발견). 신규 파이프라인(`.github/workflows/dart-events.yml`, 매일 06:10 KST)은 DART `list.json` 공시검색으로 보고서명에 "주주총회소집공고"/"배당"/"주요사항보고서"가 포함된 공시만 골라 원본 문서(`document.xml`, 순수 Node `zlib` 자체 ZIP 파서 `scripts/dart-zip-utils.mjs`)에서 실제 날짜(배당기준일·주총일시)를 정규식으로 추출(과거·6개월초과 날짜 자동 제외, 앵커 문구가 여러 번 나오면 순서대로 시도해 유효 날짜를 주는 첫 위치 채택, 탐색창 40자로 좁혀 인접한 무관 날짜 오추출 방지), 자사주 취득/처분은 DART 구조화 API(`tsstkAqDecsn.json`/`tsstkDpDecsn.json`) 직접 사용. 실적발표는 정확한 날짜를 추측하지 않고 결산월 기준 법정 제출기한(분기·반기 45일/연간 90일)으로 계산한 "예상 구간"만 표시, 데이터 없는 종목은 뉴스로 채우지 않고 "현재 확인된 예정 이벤트 없음"으로 빈 상태 표시. 워치리스트 15종목(`earnings-calendar.mjs`의 `KR_CODES` 재사용) 결과를 `data/dart-events.json`에 커밋 → `api/analyze.js`가 GitHub Contents API로 30분 캐시 서빙하며 KR종목이면 AI가 생성한 events를 무조건 버리고 이 데이터로 교체(`normalizeAnalysis` 비동기 전환). corp_code 매핑은 대용량 `corpCode.xml`(87,346개) 다운로드가 GitHub Actions에서 계속 타임아웃돼 포기하고 `list.json`을 종목코드 기준 스캔으로 대체(30일 캐시). Claude-in-Chrome으로 SK하이닉스(배당기준일 2026-08-31 단일 이벤트, 재료분석과 중복 없음)·삼성전자(빈 상태 정상 표시) 라이브 검증 완료(6장 상세). **(2026-09-01)** 사용자 제보("어투가 너무 딱딱하다, 읽을 때 질린다")로 **리포트 문체 규칙 전면 개편** — `ANALYST_PERSONA_RULES`에 구체적 문체 블록 신설(문장 40~70자·90자 상한, 절 3개 이상 장문 금지, 2~4문장마다 문단 분리, 두괄식, 종결어미 연속 반복 금지, 상투구 9종 명시 금지, 카드당 단문 1회), 섹션 역할 분담으로 같은 재료 3개 이상 섹션 반복 금지(최대 2곳), 이미 끝난 이벤트(반영도 90%+)를 재료 슬롯에 넣지 못하게 금지. 어투는 사용자 선택으로 '~다'체 유지. 라이브 검증 중 발견한 2건도 후속 수정 — 내부 필드명 노출(`scenarioB.entry를 …`)은 프롬프트 금지 + `stripInternalFieldNames()` 안전망 이중 방어, 수급 본문의 FACT 표 숫자 전사는 **3번 섹션 규칙 본체에 남아 있던 정반대 지시("1일/5일/20일을 반드시 함께 언급")가 상위 톤 규칙을 이기고 있던 게 원인**이라 그 지시를 교체하고 "본문 금액 표기 최대 2개" 정량 상한을 섹션 규칙 안에 직접 삽입(금액 6개→2개 확인). 같은 세션에서 **진입/목표/손절 색상 시각화**도 적용 — 한국 관습대로 목표=빨강/손절=파랑/진입=앰버 토큰 신설(라이트·다크 별도), 가격 블록에 액센트 바+그라데이션+그림자로 입체화하고 2×2 그리드 확정, 가격 순서를 진입→목표→손절로 통일, 손익비는 캡션 한 줄로 분리, 시나리오 카드 가격 행은 좌라벨/우숫자+점선 구분, 수급 FACT 표는 순매수=빨강/순매도=파랑 + 그리드 정렬(숫자 중간 줄바꿈 제거). Playwright 3뷰포트 + Claude-in-Chrome 라이브 3회 실행으로 검증 완료(6장 상세). **(2026-09-02) 비로그인 소개 랜딩 신설** — 그동안 비로그인 방문자는 페이지 전체를 덮는 "회원가입이 필요합니다" 모달만 보고 제품을 한 줄도 못 본 채 이탈했다(유료 전환 퍼널의 가장 큰 구멍). 비로그인 상태에서는 모달 대신 `#ai-landing` 섹션을 페이지에 직접 렌더 — 리포트 카드 7종 구성, 실제 데이터 출처(KIS·DART·뉴스), 무료 월 3회 안내, 회원가입/로그인 CTA. **가짜 수치는 넣지 않고 구조와 사실만 노출**(숫자 목업은 오히려 렌더링 오류처럼 보여 제거). 로그인 상태·서비스 준비중(`setupPending`) 분기는 기존 모달 로직 그대로 유지. **(2026-09-03 밤) 유료 퀄리티 향상 1차 — 카드 8장 체제로 확장**: ①**"시장 대비 위치"(카드2) 신설** — 매매시그널이 쓰던 전종목 지표 캐시(`data/kr-screener-cache.json`, 2,765종목)에서 PER·PBR·1/3/12개월 수익률·시가총액·거래대금·외국인보유율의 **비교군 내 백분위와 중앙값을 서버가 코드로 직접 집계**한다(`computeMarketPosition`). 신규 API 호출 0(캐시는 이미 함수 번들에 포함), AI 추정 0. 비교군은 같은 업종 → 같은 시장 → 전체 순으로 폴백하고 무엇과 비교했는지(`peerLabel`/`peerCount`)를 화면에 반드시 표기. 같은 집계를 프롬프트에도 주입해 본문 서술과 화면 도표가 어긋나지 않게 함. 카드 번호는 배열 기반 동적 부여로 바꿔 **국내 8장 / 해외·암호화폐 7장**을 자동 처리(비로그인 랜딩도 8카드로 갱신) ②**업종명 수집 시작** — KIS `inquire-price` 응답에 이미 있던 `bstp_kor_isnm`을 추가 호출 없이 캐시·시세에 함께 수집(`lib/kis-indicators.js`→`lib/trade-condition-eval.js`→`build-screener-cache.mjs`/`trade-signal-scan.mjs`). **다음 캐시 빌드(평일 16:00 KST)부터** 비교군이 "코스피 전체"에서 "○○ 업종 N개"로 좁혀짐 ③**출처 배지 전면 제거**(사용자 피드백: "사실/AI해석 같은 문구는 구독자가 알 필요 없다, 그럼 안 써있으면 거짓인가") — `ai-fact-badge`("사실 · KIS 시세데이터")·`ai-interp-badge`("AI 해석")·"FACT · 실측 집계"를 JS·CSS에서 전부 삭제 ④**도표 4종 추가**(사용자 피드백: "중구난방 나열, PPT처럼 정리됐으면") — 카드2를 8행 나열에서 **밸류에이션/주가 수익률/규모·수급 3묶음 타일 그리드**로 재설계(타일 = 값 + 위치 눈금 + 순위), 카드4 수급에 1일·5일·20일 **다이버징 미니 막대**(순매수 빨강/순매도 파랑, 6개 값의 최대 절댓값 기준), 카드7 차트 상단에 **이평 이격 다이버징 막대 + RSI 게이지**(`renderTechSnapshot`, 정배열/역배열과 "몇 개 선 위"도 코드가 직접 판정), 카드8에 **시나리오 A/B/C 확률 100% 스택 막대**(`renderScenarioProbBar`) ⑤**카드2 요약 문구 재작성** — "상위 20%"를 "최상위권"이라 부르던 과장을 5/20/40/60/80/95% 단계로 교정하고, 한 마디로 끝나던 코멘트를 밸류에이션·수익률 흐름·시장 관심 3문장으로 확장(중앙값 대비 배수 표기 추가, 한글 조사 오류 `하위권로` 수정). "주가 성과" → "주가 수익률(해당 기간 상승률)"로 라벨 명확화 ⑥**재료 분석에 최신 뉴스가 안 들어가던 문제 근본 수정**(사용자 제보: SK이노베이션 리포트에 미국 수주·SKIET 합병·SK온이 하나도 없음) — 원인 두 가지였다. **(a) 프롬프트가 "재료가 없으면 실적 모멘텀·업종 트렌드로 채우라"고 시켜서** 모델이 검색을 대충 해도 일반론으로 슬롯을 메울 수 있었다 → 그 지시를 삭제하고 "**일반론 금지**(제목에 발주처·상대사·자회사명·금액 중 최소 하나의 고유명사나 숫자 필수) + 3개 이상, 그중 2개는 **최근 45일 내 개별 사건**" 규칙으로 교체 **(b) web_search가 포괄 1회로 끝나** 개별 이벤트가 안 잡혔다 → 검색 각도를 3개(최근 뉴스 / 수주·계약·인수·합병·공시 / 실적·목표주가)로 나눠 **최소 3회** 호출하도록 지시 강화. 여기에 더해 **`fetchStockNewsBundle()` 신설** — 코드가 같은 3각도로 구글뉴스 RSS를 직접 모아(중복 제거·최신순·최대 14건, 날짜 포함) 프롬프트에 함께 넣는다. 모델 검색이 부실해도 최신 사건이 입력에 남는 이중 안전망. 오프라인 검증에서 SK이노베이션 쿼리로 "SK온 美 ESS 수주", "SKIET 흡수합병", "물적분할 7년 만에 흡수합병", "SK동남아투자법인 감자" 등 제보된 재료가 전부 수집됨을 확인. **(2026-09-04 새벽) 유료 퀄리티 향상 2차 — 카드 9장 체제(국내)**: ①**"실적 추이"(카드3) 신설** — AI 종목분석의 가장 큰 구멍이던 "재무·실적 실데이터가 하나도 없다"를 해소. `lib/dart-financials.js`가 DART 단일회사 주요계정(`fnlttSinglAcnt.json`)에서 **최근 3개년 연간 + 최근 분기 누적**의 매출액·영업이익·당기순이익을 파싱해 막대그래프 + 전년동기 증감률로 렌더한다. 연결(CFS) 우선·없으면 개별(OFS), 금융·지주사의 "영업수익" 대체 계정명 인식, 값 없는 기간·항목은 지어내지 않고 생략. **기간 혼합 방지가 핵심 설계** — 누적/3개월 기준을 항목마다 따로 고르면 "매출은 반기 누적인데 순이익은 3개월"인 카드가 나오므로(오프라인 검증에서 실제 재현) 매출액 행에서 **한 번만** 정해 세 항목에 동일 적용한다. Vercel 함수 12개 한도 때문에 새 `api/*.js` 없이 `api/analyze.js`가 lib을 직접 호출. 같은 숫자를 프롬프트에도 한국식 조/억 단위로 주입해 본문과 도표가 어긋나지 않게 함 ②**일목균형표 완전 제거**(사용자 제보: "분석할 때마다 수치가 없다고 하거나 보이지도 않는 구름 얘기함") — 근본 원인은 **서버가 전환선·기준선·선행스팬 값을 AI에 아예 안 넘긴다는 것**이었다(MA/RSI만 준다). 쓰라고 시켜놓고 데이터는 안 주니 얼버무리거나 지어낼 수밖에 없었다. 프롬프트 3종에서 항목을 빼고 관련 용어 6개 사용을 명시 금지했으며, **이미 저장된 과거 리포트와 모델의 습관적 삽입까지 막으려 화면에서도 해당 문장을 통째로 버린다**(`CHART_DROP_RE`, 제목 없는 회색 문단으로 되살아나는 폴백 경로까지 차단) ③**"중앙값" → "종목 중앙값"** 표기 명확화(사용자 제보: 뉴스의 코스피 PBR과 헷갈림) — 카드 각주에 "한 종목당 한 표로 센 값이고 시총 가중한 지수 PBR과는 다른 지표"라는 설명 추가, 프롬프트에도 같은 주의문을 넣어 본문이 지수 지표인 척 쓰지 못하게 함 ④**재료 분석 라이브 검증 후 2건 보강** — 실제 3종목 로그를 확인한 결과 원래 문제였던 순수 일반론은 사라졌으나(3장 참고) "2분기 역대 최대 실적" 류가 반복되고, 4칸을 맞추려 "방문상담 예약 서비스 도입" 같은 무관한 항목이 들어갔다. **실적 자체를 재료 제목으로 쓰는 것을 금지**(실적 추이 카드가 이미 실수치로 같은 얘기를 한다)하고, 프롬프트·서버 상한을 **"3개 또는 4개"로 일치**시킨 뒤 "넷째가 약하면 3개로 내라"를 명시 **(2026-09-18 2차)** 수급 방향은 이제 코드가 확정한다 — `buildSupplyDirectionFact()`가 외국인·기관의 오늘/5일/20일 방향과 "둘이 같은 방향인지"를 한 문장으로 만들어 프롬프트 3경로에 `supplyDirectionFact`로 주입하고, 모든 서술이 그 방향과 일치하도록 강제한다. 2번 카드 요약문(`mpHeadline`)과 하단 각주 2곳도 정중체로 전환. 어색하다는 지적을 받은 "정교합니다"는 금지 상투구에 추가(원인은 예문에 그 단어가 있었던 것). **(2026-09-18 1차)** 어투 전면 개편 — 종결어미를 **정중체 '-습니다'**로 전환, 독자에게 말 걸듯 쓰는 규칙 신설, 전문용어 첫 등장 시 괄호 풀이 강제(`[쉬운 설명]`), 실제 리포트 문장을 좋은 예/나쁜 예 쌍으로 프롬프트에 앵커로 삽입, 문장 길이 80자 기본/110자 허용. 장중 수급 "당일 잠정치" 참고문구는 AI 프로즈가 아니라 코드가 고정 문구로 얹는다(`buildSupplyFlowFact` → `supplyFlow.isToday` → `renderSupplyFlowFact`). 영향 범위는 이 페이지뿐 — 모닝브리핑·마감시황은 별도 스크립트라 그대로다 |
| 매매시그널 | `trade-signal.html`, `lib/kis-indicators.js`, `lib/trade-condition-eval.js` | (표 셀 안이라 조건 스키마의 세로줄은 `\|`로 이스케이프해 표기) 운영 중(국내 KOSPI+KOSDAQ 전종목, 일봉 기준). 자연어 검색 → AI가 구조화 조건 파싱 → 캐시 필터링, RSI 다이버전스 등 지원, 저장 시 알림. **암호화폐는 스캔 범위 밖**(별도 데이터소스 필요). **(같은 날 3차 후속 세션)** 사용자 제보("검색결과중에 거래중지 종목도 나오는데 안 나오게 해줘. 그리고 감자나 액면병합 같은 이슈로 가격이 높아지는 것도 걸러주고") — 거래중지·정리매매 종목을 KIS `inquire-price`의 `temp_stop_yn`/`sltr_yn` 필드(이미 호출 중인 응답에서 추출, API 호출 추가 없음)로 걸러내 즉시검색 결과에서 제외. 코스피/코스닥 법정 가격제한폭(±30%, 여유 포함 32%)을 초과하는 일별 급등락을 감자/액면병합성 이벤트로 간주해, 그 구간을 포함하는 기간수익률(5/21/63/126/252일)만 표시를 생략(null 처리)하도록 `computePeriodReturns`에 이상감지 로직 추가(`3bd00239`). 합성 데이터로 두 로직 모두 오프라인 검증(급등 구간을 포함하는 기간만 null 처리되고 그 외 기간·완만한 정상 상승은 그대로 계산됨을 확인), Vercel 배포까지 확인. **다음 정기 `build-screener-cache.mjs` 실행(평일 16:00 KST) 이후에야 캐시에 실제 반영되므로 라이브 재확인은 다음 세션 숙제**(3·4장 반영). **(2026-08-26) HTS급 조건검색으로 대폭 확장**(사용자 요청: "hts와 같은 규모의 조건", "키움hts 조건검색식을 그대로 붙여넣어도 되게") — 조건 스키마를 `{logic:AND, clauses:[...]}` 단순 나열에서 `{logic:AND\|OR, clauses:[leaf\|group]}` 중첩 Boolean 트리로 확장(기존 저장 전략은 100% 동일하게 그대로 평가되도록 하위호환 유지), `negate`(NOT)와 `within`(최근 N봉 이내 발생) 지원 추가. 신규 조건 11종: `price`(현재가 자체 — 원래 리포트된 버그였던 "주가 1,000원 이상" 미지원을 이걸로 해결), `per`/`pbr`/`eps`/`foreign_hold_rate`/`volume_turnover_rate`(기존에 이미 호출하던 KIS 응답에서 더 추출하는 것이라 API 호출 추가 없음), `foreign_net_buy`/`institution_net_buy`(KIS `inquire-investor` 신규 1회 호출 필요), `high_breakout_n`/`low_breakdown_n`/`volume_record_high_n`(52주 고정 대신 20/60/120/240봉 커스텀 신고가·신저가·신고거래량), 이격도·이평돌파에 240일선 추가. 대화창에 키움 HTS 조건검색식(지표표 + "A and B and !C" 형태 수식)을 그대로 붙여넣으면 AI가 파싱하도록 프롬프트·스키마 확장, 입력 길이 제한도 300자→4,000자로 상향. `lib/trade-condition-eval.js` 신규 로직은 합성 스냅샷 기반 노드 유닛테스트 22건(신규타입/부정/OR그룹/within/하위호환)으로 검증 완료, 커밋 `bf5d2df` 배포·라이브 반영 확인. **다음 확인 필요**: 신규 필드는 다음 `build-screener-cache.mjs` 정기 실행 후에야 캐시에 실제 채워짐(종목당 API 호출 약 1.5배 증가 — 이 세션엔 실제 KIS 자격증명이 없어 실행시간/성공률 직접 검증 못함), 화면 종단 테스트는 로그인+Pro 게이트 때문에 Claude가 직접 못하므로 사용자가 실제 사이트에서 확인 필요. **(같은 날 후속 세션)** 사용자 제보("한달동안 상승률 50% 이상" 검색 시 0건)로 `period_return`(기간수익률) 조건의 `days` 필드가 정규화 단계에서 통째로 누락되는 별개 버그(7/19 최초 추가 시점부터 존재, HTS 확장과 무관) 발견·수정(`4f05d5a8`) — Claude-in-Chrome으로 실제 로그인 세션에서 재검색해 52개 종목 정상 표시까지 라이브 확인 완료. **(같은 세션 추가 발견)** 사용자가 "1년동안 상승률 높은 순서대로 20종목"을 검색하자 결과가 항상 거래대금 내림차순으로만 나오고(순위 개념 자체가 없었음) 화면엔 당일등락률만 표시돼 요청한 지표 자체가 안 보이던 문제 발견 — 즉시검색에 `sort{by,days?,period?,direction}`/`limit` 개념을 신규 추가(`27cabcb0`), 결과 리스트에 순위(N위)+실제 정렬 지표 값 표시하도록 화면도 갱신. 실제 캐시로 1년수익률 내림차순 검증(디에이테크놀로지 +2900% 1위 등) 후 Claude-in-Chrome으로 라이브 재확인 완료. **(2026-09-02) 비로그인 게이트 개선** — 제목·안내문·요금제 버튼뿐이라 방문자가 이 기능이 뭘 해주는지 모르고 이탈하던 화면에 실제 검색 가능한 조건 예시 4종(RSI 다이버전스 / 기간수익률 정렬 / 주가·PBR·외국인 순매수 복합 / 키움 HTS 조건검색식 붙여넣기)과 스캔 범위(코스피·코스닥 전 종목 일봉, 거래정지·정리매매 제외)를 노출. 비로그인인데 CTA가 "요금제 보기"뿐이던 문제도 회원가입/로그인으로 분기. **(2026-09-04 새벽) 백테스트용 일자별 스냅샷 축적 시작 (로드맵 F 선행과제)** — 이 기능의 가장 큰 구멍은 "이 조건을 3개월 전에 돌렸으면 뭐가 잡혔을까"를 물어볼 **과거 데이터가 아예 없다는 것**이었다(`kr-screener-cache.json`은 매일 덮어써진다). 백테스트를 언제 만들든 데이터는 오늘부터 쌓아야 하므로 축적부터 시작했다. `scripts/archive-screener-snapshot.mjs`가 8.2MB 캐시에서 백테스트에 필요한 **25개 열만 뽑아 CSV로 만들고 gzip** → **141KB**(실측, 2,765종목). `.github/workflows/screener-snapshot.yml`이 `workflow_run`으로 캐시 빌드 성공 직후 자동 실행된다. **핵심 설계: main이 아니라 `data-archive` 브랜치에 쌓는다** — main에 넣으면 매일 파일이 하나씩 Vercel 배포에 얹혀 배포가 영구히 무거워진다. `data-archive`는 Vercel 배포 대상이 아니라 서비스에 영향이 0이다. 열 스키마는 `screener/index.json`에 보유 날짜 목록과 함께 기록되고, **열 추가 시 반드시 뒤에 붙일 것**(과거 파일은 열 수가 적은 채로 남고 읽는 쪽이 헤더 이름으로 찾는다). 거래정지·정리매매 플래그도 함께 보존한다(백테스트에서 반드시 걸러야 하는 값). **(2026-09-03 오후) "왜 걸렸는지" 근거 표시 + 재무비율 조건검색 신설 (로드맵 F의 나머지 절반)** — ①**근거 칩**(`describeMatch`, `lib/trade-condition-eval.js`): 그동안 검색 결과는 종목명·가격·등락률만 보여줘서 "내 조건 중 어디에 어떤 값으로 걸린 건지"를 알 수 없었다. 무료 시세 사이트와 구분되는 유료 가치는 "걸렀다"가 아니라 **"이 값이라서 걸렀다"**에서 나온다. 조건에 통과한 종목마다 실제 측정값을 칩으로 붙인다(예: `PBR 0.49 | 거래대금 384억 | 1년 수익률 +27.3%`). **신규 API 호출 0** — 이미 캐시에 있는 snapshot을 읽어 문자열로 만들 뿐이다. 설계 3원칙: 실측값이 없으면 지어내지 않고 **항목 자체를 생략**, **OR 그룹은 실제로 통과한 가지만** 남김(통과 안 한 가지를 근거처럼 보이면 그 자체가 거짓말), **negate(NOT)는 보여줄 실측값이 없어 생략**. 조건 40종 전부 근거가 나오는 것을 실캐시로 확인 ②**재무비율 조건검색 5종 신설**(`debt_ratio`/`operating_margin`/`net_margin`/`roe`/`current_ratio`) — 사용자가 키움 HTS 조건식(A 시가총액·B 부채비율·C 영업이익률·D 이자보상배율·E PER)을 붙여넣었는데 실제로 걸리는 건 A·E뿐이라는 제보에서 시작. 원인은 스크리너 캐시가 KIS **시세·수급** 데이터라 재무 항목이 한 줄도 없었던 것. `scripts/build-financial-ratios.mjs` + `.github/workflows/financial-ratios.yml`(주 1회 일요일 08:30 KST + `repository_dispatch: financial-ratios`)이 DART 사업보고서에서 상장사 전체를 훑어 `data/dart-financial-ratios.json`으로 커밋한다(**2,858종목 · 357KB** 실측, FY2025 2,788 + FY2024 70). **계산은 전부 코드가 하고 AI 추정값은 하나도 없다.** 분모가 0 이하면 비율을 만들지 않는다(자본잠식 기업의 부채비율은 음수가 나와 "부채가 적은 것"처럼 보인다). 연결(CFS) 우선·없으면 개별(OFS). **이자보상배율은 의도적으로 미지원** — 이자비용이 주요계정에 없어 종목당 전체 재무제표 API를 따로 불러야 하는데 상장사 전체엔 호출량이 과하다. **핵심 설계: 시세 캐시와 파일을 분리하고 검색 시점에 메모리에서 조인한다**(`tsLoadFinancialRatios`) — 갱신 주기가 완전히 다르기 때문이다(시세 평일 16:00 / 재무 연 1회). 합치면 재무가 바뀔 때마다 8MB 시세 캐시를 다시 만들어야 하고, 반대로 시세 캐시를 다시 만들기 전엔 재무가 반영되지 않는다. **즉시검색(`api/analyze.js`)과 감시알림(`scripts/trade-signal-scan.mjs`) 양쪽에 같은 조인을 넣었다** — 한쪽만 넣으면 "검색해서 나온 조건인데 알림은 영영 안 오는" 상태가 된다 ③**미지원 조건을 조용히 버리던 구멍 차단**(`tsCollectUnsupportedTypes`) — `tsNormalizeClause`가 모르는 type에 null을 돌려주고 호출부가 `.filter(Boolean)`으로 걸러내서, 모델이 "이해했다"면서 미지원 조건을 끼워 보내면 **서버가 그 조건만 말없이 버리고 나머지로 검색**했다(사용자는 5개 조건이 다 걸린 줄 안다). 파싱 가이드에 "일부만 반영하지 말 것"이 이미 있었지만 모델 준수에만 기대면 안 되므로 서버가 직접 확인해 거절한다. **감시저장 경로(`tsNormalizeParseResult`)에도 동일 적용 — 이쪽이 더 위험하다**(조건이 빠진 채 저장되면 원하지 않은 시점에 매수/매도 알림이 계속 간다). 자주 요청되는 미지원 지표 13종은 한글 이름으로 무엇이 빠졌는지 알린다(`TS_UNSUPPORTED_LABELS`). 라이브 검증: 사용자의 A~E 원문을 그대로 넣으면 "이자보상배율은 지원하지 않는다"고 명시하며 거절, D만 빼면 **70종목** 정상 반환. **(2026-09-08 밤) 매매기법 프리셋 3종 + 장 막판 종가 스캔 경로 신설** — ①**"매매기법으로 찾기" 프리셋 3종**(종가 수급매매 / 수급 눌림목 / 신고가 돌파). 조건 문장을 직접 만들 줄 아는 사용자만 쓸 수 있던 기능을 "누르면 나오는 전략"으로 바꾸는 진입점이고, 구조화된 condition을 그대로 보내 **AI 파싱을 건너뛴다(응답 빠름·토큰 0)**. 임계값은 실제 캐시(9/7 종가, 2,765종목)로 결과 개수를 확인해 정했다 — 매일 0개가 나오면 기능이 죽고 너무 풀면 스크리너가 아니게 된다. 라이브 실측 눌림목 23종목·돌파 9종목 ②**조건 5종 신설** — `foreign/institution_net_buy_amount`(순매수 금액, 억원), `foreign/institution_net_buy_streak`(연속 순매수 일수), `major_net_buy_streak`(외국인·기관 양매수 연속 일수). `inquire-investor` 응답이 원래 최근 여러 영업일치를 배열로 주는데 첫 행만 쓰고 버리고 있던 것을 활용 — **KIS 추가 호출 0회** ③**장 막판 종가 스캔**(아래 별도 항목) ④버그 3건 — `volume_ratio`가 gt/gte만 처리하고 lt/lte를 조용히 gte로 취급(="거래량 줄어든 종목"이 정반대 결과를 주던 상태, 눌림목 조건 자체가 불가능했던 원인), 조건 요약이 `>`와 `≥`를 뭉뚱그려 "외국인 순매수 ≥ 0주"처럼 경계값 포함으로 읽히던 표기, 순매수 0주 초과를 수식으로 노출하던 문구 |
| **종가시그널**(구 종가베팅) | `close-signal.html`, `lib/close-betting-score.js`, `lib/close-signal-strategies.js`, `lib/close-signal-results.js`, `lib/close-betting-catalyst.js`, `scripts/intraday-close-scan.mjs`(1520 슬롯), `scripts/close-betting-review.mjs`, `scripts/close-signal-backfill.mjs`, `api/analyze.js?feature=close-betting`, Supabase `close_signal_results` | **(2026-09-12 신설 · 09-18 개명 · 09-21 v2 전면 개편)** 운영 중. 화면 문구는 "종가시그널", 코드·인프라 이름은 `close-betting` 그대로다(5장 개명 규칙). **정규장 종가(15:20~15:30) 매수 → 익일 시가 또는 익일 종가 매도** 전제로 거래대금 상위 400종목을 채점해 **상위 5종목**을 보여주고, **다음 거래일 결과를 매일 기록해 달력으로 공개**한다. **(2026-09-21) v1을 갈아엎은 이유는 실측이다** — 9/14~9/18 저장된 1520 스냅샷으로 되짚으니 v1 상위 10종목의 익일 시가 수익률은 +0.67%인데 **상위 3종목은 -0.07%(종가 -1.33%)로 전체보다 나빴다.** 점수가 높을수록 결과가 나빴다는 뜻이라 순위 변별력이 없었다. 팩터별로 갈라 보니 v1이 만점을 주던 항목(종가위치 0.9 이상·윗꼬리 20% 이하·완전 정배열·60일 신고가·5일 누적 10% 미만·막판강도 0% 이상)이 전부 실측에서 반대로 움직였다. **핵심 설계 3가지**: ①성격이 다른 **매매기법 9종**(`lib/close-signal-strategies.js` — 기관 동반 강세 / 외국인·기관 양매수 / 갭업 지속 / 중소형 회전 집중 / 모멘텀 연장 / 코스닥 주도 강세 / 장기 추세 상위 / 대금 집중 / 재료 동반)을 **독립적으로 돌려 몇 개에 동시에 걸렸는지(합의도)를 최대 배점 축(35점)으로** 삼는다 — 실측 합의 0개 시가 -0.56% / 3개↑ +0.87% / 4개↑ +0.91%로 단조 증가한다 ②5거래일 표본으로 배점을 **뒤집지 않았다** — 근거가 약한 팩터는 반대로 주는 대신 **배점에서 뺐고**, 실측과 이론이 함께 가리킨 것(기관 순매수·양매수·갭 상승 출발·중소형·1년 모멘텀)만 남겼다 ③재료(DART) 20점은 **분모에서 빼고 가산점 최대 10점**으로 돌렸다 — 재료가 없는 날이 대부분이라 분모만 커지고 점수대 전체가 눌렸다. 배점은 **기법 합의 35 · 수급 25 · 당일 흐름 20 · 추세 위치 20 + 재료 가산 10**. 하드필터도 조정(시총 하한 1,000→800억, 등락 상한 25→20%, 종가위치 0.6→0.55)했고 **기법이 0개인 종목은 상위에 올리지 않는다**. **성과 추적(이번 개편의 본체)**: 전용 테이블 `close_signal_results`(as_of_date PK, 하루 한 행)에 익일 **09:10 시가(phase=open) / 15:45 시가·고가·저가·종가 확정(phase=close)** 2단계로 기록한다. 화면은 히어로 누적 성적 → 오늘의 5종목(기법 칩 + 펼치면 근거) → 직전 선정 결과표(매수가 → 다음날 시가/종가 → 각 매도 수익률 + 하루 고·저) → **성과 달력**(거래일별 평균, 시가/종가 전환, 날짜 클릭 시 그날 5종목) → 기법 카탈로그. **과거 성과 기록은 잠그지 않는다** — 무료 상위 3 / Pro 5는 오늘 랭킹에만 적용한다(전환의 유일한 근거라서). 저장된 스캔으로 9/14~9/18을 KIS 일봉 실값으로 백필했고 `source='backfill'`(화면 회색 점)로 실기록과 구분한다. **백테스트(같은 5거래일 상위 5종목)**: 시가 +0.91%(승률 60%) · 종가 +3.24%(승률 56%) · 플러스 거래일 4/5 (v1 상위 5종목은 시가 +0.56%/60%, 종가 -0.06%/44%, 3/5). |
| 매매시그널 — 장 막판 종가 스캔 | `scripts/intraday-close-scan.mjs`, `lib/intraday-snapshot.js`, `.github/workflows/intraday-close-scan.yml`, Supabase `trade_signal_intraday` | **(2026-09-08 밤 신설, 첫 자동 실행은 9/9 14:30)** 전종목 캐시는 빌드에 80분이 걸려 하루 1회(그것도 밤늦게)뿐이라 **장중에 검색하면 전날 종가가 나왔고**, 15:30 마감 직후 NXT 시간외 종가매수를 노리는 "종가매매"에는 쓸 수 없었다. 전종목 재계산 대신 후보를 좁히고 오늘 봉 하나만 얹는 경량 경로를 만들었다. **핵심 설계**: 밤 캐시 빌드가 각 종목에 `snapshot.next`(20일 평균거래량 / 이동평균에서 빠져나갈 종가 / 기간수익률 기준가 / 구간 최고가·최대거래량)를 미리 접어두므로, 장중엔 **일봉을 다시 안 받아도 된다** → 종목당 시세 1회 + 수급 1회, **총 2회**면 끝. 400종목 4~5분(15:31 트리거 → 15:36 반영). **정확도**: 난수 300봉 시계열 20회 시뮬레이션에서 전체 재계산 결과와 **560개 검사 전부 일치**(근사가 아니라 동일값). **갱신되는 것**: 종가·등락률·거래대금·거래량비·이동평균·이격도·신고가·기간수익률·수급(연속일수 포함). **전일 값 유지**: RSI·MACD·볼린저·스토캐스틱·ADX·캔들패턴·다이버전스(시계열 전체가 필요해 2회 호출로는 불가) — 무엇이 갱신됐는지는 `lib/intraday-snapshot.js`의 `INTRADAY_UPDATED`/`INTRADAY_STALE`에 명시돼 있고 프리셋 3종은 전부 갱신되는 지표만 쓴다. **후보 선정**: KIS 순위 API가 한 번에 30건씩만 준다(실측 코스피+코스닥 59건). 거래대금순만 보면 오늘 처음 터진 중소형주가 상위 30위 밖이라 통째로 빠지는데 종가매매가 노리는 자리가 정확히 거기라, 정렬 기준 3종(거래금액순·거래증가율순·평균거래량순) × 2시장 6회 호출로 132건까지 넓히고 나머지는 전일 거래대금 상위로 채운다. **저장은 git이 아니라 Supabase** — data/ 커밋은 Vercel `ignoreCommand` 때문에 별도 배포가 필요해 2분이 더 들고 Hobby 배포 한도까지 깎는다. **정시 트리거는 pg_cron 경로**(아래 5장) — 실측 20초 이내 실행. 화면에는 "오늘 15:31 장 막판 기준 · 거래대금 상위 400종목 · **수급은 잠정치**"로 표기(장중 외국인·기관 수급은 확정치가 아니다). 오늘 스캔이 없으면 전일 종가로 자동 폴백하고 응답 `dataSource`로 어느 쪽인지 알린다 |
| 미국주식 | `us-market.html/.js` | 운영 중. 랭킹/상세/차트, 자체 캔들차트 전환 완료. **(2026-08-11)** `api/us-market-data.js`의 등락폭(changePoints) 계산이 잘못된 거래소(NAS/NYS) 데이터로 오염되면 등락률과 모순되는 값(예: META "+575.18" 그대로 표시되면서 등락률은 0%)을 낼 수 있던 버그를 `resolveChangePoints`에 교차검증 가드 추가로 수정(전종목 공통 방어). **(2026-08-26 3차 후속 세션)** AI종목분석/실시간시세에서 발견한 검색 정렬/스크롤 버그(위 항목 참고)가 이 페이지(`filterLocalRows`)에도 동일하게 있어(관련성 순위 없이 substring 필터만 함, 8개 하드컷+스크롤 불가) 같은 점수체계로 통일한 `rankUsMatches` 신규 추가해 수정(`225781eb`). 합성데이터로 "AAPL"/"Apple" 검색 시 진짜 애플이 항상 1위, AAPD/AAPU 같은 레버리지 상품은 후순위로 정렬됨을 오프라인 검증 — 배포는 Vercel 한도소진으로 지연. **(2026-09-03)** 표 위에 공통 시장 요약 스트립 추가(위 "시장 요약 스트립(공통)" 항목 참고). 이 페이지는 지수 API를 따로 받지 않으므로(함수 12개 한도) 지수 칩 없이 브레드스+특징 종목만 표시 — 실사이트 실측: 시총 상위 50종목 중 상승 29·하락 21, 메타 +3.14% / 팔란티어 −3.14% |
| **시장 요약 스트립(공통)** | `assets/market-strip.js`, `assets/market-strip.css` | **(2026-09-03 신설)** 감사 지적 "시세 3종 + 글로벌랭킹이 50~100행 원시 표뿐이라 네이버금융과 차별점 0"에 대한 대응. 표 위에 **지수 칩 / 등락 브레드스 막대 / 특징 종목 칩**을 얹는 공통 컴포넌트 하나로 4개 페이지를 동시에 개선했다(`TMStrip.render(target, model)` + `TMStrip.countBreadth(rows, pick)`). **설계 원칙 3가지**: ①**새 API를 만들지 않는다** — Vercel Hobby 함수 12개가 이미 꽉 차 있어(`api/*.js` 11개 + `api/payment/complete.js`) 각 페이지가 **이미 받아 둔 state 데이터로만** 계산한다 ②**표본을 속이지 않는다** — 브레드스를 '상승률 TOP100' 탭으로 세면 당연히 100건 전부 상승이라 거짓말이 되므로 거래대금/시가총액 상위를 표본으로 쓰고, `"거래대금 상위 100종목 기준"`처럼 **무엇을 센 것인지 라벨로 명시**한다(전체 시장인 척하지 않음) ③**실데이터가 없으면 블록을 통째로 감춘다**(`el.hidden`). 페이지별 연결: 실시간시세(`renderSummaryStrip`, 지수는 `state.indexes`, 브레드스는 `tvTop100`→없으면 `capTop100`), 미국주식(`rowsByTab`, 지수 없음), 암호화폐(`state.coins` TOP400 24H + BTC 칩, 기존 `crypto-global` 카드 아래), 글로벌랭킹(TOP100 + 국내 기업 진입 수). 상승률/거래대금 탭이 아직 로드되지 않았을 때도 현재 표본에서 최고 상승·최저 하락을 뽑아 **항상 양쪽을 보여준다**(커밋 `59b5e57`) |
| 세계시장/글로벌랭킹 | `world-market.html` | 운영 중. TOP100 캐시(`data/world-market-cache.json`), 삼성전자·SK하이닉스 시총 오류 수정(KIS 실시간 값으로 덮어쓰기 로직 추가). **(2026-09-03)** 표 위에 공통 시장 요약 스트립 추가(위 항목 참고) — 실사이트 실측: 상승 32·하락 66, 상승 1위 노바티스 +6.04%, 국내 기업 2개 진입 |
| 암호화폐 | `crypto.html` | 운영 중. TOP400, FDV 컬럼, 7일 스파크라인, 뉴스 탭, 정렬기준 버그(rank 대신 marketCap) 수정 완료. **(2026-09-03)** 도미넌스·시총 카드만으로는 "지금 코인판이 오르는 장인지"를 못 읽어서 등락 브레드스 스트립 추가 — 실사이트 실측: 상승 124·보합 4·하락 272, BTC 24H −1.49% |
| 회원가입/로그인/마이페이지/요금제 | `signup.html`, `login.html`, `mypage.html`, `pricing.html`, `reset-password.html`, `auth-callback.html`, `consent.html`, `assets/auth.js`, `supabase/functions/naver-auth/` | **(2026-09-04) 인증 전면 강화 — 커밋 8건.** ①**소셜 로그인 3종**: 구글·카카오는 Supabase 기본 provider라 클라이언트 코드만으로 되지만, **네이버는 Supabase가 지원하지 않아 서버 코드가 필수**인데 **Vercel Hobby 함수가 이미 12/12로 꽉 차** 있었다 → `supabase/functions/naver-auth/index.ts`(Deno)로 분리해 Vercel 슬롯을 쓰지 않음. 흐름: 네이버 authorize → Edge Function이 code 교환·프로필 조회 → admin API로 계정 생성/조회 → **1회용 magiclink `hashed_token`** 발급 → `/auth-callback.html#naver=…`로 302 → 브라우저가 `verifyOtp`로 세션 개시. state에 nonce+next를 담아 sessionStorage와 대조(변조 방지). **⚠️ `--no-verify-jwt`로 배포해야 함**(네이버가 인증 헤더 없이 호출). `SUPABASE_SERVICE_ROLE_KEY`가 deprecated 처리되고 있어 신규 `SUPABASE_SECRET_KEYS`(JSON 사전)도 함께 읽는 폴백 내장 ②**노출 스위치** `assets/auth-config.js`의 `SOCIAL_ENABLED{google,kakao,naver}` — 콘솔 설정이 안 끝난 provider의 버튼이 떠 있으면 눌렀을 때 에러만 나서 이탈로 이어지므로 기본 off. 현재 **naver만 true** ③**비밀번호 정책 8자 + 영문·숫자**(기존 6자), 확인 입력·표시 토글·실시간 강도바. 판정은 `TMAuth.validatePassword()` 한 곳이 기준점이며 **Supabase 대시보드의 서버 측 정책과 값을 맞춰둬야 실효**가 있다 ④**비밀번호 찾기/재설정 신설**(`reset-password.html`) — 이전엔 복구 수단이 아예 없어 비번을 잊으면 계정이 영구 잠겼다. 메일 요청/새 비번 설정 2모드 겸용, 해시 토큰·PKCE `code` 양쪽 지원, 발송 안내는 **계정 존재 여부가 새지 않도록 항상 동일 문구** ⑤**약관·개인정보·만14세 필수 동의 + 마케팅 선택 동의**를 `user_metadata`에 시각까지 기록(`consent_version`). 소셜 최초 가입자는 동의 이력이 없으므로 `consent.html`로 유도(회원가입 페이지에서 소셜을 눌렀을 땐 동의 내역을 localStorage에 잠시 보관했다가 콜백에서 기록) ⑥마이페이지에 **로그인 방식 표시·비밀번호 변경 버튼·마케팅 수신 토글** 추가 ⑦로그인 실패 메시지 한글화. **미완료(4장 참고)**: 구글·카카오 콘솔 키 발급, Supabase Redirect URLs 등록, 서버 측 비밀번호 정책, 네이버 검수요청. — 이하 이전 기록: 운영 중(2026-07-09 구축, 07-10 안정화). Supabase 인증, 모바일 반응형 포함. **결제는 현재 비활성(테스트 채널만 구축). 유사투자자문업 교육 수료 → 사업자등록 완료 후 정식 오픈 예정**. **(2026-09-03) Premium 상품 정의 — 문구가 아니라 상품 정의 문제였음**: 감사에서 "Premium(19,900원)에 실질 차별점 없음"으로 지적됐고, 코드를 열어보니 **Pro와 Premium이 `hasProAccess` 하나로만 갈려 권한이 완전히 동일**했다(매매시그널 감시 슬롯도 등급 무관 전원 20개). ①`assets/auth.js`의 `TM_AUTH_STATE`에 **`hasPremiumAccess`** 추가 — 앞으로 게이팅을 만들 때 `hasProAccess`(Pro 이상)와 구분해서 쓸 것 ②**지금 실제로 작동하는 차별점**: 매매시그널 감시 슬롯을 `api/analyze.js`의 `TS_MAX_ACTIVE_STRATEGIES_BY_PLAN = { pro: 5, premium: 30 }`으로 등급 차등화(`tsRequireProUser`가 `user.tmPlan`을 실어 보내고 저장 시 상한 검사). Pro가 상한에 닿으면 에러 메시지와 `내 전략 (5/5)` 옆 링크로 Premium 안내. 화면 상수(`trade-signal.html`의 `TS_STRATEGY_LIMIT_BY_PLAN`)와 서버 상수는 **반드시 같은 값으로 유지**할 것 ③`assets/pricing-config.js` 특전 전면 재작성 + 아직 동작하지 않는 3축(AI 종목분석 심화 리포트 / 관심종목 데일리 리포트 / 과거 데이터·백테스트 열람)은 `{ text, soon: true }`로 표기해 **"준비 중" 배지**가 붙게 함 — 결제 오픈 전이라 없는 기능을 있는 것처럼 파는 문구를 넣지 않기 위한 장치(`pricing.html`의 `featureLi()`가 처리). 사용자가 고른 Premium 4축 중 슬롯 확대만 구현됐고 나머지 3축은 4장 할 일로 이월. **(2026-09-02) 요금제 페이지에서 고객에게 노출되던 내부 사정 문구 3곳 정리** — "결제는 포트원(PortOne) 테스트 결제 모듈로 진행됩니다", "⚠️ 결제 시스템 설정이 아직 완료되지 않았습니다 (테스트 준비 중). 관리자에게 문의해주세요", 결제 시도 시 alert("결제 모듈이 아직 설정되지 않았습니다") → 전부 "유료 플랜 결제는 정식 오픈 준비 중입니다" 계열의 고객용 안내로 교체(5장 "내부 사정 문구 노출 금지" 규칙 위반이었음) |
| 법적 페이지 | `terms.html`, `privacy.html` | 운영 중(12개 조항+면책조항, 9개 섹션 개인정보처리방침), 전체 페이지 푸터 링크 연결 완료 |
| 인스타그램 자동 발행 | `scripts/promo-build-deck*.mjs`, `promo-render-carousel.mjs`, `promo-carousel-slides.mjs`, `promo-carousel-css.mjs`, `promo-deck-ai.mjs`, `promo-publish-carousel.mjs` / `.github/workflows/instagram-carousel-*.yml` | **(2026-09-07) 카드뉴스 v2 — 릴스에서 캐러셀로 전면 전환, 무인 자동 발행 중.** 현재 스케줄: **아침 브리핑 평일 08:03(5장) · 마감 시황 평일 17:26(6장) · 글로벌 랭킹 토요일 10:30(5장)**, 규격 1080×1350. 릴스 워크플로 3종(`instagram-card-post*.yml`, `instagram-globalranking-post.yml`)은 **스케줄만 끄고 수동 실행은 남겨 롤백 경로 유지**. **파이프라인**: 데이터 → `promo-build-deck*.mjs`가 덱 JSON 생성 → `promo-render-carousel.mjs`가 PNG 렌더 → 커밋·push → `promo-publish-carousel.mjs`가 Graph API 캐러셀 발행. **핵심 설계 3가지** ①**숫자는 전부 코드가 계산하고 문장만 AI가 쓴다** — 거래대금·비중·수급·지수·환산시총을 코드가 만들고 그 값을 프롬프트에 통째로 주입해 화면 도표와 본문이 어긋날 여지를 없앰. AI에게 숫자를 만들게 하지 않는다 ②**Claude → OpenAI(gpt-4o) → 코드 폴백 3단**이라 AI가 전부 죽어도 발행이 멈추지 않는다 ③**훅 유형을 코드가 데이터로 판정**하고 각 유형은 2번 카드(답)와 짝이 맞게 설계 — 마감 A(쏠림, 상위2 거래대금≥45%)/D(불일치, 지수와 개인수급 반대)/E(경고, 급등 3종목↑)/C(범인지목), 아침 EWY(±2%↑)/SPLIT(지수와 반도체 반대)/SAFE(위험+안전 동반상승)/SYNC, 랭킹 ABSENT/PRESENT/GAP. **카드 구성**: 1번은 항상 훅, 마지막은 항상 CTA, 가운데만 슬롯별로 다름(마감=쏠림공개/AI판단/돈의흐름+수급/특징주+리스크, 아침=간밤지수/AI전망/오늘볼것, 랭킹=TOP10/국내기업위치/주간등락). **차별점은 "리스크를 같이 쓴다"** — 급등 이유만 말하는 계정과 구분되는 지점이고 유료 전환의 씨앗이다. **타이포는 아이폰 실기기 기준으로 역산**(1080px 카드가 iPhone 피드에서 약 390pt로 축소돼 배율 0.36 → 기존 본문 24px는 손에서 8.7pt, 읽기 하한 13pt로 잡아 34~36px로 상향). **우상단에 요소를 두지 말 것** — 인스타 캐러셀이 자체 페이지 배지(1/6)를 덮어 그린다. 폰트는 `templates/fonts/PretendardVariable.woff2`(OFL 1.1) 저장소 포함(CDN 의존 시 렌더 시점 네트워크에 매일 발행이 걸린다). 렌더러는 **콘텐츠가 1350px를 넘으면 발행을 실패시킨다**(글자 잘린 채 나가는 게 최악). — 이하 이전 기록(릴스 시절): 모닝브리핑(평일 08:30 KST)·마감시황(평일 17:30 KST) 릴스+스토리, 글로벌랭킹(매주 토요일 10:00 KST) 릴스+스토리. 포맷은 5장 카드뉴스 캐러셀 → 1장 세로 릴스로 전환됐었음(아래 "포맷 변천사" 참고). **(2026-08-11)** 모닝브리핑 문구(`scripts/promo-morning-copy.mjs`)에서 코인베이스·팔란티어처럼 시총 상위가 아닌 종목이 단순히 등락폭이 컸다는 이유로 "미국 대형주"로 잘못 라벨링되던 문제 수정 — `data/world-market-cache.json` 기반 실제 미국 시총 TOP20을 동적으로 계산해 그 안에 든 종목만 대형주로 언급하도록 변경(정적 20종 폴백 포함). **(2026-08-13)** 글로벌랭킹 릴스 **TOP20 → TOP10으로 축소 + 리디자인**(사용자 요청: "좀 더 임팩트 있고 화려하게") — 골드 그라데이션 타이틀·1~3위 메달 이모지로 시각 강화, 삼성전자·SK하이닉스 등 TOP10 밖의 국내(한국) 기업은 별도 "국내기업" 섹션으로 분리해 국내순위 배지 + "글로벌 N위" 텍스트 배지로 표시, 사용자가 "지저분하다"고 지적한 순위변동(▲/▼) 화살표 배지는 완전 제거(관련 history 계산 로직도 삭제). `templates/card-globalranking-reel.html`·`scripts/promo-render-globalranking.mjs`·`scripts/instagram-card-post.mjs` 수정, 사용자 승인 후 실제 릴스+스토리 발행까지 완료·확인(아래 6장 상세) |
| 텔레그램 자동 발송 | `scripts/telegram-*.mjs` | 운영 중. 마감시황(push 트리거)·장전브리핑·주간일정 발송. 문장 중간절단·4096자 초과 발송실패 버그 수정 완료 |
| X(트위터)/Threads | (2026-08-09 완전 제거됨) | main의 `twitter-post.yml`/`twitter-post.mjs`/`promo-oauth1.mjs` 삭제, `package.json`의 `post:twitter` 스크립트도 제거. 제거 전까지 계정/토큰 없이 평일 09:00~17:30 KST 30분 간격으로 계속 실패만 하고 있었음. 브랜치 `promo/instagram-x-cardnews`엔 초기 설계 코드가 남아있으나 main에 병합된 적 없는 죽은 브랜치(정리 여부 4장 참고) |
| 정기 헬스체크 | `scripts/health-check.mjs`, `.github/workflows/health-check.yml` | 신규 운영 중(2026-08). 매시 5분 실행, 7개 항목 점검(2026-08-09: package.json 문법 체크 추가, **2026-08-12: 장전 브리핑 발행 스탬프 체크 추가**), 이상 시에만 텔레그램 개인 DM(@mandoo_market_bot) 알림+3시간 리마인더+복구알림. 과거 있었던 `daily-report-watchdog.yml`(미커밋 상태로 남아있던 초안)을 사실상 대체. **(2026-09-18)** 인스타 감시 항목이 이미 없어진 `morning`/`closing` 슬롯을 보고 있어 9/7~9/8 옛 데이터로 거짓 경보를 계속 냈다 — 현재 구조인 `carousel-morning`·`carousel-v2`·`carousel-ranking`으로 교체, 주간 랭킹용 `saturdayOnly` 플래그 추가, `generated/health-check/state.json` 초기화 |
| package.json 무결성 감시 | `.github/workflows/validate-package-json.yml` | 신규 운영 중(2026-08-09). package.json이 main에 push될 때마다 즉시 JSON 문법 검사 → 깨지면 텔레그램 즉시 알림 + 워크플로우 자체를 실패 표시. 정기 헬스체크(최대 1시간 지연)보다 훨씬 빠른 1차 안전망 |
| 종목리스트 자동 업데이트 | `.github/workflows/update-stock-list.yml`, `scripts/update_stock_list.py` | 운영 중(매일 KST 08:00). **(2026-08-11)** KRX KIND 다운로드가 간헐적으로 403을 반환(약 10% 확률)해 워크플로우가 빨간 X로 실패하던 문제를 재시도(5회, 지수백오프)+소프트-fail(완전 실패해도 경고만 남기고 초록으로 종료, 다음날 자동 재시도) 방식으로 보강 |
| GitHub 저장소 홈페이지(README) | `README.md`(repo root), `package.json` | **(2026-08-11)** 예전에 "간단 계산기" 템플릿으로 쓰던 프로젝트를 재활용하면서 남아있던 계산기 소개 문구를 TotalMoney AI 실제 소개로 전면 교체. `package.json`의 `name`/`description`도 함께 정리(코드에서 참조하는 곳 없음, 기능 영향 없음 확인). ※ `docs/README.md`는 별개 파일로 여전히 무관한 계산기 문서 상태 — 7장 참고 |
| 종목 아이콘(국내·해외) | `assets/stock-logo.js`, `assets/logos/kr`·`us`, `scripts/build-stock-favicons.mjs`, `scripts/build-stock-logos.mjs`, `scripts/build-us-logos.mjs` | **(2026-09-13 신설)** 표 앞에 종목 아이콘을 붙여 눈이 줄을 잡게 한다. 3단 폴백 — ①회사 공식 파비콘(국내 145) ②FMP 로고(국내 나머지 + 미국 107) ③브랜드색 이니셜/티커 배지(커버리지 100%). 국내 **503종목**(2026-09-18 기준), 미국·해외 **107종목**. **(2026-09-18)** 재벌 그룹 계열사가 각자 다른 파비콘을 쓰던 것을 **그룹 대표 마크로 통일**(삼성 → 삼성전자 S / SK → SK스퀘어 / HD → HD현대 / 현대차 → 현대자동차 / LG → LG전자 / 카카오 / LS), 우선주·지주사까지 채움. **런타임에 외부 CDN을 링크하지 않는다** — 전부 우리 저장소 파일이다. manifest에 있는 코드에만 `<img>`를 요청해 404가 아예 안 난다. 주간 갱신은 `.github/workflows/stock-logos.yml`(월 04:00 KST, favicons → stock-logos → us-logos 3단계). 붙어 있는 곳: 홈 카드·실시간시세·마감시황 종목순위·매매시그널 검색결과·미국주식·세계 시총 랭킹 |
| **다크모드 팔레트("Jet Black & Steel") + 네비 인터랙션** | `theme.css`, `refresh.css`, `site-shell.css`, `realtime-board.js/.css`, `stock-analysis.js/.css`, `assets/lw-candle-chart.js`, 각 페이지 인라인 `<style>` | **(2026-09-20)** 시우가 참고 사이트(four.meme)를 보여주며 "배경은 완전 검정, 박스는 다크그레이"를 요청 — `theme.css`의 `[data-theme="dark"]`를 무채색(zinc) 배경 + 에메랄드 그린 포인트로 전환(`--body-bg`/`--header-bg` #000000, `--card-bg` #161616, `--border-color` #262626, `--accent-gold`는 이름은 그대로 두고 실제 값만 에메랄드 #10b981로 재정의). `refresh.css`가 마지막에 `!important`로 body 배경을 덮어써서 `--r-bg` 계열도 같이 고쳐야 실제 화면에 반영됨. 이후 스크린샷 제보로 잔존 네이비/녹색 틴트를 여러 차례 스윕 — 마감시황 달력 패널, 주간일정 KOSDAQ 배지, 매매시그널/AI종목분석/종가시그널 히어로 카드 글로우, 실시간시세 테이블 테두리, 미국주식 캔들차트 배경(원인: `assets/lw-candle-chart.js`의 자체 캔버스 렌더러가 CSS 팔레트와 무관하게 동작), 실시간시세 로딩 스플래시(2차례). **핵심 발견**: `realtime-board.js`(실시간시세·매매시그널 공용)와 `stock-analysis.js`(AI종목분석)가 `assets/lw-candle-chart.js`를 재사용하지 않고 **각자 차트 테마 코드를 따로 복붙**해 갖고 있어서, 캔들차트 파일 하나만 고친 게 이 페이지들 차트·OHLC 툴팁·AI종목분석 검색창(네이비 rgba(9,17,33))에는 전혀 반영되지 않고 있었다 — `grep -rl "getLwTheme"`로 실사용처를 확인해 3곳 모두 동일하게 정리(crypto.html/us-market.html 차트 컨테이너, us-market.html 랭킹 테이블 고정열 포함). **네비 메뉴 인터랙션 개편**(사용자 요청) — 호버 시 색상만 바뀌어 클릭 가능한 요소로 안 느껴진다는 지적으로 은은한 배경 틴트 + `translateY(-1px)` 들림 애니메이션 추가. 현재 페이지 메뉴에 채워지던 녹색 배경(라이트 `rgba(5,150,105,.12)`/다크 `rgba(16,185,129,.16)`)도 제거하고 무채색 필 배경 + 하단 accent 밑줄 바로 교체(로고/홈 버튼은 제외). 전부 Vercel 배포 확인 후 Claude-in-Chrome 강제 다크테마 렌더 + `getComputedStyle` 대조로 실측 검증 완료(6장 상세). |

### 2026-09-23 추가 반영 (요약 — 상세는 6장)

- **디자인 마감 레이어 `premium.css`** — 21개 페이지 `refresh.css` 다음 줄에 링크. 표면 체계(바탕 한 색·카드 한 겹·보조 면), 서체(모노 제거), 다크 주요 버튼 대비, 등락 <em> 색, 모바일 여백 정렬을 여기서만 덮어쓴다. 토큰: `--p-page/--p-card/--p-inset/--p-border/--p-line/--p-radius`.
- **로고 = 스텝 마크** — 헤더 인라인 SVG 21개, 파비콘·앱 아이콘은 에메랄드 타일+검정 막대(16/32px는 `assets/favicon-small.svg` 확대판). 캐시버스터 v8.
- **일정(weekly-market)** — `wkDayCardHtml()` 하나로 선택일·이후 날짜 카드를 그린다. 모바일(≤960px) 사이드 숨김 + `#wk-mtop`(다음 핵심 지표·가로 날짜 띠). 신규상장은 앰버 칩·행 강조.
- **차트** — `realtime-board.js`(1패널)·`assets/lw-candle-chart.js`·`stock-analysis.js` 모두 매매시그널 설정(배경 투명·세로격자 없음·하락 #2d7ff9·거래량 0.55·가격선 없음·200일선 --text-muted-ui).
- **글로벌랭킹** 표 머리 한글화, 시세 없는 종목 "$0"→"—".

### 2026-09-24 심야 추가 반영 (요약 — 상세는 6장)

- **구글 로그인 가동** — GCP 프로젝트 `totalmoney-507508`, OAuth 클라이언트 "TotalMoney Web (Supabase)", 동의 화면 **프로덕션 게시**, Supabase Google Enabled, `SOCIAL_ENABLED.google=true`. 같은 이메일은 기존 계정에 자동 연결. 마이페이지 '로그인 방식'은 연결된 수단 전부 표시(`state.providers`).
- **상단 메뉴(GNB)** — `[시장▾] 실시간시세 미국주식 암호화폐 매매시그널PRO 종가시그널PRO AI종목분석PRO`. "시장" 드롭다운(`#home-nav-market`, `.home-nav__group`) 하위 = 시장지표·장전브리핑·마감시황·일정·글로벌 랭킹(아이콘+설명 카드). 항목 아이콘은 21px 초록 타일, 데스크톱 홈 버튼 숨김(로고가 홈), 좌우 여백 24px 대칭, 더보기는 넘칠 때만 흐름에 들어옴(absolute). 21개 HTML 일괄 치환 + `site-shell.js`(`bindNavGroups`, NAV_PRIORITY 재설정, 더보기 예약폭 보정) + `site-shell.css` 끝부분. 모바일 전체보기 시트는 미변경.
- **종가시그널 결과표** — PC: 선정번호 | 종목명(+코드·점수·기법) | 수익 순위 | 숫자. 수익 순위 배지: 상승 빨강▲ / 하락 파랑▼ / 변동없음 '–', 최소폭 통일(기호는 span — em은 전역 초록 규칙에 칠해짐). 모바일은 배지를 종목명 아래 전체 폭 한 줄.
- **리뉴얼 프로토타입 `/lab-realtime.html`**(메뉴 미연결·noindex, 단독 CSS) — 최대 1760px 전체 폭, 왼쪽 메뉴(시세/시장/AI 도구), 오른쪽 레일(종가시그널·누적 기록), 국내 **전 종목 4,305개**(주식 2,765·ETF 1,175·ETN) 시총/거래대금/상승/하락/거래량 정렬, 코스피·코스닥·주식/ETF/ETN 필터, 검색, 종목→AI분석.
- **`data/kr-all.json`** — `scripts/sync-market-data.mjs`의 `syncKrAll()`이 네이버 모바일 시가총액 목록 전 페이지를 받아 행 배열로 저장(약 300KB), `market-data-sync.yml` 30분마다. ⚠️ 데이터 출처 이슈는 3장.

### 2026-09-25 새벽 추가 반영 (요약 — 상세는 6장)

- **개인 대시보드 `/lab-realtime.html` = 운영자 전용** — 로그인 + `is_owner()` 확인 후에만 본문 표시(아니면 "개인 도구" 안내 카드). 데이터는 Supabase `personal_snapshots`(id `kr-all`)에서 읽음. 네이버 종목 로고(ssl.pstatic.net, 실패 시 배지), NXT 가격(정규장가와 다를 때 현재가 아래), **관심종목 ★**(`personal_watchlist`, "★ 관심" 필터). 메뉴 미연결·noindex 유지.
- **개인 수집기** — Supabase Edge Function `personal-kr-collector`(verify_jwt=false, `x-cron-secret` = vault `cron_shared_secret`, `get_dispatch_secrets` RPC 재사용). 네이버 m.stock 시가총액 목록 전 페이지(6개씩 병렬) → 4,305행·약 220KB jsonb upsert. pg_cron **job 13** `personal-kr-collector-am`(`* 23 * * 0-4` UTC = 평일 08시대) · **job 14** `personal-kr-collector-day`(`* 0-10 * * 1-5` UTC = 09~19시대). 필드: code,name,mkt,price,chgAmt,pct,volume,tvMillion,mcapEok,kind,nxtPrice,nxtPct,nxtSession.
- **운영자 판별** — `public.owner_accounts`(정책 없음=클라이언트 못 읽음) + `public.is_owner()`(security definer). 현재 시우 uid `9ef8b6a9-…`(aaron20177) 1명. 개인 테이블은 전부 `is_owner()` RLS.
- **공개 저장소에서 `data/kr-all.json` 삭제** + `sync-market-data.mjs`의 `syncKrAll()` 호출 중단(함수는 참고용으로 남김), `market-data-sync.yml`의 git add 줄 제거.
- 설계 문서: Claude Docs "TotalMoney 기능 재편: 개인 도구 vs 공개 콘텐츠"(claude.ai/code/artifact/98a28708-557b-4d66-8e93-14635a3fa82a) · 라이선스 근거: 프로젝트 문서 `claude/MARKET_DATA_LICENSE.md`.

### 2026-09-25 낮~저녁 추가 반영 (요약 — 상세는 6장, Cowork 세션 4)

- **사이트 비공개(회원 승인제)** — `middleware.js`(Vercel Routing Middleware)가 모든 페이지·API 앞에서 `tm_pass` 쿠키(HMAC, 키 = sha256("tm-site-pass-v1:"+SERVICE_ROLE_KEY), 7일)를 검사. 없으면 페이지는 `/login.html?next=`, 데이터·API는 401. 쿠키는 `api/analyze?feature=site-pass`가 승인 회원에게만 발급. 점검봇 등 기계 호출은 `x-tm-machine` 헤더. Supabase `site_members`(approved/pending/blocked, auth.users 트리거로 신규가입=pending), RPC `is_site_member`·`owner_list_members`(가입일=auth.users.created_at)·`owner_set_plan`. 마이페이지 "회원 승인 관리"(운영자만, OWNER 배지, 요금제 버튼). 로그인 전·대기 화면은 메뉴·지표띠 숨김(`html.tm-guest`). robots·X-Robots-Tag noindex. **공개 재개 = middleware.js 삭제.**
- **발행 일시중지** — 인스타 워크플로 7개 잡 `if: ${{ false }}`, 텔레그램 공개 채널은 `config/public-publish-paused.json` 있으면 건너뜀(운영자 DM은 유지).
- **종가시그널** — 결과표 가격 제거 **A안 확정**(원 단위 가격 열만 삭제, 종목별 % 유지). 로그인 전 화면 = 소개 + 가입 카드(전일 성과) + 푸터. AI 종목분석 로그인 전 화면도 같은 구성, 메뉴 팝업 제거.
- **국내주식(구 실시간시세)·미국주식** — 탭 6개(시총·상승·하락 / 거래대금·거래량·거래급증), PC 1100px+ 좌목록(20개×페이지, 국내 5·미국 3)·우상세(정보표+차트) 분할(`assets/rt-split.css`), 로고. `?q=코드/티커`로 들어오면 그 종목 바로 조회(히트맵 클릭용).
- **전 페이지 전체 폭 + 상단 메뉴 11개 펼침**(시장지표·오전브리핑·마감시황·일정·국내주식·미국주식·암호화폐·글로벌랭킹·매매시그널·종가시그널·AI종목분석), 페이지 제목 배너 제거.
- **넓은 화면 규칙 `assets/wide.css`**(모든 HTML 마지막 링크, v=2) — 공통 여백 24/32px, 시장지표 2열·VIX 게이지, 브리핑/일정/마감시황 그리드, 매매·종가시그널 히어로 2열, 매매시그널 기법 가로 배치·검색결과 2열(펼친 종목 전체폭), 종가시그널 회원화면 2열, AI분석 목록 그리드, **암호화폐 좌목록+우상세(sticky)**, 마이페이지 2열·회원 상태 색.
- **차트 공용 규칙 `assets/tm-chart-style.js`** — 차트 코드 4곳(국내주식·미국/코인·AI분석·매매시그널)이 모두 사용. `fit()`: 격자 제거·국내 콤마/미국·코인 $ 눈금·거래량 구역 음수 눈금 제거. `recent()`: 일봉 400개를 한 화면에 몰지 않고 최근 구간만(폭/8봉, 과거는 드래그).
- **차트 API 안정화** — `kis-stock-quote`·`kis-realtime-data` 캔들: 동시 요청 중 KIS 호출 제한으로 실패한 창을 재시도, 그래도 빠지면 캐시 20초(엔비디아 차트가 2025-02→2026-01로 건너뛰고 6월 이후가 없던 원인).
- **홈 섹터 히트맵**(`assets/home-heatmap.js`·`.css`) — 홈 첫 화면(지수·시황 요약·버튼·실시간 AI 분석 칸 대체). 탭 코스피/코스닥/미국/암호화폐, 크기=시총·색=등락률(상승 빨강), 섹터 머리글=시총가중 등락률+급등10 평균, 이름은 자르지 않음(글자 축소→두 줄→안 들어가면 칸 제외 후 재배치), 칸 클릭→국내/미국주식 조회, 머리글에 코스피·코스닥·나스닥선물·원/달러(`/api/market-ticker`). 국내 섹터=WICS(`scripts/build-sector-map.mjs`→`data/kr-sector-map.json`, 시세 동기화가 7일마다 갱신), 미국=`data/us-sector-map.json`(GICS 수기), 코인=JS 분류표. 시세 API `kis-realtime-data?action=heatmap`(네이버 시총 6페이지, 장중 60초 캐시) — 새 서버 함수 없음.
- **AI 자동 분석 폐기** — `api/analyze.js`의 내부 시드 키(`x-internal-seed-key`) 경로 삭제. 이제 AI 종목분석은 로그인 회원이 직접 요청할 때만 돈다.
- 기타: 마감시황 휴장일 KRX 달력 판정 + 최신 리포트 표시(`lib/krx-calendar.js` 브라우저 겸용), 상단 시세 띠 실패 시 이전 값 유지, 글로벌랭킹 로고 오류 함수 노출, KIS 토큰 재발급 `repository_dispatch`+curl 재시도.

### 2026-09-26 새벽 추가 반영 (요약 — 상세는 6장, 세션 5·6)

- **홈 히트맵 머리줄** — 지수 8개 한 줄(코스피·코스닥·나스닥선물·원/달러·WTI·비트코인·삼성전자·SK하이닉스), 제목+시장 탭을 히트맵 카드 머리줄로. 모바일(≤700)은 지수 3개, 지도 높이를 첫 화면 남은 높이에 맞춤.
- **홈 리포트 줄 3칸 = 오전 브리핑 · 마감시황 · 일정** — AI 종목분석 입력 카드 삭제(상단 메뉴로 충분). 사이드의 '오늘 경제지표'를 "일정" 카드로 올려 오늘 이후 한·미 지표 5개 표시. 사이드는 '오늘의 특징주'만 남고 최대 8개.

### 2026-09-26 오전 추가 반영 (요약 — 상세는 6장, 세션 7)

- **홈 히트맵** — 섹터 면적 = 시총^0.5, 섹터 안 종목 = 시총^0.75(코스피 IT 64%→33%, 크기 순서는 유지). 범례 "크기 = 시가총액 순(큰 섹터는 줄여 표시)". **라이트 모드 대응**(지도·섹터·말풍선·범례 색, 테마 전환 시 즉시 다시 칠함).
- **읽는 페이지 넓은 화면 배치**(`assets/read-layout.css`·`.js`, 1200px 이상만, 모바일 무변경) — 오전브리핑·마감시황: 숫자 섹션(지수·환율·시장 온도)을 왼쪽 고정 칸으로, 본문은 오른쪽. 일정: 달력·요약 칸을 왼쪽으로. 폭 1640px, 본문 영역 zoom 1.14(1500px 미만 1.08). 마감시황 날짜 버튼을 탭 줄 맨 앞으로.
- **매매시그널·AI 종목분석** — 폭만 1640px. zoom은 캔들 차트 십자선이 어긋나서 안 씀 → 11~13px 글씨만 실측 목록으로 +1.5~2px. **종가시그널**은 차트가 없어 zoom 적용, 두 칸 머리글 높이 맞춤.
- **국내주식·미국주식·암호화폐** — PC 분할 화면에서 목록 페이지를 넘겨도 오른쪽 상세 유지(다른 종목 클릭 때만 바뀜). 암호화폐가 5분 폴링마다 1페이지로 돌아가던 버그 수정.
- **첫 로딩 속도** — `/api/stock-analysis?quoteOnly=1`·`/api/kis-stock-quote` 현재가 응답 CDN `s-maxage=10, stale-while-revalidate=20`. 국내주식은 삼성전자 상세를 목록과 동시에 미리 받고 같은 종목 요청을 20초 합침.

### 2026-09-26 낮 추가 반영 (요약 — 상세는 6장, 세션 8)

- **국내주식 모바일 차트** — 차트 아래 70px 빈칸 제거(모바일 차트 280px로 칸과 일치).
- **암호화폐** — 크립토 뉴스 탭·패널·`/api/crypto-news` 폐지. 넓은 화면 왼쪽 순위 폭 30%로 축소. 오른쪽 상세에 시가총액·24시간 거래대금(+변화율)·거래대금/시총·FDV·유통량(최대 대비 막대)·총/최대 발행량(무제한 표기)·시총 점유율·1시간~90일 등락·웹사이트/백서/탐색기/소셜 링크.
- **AI 종목분석** — ①상단 진입·목표·손절 = **확률이 가장 높은 시나리오 숫자 하나로 통일**(종합 의견 문장 속 옛 숫자도 교체, "기준 계획" 배지) ②**다가오는 이벤트에 실적 발표** — 예상일 파일에 있으면 그 날짜(예: SK하이닉스 10/27 예상), 없으면 정기보고서 법정 제출 기한 ③**과거 비슷한 국면 통계** — 이 종목 4년 일봉에서 오늘과 같은 조건(20일선 위/아래·20/60 배열·RSI 구간)을 코드가 세어 "N번 중 M번 상승"을 확률 막대 위에 표시, AI는 이 숫자만 인용 ④**AI 분석 성적표** — 저장된 국내 리포트의 매매 계획을 매일 실제 일봉으로 채점(목표 도달/손절/기간 종료/미진입/진행 중)하고 달력으로 표시 ⑤순이익>영업이익일 때 "본업 밖 이익" 설명 한 줄.
- **종가시그널** — "직전 선정 결과"를 표·배지 대신 "오늘의 시그널"과 같은 카드(순위 칩·이름·코드·메타 한 줄 + 오른쪽 시가/종가 매도 두 숫자)로 재구성. 요약 4칸은 히어로 성적 칸과 같은 모양, 대기 안내는 한 줄 띠로.

### 2026-09-26 점심 추가 반영 (요약 — 상세는 6장, 세션 9)

- **종가시그널** — 넓은 화면에서 "오늘의 시그널"·"직전 선정 결과" 카드 하단 높이 일치(`assets/wide.css` align-self: stretch). 직전 결과 요약 4칸 수익률 상승 빨강·하락 파랑(`.cb-rec__v` 흰색이 `.cb-up`을 덮던 것).
- **AI 종목분석(외부 AI 리뷰 반영)** — 요약 카드 맨 위 **초보자용 한눈에 보기**(좋은 점·주의할 점·앞으로 볼 가격 3개, AI `quick` 필드), 큰 "상승 확률 N%" 대신 **현재 판단**(강세 우위/중립~강세/중립/…, 시나리오 확률에서 코드가 산출). 시나리오 확률 막대 아래 **과거 통계 기준 확률**(20거래일 뒤 ±5% 정의, 표본+전체 기간 수축 추정)과 정의 문구, AI 최종 확률은 기준 ±20%p로 서버가 묶음. 매매 계획 옆 **돌파형/눌림형** 표기, 목표·손절 거리의 **ATR 배수**, **채점 기준** 한 줄(성적표 링크). 실적 카드에 **최근 4개 분기 기준 PER**(SK하이닉스 8.4배 vs 시세 PER 31.6배). 차트 분석에 **보조지표 교차 확인** 칸. 표기: "종합 +1 (범위 −8~+8)", "매수 관점", 약세안 "재진입 검토가".

### 2026-09-26 저녁 추가 반영 (요약 — 상세는 6장, 세션 11)

- **AI 종목분석 JSON 복구 강화** — `safeParseJSON`: 파싱 실패 시 ①괄호짝 추출(`extractBalancedJson`, 끝에 붙은 여분 `}` 제거) ②따옴표 복구 ③둘 다. 그래도 실패하면 `repairJsonViaOpenAI`(chat completions `json_object`, 문법만 교정 지시, 실패할 때만 호출).
- **실패 처리** — 서버: 분석 실패 시 무료 사용횟수 환원(`lib/supabase-server.js` `refundFreeUsage`). 화면: 빈 리포트(관망 50%)·내부 오류문구 대신 안내 한 줄("일시적인 문제… 다시 시도", 무료 회원에게만 "이용 횟수에 포함되지 않습니다"), 리포트 저장·남은 횟수 차감 안 함. `stock-analysis.js?v=33`.

### 2026-09-26 오후 추가 반영 (요약 — 상세는 6장, 세션 10)

- **AI 종목분석(Opus 재채점 75점 반영)** — "시장 대비 위치" PER 타일이 **최근 4개 분기 실적 기준**(표시 "최근 4개 분기 실적 기준", 비교군 중앙값은 기존 캐시)으로 바뀜. 한 줄 요약도 ROE 문장 대신 "최근 4개 분기 PER은 중앙값보다 낮다 — PBR이 높은 건 이익 급증 영향". 확률 막대 문구 "시나리오당 ±10%p 이내. 모두 AI 추정치이며 결과를 보장하지 않습니다". 요약 카드 대체 라벨 "강세 시나리오 추정 / AI 모델 추정치". 면책 문구에 "누구에게나 같은 내용·개인별 상담 없음·확률/통계/성적표는 보장 아님". stock-analysis.js v=31, css v=29.

### 인스타그램 콘텐츠 포맷 변천사 (참고용)

1. **5장 카드뉴스 캐러셀** (최초 구현, 07-15) — 커버/지수/특징주TOP5/AI코멘트/CTA. 완전 자동화까지 갔었음
2. 경쟁사 벤치마킹 리디자인 시도(라이트테마 유지 + 정보밀도 강화) — 목업 단계에서 릴스 전환으로 방향이 바뀌며 프로덕션 반영 여부 불확실
3. **1장 세로 릴스**(9:16)로 전환 — AI(Gemini) 배경 일러스트 위에 코드가 정확한 수치를 얹는 방식으로 재설계(할루시네이션 방지 원칙 고정). "만평" 컨셉 → `card-usmarket-reel.html`(모닝)/`card-marketcap-reel.html`(마감) 템플릿으로 정착해 현재 운영 중
4. 글로벌랭킹 주간 릴스(`card-globalranking-reel.html`)는 처음부터 이 릴스 포맷으로 신규 개발
5. **(2026-08-13)** 글로벌랭킹 릴스 TOP20 → TOP10 축소 + 국내기업 별도 섹션 분리 + 순위변동 화살표 배지 제거 리디자인. 배포 전 임시 시안 페이지(`preview-globalranking-v2.html`, 비공개 링크)로 사용자 승인을 먼저 받은 뒤 프로덕션 템플릿에 반영

---

## 3. 진행 중이거나 미해결인 이슈

### 우선순위 높음 — 다음 세션에서 먼저 확인

#### ▶ 다음 세션 체크리스트 (2026-09-26 저녁 작성, 세션 11 — 이것부터)
1. **코스콤 답변 확인** — 9/26 문의 접수(개인사업자 계약 가능 여부, 비표출 이용 요금, 표출 요금 체계, 지연시세, 푸시·네이버 프리미엄콘텐츠 게시, 스타트업 무상지원). 답을 받으면 `claude/MARKET_DATA_LICENSE.md`에 반영하고 KRX 연간 구독(약 169만원/년)과 비교.
2. **JSON 복구 로그** — Vercel에서 `JSON 자동 복구 성공(괄호짝|따옴표…)` / `JSON 교정 호출로 복구 성공` / `OpenAI 실패` 빈도 확인. 교정 호출도 실패하는 사례가 있으면 원문 보고 판단.
3. 아래 세션 10 체크리스트 1·3·4번 그대로 유효(2번은 위로 대체). 새 약점: 한눈에 보기 '좋은 점'에 "수급이 엇갈림" 같은 중립 문구가 섞임.

#### ▶ 다음 세션 체크리스트 (2026-09-26 오후 작성, 세션 10 — 이것부터)
1. **9/28(월) 16:00 스크리너 캐시 뒤 SK하이닉스 1년 수익률** — 지금 +512.8%(252거래일 전 30.4만원 기준, 캐시가 9/23 그대로). 새 기준(52주 창)이면 네이버 +416% 근처가 나와야 하고 52주 저점 33.3만원과 모순이 없어진다. 9/26에 수동 재생성(repository_dispatch)을 걸었지만 **휴장일이라 워크플로가 스킵**했다.
2. **"JSON 자동 복구" 로그 확인** — Vercel 로그에서 `[analyze] JSON 자동 복구 성공` / `JSON parse error … 근처:`. 복구 실패가 있으면 근처 원문을 보고 `repairJsonText` 보강(9/26에 "non-JSON" 2회째 발생 → 복구 추가).
3. **여러 종목 눈 검수(세션 9 체크리스트 2번 그대로)** — 남은 약점: 한눈에 보기 cons에 "주가 수준이 높은 편"(TTM PER은 낮은데도) 같은 AI 표현 흔들림, 시나리오 확률이 기준값(61/31/8)에 거의 붙어 나옴(±10%p 제한 효과 — 성적표로 검증 필요).
4. **유사투자자문업 신고 후** 면책 문구에 "유사투자자문업자이며 개별 투자상담은 하지 않습니다" 고지 추가(신고 전이라 지금은 넣지 않음).

#### ▶ 다음 세션 체크리스트 (2026-09-26 점심 작성, 세션 9 — 이것부터)
1. **AI 분석 "OpenAI returned non-JSON" 재발 여부** — 9/26 점심 검증 중 SK하이닉스 1회 발생(바로 재시도는 정상). 프롬프트에 규칙 블록(보조지표·정직성·초보자·계획 일치)이 늘어난 뒤라 응답이 길어지며 JSON이 깨졌을 가능성. 9/28부터 실사용에서 또 나오면 Vercel 로그로 원문 확인 → 규칙 압축 또는 파싱 보강.
2. **리포트 여러 종목으로 눈 검수** — SK하이닉스 외 2~3종목(역배열 종목·소형주·약세 기준 계획 종목)으로 새 리포트를 뽑아 ①한눈에 보기 3칸 ②현재 판단 ③기준 확률 vs 최종 ④종합의견 첫 문장(기준 계획+대안) ⑤목표가 ATR 1.5배 이상 ⑥TTM PER 문구가 자연스러운지. **큰 수정 뒤엔 반드시 리포트 전체를 처음 보는 사람처럼 읽고 모순 점검**(9/26 교훈: 칸별 확인만 해서 PER·추세 모순을 놓쳤다).
3. **기준 확률이 성적표와 맞는지** — 표본이 쌓이면(수십 건) 기준 확률 A/B/C 구간별로 실제 20거래일 결과 비율과 비교. 크게 어긋나면 BAND(±5%)·PRIOR_K(10) 조정(`lib/pattern-stats.js`).
4. 세션 8 체크리스트(아래) 1·2·4번 그대로 유효 — 특히 9/28 16:00 뒤 **52주 저점 vs 1년 수익률 모순 해소** 확인(리뷰에서도 지적됨).

#### ▶ 다음 세션 체크리스트 (2026-09-26 낮 작성, 세션 8 — 이것부터)
1. **9/28(월) 16:00 스크리너 캐시 재생성 뒤 "1년 수익률" 확인** — 기준을 252거래일 전 → 52주 창 첫 거래일로 바꿨다(`lib/kis-indicators.js` `yearBaseIndex`). 캐시는 월요일 16:00 배치가 다시 만들어야 반영된다(주말엔 배치가 쉼). SK하이닉스 1년 수익률이 +512.8% → +459% 이하로 바뀌고(기준가가 52주 저점 33.3만 이상이 되므로), 모순이 사라졌는지 확인. 매매시그널 "1년 수익률" 조건·종가시그널 "1년 +50%" 기법도 같은 값을 쓴다(의도된 변경).
2. **9/28(월) 17:40 AI 분석 성적표 첫 정기 채점** — GitHub Actions `ai-report-grade.yml`(평일 17:40 KST, schedule). 9/26에 수동으로 한 번 돌려 14건 채점 완료(목표 4·손절 4·대기 6). 월요일 뒤 '진입 대기'였던 9/22~9/25 리포트가 진행/결과로 바뀌는지.
3. **AI 종목분석 새 리포트 한 번 눈으로** — 확률 막대 위 "과거 비슷한 국면" 칸, 상단 진입가 = 기준 계획 시나리오 숫자, 이벤트 칸 실적 발표, 성적표 달력. (9/26 SK하이닉스로 API 실측 완료: 표본 15회, 5일 뒤 11회 상승, 확률 근거에 인용됨, 이벤트 10/27.)
4. **실적 예상일 파일 관리** — `data/kr-earnings-manual.json`에 없는 종목은 법정 기한(3분기 11/14)으로 나온다. 주요 종목 예상일을 넣으면 더 정확해진다(지난 날짜는 수동 삭제 규칙 그대로).
5. 세션 7 체크리스트(아래)는 그대로 유효.

#### ▶ 다음 세션 체크리스트 (2026-09-26 오전 작성, 세션 7 — 이것부터)
1. **시우 눈으로 확인** — 브리핑·마감시황·일정·종가시그널의 글씨 크기(zoom 1.14)와 폭(1640px)이 적당한지. 더 키우거나 줄이려면 `assets/read-layout.css` 맨 위 `--rd-z`·`--rd-max` 숫자만 바꾸면 된다.
2. **9/28(월) 장중 국내주식 상세** — 현재가가 CDN 캐시로 최대 약 30초 늦을 수 있다(10초+SWR 20초). 목록 가격과 상세 가격 차이가 눈에 띄는지 확인. 문제면 s-maxage를 줄이거나 아래 3번으로.
3. **남은 속도 과제** — 방문자가 적어 30초 넘게 아무도 안 보면 캐시가 비고, 그 뒤 첫 방문자는 국내 상세까지 5~6초. 근본 대책 후보: 상세를 목록에 있는 가격으로 먼저 그리고 나머지(PER·수급 등)를 나중에 채우기(`realtime-board.js` `mountTableDetailAccordion`).
4. **zoom 안 페이지에서 새 기능을 만들 때** — 브리핑·마감시황·일정·종가시그널은 본문이 CSS zoom 안에 있다. 캔들 차트나 마우스 좌표를 쓰는 요소를 넣으면 어긋난다(매매시그널·AI종목분석이 zoom 제외된 이유). 차트를 넣게 되면 그 페이지를 zoom 목록에서 빼고 글씨만 키우는 방식으로.
5. 세션 4 체크리스트(아래)의 1·2·4·5번은 그대로 유효 — 특히 **`totalmoney-ai-feed-seed` 예약작업 삭제**(시우 PC).

#### ▶ 다음 세션 체크리스트 (2026-09-25 저녁 작성, 세션 4 — 이것부터)
1. **🔴 시우 할 일: Claude 데스크톱 앱 예약 작업에서 `totalmoney-ai-feed-seed`(매일 09:10 AI 분석 시드) 삭제.** 서버는 이미 그 요청을 거절하지만(시드 키 경로 폐기 + 비공개 미들웨어), 예약작업 자체는 PC 로컬에 있어 세션에서 못 지운다 — 남겨두면 매일 헛도는 세션이 Claude 사용량을 쓴다.
2. **9/28(월) 장중 히트맵 확인** — 장중 `status=OPEN`일 때 머리글 "M/D 장중 · 1분마다 갱신", 1분마다 색이 바뀌는지, 칸 클릭 → 국내주식 `?q=` 조회. 미국 탭은 KIS 시총 목록(약 50종목) 기준이라 새 종목이 상위권에 들면 "기타"로 묶임 → `data/us-sector-map.json`에 추가.
3. **히트맵 RS(상대강도) 미구현** — 섹터 일별 등락 기록이 없어 못 넣었다. 원하면 매일 섹터 등락을 저장(예: Supabase 테이블 또는 data 파일)하고 20거래일 뒤부터 RS 표시.
4. **비공개 운영 확인** — 새 가입자가 '대기'로 들어오는지, 마이페이지에서 승인·요금제 변경 동작. 점검봇(`x-tm-machine`)이 401 없이 도는지 health-check 로그.
5. **차트 API 재시도 효과** — 엔비디아 등 미국 차트가 빈 구간 없이 오늘까지 오는지(9/25 저녁 라이브 확인 시 500봉·9/25까지 정상). 가끔 20초 캐시(부분 응답)가 뜨는 빈도는 로그로 관찰.
6. 새벽 체크리스트 2번(종가시그널 가격 제거)은 **A안으로 완료**. 3번(공개 realtime·매매시그널 메뉴)은 사이트 비공개로 당분간 보류. 인스타·텔레그램 공개 발행 재개 시점은 시우 결정(재개 = 워크플로 `if: false` 제거 + `config/public-publish-paused.json` 삭제).

#### ▶ 재편 진행 체크리스트 (2026-09-25 새벽 작성 — 이것부터)
1. **9/28(월) 08:00 개인 수집기 첫 자동 실행 확인** — `select status_code, left(content::text,200) from net._http_response order by id desc limit 5;` + `personal_snapshots.updated_at`이 1분마다 바뀌는지. 실패하면 Edge Function 로그부터.
2. ~~**시우 선택 대기: 종가시그널 결과표 가격 제거 시안**~~ **(9/25 낮 A안 확정·배포 `a924476a`)** — A(매수가·시가·종가 원 단위 열만 제거, 수익률 % 유지) / B(종목별 "수익/손실"만, % 는 5종목 평균만). 시안 이미지는 세션 3 채팅에 있음. 고르면 `resultTableHtml()`(close-signal.html)과 모바일 목록 `mflow` 수정 → 렌더 검증 → 배포. 달력 상세도 같은 함수를 쓴다.
3. **시우 결정 대기: 공개 실시간시세(realtime.html)·매매시그널 메뉴를 언제 뺄지** — KIS 가격이 가장 많이 공개 노출되는 곳. 빼면 개인 대시보드로만 본다.
4. **인스타 카드 가격 제거** — 무인 발행 중이라 시우와 함께, 발행 전 실제 렌더로 검증. 텔레그램 마감 발송은 **공개 채널**(시우 확인) → 같은 규칙 적용 대상.
5. **토스 수집** — `wts-info-api.tossinvest.com/api/v2/stock-infos/A005930`는 GET으로 열림. 랭킹·인기 API는 주소 추측 실패(`/api/v2/dashboard/wts/overview/ranking` POST 400·404) → 브라우저 네트워크 탭으로 실제 요청을 캡처해야 함.
6. 이후: 마감시황 순위표 가격 제거 → AI 종목분석 "요청형 → 매일 N건 자동 게시형" 전환(건수 = 토큰 비용, 시우 결정) → 데이터 방법론 페이지.

#### ▶ 다음 세션 체크리스트 (2026-09-24 밤 작성)
1. **9/28(월) 연휴 후 첫 거래일 결과 확인** — 이번 세션 변경이 한꺼번에 처음 돈다.
   - 08:03 아침 카드 v3(이슈 스토리·사이트 색) 실발행 → 인스타 실물 확인
   - 09:10 종가시그널 9/23 선정분 채점(close_signal_results 9/23 행, review_date 9/28) → 화면 대기 문구 사라지고 결과 표시
   - 15:20 스캔 → 16:05 자기학습 collect(9/23 선정분 결과 적재) → close_signal_outcomes 9/23 행
   - 17:30 마감 카드 v3 실발행 → 인스타 실물 확인(이슈 카드 2장·숫자 검증 로그)
   - 점검봇 새 항목(09:40 종가시그널 결과, 15:40 스캔 등) 오탐 없는지, 휴장일 오알림 재발 없는지
2. **토요일 09:00 자기학습 첫 주간 보고(텔레그램)** 도착 확인 — 20거래일 전이라 가중치 변경 없이 관찰만(10월 중순부터 실조정).
3. **카드뉴스 품질 관찰** — AI가 이슈 사슬을 짧게 쓰는 날(재요청 로직 동작 여부), 과장 표현, 숫자 검증으로 항목이 빠지는 빈도. 필요하면 프롬프트 보정.
4. **12월**: 한국거래소 2027 휴장일 공지와 `lib/krx-calendar.js` 대조(2027은 잠정치). 점검봇이 12월에 알림.
5. (보류·선택) 새 기법 후보가 주간 보고에 쌓이면 시우가 "후보 기법 추가해줘"로 정식 기법화.
6. **소셜 로그인 오픈 — 사업자등록(2027 연기)을 기다리지 않고 진행(2026-09-24 시우 결정).** 순서: ①Supabase Redirect URLs 등록 → ②구글(OAuth 클라이언트 발급 + 동의 화면 프로덕션 게시 → Providers → `SOCIAL_ENABLED.google=true`) → ③카카오 '개인 개발자 비즈 앱 전환'(본인인증 + 데브톡 `account_email` 신청, 신청 전 절차 재확인). 서버 비밀번호 정책·Premium 나머지 축은 그다음.
   → **(9/24 심야) ①②완료 — 구글 로그인 가동·실로그인 확인.** Redirect URLs는 원래 등록돼 있었다. 남은 것: ③카카오.
7. 🔴 **시세 데이터 출처(라이선스) 결정 — 리뉴얼 정식 공개 전 필수.**
   - **KIS Open API 약관 제5조③**(2차 출처 GitHub 이슈 인용, **시우가 KIS 개발자센터 원문 확인 필요**): "회사가 제공하는 시세정보를 자체 개발 프로그램으로 본인 업무 목적에만 사용해야 하며 제3자에게 제공해서는 안 된다." 위반 시 이용 정지·해지(제9·10조). **지금 사이트가 이미 KIS 시세를 화면에 보여주고 있다**(실시간시세·미국주식·매매시그널·AI분석 등) → 현 구조 자체가 위반 소지.
   - 네이버 m.stock 내부 API(`kr-all.json`)도 비공식·약관상 회색지대(차단 위험). 시우 결정: 메뉴 미연결 시험 단계에서만 사용, 정식 공개 전 교체.
   - KRX Data Marketplace OPEN API = **공식·무료지만 일별(전일 종가)만**. 장중 실시간은 코스콤·KRX 시세 계약(유료).
   - 다음 세션: KIS 약관 원문 확인 → 선택지 정리(①KRX 일별 무료 + 장중 표시 축소 ②시세 유료 계약 ③데이터 벤더) → 시우 결정. KRX 인증키는 미리 신청해 두면 좋다.
8. **전체 리뉴얼(시우 결정 9/24)** — "네이버·토스급 기본 서비스, 무료로 되는 건 최대한, 토큰 드는 건 미리 말할 것." 순서: 공통 틀(상단·시세띠·왼쪽메뉴) 부품화 → 표 위주 페이지 → 홈(4칸 랭킹판) → 글 위주 페이지 → 모바일 시트. 프로토타입은 `/lab-realtime.html`. 서버 함수 12/12 — 새 API 파일 금지.

- ~~**(2026-09-23) 라이트모드 로그인·회원가입·마이페이지 주요 버튼을 검정 그라데이션 → 초록으로 바꿨다.** 사이트 주요 버튼과 통일하려는 것이었지만 검정이 의도된 선택이었는지 확인 못 했다 — 시우 답에 따라 `premium.css` 맨 끝 "라이트: 로그인…" 블록만 지우면 원복.~~ **(2026-09-24 시우 결정: 초록 유지 — 종결)**
- ~~**(2026-09-23) 인스타 카드뉴스 템플릿(`templates/*.html`)에 옛 M 모노그램 로고가 남아 있는지 확인** — 사이트·파비콘만 새 스텝 마크로 바꿨다. 템플릿은 이번에 건드리지 않았다.~~ **(2026-09-24 해결)** M 로고는 없었고 캐러셀 v2는 "TM" 글자 배지를 쓰고 있었다 → 시안 3종 비교 후 시우가 **스텝 마크·청록 타일** 선택, `scripts/promo-carousel-css.mjs` `topBar` 한 곳 교체(커밋 `e9d0bc69`), 4슬롯 21장 렌더 검증. `templates/card-*.html`·`scripts/tm-shell-snippet.mjs`에도 TM 배지가 남아 있지만 어떤 워크플로도 호출하지 않는 구버전이다.
- ~~**(2026-09-24) 아침 브리핑 AI 분석 본문의 숫자가 실측과 다르다** — 9/24: 원문 "나스닥 1%대 하락"·EWY -3.62%·WTI -2.49% vs 실측 나스닥100 -0.04%·EWY -1.86%·WTI -3.18%. 카드뉴스는 코드가 걸러내지만 사이트·텔레그램 아침 브리핑 본문은 확인 필요(분석 생성 시점과 수치 수집 시점이 다른 것으로 추정).~~ **(같은 날 해결 — 발행 후 재수집이 덮어쓴 것. 6장)**
- **(신규, 2026-09-24) 9/28(월) 첫 거래일 확인**: 09:10 종가시그널 9/23 선정분 채점, 17:30 마감 카드 v3 첫 실발행, 08:03 아침 카드 v3, 점검봇 새 항목 첫 가동. **12월: 한국거래소 2027 휴장일 공지와 `lib/krx-calendar.js` 대조(2027은 잠정치).**
- ~~**(2026-09-24) 아침 브리핑 카드 1번의 수치 칩이 오른쪽으로 잘린다** — 9/24 덱에서 칩 3개(한국 ETF·필라 반도체·나스닥100)가 한 줄에 안 들어가 "나스닥100 ▼ 0.04"가 카드 밖으로 나갔다. 실제 발행분에도 그대로 나갔을 것. 칩 줄바꿈 또는 3번째 칩 생략 처리 필요(`promo-carousel-slides.mjs` slideHook).~~ **(같은 날 해결 — v3 전환으로 칩 줄바꿈 허용)**
- **(신규, 2026-09-23) AI 종목분석 결과 화면 차트는 새 스타일로 코드만 바꾸고 실물 확인을 못 했다**(분석 1회 실행 필요 — 유료 호출이라 생략). 다음에 분석할 때 차트·범례 200일 색 확인.
- **(신규, 2026-09-23) 글로벌랭킹 일부 종목 로고 누락**(아마존·사우디 아람코·SK하이닉스·비자 등 검은 원) — 로고 데이터 문제, 디자인 작업에서 제외.

- **(2026-09-24 종결 — 버그 아님)** 화면 값이 공시와 일치한다: 삼성전자 2026 1분기 매출 133.9조·영업이익 57.2조 + 2분기 매출 171.5조·영업이익 약 89.5조 = 상반기 305.4조 / 146.7조. 전년 상반기 영업이익(약 11.4조) 대비 +1,191%도 맞다. 메모리 슈퍼사이클로 실제 실적이 그만큼 튄 것. `lib/dart-financials.js` 수정 불필요. 아래는 당시 기록 — ~~**AI 종목분석 "실적 추이" 카드의 2026 상반기 숫자가 의심스럽다.**~~ 삼성전자 리포트에 **2026년 상반기 누적 매출 305조 3,729억 / 영업이익 146조 7,252억(+1191.4%) / 당기순이익 118조 8,497억(+791%)**으로 표시된다. 의심 근거 셋: ①2025년 **연간** 매출이 333조인데 2026 **반기**가 305조면 연 환산 610조로 1년에 두 배 ②영업이익률이 48%인데 `dart-financial-ratios.json`의 2025년 연결 영업이익률은 13.07%다(가전·모바일이 섞인 구조에서 48%는 나오기 어렵다) ③반기 순이익 118조를 연 환산하면 시총 1,496조 대비 PER이 6배대인데 화면의 PER은 39배다.
  - 다만 **버그라고 단정하지는 못했다.** 이 구간은 1년 주가 +265%의 메모리 슈퍼사이클이고, 화면의 PER 39배는 KIS의 과거 12개월 기준이라 2025년 이익(45조)으로 계산하면 39배가 맞게 나온다 — 즉 두 숫자가 각자 다른 기준을 쓰고 있어서 모순처럼 보일 수 있다.
  - 확인 순서: ①DART 반기보고서 원문(삼성전자 2026 반기, 연결 손익계산서)의 매출·영업이익과 화면 값을 직접 대조 ②다른 대형주 2~3개(SK하이닉스·현대차)도 같은 패턴으로 튀는지 — 전부 튀면 `lib/dart-financials.js`의 파싱 문제, 삼성만 튀면 데이터 문제다 ③`lib/dart-financials.js`는 `sj_div` IS/CIS + `fs_div` CFS 필터에 `account_nm` 정확일치, 매출액 행에서 누적/3개월 기준을 한 번만 정하는 구조라 설계상 문제는 안 보였다 — 그래서 `pool.find()`가 동명 계정 여러 행 중 첫 행을 집는 경우, 또는 `parseAmount` 단위 처리를 먼저 볼 것.
- **(신규, 2026-09-18 2차) 수급 숫자 인용 상한이 지켜지지 않는다 — 경미하지만 반복된다.** 검증 리포트의 수급 카드가 "외국인은 4,351억원을 순매도했고, 기관은 7,142억원을 순매수했습니다"로 **바로 위 FACT 표의 숫자 2개를 다시 적었다.** 규칙은 있는데(`[수급 숫자 인용 상한]`) 방향 엇갈림을 설명하려면 근거 숫자 1개는 허용돼 있어서 모델이 2개까지 늘린 것으로 보인다. 상한을 "엇갈릴 때 1개"로 더 못 박거나, 서버가 문장 안의 억/조 단위 숫자 개수를 세서 초과 시 잘라내는 쪽을 검토할 것.

- ~~**(신규, 2026-09-18) 종가시그널 재료(DART) 팩터의 첫 실전 반영은 2026-09-21(월) 15:20 스캔이다.**~~ **(2026-09-21 확인 완료)** Actions `Intraday Close Scan`(slot 1520) success, 하드필터 통과 후보 **41종목 전원에게 `catalyst`가 붙었다**(분모가 갈리지 않는다는 뜻 — 의도대로다). 다만 **41종목 전부 `{present:false, score:0, "재료 없음"}`이었다.** `DART_API_KEY`는 GHA secret에 정상 등록돼 있고(로그에 마스킹되어 찍힘), `[catalyst] ... 조회 실패` 경고도 0건이라 **호출은 정상이고 그날 후보 중 호재 공시 분류에 걸린 종목이 실제로 없었던 것**이다. → 재료가 없는 날이 흔하다는 게 확인됐고, 이것이 09-21 v2에서 재료를 **분모에서 빼고 가산점으로 돌린** 직접적 근거가 됐다.
- **🔴 (신규, 2026-09-21) 종가시그널 성과 추적 3종의 첫 실전 실행 확인 — 2026-09-22(화).** ①**15:45 `close-signal-review-close`(pg_cron jobid 12→11, phase=close)는 완전 신규 잡이라 한 번도 안 돌았다.** 실패하면 달력의 종가 수익률이 영영 안 채워진다 ②09:10 `Close Betting Review`(phase=open)가 `close_signal_results`의 2026-09-21 행을 `source='live'`로 만드는지(지금은 백필이 안 들어간 날이라 행 자체가 없다) ③15:20 스캔이 새 로직으로 **5종목**을 저장하는지 — `payload.closeBetting.ranked` 길이 5, 각 행에 `consensus`/`strategies` 필드가 붙는지. 확인은 Actions 로그 + `select * from close_signal_results order by as_of_date desc` 직접 조회.
- **(신규, 2026-09-21) 종가시그널 성적표 카드뉴스의 첫 실발행은 기록 10거래일이 쌓인 뒤다.** `instagram-carousel-close-signal.yml`이 토요일 11:00 KST(pg_cron jobid 12)에 돌지만 `CS_MIN_DAYS`(기본 10) 미만이면 덱 파일을 만들지 않고 **성공으로 조용히 종료**한다. 9/21 기준 기록은 5거래일이므로 9/26(토)은 스킵되고 10/3(토)쯤 첫 발행이 예상된다. dry_run + min_days=3으로 **덱 생성 → 렌더 → 커밋 → 발행 스킵까지 CI 전 구간을 이미 실검증했다**(run 35605063330).
- ~~**(신규, 2026-09-18) AI 종목분석 정중체('-습니다') 전환이 실제 리포트에 나오는지 확인.**~~ **(2026-09-18 2차 확인 완료 — 삼성전자 실물 리포트로 검증.** 본문·요약·시나리오 전부 정중체, 전문용어 괄호 풀이도 나온다. 다만 **2번 카드 요약문과 하단 각주 2곳은 코드가 만드는 문장이라 1차 수정에서 빠져 있었고**(`mpHeadline`, `.mpos__note`, `.fin__note`) 2차에서 전환했다. 그마저도 `stock-analysis.js?v=20` 캐시 버스터를 안 올려서 화면에 안 나왔다 — v21로 올려 해결.**)** 프롬프트만 고친 변경이라 실물을 봐야 한다. 국내 1종목·미국 1종목을 **새로** 분석해서(캐시된 과거 리포트는 옛 어투 그대로다) ①종결어미가 전부 '-습니다'인지 ②전문용어 첫 등장에 괄호 풀이가 붙는지 ③독자에게 말 거는 문장이 실제로 섞이는지 ④문장이 110자를 넘지 않는지 본다. 어색하면 프롬프트의 좋은 예/나쁜 예 예문을 교체하는 것이 규칙 문장을 늘리는 것보다 효과가 크다.

- ~~**(신규, 2026-09-13) 배포 스크립트 3개가 아직 안 돌아갔을 수 있다 — 순서대로 실행해야 한다.**~~ **(2026-09-18 확인 완료 — v21~v23 + v24(모바일 지수 블록)까지 전부 origin/main에 반영됨.)** 아래는 당시 기록.
  `D:\vibe\_incoming-v21\_apply-v21.ps1`(국내 로고 484종목) → `_incoming-v22\_apply-v22.ps1`(미국 로고 107종목 + 핫링크 제거) → `_incoming-v23\_apply-v23.ps1`(매매시그널 스크롤 튐 + 표 밀도). `_incoming-v20`은 v21에 포함됐으니 무시/삭제.
  확인법: `git log --oneline origin/main -1` → v21 "restore Samsung mark…" / v22 "self-hosted US logos…" / v23 "no scroll jump…".
  **배포 후 반드시 Ctrl+Shift+R** — 예전 파일을 브라우저가 쥐고 있으면 "아무것도 안 바뀐 것"으로 보인다(이번 세션에 실제로 그렇게 보였다).

- ~~**(신규, 2026-09-13) 9/14(월) 04:00 KST `Stock Logos Refresh`의 첫 자동 실행 결과를 봐야 한다.**~~ **(2026-09-18 확인 완료 — 9/14 06:12 KST `chore(assets): 종목 로고 갱신` 커밋이 정상 생성됨. 이후 재벌 그룹주 통일까지 거쳐 국내 manifest count는 503종목.)**
  단계가 3개로 늘었고(favicons → stock-logos → **us-logos**) GHA에서 한 번도 안 돌아봤다. **국내 manifest `count`가 484 아래로 떨어지거나 `tints` 개수가 급감하면** 보호 패치가 안 먹은 것이다 — (ⅰ)파비콘본을 `--force`에서도 FMP가 덮지 않게 한 것 (ⅱ)이전 manifest의 tints를 병합하는 것, 둘 중 하나다.

- **(신규, 2026-09-13) 아직 이니셜 배지로 남은 대표 종목이 있다 — 구글에 파비콘이 없는 회사들이다.**
  유한양행·S-Oil·팬오션·금호석유화학·현대백화점·동진쎄미켐·HDC·영원무역·한국가스공사·제일기획 등. 도메인은 맞는데 구글이 아이콘을 안 갖고 있고, 회사 서버 직접 접속은 컨테이너 egress가 막아서 더는 못 뚫는다. 기아·KT&G·BNK는 로고 자체가 워드마크라 정사각 원본이 없다(배지가 맞다).


- ~~**(신규, 2026-09-12) ⚠️ 종가베팅 랭킹의 첫 무인 실행이 9/14(월) 15:18 — 실데이터로 도는 걸 아무도 못 봤다.**~~ **(2026-09-18 확인 완료 — Supabase 직접 조회로 9/14~9/18 5거래일 전부 정상. 스캔 398~400종목 → 통과 27~32종목 → 랭킹 10종목, 181~232초. 통과 수는 9/11 캐시 시뮬레이션의 51종목보다 적지만 하한 10개 위로 안정적이고, 탈락은 F1(134~154)·F3(119~137)이 대부분이다.)** 토요일에 만들어 배포까지 끝냈지만 장이 닫혀 있어 **1520 슬롯 스캔과 채점이 실제 장중 데이터로 한 번도 돌지 않았다.** 확인 순서: ①15:25쯤 Actions `Intraday Close Scan`(slot 1520)이 success인지 + 로그 끝에 랭킹 10줄이 찍혔는지 ②`/close-betting.html`에 실제 종목이 뜨는지 ③**통과 종목 수** — 9/11 캐시 시뮬레이션에선 2,765 → 51종목이었다. 10개 미만이면 거래대금 하한(100억)을 낮추고, 100개를 넘으면 올린다 ④화요일 09:15에 `Close Betting Review`가 전일 상위 10종목의 익일 시가 수익률을 남기는지. 실패해도 스캔 데이터 자체는 보존되게 try/catch를 걸어놨으므로(2장) 랭킹만 비고 슬롯은 남는다.
- **(신규, 2026-09-12) 종가베팅 배점은 아직 검증되지 않은 추정치다 — 성과 기록이 쌓이기 전까지 "가설 랭킹"으로 취급할 것.** 배점은 공개 연구(오버나이트 수익률 편향, 장 막판 모멘텀)와 실전 통설을 합쳐 세웠고, 9/11 캐시 1일치로 임계값만 보정했다(6장). **`.snapshots` 과거 스냅샷이 저장소에 없어 백테스트는 불가능하다** — `archive-screener-snapshot.mjs`는 있는데 출력물이 커밋되지 않는다. 그래서 사후 기록(`close-betting-review.mjs`)을 1차에 같이 넣었다. 한 달(≈600건) 쌓이면 팩터별 적중률로 배점을 고칠 것.

- **(2026-09-11 해결) Windows 보안 업데이트 KB5124008이 Cowork 로컬 샌드박스를 깨뜨린 건에 대응해 마감시황 생성을 GitHub Actions로 이관했다 — PC 의존이 끊어졌다.** 9/10 설치(업데이트 기록에서 확인). 9/10·9/11 이틀 연속으로 예약작업이 돌지 못해 `data/daily-market.json`의 본문(지수·총평·수급·특징주)이 비었고, 인스타 마감 카드뉴스도 이틀 미발행됐다. 카드뉴스 게이트(`check-promo-data-ready.mjs`)는 정상적으로 막았지만 워크플로가 Success로 끝나 **아무 알림도 가지 않았다**. → `scripts/daily-closing-report.mjs` + `daily-closing-report.yml`로 **러너 안에서 본문까지 생성**하도록 전환(6장 참조). **Cowork bash 도구는 2026-09-11 20:00 기준 여전히 죽어 있다**(`Plan9 share "c" is not mounted`) — Anthropic 추적 중, Claude Code는 영향 없음. 보안 패치라 롤백은 권하지 않는다(운영자 확인 완료). **남은 확인**: ①9/14(월) 16:20 첫 무인 자동 실행이 실제로 지수·수급·본문을 채우는지 ②2026-09-10 백필(4장).
- **(2026-09-11 완료) 워치독 + 마감 리포트 pg_cron 등록 완료.** `daily-report-watchdog.yml`은 9/10 → "거래일인데 누락" + 텔레그램 발송, 9/11 장중 → 조용히 스킵으로 실행 검증까지 마쳤다. GitHub schedule은 이 저장소에서 2~6시간 밀리므로 **Supabase pg_cron 3건을 신규 등록**했다(jobid 6·7·8): `daily-closing-report-dispatch` 평일 16:20 KST, `daily-report-watchdog-first` 16:40 `stage=first`, `daily-report-watchdog-final` 19:00 `stage=final`. 등록과 함께 edge function `github-workflow-dispatch`의 ALLOWED_WORKFLOWS에 두 워크플로를 추가해 v10으로 재배포했다 — **이 allowlist를 빠뜨리면 pg_cron이 400을 받고 조용히 실패하므로, 새 워크플로를 pg_cron에 걸 때는 반드시 함께 고칠 것.** 체인 실측 검증: pg_cron → edge function → GitHub 모두 204/200 통과. 참고로 같은 워치독이 2026-06-23에도 만들어졌었는데 저장소에 푸시되지 않아 한 번도 돌지 않았다.
- **(신규, 2026-09-11) ⚠️ 이모지 일괄 제거 회귀 사고 — 복구 완료, 재발 방지 필요.** 9/11 수동 적용 스크립트의 "고아 서러게이트 제거" 정규식이 **정상 이모지(서러게이트 쌍)까지 삭제**해 `data/daily-market.json` 전체에서 789개 필드가 훼손됐다(analysis 57일치 · telegramMessages 640건 · marketExtras 92건). 당시 후속 수정은 9/11 하루만 되돌려 나머지는 그대로 남아 있었다. `e22511a` 기준으로 전량 복원 완료(`627aa58`, diff 791줄, 손상 0건 검증). **다만 텔레그램 채널로는 이모지가 빠진 본문이 이미 발송됐고 되돌릴 수 없다.** **교훈**: 서러게이트를 다루는 정규식은 `[\uD800-\uDBFF][\uDC00-\uDFFF]`(쌍)을 먼저 보존한 뒤 고아만 지워야 한다. 대용량 JSON 일괄 치환 전에는 반드시 표본 diff를 눈으로 확인할 것.
- **(신규, 2026-09-11) Anthropic 잔액 소진이 확정됐다 — 운영자가 OpenAI로 쓰고 있다.** 9/7 항목(아래)의 추정이 사실로 확인됐다(2026-09-11 운영자 확인). 카드뉴스 덱 생성은 `Claude 시도 → 실패 → OpenAI` 순서라 **매일 실패 호출이 한 번씩 나가고 있다** — 잔액 충전 전까지는 순서를 바꾸거나 환경변수로 고르게 하는 게 깔끔하다. **중요**: 마감시황 API 폴백을 만들 계획이라면 `scripts/daily-market-ai.mjs`는 **Anthropic web_search 전용이라 그대로 못 쓴다**(OpenAI로 다시 짜야 함).

- **(신규, 2026-09-08 밤) 장 막판 종가 스캔이 실제 데이터로 도는지 확인 — 첫 자동 실행은 9/9 14:30·15:31.** 작업 시점에 배포돼 있던 전종목 캐시가 아직 `snapshot.next` 없이 만들어진 상태여서, 스캔이 "기준없음"으로 전 종목을 건너뛰는 것까지만 확인했다(경로·인증·순위 API·pg_cron 트리거는 전부 정상 동작 확인). 9/8 밤 캐시 빌드가 새 코드로 돌면 풀린다. **확인할 것**: ①`data/kr-screener-cache.json`의 스냅샷에 `next` 필드가 실제로 들어갔는지(fresh clone 기준) ②스캔 **소요 시간이 300초를 넘지 않는지 — 넘으면 15:36 목표를 못 맞추므로 후보 수(`INTRADAY_LIMIT`)나 `INTRADAY_GAP_MS`를 줄여야 한다** ③`trade_signal_intraday` 테이블에 행이 쌓이는지 ④사이트에서 종가 수급매매 프리셋이 종목을 반환하고 안내 문구가 "오늘 15:31 장 막판 기준"으로 뜨는지. 검증용 일회성 Cowork 예약작업 `verify-intraday-close-scan`(9/8 23:40)을 걸어뒀다 — 그게 돌았으면 결과부터 확인할 것
- **(신규, 2026-09-08 밤) 장중 수급이 "당일 잠정치"로 실제 내려오는지 확인.** `inquire-investor`가 장중/장마감 직후에 **당일 행을 주는지 아니면 전일까지만 주는지 실호출로 확인이 안 된 상태**다. 전일까지만 준다면 `major_net_buy_streak`이 장중에도 전일 기준으로 계산돼 "오늘 양매수"가 반영되지 않는다. 스캔 결과에서 한 종목을 골라 순매수 값이 HTS 당일 잠정치와 맞는지 대조할 것. 안 맞으면 장중 슬롯에서는 수급 조건을 빼고 거래대금·거래량·차트로만 좁히는 쪽으로 조정 필요
- **(신규, 2026-09-08 밤) GitHub Actions `schedule:` 트리거가 이 저장소에서 상시 1.5~6시간 밀리고 있다 — 영향 범위를 한 번 훑을 것.** 인스타 3종은 이미 pg_cron으로 이관했지만, 실측해 보니 **다른 워크플로도 전부 밀린다**: 장전 브리핑 `morning-briefing.yml`(07:55 예정 → 실제 09:26~09:48 KST, 즉 **장 시작 후에 "장전" 브리핑이 실행**되고 있음), `daily-market-sync.yml`(15:40 예정 → 20:40~21:58), `kr-screener-cache.yml`(16:00 예정 → 20:52~23:42). 지금까지 눈에 안 띈 이유는 대부분 결과물이 늦게라도 나오기 때문인데, 장전 브리핑은 **시각 자체가 상품 가치**라 그냥 두면 안 된다. pg_cron 경로가 이미 검증됐으므로(20초 이내) 옮기는 비용은 작다

- **(신규, 2026-09-08) 인스타 pg_cron 이관이 실제로 도는지 확인할 것.** 9/8에 Supabase pg_cron 3개의 호출 대상을 구 릴스에서 캐러셀로 바꿨는데(6장 상세), **정시 트리거가 실제로 발행까지 성공하는 건 아직 한 번도 못 봤다**(그날 마감분은 수동 `repository_dispatch`로 발행했다). 확인 순서: ①**9/9(수) 08:30 KST** 아침 캐러셀이 `workflow_dispatch` 이벤트로 실행돼 success인지 ②같은 날 **17:30** 마감 캐러셀도 동일한지 ③**9/12(토) 10:30** 랭킹 캐러셀 첫 정시 실행(이건 그동안 GitHub schedule로 한 번도 안 돌았다) ④구 릴스 워크플로 2종(`instagram-card-post*.yml`)에 새 실행 기록이 더는 안 생기는지. 실패 시 가장 유력한 원인은 Edge Function allowlist(v8에 캐러셀 3종을 넣었다)나 새 가드 로직(`FORCE_DATE`/`FORCE_FLAG` 판정)이다. 확인 명령은 `GET /repos/mandoo411/vibe/actions/workflows/<파일>/runs?per_page=5`, pg_cron 상태는 Supabase `select * from cron.job_run_details order by start_time desc limit 10`.
- **(신규, 2026-09-08) 마감시황 재시도 작업이 "이미 발행됨"을 제대로 감지하는지 첫 실행에서 볼 것.** `totalmoney-daily-closing-report-retry`(평일 16:50)는 정상적인 날엔 아무것도 하지 않고 즉시 끝나야 한다. 첫 실행에서 ①1차가 성공한 날 정말 조용히 끝나는지(토큰을 축내면 안 된다) ②실패한 날 실제로 대신 발행하는지를 확인. **Cowork 도구 승인 프롬프트에서 멈추지 않도록 한 번은 "지금 실행"으로 미리 승인해 두는 게 안전하다**(예약작업 승인은 작업 단위로 저장된다).
- **(신규, 2026-09-07) ★ Anthropic API 크레딧이 소진됐다 — 사이트 품질에 직결되므로 가장 먼저 확인할 것.** 카드뉴스 덱 생성 첫 GHA 실행에서 드러났다: `400 invalid_request_error "Your credit balance is too low to access the Anthropic API"`. 카드뉴스 쪽은 OpenAI(gpt-4o) 폴백을 넣어 발행이 멈추지 않지만, **같은 `ANTHROPIC_API_KEY`를 쓰는 다른 자동화가 조용히 폴백 리포트를 내보내고 있을 가능성이 크다** — `scripts/daily-market-ai.mjs`(마감시황 분석), `api/analyze.js`(AI 종목분석·매매시그널 조건 파싱), `weekly-schedule.mjs`, 텔레그램 리포트류. 특히 AI 종목분석은 **유료 핵심 기능**이라 폴백 품질로 나가면 그대로 이탈로 이어진다. **확인 방법**: 최근 마감시황 `analysis` 길이가 평소(7,000자 내외)보다 크게 짧은지, GHA 로그에 `Claude failed (billing/unavailable)`가 찍히는지. **조치**: 크레딧 충전 또는 각 스크립트의 OpenAI 폴백 경로 점검.
- **(신규, 2026-09-07) 카드뉴스 문장 품질이 모델에 따라 크게 갈린다 — 크레딧 복구 전까지 지켜볼 것.** `gpt-4o-mini`는 훅에 사족을 붙이고, 2번 카드에서 공개한 종목을 4번 카드에서 또 쓰고, 판단 카드를 "강세로 마감했습니다" 같은 요약으로 써서 카드의 존재 이유를 없앴다. `gpt-4o`로 올리고 프롬프트에 좋은 예/나쁜 예를 넣어 해결했지만, **모델 준수에만 기대지 않도록 코드가 강제하는 장치**(`clampCopy`: 훅은 첫 문장만 남김, 길이 초과 시 절단, flowIn 첫 항목에서 거래대금 1·2위 중복 제거)를 함께 넣어뒀다. Claude 복구 시 품질이 올라갈 것이므로 그때 프롬프트 톤을 다시 볼 것.
- **(신규, 2026-09-07) 새 캐러셀 자동 발행 3종의 첫 실전 결과를 며칠 확인할 것.** 마감(17:26)은 09-04 데이터로 dry-run·실발행까지 검증했고, 아침(08:03)·랭킹(10:30)은 **dry-run까지만** 확인했다(실제 스케줄 발행은 아직 없음). 첫 자동 실행 뒤 ①글자 넘침으로 렌더가 실패하지 않았는지 ②훅 유형 판정이 그날 데이터와 맞는지 ③캡션 첫 두 줄이 잘 읽히는지 확인 필요.
- **(2026-09-07 정리) 초기 테스트 발행분 2건이 피드에 남아 있을 수 있다.** 09-04 마감시황을 릴스 1건 + 5장 캐러셀 1건으로 중복 발행했다(형식 검증 목적). Graph API는 인스타 미디어 삭제를 지원하지 않아 **앱에서 수동 삭제해야 한다.**
- **(신규, 2026-09-04) 소셜 로그인 콘솔 설정이 아직 안 끝났다 — 코드는 전부 완료, 대시보드 작업만 남음.** 상세 절차는 `docs/소셜로그인_비밀번호정책_셋업가이드.md`에 단계별로 정리돼 있다.
  - **구글**: Google Cloud Console에서 OAuth 클라이언트 미발급 → 로그인 시 `{"code":400,"error_code":"validation_failed","msg":"Unsupported provider: provider is not enabled"}`. 리디렉션 URI는 `https://slcplrblmdiydakwzqdc.supabase.co/auth/v1/callback`. **OAuth 동의 화면을 "프로덕션으로 게시"하지 않으면 등록한 테스트 사용자만 로그인된다** — 가장 흔한 함정. 발급 후 Supabase Providers에 넣고 `assets/auth-config.js`의 `SOCIAL_ENABLED.google`을 `true`로.
  - **카카오**: **이메일 동의항목이 "비즈 앱 전환" 승인 후에야 열린다**(2026-09-04 확인). 사업자등록번호 없이도 "개인 개발자 비즈 앱 전환"(본인인증 + 데브톡 `account_email` 신청)으로 가능하지만, 운영자 판단으로 **사업자등록 완료 후 일괄 처리하기로 보류**했으나, **2026-09-24 사업자등록이 2027년으로 밀려 개인 개발자 비즈 앱 전환으로 지금 진행하기로 변경**. **이메일 없이 열면 이메일 없는 계정이 생겨 결제·구독 연동이 깨지므로 승인 전에는 절대 켜지 말 것.**
  - **네이버**: 연동 완료·가동 중이나 개발자센터 상태가 **"개발 중"이라 애플리케이션 등록자(시우) 계정만 로그인된다.** 일반 사용자에게 열려면 **로고 이미지 등록(현재 "없음" — 반려 사유가 됨) 후 검수요청** 필요. 검수 1~3영업일.
- **(신규, 2026-09-04) ⚠️ 비밀번호 정책이 화면에만 걸려 있고 서버에는 미적용.** `TMAuth.validatePassword()`가 8자+영문숫자를 막지만 **Supabase 서버는 여전히 6자를 통과시킨다** — API를 직접 호출하면 뚫린다. Supabase 대시보드 → Authentication → Sign In/Providers → Email에서 **Minimum password length = 8**, **Required characters = Letters and digits**, **Leaked password protection = ON**(무료, 유출 이력 비밀번호 자동 거부) 설정 필요.
- **(신규, 2026-09-04) ⚠️ Supabase Redirect URLs 미등록 — 구글·카카오는 이게 없으면 로그인 직후 튕긴다.** Authentication → URL Configuration에 `https://www.totalmoney.kr/auth-callback.html`, `/reset-password.html`, `/login.html`(www 없는 도메인도 함께) 등록 필요. 네이버는 Edge Function이 직접 리다이렉트해서 이 설정 없이도 동작했다 — **구글·카카오를 켜기 전에 반드시 먼저 할 것.**
- **(신규, 2026-09-04) 핵심지표에 주간 항목이 늘어난 것을 며칠 지켜볼 것.** 운영자가 EIA 원유재고와 주간 신규실업수당을 포함하기로 선택해 **이번 달 핵심 지표가 14 → 29건**으로 늘었고, 그중 9건 정도가 이 둘이다(매주 반복). 실제 텔레그램 알림을 며칠 받아보고 많다 싶으면 `assets/key-indicators.js`의 해당 두 줄만 제거하면 된다(주석으로 표시해둠).

- **(신규, 2026-09-03 오후) 16:00 캐시 빌드 이후에만 확인 가능한 항목 3건 — 다음 세션 최우선**: 이번 세션은 13:56~15:0x에 진행돼서 아래 셋은 **아직 한 번도 못 봤다.** ①**업종 비교군 활성화**(09-03 16:40 예약작업 `totalmoney-sector-peer-verify` 결과 — 아래 항목 참고) ②**`data-archive` 브랜치 첫 스냅샷 생성** — 15:00 시점 `git ls-remote`로 **브랜치가 아직 없음**을 확인했다. `screener-snapshot.yml`이 `workflow_run`으로 캐시 빌드 성공 직후 돌면서 orphan 브랜치를 스스로 만들어야 하는데 이 생성 경로는 실동작 미확인이다 ③**갭 판정 기준 통일이 실제로 반영되는지** — 아래 신규 버그 수정 항목 참고. `daysSince`는 캐시에 저장된 값이라 **다음 빌드 전까지는 옛 기준 그대로**다.
- **(신규, 2026-09-03 오후) 재무비율 배치 안정성 관찰 — 신규 파이프라인이라 며칠 지켜볼 것**: `financial-ratios.yml` 첫 실행은 성공했다(2,858종목 · 357KB · 약 9분). 확인할 것: ①**주 1회 스케줄(일요일 08:30 KST)이 실제로 도는지** — corp_code 매핑 갱신(일 07:00) 뒤에 돌도록 배치했다 ②**3월 결산법인·신규상장이 늘어나는 시기에도 수집률이 유지되는지** — 수집률이 30% 미만이면 기존 파일을 덮어쓰지 않고 실패하는 가드를 넣어놨다(부실한 결과로 멀쩡한 파일을 날리지 않기 위함) ③**금융사·지주사의 `operatingMargin` null이 정상 범위인지** — 신한지주는 부채비율 1201.9%·ROE 8.42%는 나오는데 영업이익률·순이익률·유동비율이 null이다(금융업은 주요계정 구조가 달라 매출액/유동자산 항목이 없음). **지어내지 않고 생략하는 게 맞는 동작**이지만, 금융주로 재무 검색을 하면 결과가 비는 걸 사용자가 이상하게 느낄 수 있어 안내 문구가 필요한지 판단할 것 ④**사업보고서가 갱신되는 3월경**에 FY 전환이 매끄럽게 되는지.
- **(신규, 2026-09-03 오후) 재무 조건으로 저장한 감시 전략이 실제로 알림을 보내는지 — 아직 실동작 미확인**: `trade-signal-scan.mjs`에 재무비율 조인을 넣었지만(넣지 않으면 "부채비율 100% 이하이면서 RSI 30 이하로 떨어지면 알려줘" 같은 전략이 **영원히 알림을 안 보낸다** — 필드가 없어 항상 false), 실제 스캔 실행으로 검증하지는 못했다. 다음 `Trade Signal Scan` 실행 로그에서 **`[trade-signal-scan] 재무비율 로드 N종목`**이 찍히는지 확인할 것(찍히지 않으면 배포 번들에 `data/dart-financial-ratios.json`이 안 들어간 것이다). 재무 조건을 포함한 전략을 하나 저장해 두고 며칠 지켜보는 게 가장 확실하다.

- ~~**(신규, 2026-09-03 밤) AI 종목분석 재료 카드에 실제로 최신 뉴스가 들어오는지 — 다음 세션 1순위 검증**~~ **(2026-09-04 새벽 검증 완료 — 통과, 다만 2건 보강함)** 로그인 게이트 때문에 화면으로는 못 봐서, **`[analyze] materials(N) 코드: 재료명[반영률%]` 진단 로그를 신설**해 Vercel 함수 로그로 확인하는 방식으로 전환했다(이 방법은 앞으로도 계속 쓸 것 — 유료 게이트 뒤의 출력을 검증하는 유일한 수단이다). 실제 3종목 결과:
  - **삼성전자(005930)**: 2분기 역대급 실적 확인[90%] / HVAC 사업 확장과 광주 신규 생산라인[50%] / 2026년 주주환원 강화와 10월말 이사회[60%] / 임직원 주식보상 목적 자기주식 취득[30%]
  - **SK하이닉스(000660)**: 40조 자사주 취득·소각[70%] / 2분기 역대 최대 실적[80%] / 키옥시아와의 협력·일본 공장 검토[30%] / 미국·이란 갈등에 따른 반도체 밸류에이션 압박[40%]
  - **현대차(005380)**: 아반떼 완전변경 계약 개시[40%] / 노사 임금협상 타결[30%] / 방문상담 예약 서비스 도입[20%] / 2분기 배당 기준일 공고[70%]
  - **판정**: 원래 문제였던 순수 일반론("실적 모멘텀", "업종 트렌드")은 **한 건도 없음**. 40조 자사주·키옥시아·광주 생산라인·아반떼 완전변경 등 고유명사와 금액이 들어간 개별 사건으로 채워졌다. 3종목 모두 재료 4개, `사전수집 헤드라인 14건` 로그 확인(= `fetchStockNewsBundle` 안전망 작동), 응답 지연 25~45초로 이전과 차이 없음.
  - **발견된 약점 2건 → 같은 세션에 수정**: ①"2분기 역대 최대 실적" 류가 두 종목에서 반복 — 제목만으로 정보가 없고, 이제 실적 추이 카드가 DART 실수치로 같은 얘기를 해서 슬롯 낭비다 → **실적 자체를 재료 제목으로 쓰는 것 금지**(그 실적을 만든 개별 사건을 쓰도록) ②현대차 "방문상담 예약 서비스 도입[20%]"처럼 주가와 무관한 항목이 4칸을 맞추려다 들어감 — 프롬프트는 "3개 이상(최대 5개)"인데 서버는 4개로 자르는 **불일치**도 있었다 → 양쪽을 **"3개 또는 4개"로 일치**시키고 "넷째가 약하면 3개로 내라" 명시. **다음 세션 확인**: 이 두 규칙이 실제로 먹히는지 로그로 재확인할 것.
  <details><summary>당시 기록</summary>

  **(신규, 2026-09-03 밤) AI 종목분석 재료 카드에 실제로 최신 뉴스가 들어오는지 — 다음 세션 1순위 검증**: 프롬프트 3건(일반론 금지 / 최근 45일 2건 이상 / 검색 3각도 최소 3회)과 코드 1건(`fetchStockNewsBundle`이 구글뉴스 RSS를 3각도로 미리 수집해 프롬프트에 주입)을 함께 넣었지만, **실제 유료 리포트 출력으로는 아직 한 번도 확인 못 했다**(로그인 게이트 때문에 Claude가 직접 실행 불가). 확인할 것: ①SK이노베이션·SK하이닉스처럼 최근 굵직한 이벤트가 있는 종목으로 돌려서 **수주·합병·자회사 이슈가 materials에 실제로 들어오는지** ②"실적 모멘텀"·"업종 트렌드" 같은 일반론 제목이 여전히 나오는지(나오면 프롬프트를 더 조일 것) ③재료가 3개 이상 채워지는지 ④뉴스 3쿼리 추가로 응답 지연이 체감될 정도로 늘었는지(구글뉴스 RSS는 병렬 호출·8초 타임아웃이라 영향은 작을 것으로 예상하나 실측 필요). **참고: 이 경로는 OpenAI(`gpt-5.4-mini`) web_search가 1순위이고, 검색 흔적이 없으면 RSS 폴백으로 넘어간다** — Vercel 함수 로그에서 `[analyze] OpenAI web_search 경로 사용 (사전수집 헤드라인 N건)`이 찍히는지 보면 어느 경로를 탔는지 바로 알 수 있다.
  </details>
- **(신규, 2026-09-03 밤 → 2026-09-04 새벽 예약검증 대기) 업종 비교군이 실제로 활성화되는지**: 2026-09-03 11:21 KST 확인 시점의 캐시는 전날(09-02) 것이라 `snapshot.sector`가 **0건**이었다(정상 — 코드는 배포됐지만 캐시가 다시 만들어져야 채워진다). 그래서 **09-03 16:40 KST 일회성 예약작업 `totalmoney-sector-peer-verify`를 걸어뒀다** — 캐시 빌드 성공 여부, sector 채움 비율, 업종 분포, ETF·리츠 이상값, `MP_MIN_SECTOR_PEERS` 기준 폴백 비율까지 확인해 보고한다. **그 결과를 다음 세션에서 반드시 확인할 것.** 아래는 원래 항목 — `sector`(KIS `bstp_kor_isnm`) 수집 코드는 넣었지만, `data/kr-screener-cache.json`이 다시 만들어져야 실제로 채워진다. 그 전까지 "시장 대비 위치" 카드의 비교군은 계속 "코스피 전체 943개"로 표시된다(정상 폴백). 빌드 후 확인할 것: ①캐시의 `snapshot.sector`가 2,700여 종목에 채워졌는지 ②카드 헤더가 "○○ 업종 N개 종목 중 위치"로 바뀌는지 ③업종 표본이 15개 미만인 종목은 시장 전체로 잘 폴백되는지 ④**ETF·리츠 등에서 `bstp_kor_isnm`이 비거나 이상한 값이 오지 않는지**(오면 `mpPickPeers`가 엉뚱한 비교군을 만들 수 있다).

- **(신규, 2026-09-03) 증시 일정 "일정 분석"의 새 문체가 9/3 06:00 배치부터 실제로 나오는지 — 이번 세션 후속 검증 1순위**: `scripts/weekly-schedule.mjs`의 `buildAnalysisPrompt`를 `why`/`bull`/`bear`/`watch` 4필드로 바꿨지만, **9/2까지의 데이터는 여전히 구버전 `analysis` 한 덩어리**라 지금 화면에 보이는 건 폴백 렌더다(폴백 자체는 실사이트에서 정상 동작 확인). 다음 배치가 돈 뒤 확인할 것: ①`data/weekly-schedule.json`의 `eventAnalysis[]`에 `why`/`bull`/`bear`가 채워지는지 ②화면에 `.ea-scenarios`(상회/하회 2단 카드)와 `.ea-tag`(관전 포인트)가 뜨는지 ③**bull/bear가 "예상 상회 시…"로 시작하지 않는지**(프롬프트로 금지했지만 모델이 습관적으로 붙일 수 있음 — 붙으면 라벨과 중복돼 더 촌스러워진다) ④6건의 문형이 실제로 서로 다른지. 부실하면 프롬프트를 더 조일 것. **참고: 이 파일은 GHA가 실행하는 살아있는 실행 경로가 맞다**(`.github/workflows/weekly-schedule.yml` → `buildAnalysisPrompt` 호출 확인함) — 마감시황의 `daily-market-ai.mjs` 죽은 코드 사고와는 다른 경우.
- **(신규, 2026-09-02 밤) 마감시황 구조화 필드(STEP 3b)가 내일 16:15 발행분에서 실제로 채워지는지 — 이번 세션 최대 수확의 후속 검증**: 9/1 세션이 `marketTone`/`indexDetail`/`sectors`/`netBuyLeaders`/`featured_stocks[].risk` 규칙을 `scripts/daily-market-ai.mjs`에 넣었는데, **그 파일은 저장소 어디에서도 호출되지 않는 죽은 코드**였다(`analyzeDailyClosingReport`가 export만 되고 caller 없음). 실제 리포트를 쓰는 건 Cowork 예약작업 `totalmoney-daily-closing-report`의 SKILL.md인데 거기엔 새 필드 얘기가 한 줄도 없어서, 9/1·9/2 리포트 모두 이 필드들이 전부 `null`이었고 라이브 화면에서 업종별 등락·기관/외국인 순매수·장중 레인지바 블록이 통째로 안 그려지고 있었다. 예약작업 프롬프트에 **STEP 3b**(필드 5종 정의 + "확인 안 되면 키 생략" 규칙)를 신설하고, STEP 2 검색 항목(장중 고저·업종 등락률·순매수 상위)과 STEP 4 검증·최종 보고 항목까지 연결했다. **9/3 16:15 발행분이 첫 실검증** — 확인할 것: ①`marketTone`이 히어로 태그로 뜨는지 ②`indexDetail`로 레인지 바가 그려지는지(고가 없으면 "종" 라벨) ③`sectors`가 강세/약세 양쪽 다 담겼는지 ④`netBuyLeaders` 블록이 뜨는지 ⑤`risk`가 `point`와 안 겹치는지. 하나라도 부실하면 STEP 3b 문구를 더 조일 것. **교훈: 앞으로 리포트 생성 규칙을 바꿀 때는 반드시 `D:\Claude\Scheduled\<작업명>\SKILL.md`를 고쳐야 한다. 저장소의 `scripts/*-ai.mjs`는 실행 경로가 아니다.**
- ~~**(신규, 2026-08-26 4차 후속 세션) AI종목분석 요약카드 세로텍스트 2차 수정본(`3e83e7da`) 배포 대기 — Vercel 배포한도 재소진 확인**~~ **(2026-08-26 5차 후속 세션에서 해결 확인)** `summary.description`(한눈에 요약 카드 설명문)이 `formatProseText`를 거치지 않는 별도 렌더 경로라 1차 수정이 놓쳤던 걸 `sanitizeOneLineText`로 방어 추가한 수정. 당시 GitHub 커밋 상태 API로 Vercel `"Deployment rate limited — retry in 24 hours"`(09:56 UTC)를 확정했었으나, 이번 5차 세션에서 확인해보니 배포한도가 정상 회복돼 있어 정상 배포됨 — Claude-in-Chrome으로 실제 로그인 세션에서 삼성전자 분석을 재실행해 세로텍스트가 재현 안 됨을 최종 확인. `vercel-deploy-catchup.yml` cron 수정(연1회→매일)이 실제로 밀린 배포를 잡아준 건지, 24시간 자연 회복만으로 풀린 건지는 이번엔 명확히 구분 안 됨(다음에 배포한도 소진이 재발하면 캐치업 워크플로우 실행 로그를 직접 확인해볼 것). 같은 세션에서 카드1~7 UI 전면 리디자인(커밋 `8e94e90`)도 배포·라이브 검증까지 완료(2·6장 상세)
- ~~**(신규, 2026-08-26 3차 후속 세션) Vercel 일일 배포한도(100회) 재소진 확정 + catch-up cron이 사실은 "연 1회"였던 버그 발견·수정**~~ **(2026-09-02 밤 종결)** 배포한도는 정상 회복돼 있고, 9/2 하루 동안 커밋 10여 건이 전부 즉시 자동배포됐음을 Vercel 배포 목록으로 확인. `cfc312cc`의 cron 수정(매일 09:00 KST)도 그대로 살아있다. 아래는 당시 기록 보존용 —
  <details><summary>당시 상세</summary>

  **(신규, 2026-08-26 3차 후속 세션) Vercel 일일 배포한도(100회) 재소진 확정 + catch-up cron이 사실은 "연 1회"였던 버그 발견·수정**: 오늘 낮 세션(2번째 후속)에서 커밋 4개(`3bd00239`/`ec256579`/`225781eb`)를 push했는데도 자동배포가 전혀 안 걸려서 조사한 결과, 사용자가 GitHub Actions에서 "Deploy Vercel (catch-up)"을 수동 실행해봤더니 **`Error: Resource is limited - try again in 24 hours (more than 100, code: "api-deployments-free-per-day")`** 로그로 한도 소진이 명시적으로 확인됨(추정이 아니라 실제 에러로 확정). 원래 "다음날 09:00 KST에 `vercel-deploy-catchup.yml`이 자동으로 따라잡아준다"고 안내했는데, 실제 그 워크플로우의 cron을 열어보니 `"0 0 21 5 *"`(매년 5월 21일 단 하루, 5월에 한 번 쓰고 남은 값)로 박혀 있어서 이름·주석과 다르게 **한 번도 "매일" 돈 적이 없었던 별개의 버그**였음 — `cfc312cc`로 `"0 0 * * *"`(매일 09:00 KST)로 수정 완료(YAML 문법 검증 통과). **참고**: 이 cron 수정은 GitHub Actions가 저장소의 workflow 파일을 직접 읽어 실행하는 것이라 Vercel 배포와는 무관 — `main`에 push된 시점(오늘)부터 이미 유효하며 Vercel 한도/배포 상태와 상관없이 내일 09:00 KST에 정상 작동함. **다음 세션에서 확인**: 09:00 KST 캐치업이 실제로 자동 실행돼서 밀린 커밋(`ec256579`/`225781eb`/`cfc312cc`)까지 Vercel에 전부 배포됐는지
  </details>
- **결제 오픈 대기 중(확인됨, 2026-08-09 / 갱신 2026-09-24: 유사투자자문업 교육 수료 완료, 사업자등록은 2027년으로 연기 → 결제 오픈도 2027년)**: 결제는 아직 비활성. 유사투자자문업 교육 수료 → 사업자등록 완료가 선행 조건이며, 완료되면 `assets/auth-config.js`의 `SETUP_PENDING`을 `false`로, 포트원 콘솔의 채널을 테스트→실채널로 전환하는 두 단계만 남음(코드 수정 불필요). 2026-08-09 기준 아직 교육/등록 전. 사업자등록 진행 상황을 계속 트래킹할 것
- **Cowork 예약작업의 `D:\vibe` 폴더 마운트 간헐적 누락 — 3차례 재발(2026-08-12 최초 발견 → 08-13 재발 → 08-14 재발 + 새 실패 패턴 추가 발견)**: `totalmoney-morning-briefing`(평일 08:03 KST) 세션에서 8/7·8/10·8/11·8/12·8/13·**8/14**에 D:\vibe 마운트가 세션에 붙지 않아 GitHub PAT를 못 읽고 매번 조용히 실패. 8/12에 마운트 재시도 로직(최대 2분)을 추가했음에도 8/13에 재발해서 08-13에 2단계 재시도 예약작업(`totalmoney-morning-briefing-retry`, 평일 08:15 KST)을 신규 추가했는데, **08-14 실행에서 이 재시도 작업도 절반만 성공하는 새로운 실패 패턴이 나타남** — 마운트 자체는 성공했고 수치 데이터 갱신 트리거(STEP 1)까지는 완료했지만(08:16 커밋 확인됨), 그 뒤 뉴스 리서치·`aiAnalysis` 작성·커밋(STEP 2~5)은 진행하지 못하고 중단됨. 정확한 중단 원인은 해당 세션 로그를 `mcp__session_info` 도구로 찾지 못해 특정하지 못함(세션 목록에 타이틀이 겹쳐서 구분 안 됨). 같은 날 **09:00 KST health-check 알림도 지연**됐음이 함께 확인됨 — health-check 자체의 시간별(매시 5분) 실행이 08:52 KST와 11:38 KST 사이에 약 2시간 46분 공백이 있어서, 실제 알림은 09시가 아니라 11:38 KST에야 발송됨(이 cron 공백의 원인도 불명, GitHub Actions schedule 트리거 자체의 지연/유실 가능성). 근본 원인은 Cowork 플랫폼 쪽 마운트 타이밍이라 코드로 완전 제거 불가. **다음에 재발하면 우선 확인할 것**: (1) 재시도 작업 SKILL.md에 STEP별 진행 상황을 중간중간 커밋/로그로 남기는 체크포인트를 추가해 어디서 멈췄는지 사후에 알 수 있게 할지, (2) health-check 워크플로우 자체의 GitHub Actions schedule 트리거 지연이 재발하는지, 재발하면 cron-job.org 같은 외부 백업 트리거를 health-check에도 붙일지 검토

### 우선순위 중간

- **(신규, 2026-09-03) Premium 나머지 3축을 언제 어떻게 만들지 — 결제 오픈 전까지 "준비 중" 배지가 계속 붙어 있게 두면 안 됨**: 현재 Premium이 실제로 파는 건 감시 슬롯 30개 하나뿐이다. 요금제 화면에 "준비 중" 3개가 그대로 노출된 채 결제를 열면 오히려 신뢰를 깎는다. 사업자등록·결제 오픈 시점 전에 **최소 1축은 더 완성**하는 게 목표. 난이도순으로 ①**과거 데이터·백테스트 열람** — `data/daily/`·`data/briefing/` 아카이브와 `data/kr-screener-cache.json`이 이미 있어 신규 파이프라인이 필요 없다(다만 지금 무료로 열려 있는 아카이브를 막는 건 기존 사용자 기능 회수라 하지 말 것 — Premium은 "조건검색 과거 시점 재현" 같은 **새 기능**으로 얹어야 한다) ②**관심종목 데일리 리포트** — DART 이벤트 배치(`dart-events.yml`)와 텔레그램 발송이 이미 있어 재활용 가능, 워치리스트를 사용자별로 저장할 Supabase 테이블만 추가하면 됨 ③**AI 종목분석 심화 리포트** — 무엇을 더 줄지 상품 기획이 먼저 필요.
- **(신규, 2026-09-03) 매매시그널 감시 슬롯 상한이 서버·화면 두 곳에 하드코딩되어 있다**: `api/analyze.js`의 `TS_MAX_ACTIVE_STRATEGIES_BY_PLAN`과 `trade-signal.html`의 `TS_STRATEGY_LIMIT_BY_PLAN`이 같은 값(pro 5 / premium 30)을 각자 들고 있다. **한쪽만 고치면 화면과 실제 저장 한도가 어긋난다** — 값을 바꿀 때는 반드시 두 곳을 같이 고칠 것. (공유 상수로 뺄 수도 있지만 정적 HTML이라 번들러가 없어 지금은 주석으로만 묶어 뒀다.)
- **(신규, 2026-09-03) Pro 감시 슬롯을 20개 → 5개로 줄인 것의 영향**: 결제가 아직 안 열려 유료 사용자가 없으므로 지금이 조정 적기라 판단해 내렸다. 다만 **오픈 후에 줄이는 건 불가능**하므로, 결제 오픈 전에 5개가 적정한지 한 번 더 판단할 것(너무 빡빡하면 Pro 자체가 안 팔린다).
- ~~**(신규, 2026-09-02, 사용자 확인 필요) 아이폰 사파리 자동 확대 수정이 실제 기기에서 해결됐는지**~~ **(2026-09-02 밤, 사용자가 실기기에서 "해결됨" 확인 — 종결)** 원인은 iOS Safari가 글자 크기 16px 미만인 입력창에 포커스가 가면 페이지를 강제 확대하고 그 배율·위치가 세션에 남는 동작이었고, `theme.css`에서 모바일 입력 요소를 16px로 고정한 것이 실제로 유효했다. **이후 같은 계열의 증상이 나오면 먼저 입력창 font-size부터 볼 것.**
- ~~**(신규, 2026-09-02) 모바일 접이식 섹션이 스크롤 중 튕기듯 자동으로 닫히던 버그**~~ **(2026-09-02 밤 수정·배포·검증 완료, 커밋 `c10c3cf`)** 사용자 제보: "마감시황 특징주·주목할 변수 아래로 스크롤 시 튕기듯 자동으로 닫힘". **원인은 iOS 사파리가 스크롤로 주소창을 접었다 펼 때마다 `resize` 이벤트를 쏘는 것**(폭은 그대로, 높이만 변함) — `daily-market.js`의 resize 핸들러가 그때마다 `renderAiPanels()`로 AI 패널을 통째로 다시 그렸고, 다시 그릴 때 접이식 상태가 기본값으로 리셋되면서 사용자가 펼쳐둔 블록이 닫혔다. 2겹으로 수정: ①레이아웃 모드는 폭으로만 갈리므로 **뷰포트 폭이 실제로 바뀐 경우에만** 재렌더(`lastViewportW` 비교) ②사용자가 직접 펼치거나 접은 섹션은 `dmxUserFold` Map에 기억해, 어떤 이유로 다시 그려져도 기본값으로 안 돌아감. 라이브 검증(모바일 폭 시뮬레이션): 특징주 펼침 → iOS형 resize 3연발에도 유지, 폭을 실제로 바꿔 강제 재렌더해도 특징주=펼침·내일변수=접힘 그대로. 브리핑은 resize 리스너가 없어 같은 증상 없음(확인함). **다른 페이지에 접이식을 새로 넣을 때 resize 재렌더가 있는지 먼저 볼 것.**
- **(신규, 2026-09-02) `data/daily/<날짜>.json` 아카이브에 당일 AI 분석이 안 들어간다**: 홈에서 "오늘 마감시황 핵심 한 줄"을 쓰려다 발견. 아카이브 파일은 시세 수집 단계에서 생성되고 그 뒤에 쓰이는 `analysis`가 반영되지 않아 **당일 파일에는 항상 분석이 없다**(다음날 채워짐). 3.3MB짜리 `data/daily-market.json`을 홈에서 통째로 받을 수 없고 Vercel Hobby 함수 12개도 이미 꽉 차서, `api/repo-data.js`에 `pick=latest-day` 파라미터를 붙여 **서버에서 최신 하루치만 잘라 내려주는** 방식으로 우회했다(함수 개수 변화 없음). 근본적으로는 마감시황 발행 작업이 아카이브 파일도 함께 갱신하도록 손보는 게 맞다 — 필요해지면 검토.
- **(신규, 2026-09-02) 모바일 접이식의 "기본 펼침" 구성이 적절한지**: 브리핑은 지수 마감·시장 온도·핵심 이슈 3개, 마감시황은 지수 마감·시장 온도 2개만 펼쳐 둔다. **마감시황의 '오늘의 특징주'는 값이 높은 콘텐츠인데 접혀 있다** — 며칠 써보고 "특징주는 열려 있는 게 낫다" 싶으면 `daily-market.js`의 `DMX_OPEN_ON_MOBILE` 정규식에 추가하면 된다(브리핑은 `BFX_OPEN_ON_MOBILE`).
- ~~**(신규, 2026-09-02, 사이트 전체 감사에서 발견) 증시 일정 페이지가 현재 사이트에서 가장 값싸 보이는 화면**~~ **(2026-09-03 4건 전부 수정·배포·실사이트 검증 완료, 커밋 `c5f3090`)** 2장 "증시 일정" 항목에 상세. 요약: 중복은 `renderUpcoming`을 선택일 **다음날**부터로 변경, 중요도 배지는 **소스가 high-impact만 수집해 403건 전부 `"high"`였던 게 근본원인**이라 지표 성격 기준으로 재설계(실측 핵심 11/주목 16/참고 11), 템플릿 문형은 **프롬프트가 그 문형을 스키마에 못 박아둔 게 원인**이라 `why/bull/bear/watch`로 분해, 빈 공간은 '다음 핵심 지표'+'이번 달 일정' 카드로 채움. **단 새 문체는 다음 배치(매일 06:00 KST `weekly-schedule.mjs`)부터 적용된다 — 3장 최상단 검증 항목 참고.**
- ~~**(신규, 2026-09-02) 시세 3종(실시간시세·미국주식·암호화폐)과 글로벌랭킹이 원시 표뿐**~~ **(2026-09-03 공통 컴포넌트 신설·4페이지 적용·실사이트 검증 완료, 커밋 `c5f3090`+`59b5e57`)** `assets/market-strip.js/.css` — 2장 "시장 요약 스트립(공통)" 항목에 상세. 네 페이지 모두 실데이터로 렌더되는 것까지 라이브 확인.
- ~~**(신규, 2026-09-02) 요금제의 Premium(19,900원)에 실질 차별점이 없다**~~ **(2026-09-03 상품 정의 확정 + 1축 구현 완료, 커밋 `c5f3090`)** 사용자가 4축(매매시그널 알림 / AI분석 심화 / 관심종목 데일리 리포트 / 과거 데이터·백테스트)을 선택했고, 그중 **지금 코드로 바로 되는 매매시그널 감시 슬롯(Pro 5 / Premium 30)을 구현**했다. 나머지 3축은 "준비 중" 배지로 명시하고 4장 할 일로 이월. 2장 "회원가입/로그인/마이페이지/요금제" 항목에 상세.
- ~~**(신규, 2026-09-01) 마감시황 신규 스키마 6종이 실제 리포트에서 채워지는지 — 내일(9/2) 16:15 발행분에서 첫 검증**~~ **(2026-09-02 밤 검증 → 전부 미충족, 근본원인 발견·수정. 위 "우선순위 높음" 최상단 항목으로 승격)** 아래는 당시 기록 — `marketTone`, `indexDetail`(시/고/저), `investor_trend.etc`(기타법인), `sectors`(업종 등락), `netBuyLeaders`(순매수 상위), `featured_stocks[].risk`를 `scripts/daily-market-ai.mjs` 스키마·규칙에 추가했지만, 실제로 Cowork 예약작업이 이 필드들을 채워서 내려주는지는 아직 한 번도 안 돌아봤다. 특히 확인할 것: ①`sectors`가 뉴스에서 업종 등락률을 못 찾으면 배열 자체를 생략하는지(빈 배열이나 추정치로 채우면 안 됨) ②`featured_stocks[].risk`가 `point`와 내용이 겹치지 않고 실제로 "지금 따라 사면 뭐가 위험한지"를 쓰는지 ③`indexDetail`의 고가/저가가 실제 확인된 값인지(뉴스에 없는데 지어내면 레인지 바가 거짓말을 하게 됨). 하나라도 부실하면 프롬프트를 더 조여야 함. **렌더러는 값이 없으면 블록 자체를 안 그리도록 이미 방어돼 있어서, 최악의 경우에도 화면이 깨지진 않고 블록이 사라지기만 함.**
- ~~**(신규, 2026-09-01) 마감시황 덱 렌더러의 과거 데이터 호환성 — 5~6월 구형 레코드에서 폴백이 실제로 동작하는지 미확인**~~ **(2026-09-02 밤 검증 → 폴백이 실제로는 동작하지 않는 버그 발견, 수정·배포·라이브 검증 완료. 커밋 `8bd8221`)** 5/12·6/2·7/1 세 날짜를 실사이트에서 직접 열어본 결과 화면이 깨지지는 않았지만, **7/1은 2,001자짜리 `analysis` 본문이 화면에 한 글자도 안 나오고 있었다.** 원인은 폴백 판정이 "덱이 비었는가" 기준이었던 것 — 지수·수급 같은 구조화 블록은 파싱과 무관하게 그려지므로 덱이 항상 "비어있지 않음"이 되어 상위의 줄글 폴백 경로가 영영 실행되지 않았다. `dmxRenderProseFallback()`을 신설해 파싱 실패 시 원문을 덱 안에 "AI 종합분석" 블록으로 넣도록 고쳤고(`.dmx-prose` CSS 추가), 실사이트 `#2026-07-01`에서 2,001자 정상 렌더 확인. **대상은 7/1 단 하루**(analysis가 있는 53일 중 이모지 헤더가 없는 유일한 날). 5월은 `summary`/`themes` 스키마라 analysis 자체가 없어 히어로만 표시되는 게 정상, 6월은 `aiAnalysis` 스키마.
- ~~**(신규, 2026-09-01) DART 이벤트 문구 변경이 다음 배치 실행 후 화면에 반영되는지**~~ **(2026-09-02 밤 확인 완료)** 배치가 9/2 08:18 KST에 정상 실행됐고(`meta.lastUpdatedKst`, status ok), 문구도 새 명사구 형식으로 바뀌었다 — 신한지주(055550) `"배당기준일 2026-11-03 — 이 날까지 보유해야 배당 대상"`. 나머지 14종목은 빈 배열인데, 9월 초는 배당·주총 시즌이 아니라 정상 범위로 판단(잘못된 날짜를 만들어내는 것보다 안전한 방향).
- **(2026-09-02 밤 부분 확인) DART 기반 "다가오는 이벤트" 배치 파이프라인 — 며칠간 안정성 관찰 계속**: 9/2 06:10 KST 실행분이 정상 성공(`status: ok`)했음을 확인. 다만 `corp_code` 캐시(30일 TTL) 첫 자동 갱신과 배당·주총 시즌(연말·3월) 부하는 아직 안 겪어봤으므로 관찰 항목으로 유지. 아래는 원래 기록 —
- **(신규, 2026-08-28) DART 기반 "다가오는 이벤트" 배치 파이프라인 — 신규 기능이라 며칠간 안정성 관찰 필요**: 코드·라이브 검증(SK하이닉스·삼성전자)은 이번 세션에 완료했지만, 매일 06:10 KST GHA(`dart-events.yml`)가 며칠 연속 정상 성공하는지, corp_code 캐시(`data/dart-corp-codes.json`, 30일 TTL) 첫 자동 갱신이 문제없이 도는지, 워치리스트 15종목 외 실제 배당·주총 시즌(연말·3월 정기주총 등)에 이벤트가 몰릴 때도 정규식 추출이 잘 버티는지 다음 몇 차례 실행에서 확인 권장. `findDateNear`가 앵커 다중 등장을 순서대로 시도하는 로직이라, 극단적으로 이례적인 공시 문서 포맷을 만나면 여전히 놓칠 가능성 있음(그 경우도 항상 "표시 안 함"으로 안전하게 fail — 잘못된 날짜를 보여줄 위험은 낮음).
- ~~**(신규, 2026-08-26 3차 후속 세션) 거래중지/정리매매 필터 + 감자·액면병합 비정상 급등 필터 — 캐시 재생성 후 라이브 확인 필요**~~ **(2026-09-02 밤 확인 완료)** 9/1 22:41 KST 캐시(`data/kr-screener-cache.json`, 2,765종목·실패 0)를 직접 열어 확인: `tempStopYn`/`settlementTradeYn` 2,765/2,765 전부 채워짐(정리매매 종목으로 알에프세미 1건 검출), `api/analyze.js:3567`에서 실제로 검색결과에서 제외 중. 감자·액면병합 이상감지도 동작 — **107종목**의 해당 구간 기간수익률만 `null` 처리됐고(예: 알에프세미 21/63/126/252일 전부 null, 디에이테크놀로지 126·252일 null), 1년 수익률 상위가 성호전자(+1,466%)·대한광통신(+1,316%)·삼성전기(+799%) 같은 실제 급등주로 정리됐다. 아래는 당시 기록 — 코드(`3bd00239`)는 배포까지 확인됐지만, `row.snapshot.tempStopYn`/`settlementTradeYn`과 `computePeriodReturns`의 이상감지 로직 모두 다음 정기 `build-screener-cache.mjs` 실행(평일 16:00 KST)이 캐시(`data/kr-screener-cache.json`)를 다시 만들어야 실제로 채워짐 — 그 전까지는 필드가 `undefined`라 필터가 사실상 no-op. 다음 세션에서 캐시 갱신 이후 실제로 디에이테크놀로지·피씨엘 같은 거래중지 종목이 검색결과에서 빠졌는지, "1년 상승률" 같은 정렬 검색에서 감자성 900%+ 수익률이 더 이상 안 뜨는지 확인할 것
- ~~**(신규, 2026-08-26 3차 후속 세션) 종목검색 정렬/스크롤 버그 수정 3건 — 배포 후 라이브 확인 필요**~~ **(2026-09-02 밤 라이브 확인 완료)** 실시간시세에서 "삼성전자" 검색 시 **삼성전자(005930)가 1위, 삼성전자우(005935) 2위**, 그 뒤에 KODEX/ACE/RISE 같은 ETF — 원래 문제였던 "ETF가 먼저 뜨고 삼성전자는 목록 밖" 증상 해소 확인. 드롭다운도 `max-height:360px` + `overflow-y:auto`로 스크롤됨. 미국주식은 "AAPL" 검색 시 **애플(AAPL) 1위**, 해외 상장 변형(AAPL19.BK / AAPL.SN / AAPLC.BA)이 후순위. AI 종목분석 페이지는 비로그인 게이트 때문에 검색창에 직접 접근할 수 없어 미확인이지만, 실시간시세와 **동일한 점수체계(`findStockMatches`)를 공유하도록 통일한 커밋**이라 함께 해결된 것으로 판단(정 필요하면 로그인 세션에서 한 번 더 볼 것)
- ~~**(신규, 2026-08-26) 매매시그널 신규 필드 실데이터 채움 확인 필요**~~ **(2026-09-02 밤 확인 완료)** 9/1 캐시 기준 2,765종목 중 `per`/`pbr`/`eps`/`foreignHoldRate`/`volTurnoverRate` **2,765건 전부**, `foreignNetBuy`/`institutionNetBuy` **2,641건**(나머지는 KIS `inquire-investor`에서 값이 안 오는 종목으로 추정) 채워짐. 캐시 빌드 실패 종목 0개(`failedCount: 0`)라 API 호출 1.5배 증가로 인한 실패율 상승도 없었다. ※ 필드명은 캐시·평가 코드 모두 `volTurnoverRate`(`volumeTurnoverRate` 아님)로 일관되어 있으니 조회할 때 헷갈리지 말 것. 아래는 당시 기록 — `lib/kis-indicators.js`(`fetchMarketSnapshot`/`fetchInvestorFlow`), `scripts/build-screener-cache.mjs`/`scripts/trade-signal-scan.mjs`에 PER/PBR/EPS/외국인·기관 순매수 등을 추가했지만 이 세션 sandbox엔 실제 KIS 자격증명이 없어 직접 실행 검증은 못 함. 다음 정기 `build-screener-cache.mjs` 실행(전종목 스캔, 종목당 API 호출 약 1.5배 증가) 후 `data/kr-screener-cache.json`에서 신규 필드가 실제 non-null 값으로 채워지는지, 실행시간·실패율이 늘지 않았는지 확인할 것
- **(2026-08-26 갱신) 매매시그널 HTS급 확장 실사용 종단 테스트 — 일부 완료**: `period_return`(기간수익률) 조건은 버그 수정 후 Claude-in-Chrome으로 실제 로그인 세션에서 직접 재현·검증 완료(52개 종목 정상 표시). 그 외 신규 타입(키움 조건표 붙여넣기, price/per/pbr/foreign_net_buy 등, OR/NOT/within 조합)은 여전히 Claude가 직접 못 해봤음(코드 로직은 합성 스냅샷 유닛테스트 22건으로만 검증됨) — 사용자가 한 번씩 테스트해보고 이상 있으면 제보할 것
- ~~**(신규, 2026-08-26) Vercel 'vibe' 중복 프로젝트 — 사용자가 최종 삭제 완료**~~ **(2026-09-02 밤 재발 없음 확인 — 종결)** `list_projects` 결과 팀 내 프로젝트는 `bori-cal`, `big5-spectrum-test`, `matchuplab` 3개뿐이고 'vibe'는 없다. 근본원인이었던 `kr-screener-cache.yml` env 누락 수정(`27ea7642`) 이후 정기 실행이 여러 차례 돌았는데도 재생성되지 않았으므로 종결로 본다. 아래는 당시 기록 — 8/26 낮에 삭제 → 저녁에 재발 확인 → 근본원인(`kr-screener-cache.yml` env 누락) 수정(`27ea7642`) → 사용자가 최종적으로 다시 삭제해 `list_projects`로 완전히 사라진 것까지 확인(3장·5장·6장 상세). 재발 원인은 제거됐으니 유지될 것으로 예상되나 다음 세션에서 실제로 재발 안 하는지 한 번 더 확인 권장
- **(신규, 2026-08-26) 즉시검색 정렬(sort) 파싱이 같은 문장에도 약 20% 확률로 "이해 못함"으로 실패**: "1년동안 상승률 높은 순서대로 20종목 보여줘"를 라이브에서 반복 테스트한 결과 API 직접 호출 5회 중 1회, UI 클릭 3회 중 2회가 동일 문장인데도 `understood:false`로 실패함(모델이 `condition.clauses:[]`를 "채울 게 없어서 이해 못함"으로 오인하는 것으로 추정). 가이드에 few-shot 예시 추가로 완화 시도(커밋 `2bd7a0d4`)했으나 **이 커밋은 아직 배포 확인 못 함** — 같은 날 낮 동안 'vibe' 중복 프로젝트가 배포 횟수를 2배로 소모하고 있었어서 Vercel Hobby 일일 배포 한도(100회)를 이미 넘겼을 가능성이 높음(push는 GitHub엔 정상 반영됐지만 Vercel 배포 목록에 해당 커밋의 배포 자체가 안 잡힘). **(2026-08-26 3차 후속 세션에서 갱신)** `vercel-deploy-catchup.yml`이 매일 09:00 KST에 자동 재배포하도록 되어있다고 알고 있었는데, 실제로는 cron이 `"0 0 21 5 *"`(2026년 5월 21일 단 하루)로 박혀 있어서 "매일"이 전혀 아니었던 별도 버그였음이 드러남(아래 5장·이번 세션 6장 항목 참고, `cfc312cc`로 수정). 대신 사용자가 GitHub Actions에서 "Deploy Vercel (catch-up)"을 수동 실행해 `3bd00239`(이 커밋의 상위 커밋이라 `2bd7a0d4`도 포함)까지는 배포 완료(`dpl_Gu59nbUdCt1j1sU76SvaHAFbzGLt`, READY) — 즉 **`2bd7a0d4`의 few-shot 완화는 이제 라이브에 반영된 것으로 판단됨.** 다만 실제 재현율(원래 ~20% 실패)이 얼마나 줄었는지는 아직 재측정 안 함 — 다음 세션 숙제로 유지
- **(신규, 2026-08-18) Cowork 클라우드 세션 git-proxy 버그 — Anthropic 측 미해결, 재발 여부 지켜볼 것**: 5장에 상세 기록. 현재는 "클라우드에서 새 작업 실행" 토글을 꺼서 로컬 실행으로 우회 중이라 당장 급하지 않지만, Anthropic이 이 이슈를 언제 수정하는지, 혹은 로컬 실행 방식이 다른 부작용(아래 항목 참고)을 내는지 다음 세션들에서 계속 관찰할 것
- **(신규, 2026-08-18) 로컬(PC) Cowork 워크스페이스 디스크 부족 에러 재발 가능성**: 위 우회책 전환 직후 실제로 한 번 발생(예약작업이 "워크스페이스 설정 디스크 공간 부족"으로 실패) → 사용자가 C: 드라이브 여유공간을 확보해 해결했으나, 원인이 Cowork 로컬 샌드박스 자체의 디스크 할당(D:\vibe 마운트와는 별개)으로 추정되어 C: 여유공간이 다시 줄면 재발할 수 있음. 재발 시 D: 드라이브가 아니라 **C: 드라이브 여유공간**부터 확인할 것
- **(신규, 2026-08-18) 마감시황 특징주 검증 수준 확인 필요**: 로컬 실행으로 전환된 첫 예약작업(08-18)이 생성한 `featured_stocks`(10종목)에, 담당자가 별도로 뉴스 원문까지 대조하지는 않은 상태로 발행됨(재료 텍스트 자체는 구체적이었음). 5장의 "특징주 선정 규칙"(뉴스로 이유 명확한 종목만) 준수 여부를 다음 발행 몇 건에서 한 번 더 확인해볼 것
- **GitHub PAT 로테이션(급하지 않음, 2026-08-09 재평가)**: 전체 git 커밋 히스토리(2,665개)를 PAT 전체 문자열로 검색한 결과 일치 없음 확인 — 평문 노출은 로컬 전용 파일(`archive/`, 과거 Cowork SKILL.md 등)에만 있었고 GitHub에 push된 적은 없는 것으로 판단됨. 공개 유출 위험은 낮으나 안전 마진 차원의 재발급은 여전히 권장(급하지 않음)
- **인스타 카드뉴스 v2(경쟁사 벤치마킹) 목업의 실제 반영 여부 불확실**: 목업 승인 후 실제 프로덕션 템플릿 반영("Task #18")까지 이어졌는지, 아니면 릴스 전환으로 자연 폐기됐는지 문서상 불명확
- **Gemini 이미지 API 쿼터(429) 간헐적 초과(2026-08-13 관찰)**: 글로벌랭킹 릴스 배경 생성 시 Gemini가 HTTP 429(쿼터 초과)로 실패하고 OpenAI(GPT Image)로 자동 폴백되는 사례 확인. 폴백이 정상 동작해 결과물엔 문제없었으나, Gemini 쿼터가 자주 걸리면 다른 릴스(모닝브리핑·마감시황)도 계속 OpenAI 폴백만 타게 될 수 있어 Gemini 사용량/플랜 확인 권장
- **GitHub Actions `workflow_dispatch` publish 옵션의 기본값 관련 해프닝(2026-08-13)**: 글로벌랭킹 리디자인 검수용으로 `instagram-globalranking-post.yml`에 `publish`(boolean) 입력을 추가했는데 기본값이 `true`라서, 사용자가 검수 없이 그냥 "Run workflow"만 눌러 실제 발행까지 바로 진행돼버렸음(결과물 자체는 정상이라 문제는 없었음). 다음에 비슷한 "검수 후 발행" 안전장치를 만들 때는 기본값을 `false`(안전한 쪽)로 두는 것을 고려할 것
- **KIS 실시간시세 간헐적 `500 SESSION FULL` 에러**: 발견 후 원인 미조사 상태로 남아있음(2026-08 "시세 조회 실패"는 별개 원인인 KIS 토큰 갱신 실패였고 이건 해결됨 — 세션 풀 에러 자체는 미해결일 가능성)
- **매매시그널 암호화폐 미지원**: 국내주식 전용, 크립토 스크리닝은 별도 데이터소스/캐시 빌더 필요
- **네이버/KIND 실적 스크레이핑 구조적 불안정**: DART 우선 병합으로 완화했지만, "연도 없는 날짜에 현재 연도 강제 매핑" 로직 자체는 남아있어 잠재적으로 허위 날짜 생성 위험 존재
- **암호화폐 한글명 검색 제한**: 정적 별칭 테이블(27개)에만 한정, CMC는 한글 검색 미지원이라 구조적 한계

### 우선순위 낮음 / 참고용
- **저장소/폴더명 `vibe` → `TotalMoneyAI` 리네임**: 2026-08-11에 위험도(특히 `raw.githubusercontent.com` URL은 리네임 후 자동 리다이렉트 안 됨 — `daily-market.js`/`weekly-market.js`/`assets/data-url.js` 등 즉시 깨짐)를 분석해 제시했으나, 사용자가 "너무 복잡해지겠다, 이건 건들지 말자"고 보류 결정함. 사용자가 다시 요청하기 전까지는 재검토하지 않을 것
- world-market.html 로고 아이콘 미표시(테스트 환경 이모지 폰트 부재로 추정, 실사용자 환경에서는 정상일 가능성 높음, 미재확인)
- investing.com 스크레이핑 GitHub Actions IP 차단(403) — ForexFactory로 대체되어 안정 동작 중
- 네이버 금융 실적 캘린더 404 — DART가 대체 소스로 동작 중
- `url.parse()` Deprecation 경고 다수 — Node 버전 업그레이드 시 WHATWG URL API 교체 권장
- `trade-signal.html`의 죽은 정적 마크업 잔재(런타임에 항상 덮어써짐, 기능엔 문제 없음)
- 정기결제(자동 갱신) 미구현 — 현재 결제 1건당 "30일 연장" 방식. 포트원 빌링키 연동 시 가능
- 결제 취소/환불 API 미구현 — 콘솔 수동 취소 후 Supabase 테이블 직접 수정 필요
- 이메일 인증 발송량 — Supabase 기본 발송 한도가 낮음, 정식 오픈 전 커스텀 SMTP(Resend/SendGrid) 권장

---

## 4. 다음에 할 일 (후보)

- **(신규, 2026-09-26 저녁) 시세 데이터 합법화 경로 결정** — 메뉴별 결론: 리포트형(브리핑·마감시황·종가시그널·AI 종목분석)은 유사투자자문 신고로 판매 형태 OK, 단 재료 시세는 별도 계약 필요(종가시그널도 시세 가공이라 예외 아님). KRX 연간 구독(T-1, 오전 9시 수신)으로 되는 것 = AI 종목분석 통계·성적표·백테스트(오전브리핑은 9시 이후 발행 시). 당일 데이터 필요한 마감시황·종가시그널 = 코스콤 실시간 계약. 공시(OpenDART)는 상업이용 금지 조항 없음 → 공시 기반 콘텐츠(공시 후 주가 반응 통계 등)가 가장 안전한 유료 재료 후보. 코스콤 답변 후 시우 결정.
- **(신규, 2026-09-26 점심) 업황·컨센서스 데이터** — 리뷰 공통 지적: D램·HBM 가격, 증권사 영업이익 컨센서스 추이가 없다. 믿을 만한 무료 경로가 생기면 프롬프트 블록으로. 지어내기 금지 원칙 유지.
- **(신규, 2026-09-26 점심) 리포트 상세 접기** — 초보자 리뷰: "한눈에 보기는 위에, 전문 지표는 펼쳐 보는 상세로". 지금은 카드가 모두 펼쳐져 있다. 차트·수급 카드를 기본 접힘으로 할지 시우 판단.
- **(신규, 2026-09-26 오후) 유료 전환 시 법적 경계** — 다른 AI 검토 요지: 리포트 자체(개별성 없는 조언)는 유사투자자문 범위. 금지 영역 = 유료 양방향 채팅/Q&A, 보유종목 맞춤 조언, 환불·수익 보장 문구, 자동매매(bori) 연동, 좋은 기간만 고른 수익률 광고. 성적표는 손절 포함 전체 공개 유지.
- ~~(신규, 2026-09-26 점심) 시장 대비 위치의 PER 순위~~ **→ 2026-09-26 오후 완료(세션 10, 대상 종목만 TTM PER로 교체. 비교군 PER은 여전히 직전 연간 기준 — 전종목 TTM화는 DART 호출량 문제로 보류)** — 전종목 캐시 PER(직전 연간 기준)로 순위를 매겨 실적 급변 종목은 "비싼 축"으로 잘못 나올 수 있다. TTM PER은 지금 실적 카드·프롬프트에만 있다.
- **(신규, 2026-09-26 낮) AI 성적표 고도화** — 표본이 쌓이면 ①신호(매수/관망)별·기준 시나리오별 적중률 ②"과거 비슷한 국면 통계"와 실제 결과 비교(통계가 맞는지 검증) ③유료 오픈 때 공개 성적표로(유사투자자문 규칙상 목표가 표현은 점검 필요).
- **(신규, 2026-09-26 낮) 암호화폐 모바일 상세** — 오른쪽 상세 정보(공급량·링크)는 PC 분할 화면에만 있다. 모바일 펼침에도 넣을지 시우 판단.
- **(신규, 2026-09-26 낮) 유사 국면 조건에 수급 추가** — 지금은 과거 일별 외국인·기관 이력이 없어 조건에서 뺐다. 이력을 쌓기 시작하면 몇 달 뒤 넣을 수 있다.

- **(신규, 2026-09-26) 상세 패널 즉시 표시** — 국내주식 오른쪽 상세를 목록 행 데이터(가격·등락률)로 먼저 그리고, 느린 시세 API(1~4초) 값은 도착하면 채우기. 캐시가 비었을 때도 첫 화면이 빨라진다.
- **(신규, 2026-09-26) 읽는 페이지 배치를 다른 페이지로** — 시장지표·글로벌랭킹도 1640px 폭·글씨 기준을 맞출지 시우 판단.

- **(신규, 2026-09-25 저녁) 히트맵 후속 후보** — ①섹터 RS(20거래일 기록 필요) ②섹터 머리글 클릭 시 해당 섹터만 확대 ③코스피+코스닥 합친 "전체" 탭 ④일봉 외 기간(1주·1개월 등락) 전환. 전부 AI 비용 0.
- **(신규, 2026-09-25 저녁) 분봉 차트** — KIS는 분봉 제공(당일 위주). 일봉은 현재 400봉, 5~10년까지 확대 가능. 호출량 증가 주의(비공개 기간이라 부담 적음).
- ~~(2026-09-25 저녁) 마감시황 '시장 흐름' 본문 줄 길이~~ → **9/26 완료**(읽는 페이지 배치 개편, 6장).

**(2026-09-25 새벽 추가) 재편 방향 — 사업자등록(개인사업자, 2027) 전까지 "가다듬기"**
- 공개: 가격 숫자 없는 AI 분석 콘텐츠(장전 브리핑·마감시황·AI 종목 리포트·종가시그널 적중 기록). 불특정 다수에게 동일 게시(유사투자자문업 한쪽 방향 규칙), 목표가·매수·매도 지시 문구 금지.
- 개인: 네이버·토스·KIS 무제한 수집, 시우 전용 대시보드·알림·자동매매.
- 유료화: 등록 후 네이버 프리미엄콘텐츠 채널 → 구독자 쌓이면 자체 결제(포트원) 추가. 코스콤 정식 시세 계약은 **법인만** 가능해 현재 계획에선 제외.
- 개인 대시보드 확장 후보: 매매시그널 장중 스캔 결과(trade_signal_intraday — 지금 RLS 정책 없음, 운영자 읽기 정책 필요), 투자자별 수급, 업종·테마, 토스 인기 순위.

**(2026-09-24 심야 추가) 전체 리뉴얼 후보 — 토큰 0으로 가능한 것**
- 홈 4칸 실시간 랭킹판(국내 / 미국 / 미국 ETF / 업비트·빗썸 전환) — 업비트·빗썸 공개 시세 무료, 미국 ETF는 인기 50~100개 목록 기준. "인기 종목" 탭 대신 "AI 분석 많이 본 종목".
- 종목 상세 페이지(차트·시총·거래 정보), 관심 종목(로그인), 52주 최고/최저, 투자자별 매매동향, 업종·테마 등락.
- 토큰 드는 것(착수 전 시우에게 비용 고지): 종목별 AI 한 줄 요약·뉴스 요약, AI 테마 자동 분류.
- 모바일 전체보기 시트를 GNB와 같은 묶음(시장 그룹)으로.
- ⚠️ 모든 데이터 기능은 3장 7번(시세 라이선스) 결정에 묶여 있다.

**(2026-09-23 추가) 디자인 마감 후속**
- 페이지별 2차 다듬기 후보: 홈 위젯 탭(검은 알약 세그먼트)이 무겁다 / 암호화폐 요약 카드 간격이 다른 페이지(12px)보다 좁다 / 시장지표 모바일 그리드에서 홀로 남는 카드.
- `scripts/ui-audit.mjs`는 기본(다크)만 검사한다 — 라이트 전수 검사 옵션 추가하면 라이트 대비 미달도 잡힌다(라이트 초록 버튼 #059669+흰 글씨는 3.8:1).
- 일정 페이지 모바일 일정분석 캐러셀에 "몇 장 중 몇 번째" 점 표시.

**(2026-09-13 추가) 아이콘·레이아웃 후속**

- **매매시그널 소개 카드 정리, 각 페이지 내부 컴포넌트 정리** — 이번에 실시간시세 탭·표 밀도까지는 했고 나머지는 미착수.
- **아이콘이 이상한 종목 수집** — 시우가 화면을 보다가 이름만 말해 주면 내가 고친다. (`kr-domains.json` 도메인 추가 / `kr-favicon-skip.json` 제외 / `kr-tint-overrides.json` 색 지정 — 셋 다 한 줄이면 된다. **시우에게 이 파일들을 직접 고치라고 안내하지 말 것** — 코딩을 안 하는 사람에게 JSON 문법과 파일 경로를 넘기는 건 "쉬운 방법"이 아니다.)
- **동명이인 함정 주의** — 도메인을 추가할 때는 반드시 몽타주로 눈으로 확인한다. `samyang.com`은 삼양그룹(삼양식품 아님), `poongsan.com`·`ecopro.com`은 외국 동명 회사였다. 128px 정상 이미지로 내려와서 기계 검사는 통과한다.


**(2026-09-12 추가) 종가베팅 랭킹 후속**

- ~~**재료 팩터 20점 구현(2차)**~~ → **2026-09-18 완료.** 단, AI 등급 판정 대신 **DART 공시 제목의 정규식 분류**로 구현했다(할루시네이션 차단). 네이버 종목뉴스는 쓰지 않았다. 아래는 당시 계획. 현재 80점 만점으로 돌고 있다. DART 공시(`dart-events.mjs`) + 네이버 종목뉴스를 종목에 매핑하고 AI로 재료 등급(수주 8 · 실적 7 · 정책 6 · 신제품/임상 5 · 단순테마 3)을 매기는 구조. **붙일 때 후보 전원에게 붙일 것**(일부만 있으면 종목별 분모가 달라져 순위가 왜곡된다). 악재성 공시(유상증자·CB·대주주매도)는 하드필터에서 제외하는 쪽이 맞다.
- ~~**성과표 공개 + 카드뉴스화(수익화 연결)**~~ → **2026-09-21 완료.** 화면에는 성과 달력 + 누적 성적을 **전부 공개**로 넣었고(무료 게이트는 오늘 랭킹에만 적용), 인스타 성적표 카드뉴스를 **토요일 11:00 KST 주 1회 자동 발행**으로 신설했다(`instagram-carousel-close-signal.yml` + `scripts/promo-build-deck-close-signal.mjs`). **이 덱만 AI를 부르지 않는다** — 다른 덱은 '숫자는 코드, 문장은 AI'인데 성과 수치는 한 글자만 틀려도 신뢰가 날아가고 문장 자체가 숫자 서술이라 AI가 낄 이득이 없다. 발행당 토큰 비용 0.
- ~~**하드필터 임계값 실운영 튜닝**~~ → **2026-09-21 일부 반영**(시총 하한 1,000→800억, 등락 상한 25→20%, 종가위치 0.6→0.55). 거래대금 하한 100억은 그대로 뒀다 — 실측에서 거래대금 구간별 익일 성과 차이가 뚜렷하지 않았고, 하한을 올리면 통과 종목이 5개 아래로 떨어지는 날이 생긴다.

**(2026-09-21 추가) 종가시그널 v2 후속**

- **기법별 적중률로 가중치 재조정 — 기록 20거래일이 하한.** 현재 배점(기법 합의 35 / 수급 25 / 당일 흐름 20 / 추세 위치 20)은 **5거래일 근거**다. `close_signal_results`의 `picks[].strategies`에 어떤 기법에 걸렸는지가 종목마다 남으므로, 쌓이면 기법별 익일 성과를 바로 집계할 수 있다. 기법을 빼거나 가중치를 바꿀 때는 `scripts/close-signal-backfill.mjs`로 전 구간을 다시 채점해 before/after를 비교할 것.
- **시장 전체가 빠진 날 쉬는 규칙을 둘지 검토.** 9/15는 후보 400종목 평균도 마이너스였고 모델도 같이 졌다(-2.06%). 지수 방향이나 상승/하락 종목수로 '오늘은 선정하지 않음'을 만들 수 있지만, **표본 1일로 만들면 과적합이다** — 기록이 더 쌓인 뒤 판단.
- **텔레그램 발송** — 15:20 선정 5종목을 그대로 밀어주는 게 가장 실전적이다(정규장 종가 매수 전에 손에 들어와야 함). 기존 텔레그램 워크플로 패턴 재사용. 단 **유사투자자문 문구 검토가 먼저다** — 푸시로 종목을 보내는 건 화면에 띄우는 것보다 '추천'으로 읽힐 소지가 크다.
- **종가시그널 성적을 요금제 페이지에 노출** — 지금은 종가시그널 페이지 안에만 있다. Pro 전환의 근거로 쓰려면 `pricing.html`에서도 최근 누적 승률을 보여주는 게 낫다(같은 `?feature=close-betting&view=history` 응답을 재사용하면 된다).

**(2026-09-11 추가 → 09-11 밤 갱신) 남은 것 — 샌드박스 복구를 더 기다릴 필요는 없어졌다**

- **2026-09-10 마감시황 백필** — **경로 확보됨(2026-09-11).** 이제 Actions에서 `Daily Closing Report` → `date=2026-09-10`, `force=1`로 돌리면 된다. 5.1MB JSON 병합용 일회용 워크플로도, 브라우저 8~13만 토큰도 더는 필요 없다. **주의**: 과거 날짜는 언론사 RSS가 당일 기사만 주므로 종목 재료가 얇게 나온다 — 스크립트가 네이버 종목뉴스를 대상일±1일로 필터링해 엉뚱한 날 뉴스를 재료로 붙이는 건 막지만, 품질을 원하면 `news_file` 입력으로 미리 모은 뉴스 JSON을 넣어라(`NEWS_CONTEXT_FILE`). **인스타 재발행은 하지 않는다**(이틀 지나 피드가 꼬임) — 사이트 아카이브 구멍만 메우는 목적. 운영자 결정: **9/12에 진행**(2026-09-11 밤).
- ~~**카드뉴스 자동 복구 트리거**~~ → **2026-09-11 완료.** `instagram-carousel-v2.yml`에 `push: paths: [data/daily-market.json]` 추가. 리포트가 몇 시에 커밋되든 그 즉시 발행된다. 기존 가드(주말·중복발행·데이터준비)가 그대로 걸려 중복 위험 없음. **실측 검증: 9/11 19:52 KST에 자동 발행됨**(스탬프 `2026-09-11`, 슬라이드 6장). 이틀 비어 있던 계정이 이 경로로 복구됐다.
- ~~**마감시황 API 폴백(비용 발생 — 운영자 승인 필요)**~~ → **2026-09-11 완료 — 폴백이 아니라 주 경로로 승격.** 운영자 승인을 받아 OpenAI를 폴백이 아닌 기본 생성 경로로 넣었다(2장 "마감 리포트 자동 생성"). 사고일에만 쓰는 구조로는 PC 의존이 남기 때문이다. 정상일에도 매일 과금되지만 월 3천원선이다.

**(2026-09-08 밤 추가) 매매시그널 후속 후보**

- **프리셋 유료화 설계**: 매매기법 프리셋은 "조건을 직접 짜야 하는 툴"을 "누르면 나오는 전략"으로 바꾸는 지점이라 결제 이유가 되기 쉽다. 안 → 무료는 프리셋 1개 맛보기 + 결과 3종목까지, Pro는 전체 프리셋 + 장 막판 실시간 스캔 + 텔레그램 알림. **결제는 사업자등록 완료 후이므로 구조만 준비**
- **프리셋 결과 텔레그램 발송**: 15:36에 종가 수급매매 결과가 나오면 그대로 밀어주는 게 가장 실전적이다(NXT 애프터마켓 진입 전에 손에 들어와야 함). 기존 텔레그램 워크플로 패턴 재사용
- **프리셋 확장**: 지금 3종은 수급·거래대금·차트 축만 쓴다. 재무 조건(이미 지원)까지 섞은 "실적 턴어라운드" 류나, 손절·목표가 가이드를 결과에 함께 얹는 안
- **장중 스캔 슬롯 추가 검토**: 현재 14:30·15:31 2회. 수급이 늘고 있는지 줄고 있는지 **추이**를 보려면 14:00을 더하는 안(호출 2,400회/일). 다만 실패 지점이 늘어나므로 2회 운영이 안정화된 뒤에
- **`snapshot.next` 활용 확대**: 지금은 장중 스캔만 쓰지만, 같은 값으로 "오늘 이 조건이 성립하려면 종가가 얼마여야 하는가"(역산)도 계산할 수 있다. 종가 베팅에 직접 쓰이는 정보

> **2026-09-02 밤 검증 세션에서 종결된 항목**: 23(매매시그널 신규 필드 캐시 반영) · 25(Vercel 'vibe' 재발) · 27(배포한도 리셋) — 아래 번호는 기록 보존을 위해 그대로 두되 더 볼 필요 없음. 26(정렬 few-shot 배포)은 배포는 확인됐고 **재현율 재측정만 남음**.

**최우선 (2026-09-08 신설) — 자동화 안전망 검증**

- **A. 인스타 pg_cron 이관 실동작 확인** (3장 최상단 항목과 동일). 9/9 08:30·17:30, 9/12(토) 10:30 세 시점을 보고 나면 이관이 끝난 것으로 확정할 수 있다.
- **B. 마감시황 재시도 작업 첫 실행 관찰** — 정상적인 날 조용히 끝나는지 / 실패한 날 실제로 대신 발행하는지. 도구 승인 프롬프트에서 멈추지 않게 한 번은 "지금 실행"으로 미리 승인해 둘 것.
- **C. 삭제된 `daily-report-watchdog.yml`을 되살릴지 결정** — 지금은 health-check(매시 5분)가 그 역할을 대신하지만, 알림이 최대 1시간 늦다(17:00~17:05 사이에 체크가 돌면 바로, 아니면 최대 다음 정시). 인스타 카드 마감(17:26)을 놓치지 않으려면 **17:05 전용 점검**이 하나 더 있는 게 안전하다. 다만 워크플로를 늘리는 만큼 관리 지점도 늘어나므로, 재시도 작업(16:50)이 몇 주간 잘 도는 걸 본 뒤에 판단할 것.
- **D. 다른 자동화에도 같은 "무음 실패" 구멍이 있는지 점검** — 이번 건의 본질은 "예약작업이 실행은 됐는데 결과물이 없어도 아무도 모른다"였다. health-check가 발행 결과물을 직접 확인하는 대상은 현재 **장전 브리핑·마감시황·인스타 2슬롯**뿐이다. `totalmoney-earnings-update`(평일 07:01), `totalmoney-ai-feed-seed`(평일 09:10), `kr-earnings-manual-json-healthcheck`(매일 07:15)는 결과물 검증이 없다. 각각 "오늘 갱신됐어야 할 파일"이 있으므로 같은 방식으로 한 줄씩 추가 가능.

**최우선 (2026-09-07 신설) — Anthropic 크레딧 + 카드뉴스 후속**

> 1번은 사이트 유료 기능 품질에 직결된다. 나머지는 카드뉴스 확장이라 급하지 않다.

1. **Anthropic 크레딧 상태 확인 및 조치** — 3장 최상단 참고. 카드뉴스 외 다른 자동화가 조용히 폴백으로 돌고 있는지 먼저 볼 것.
2. **캐러셀 첫 실전 발행 결과 확인**(아침·랭킹) — 3장 참고.
3. **요일별 콘텐츠 라인업 2단계** — 이번 세션에서 기획만 하고 미구현. 보유 데이터로 자동화 가능한 것만 골라 뒀다.
   - **월요일 "이번 주 시장 시간표"** (`weekly-schedule.json` + `earnings-calendar.json`) — 저장률이 가장 높은 포맷이고 주 내내 다시 열린다. 완전 자동화 가능.
   - **금요일 "이번 주 요약 + 다음 주 예고"** (`data-archive` 브랜치 스냅샷) — 주간 등락·수급 집계. 완전 자동화 가능.
   - **화요일 "숫자로 거른 종목"** (`kr-screener-cache.json` + `dart-financial-ratios.json`) — "PBR 1배 미만인데 1년 수익률 상위" 류. 일반 시세 사이트엔 없는 것이라 **유료 전환 미끼**로 가장 유효하다.
   - **목요일 "이건 알고 갑시다"** (그 주 마감시황에서 용어 1개 추출) — 공유·저장이 가장 많이 나오는 포맷, 초보 유입 통로. 주제 선택만 수동.
   - 권장 순서: **매일 3종을 4주 돌려 저장·팔로우 지표를 본 뒤** 월·금 추가, 그 다음 화·목.
4. **아침 스토리 + 저녁 리마인드 스토리** — `postInstagramStory`가 이미 있어 재사용만 하면 된다. 스토리는 피드를 더럽히지 않으면서 프로필 상단에 매일 노출되고, 링크 스티커로 사이트 유입 통로가 된다. 마감시황 1번 카드를 21시(국내 인스타 피크)에 스토리로 재노출하는 것도 같은 맥락.
5. **훅 성과 추적** — 훅 유형(A/D/E/C 등)이 덱 JSON에 `hookType`으로 남는다. 발행 후 인사이트(저장·도달)와 대조하면 **어떤 훅이 실제로 먹히는지** 데이터로 알 수 있다. 유형별 성과가 쌓이면 판정 우선순위를 조정할 근거가 된다.

**최우선 (2026-09-04 신설) — 소셜 로그인 오픈 마무리**

> 코드는 전부 배포 완료. 남은 건 전부 외부 콘솔·대시보드 작업이라 시우가 직접 해야 한다.
> 순서를 지키지 않으면 원인을 찾기 어려운 실패가 난다 — **Redirect URLs를 먼저 등록한 뒤 provider를 켤 것.**

1. **Supabase Redirect URLs 등록** (2분) — 안 하면 구글·카카오가 로그인 직후 튕긴다. 3장 참고.
2. **Supabase 비밀번호 정책 서버 적용** (2분) — 지금은 화면만 8자를 막고 서버는 6자를 통과시킨다. Leaked password protection도 함께 켤 것.
3. **구글 로그인 오픈** (15분) — Cloud Console OAuth 클라이언트 발급 → **동의 화면 프로덕션 게시** → Supabase Providers → `auth-config.js`의 `SOCIAL_ENABLED.google = true` → push. 승인 대기가 없어 당일 완료 가능.
4. **네이버 로고 등록 + 검수요청** — 지금은 시우 계정만 로그인된다. 로고 없으면 반려된다.
5. **카카오** — (2026-09-24 변경) 사업자등록을 기다리지 않고 **개인 개발자 비즈 앱 전환**(본인인증 + 데브톡 `account_email` 신청) → 이메일 동의항목 → `SOCIAL_ENABLED.kakao = true`. **이메일이 열리기 전엔 켜지 말 것**(이메일 없는 계정이 생겨 결제 연동이 깨진다).
6. (선택) 마케팅 수신 동의를 받아두기 시작했으므로, 실제 발송 채널이 생기면 `user_metadata.marketing_agreed`로 대상자를 뽑는 경로를 만들 것.

**최우선 (2026-09-03 밤 신설) — 유료 핵심 기능 퀄리티 향상 로드맵**

> 이번 세션의 큰 그림: **매매시그널·AI 종목분석을 하이엔드 유료 서비스 수준으로 올린다.** 현황 파악 결과 두 기능 모두 *프롬프트 품질은 이미 상당히 다듬어져 있고*(문체 규칙만 60줄), 부족한 건 글솜씨가 아니라 **무료 사이트엔 없는 데이터**였다. 사용자와 합의한 우선순위는 **AI 종목분석 먼저**, 데이터 축은 4개(①시장 대비 백분위 ②DART 실적 추이 ③업종 분류 ④리포트 저장·사후 추적).
>
> **1차(2026-09-03 밤)에 완료**: ① 백분위 카드 + ③ 업종 수집(다음 캐시 빌드부터 활성) + 시각 정리(배지 제거·도표 4종) + 재료 최신성 수정. **남은 것이 아래 D·E다.**

> **2026-09-04 새벽 갱신 — D·E·F 전부 처리 완료.** 아래 D·E·F 항목은 기록 보존을 위해 남겨두되, 실제 구현 내용은 2장(AI 종목분석 / 매매시그널)과 6장을 볼 것. **남은 것은 검증과 다음 단계뿐이다:**
>
> - **(다음 세션 1순위) 16:40 예약검증 `totalmoney-sector-peer-verify` 결과 확인** — 업종 비교군 활성화 여부. 3장 참고.
> - **(다음 세션 2순위) 재료 규칙 2건이 실제로 먹히는지 로그 재확인** — 실적 자체 금지 / "3개 또는 4개". 3장 참고.
> - **(다음 세션) `data-archive` 브랜치에 첫 스냅샷이 실제로 생성됐는지 확인** — `screener-snapshot.yml`이 `workflow_run`으로 자동 실행되며, 브랜치가 없으면 orphan 브랜치를 스스로 만든다. 첫 실행이라 이 생성 경로는 아직 실동작 미확인.
> - ~~**F 본작업(백테스트 화면)은 스냅샷이 최소 수십 일 쌓인 뒤에** 착수 가능. 그 전까지는 "왜 걸렸는지 근거 표시"를 먼저 하는 게 순서상 맞다.~~ **(2026-09-03 오후: 근거 표시 완료 — 2장 매매시그널 항목 참고.)** 백테스트 화면은 여전히 스냅샷이 쌓인 뒤다. **다만 스냅샷 축적 자체가 아직 첫 실행 미확인**이므로(3장) 그것부터 볼 것.
> - **E 후속**: 리포트 목록에 삭제 UI가 없다(API는 `DELETE`로 이미 있음). 그리고 사후 추적이 국내 종목만 된다(전종목 지표 캐시에 미국·암호화폐가 없어서) — 필요해지면 별도 시세 조회를 붙일지 검토.

- ~~**D. DART 실적 추이 카드 (2차 과제, 다음 세션 메인)**~~ **(2026-09-04 새벽 완료 — 선행과제 포함)** — AI 종목분석의 가장 큰 구멍은 **재무·실적 실데이터가 하나도 없다는 것**이다. 지금은 KIS에서 PER/PBR/EPS 숫자 하나씩만 받고, 분기 매출·영업이익 추이는 AI가 web_search로 추측한다(검증 불가·할루시네이션 위험). 이걸 **DART 원본 공시**로 대체하면 신뢰도가 가장 크게 오른다.
  - **선행 과제 — corp_code 전체 매핑**: 현재 `data/dart-corp-codes.json`은 **워치리스트 15종목만** 들어있다(`scripts/dart-events.mjs`가 `list.json` 스캔으로 알아낸 것). 임의 종목을 분석하려면 상장사 전체 매핑이 필요한데, 과거 `corpCode.xml`(87,346개 법인 zip) 다운로드가 GitHub Actions에서 **계속 타임아웃돼 포기한 이력**이 있다(2장 "AI 종목분석" 2026-08-28 항목 참고). 재시도할 때는 fetch 타임아웃을 넉넉히 주고, 상장사(stock_code가 있는 행)만 추려 `data/dart-corp-map.json`(약 2,700건, 수백 KB)으로 커밋하는 **1회성 워크플로**로 만들 것. **DART API 키는 GH Secrets/Vercel 환경변수에만 있어 샌드박스에서 직접 못 만든다 — 반드시 GHA로 돌려야 한다.**
  - **본 작업**: `fnlttSinglAcnt.json`(단일회사 주요계정)으로 최근 3개년 연간 + 최근 분기 매출액·영업이익·당기순이익을 받아 **막대 차트 + 전년동기 대비 증감률**로 카드화. 온디맨드 호출이면 `api/analyze.js`에 인메모리 캐시를 두고, 배치라면 워치리스트를 넓혀 `data/dart-financials.json`으로. **Vercel Hobby 함수 12개가 이미 꽉 찼으므로 새 `api/*.js` 파일을 만들면 안 된다**(현재 정확히 12개) — 기존 파일에 쿼리 파라미터로 얹을 것.
- ~~**E. 리포트 저장 · 사후 추적 (3차 과제)**~~ **(2026-09-04 새벽 완료 — 원문 보관까지)** — 발행한 리포트를 Supabase에 저장하고 "이 리포트 이후 주가가 어떻게 됐는지"를 나중에 보여준다. **유료 재방문 이유 + 실력 증명**이 동시에 되는 축이고, 3장의 Premium 미완성 3축 중 "AI 종목분석 심화 리포트"의 실질적 내용물이 될 수 있다. 새 테이블(`analysis_reports`) + `api/analyze.js`에 `feature=reports` 분기(새 함수 파일 금지)로 구현. 사용자가 4개 축을 모두 선택했으므로 **하겠다고 약속된 항목**이다.
- **F. 매매시그널 차례 (AI 종목분석 정리된 뒤)** — **(2026-09-04 새벽: 선행과제인 일자별 스냅샷 축적 완료 / 2026-09-03 오후: "왜 걸렸는지 근거 표시" 완료 + 계획에 없던 재무비율 조건검색까지 신설. 남은 건 백테스트 화면 본작업이고 스냅샷이 쌓여야 한다)** — 이번 세션 현황 파악에서 확인한 가장 큰 구멍은 **백테스트·성과 추적이 없다는 것**이다(HTS급 조건검색인데 "이 조건이 과거에 통했나"를 못 보여준다). 그다음이 검색 결과에 "왜 걸렸는지" 근거 표시. 백테스트는 과거 스냅샷 축적이 필요하므로(`kr-screener-cache.json`은 매일 덮어써진다) **먼저 일자별 스냅샷을 쌓기 시작하는 것**부터 검토할 것.

**최우선 (2026-09-02 밤 신설)**

- **A. 9/3 16:15 마감시황 발행분에서 STEP 3b 구조화 필드 5종이 실제로 채워지는지 확인** — 3장 "우선순위 높음" 최상단 참고. 이번 세션에 예약작업 SKILL.md를 고쳐놨으므로 내일이 첫 실검증이다.
- **B. `scripts/daily-market-ai.mjs`를 어떻게 할지 결정** — `analyzeDailyClosingReport`가 export만 되고 호출되는 곳이 저장소 어디에도 없는 **죽은 코드**다. 이번에 "규칙을 여기에 넣었는데 아무 효과가 없던" 사고의 원인이었다. 삭제하거나, 파일 상단에 "이 파일은 실행 경로가 아니다 — 리포트 생성 규칙은 `D:\Claude\Scheduled\totalmoney-daily-closing-report\SKILL.md`에 있다"는 경고 주석을 크게 달아 같은 실수를 막을 것. (같은 이유로 `scripts/*-ai.mjs` 계열 전부가 실제 실행 경로인지 한 번 훑어볼 가치가 있다.)
- **C. 예약작업 SKILL.md들도 백업/버전관리 대상에 넣을지 검토** — 현재 `D:\Claude\Scheduled\*\SKILL.md`는 저장소 밖에 있어 변경 이력이 남지 않는다. 실제 파이프라인의 핵심 로직이 여기 들어있는데 git 추적이 안 되는 상태다. (읽기 전용이라 Cowork 파일 도구로는 못 고치고 `update_scheduled_task`로만 수정 가능하다는 점도 기억할 것.)

1. 유사투자자문업 교육 **수료 완료**, 사업자등록은 **2027년으로 연기**(2026-09-24 시우) → 등록 시점에 완료되면 포트원 실채널 전환 + `SETUP_PENDING: false` 전환 작업 진행
2. GitHub PAT 재발급(로테이션) — 급하지 않음, 시간 될 때 정리
3. ~~글로벌랭킹 릴스 순위변동 배지 2주차 이후 실제 화면 확인~~ (2026-08-13: 순위변동 배지 기능 자체를 리디자인 과정에서 완전히 제거해 더 이상 해당 없음)
4. KIS 실시간시세 세션 풀 에러 원인 조사
5. 매매시그널 암호화폐 스크리닝 확장 (사용자 요청 시)
6. 정기결제(빌링키) 및 환불 API 구현 (사업 확장 단계에서)
7. 커스텀 SMTP 연결로 이메일 인증 안정화 (정식 오픈 전)
8. (신규) 브랜치 `promo/instagram-x-cardnews` 삭제 여부 결정 — X 자동화가 main에서 완전히 제거됐으니 이 죽은 브랜치도 정리할지
9. (신규) `D:\vibe`에 남은 임시 파일 2개(`_tmp_health-check-upload.mjs`, `_tmp_validate-package-json.yml`) 정리 — GitHub 웹 편집기 업로드 시도 과정에서 생긴 로컬 스크래치 파일, 저장소/배포에는 영향 없음
10. (신규, 2026-08-11) `docs/README.md`(위 README.md 루트 파일과 별개)에 여전히 무관한 계산기 프로젝트 문서가 남아있음 — 필요 여부 확인 후 정리
11. (신규, 2026-08-11) `promo-morning-copy.mjs`의 미국 시총 TOP20 정적 폴백 리스트(world-market-cache.json 조회 실패 시에만 사용)는 시총 순위가 바뀔 수 있으니 가끔 한 번씩 최신 상태인지 확인 권장(급하지 않음)
12. (신규, 2026-08-11) `api/us-market-data.js`의 거래소 데이터 오염 문제가 META 외 다른 종목에서도 재발하는지 당분간 모니터링(교차검증 가드는 추가했으나 근본 원인인 KIS 거래소별 조회 로직 자체는 그대로임)
13. (신규, 2026-08-12) `data/briefing/` 아카이브가 앞으로도 매일 잘 쌓이는지(SKILL.md STEP 5에 추가한 아카이브 커밋 단계) 다음 평일 발행 때 확인
14. (신규, 2026-08-13, **08-14 재발로 우선순위 상향**) `totalmoney-morning-briefing-retry`(평일 08:15 KST) 예약작업이 며칠간 의도대로 동작하는지 확인 — 08-14에 "마운트·트리거는 성공, 분석 작성은 미완료"로 절반만 도는 새 실패 패턴 발견됨. 재발하면 SKILL.md에 STEP별 중간 체크포인트(진행 로그 커밋) 추가 검토
15. (신규, 2026-08-13) 글로벌랭킹 리디자인(TOP10+국내기업 섹션) 다음 주(2주차) 토요일 정기 발행에서도 로고/국기 fetch 성공률·레이아웃이 정상인지 확인
16. (신규, 2026-08-13) Gemini 이미지 API 쿼터 상태 확인(위 3장 참고) — 계속 429가 뜨면 플랜 업그레이드 또는 사용량 조절 검토
17. (신규, 2026-08-13) `docs/README.md`(9번 항목)와 별개로, 정리 미완료 상태인 죽은 브랜치·임시파일 항목(8·9번)도 아직 미처리 — 시간 될 때 일괄 정리
18. (신규, 2026-08-14) health-check.yml의 매시간 schedule 트리거가 08-14에 08:52~11:38 KST 사이 약 2시간46분 공백을 보인 원인 조사 — 재발하면 cron-job.org 등 외부 백업 트리거 추가 검토
19. (신규, 2026-08-14) `totalmoney-daily-closing-report`에 새로 추가한 `repository_dispatch` 자동 재시도 로직이 다음 KIS 실패 때 의도대로 동작하는지 확인 — 이번엔 실패가 이미 발생한 뒤 수동으로 발견해 로직만 추가한 상태라 실전 검증은 아직 안 됨
20. (신규, 2026-08-14) `daily-market-sync.yml`의 KIS 동기화 재시도(현재 3회×30초, `scripts/kis-daily-top30.mjs` 내부엔 개별 fetch 타임아웃/재시도 없음)를 조금 더 견고하게 늘릴지 검토(급하지 않음 — 최근 10거래일 중 1회뿐이라 우선순위 낮음)
21. (신규, 2026-08-18) `D:\vibe`에 남은 임시 파일 2개(`0818-ai-report.patch`, `apply-0818-patch.bat`) 정리 — 클라우드 세션 git-proxy 버그 우회 과정에서 임시로 만든 패치 파일들, 저장소/배포에는 영향 없음(위 9번 항목과 동일 성격)
22. (신규, 2026-08-18) 로컬 실행 전환 후 나머지 Cowork 예약작업들(`totalmoney-morning-briefing` 등)도 며칠 지켜보며 정상 동작하는지 확인 — 로컬 실행이 기존 "D:\vibe 마운트 간헐적 누락" 문제(3장 상단 참고)까지 함께 해결하는지, 아니면 별개 문제라 여전히 재발하는지 다음 발행 때 확인할 것
23. (신규, 2026-08-26) 매매시그널 신규 필드(PER/PBR/외국인·기관 순매수 등)가 다음 `build-screener-cache.mjs` 정기 실행 후 캐시에 정상 반영되는지, API 호출량 증가로 실행시간/실패율이 늘지 않는지 확인
24. (신규, 2026-08-26, 일부완료) 매매시그널 페이지에서 실제 로그인한 상태로 키움 조건표+수식 붙여넣기, OR/NOT/최근N봉이내 조합, price/per/pbr/foreign_net_buy 등 신규 필드 조건을 직접 테스트해보고 결과 확인 (`period_return`·정렬(sort)은 검증 완료, 나머지 미검증)
25. (신규, 2026-08-26) Vercel 'vibe' 프로젝트가 이번엔 재생성 없이 삭제 상태로 유지되는지 다음 정기 push(특히 평일 16:00 KST `kr-screener-cache.yml` 실행 후) 이후 확인
26. (신규, 2026-08-26) 정렬(sort) 프롬프트 few-shot 개선 커밋(`2bd7a0d4`)이 실제로 배포됐는지 확인 — 안 됐으면 대시보드에서 수동 Redeploy하거나 익일 09:00 KST `vercel-deploy-catchup.yml` 자동 반영 후 "1년동안 상승률 높은 순서대로 20종목" 같은 정렬 쿼리를 5회 정도 반복 검색해 "이해 못함" 재현율이 실제로 줄었는지 확인
27. (신규, 2026-08-26) 오늘 vibe 중복배포로 소모된 Vercel 일일 배포 한도(100회)가 다음날 정상 리셋됐는지, 그로 인해 밀린 배포(데이터 동기화 등)가 없는지 확인
28. (신규, 2026-08-26 3차 후속 세션) 거래중지/정리매매 제외 + 감자·액면병합 비정상 급등 필터가 다음 `build-screener-cache.mjs` 정기 실행(16:00 KST) 후 캐시에 실제 반영되는지, 화면에서 거래중지 종목·감자성 비정상 수익률이 실제로 사라졌는지 확인
29. (신규, 2026-08-26 3차 후속 세션) 종목검색 정렬/스크롤 수정 3건(`ec256579`/`225781eb`)이 배포된 후 "삼성전자"/"AAPL" 등으로 실사이트에서 재검색해 정확한 종목이 1순위로 뜨는지, 드롭다운 스크롤이 되는지 확인
30. (신규, 2026-08-26 3차 후속 세션) `vercel-deploy-catchup.yml` cron 수정(연1회→매일 09:00 KST, `cfc312cc`)이 다음날 실제로 자동 실행돼 밀린 커밋들을 배포하는지 확인 — 처음으로 "진짜 매일 도는 캐치업"이 실전 검증되는 것이라 중요
31. (신규, 2026-08-26 3차 후속 세션) 미국주식 탭 외에 다른 페이지에도 "관련성 순위 없는 검색 + 8개 하드컷" 같은 유사 패턴이 더 남아있는지 — 이번에 실시간시세/AI종목분석/미국주식 3곳에서 동시에 발견된 걸 보면 각자 따로 구현된 페이지가 더 있을 가능성 있음(예: 암호화폐 `crypto.html`), 필요시 확인
32. ~~(신규, 2026-08-26 4차 후속 세션, **최우선**) `3e83e7da`(AI종목분석 요약카드 세로텍스트 2차 수정) 배포 여부 확인~~ (2026-08-26 5차 후속 세션 완료: 배포 확인 + Claude-in-Chrome 라이브 재현 안 됨 확인)
33. (신규, 2026-08-26 4차 후속 세션) GPT 2차 리포트 반영분(`6a447f41`)의 실제 화면 육안 검토 — 수급 FACT/AI해석 배지, 시나리오C 재진입 문구, target2 장기잠재 라벨, 종합점수 분모(`+3/8`) 표기 등이 의도대로 보이는지 아직 실사이트에서 직접 확인 안 함
34. (신규, 2026-08-26 4차 후속 세션) 실시간시세 랭킹탭(상승률/거래대금/시가총액 등)의 숫자전용 코드 필터와 WebSocket 구독 코드 정규화는 우선주 버그 수정 범위에서 제외됨 — 우선주 종목이 랭킹탭에도 노출되길 원하면 후속 작업으로 확인
35. (신규, 2026-08-26 4차 후속 세션, 운영 습관) 반복 디버깅 세션에서는 커밋을 모아서 적게 push할 것 — 오늘 세션에서 짧은 시간에 push를 너무 자주 해 Vercel 배포한도를 재소진시킨 게 이번 배포 지연의 직접 원인이었음(5장 반영)
36. (신규, 2026-08-26 5차 후속 세션) AI종목분석 카드1~7 UI 리디자인이 실사용자 눈에 어떻게 보이는지 며칠 지켜보고 피드백 반영 — 이번 세션엔 삼성전자(국내주식) 1종목만 라이브 검증했으므로, 미국주식·암호화폐처럼 카드 내용 길이가 다른 종목에서도 레이아웃이 깨지지 않는지 다음에 한 번 확인 권장
37. (신규, 2026-08-28) DART 이벤트 배치(`dart-events.yml`, 매일 06:10 KST)가 며칠간 연속 정상 성공하는지, corp_code 캐시 30일 TTL 첫 자동 갱신이 문제없이 도는지 확인(3장 반영)
38. (신규, 2026-08-28) SK하이닉스·삼성전자 외 나머지 워치리스트 13종목(특히 정기 주총 시즌인 다음 해 2~3월)에서도 주총일시/배당기준일 추출이 잘 되는지, 실적발표 "예상 구간" 계산이 결산월이 다른 종목(예: 3월 결산)에서도 정확한지 한 번 더 확인 권장
39. (신규, 2026-08-28, 토큰 절약 아이디어·검토 필요) KR종목은 이제 "다가오는 이벤트"를 AI가 생성해도 어차피 `normalizeAnalysis`에서 무조건 버리고 DART 데이터로 교체하므로, 분석 프롬프트에서 KR종목 한정으로 events 필드 생성 자체를 스킵하도록 프롬프트를 손보면 출력 토큰을 소폭 절약할 수 있음 — 사용자가 토큰 절약을 우선순위로 두고 있어 여유 있을 때 검토할 만함(효과는 크지 않을 수 있음, 프롬프트 스키마 영향도 같이 확인 필요)
40. (신규, 2026-09-01, **다음 세션 최우선**) 9/2 16:15 마감시황 발행분에서 신규 스키마 6종(`marketTone`/`indexDetail`/`investor_trend.etc`/`sectors`/`netBuyLeaders`/`featured_stocks[].risk`)이 실제로 채워졌는지 확인하고, 부실하면 프롬프트 추가 조정 (3장 반영)
41. (신규, 2026-09-01) 마감시황 날짜 캘린더로 5~6월·7월 초 구형 레코드를 몇 개 열어 덱 렌더러 폴백이 정상 동작하는지 확인 (3장 반영)
42. (신규, 2026-09-01) 9/2 이후 AI종목분석 국내 종목의 "다가오는 이벤트" 문구가 새 명사구 형식으로 바뀌었는지 확인 (3장 반영)
43. (신규, 2026-09-01) AI종목분석 새 문체 규칙을 미국주식(NVDA 등)·암호화폐 종목에서도 한 번 돌려볼 것 — 이번 세션엔 국내주식(SK하이닉스·삼성전자)만 검증했고, 수급 섹션 규칙이 자산군별로 3갈래(KR/US/CRYPTO)라 US·CRYPTO 분기에도 같은 "숫자 상한" 수정이 들어갔는지는 코드상으론 반영했지만 라이브 확인은 안 함
44. (신규, 2026-09-01, 운영 지식) **프롬프트 수정 시 같은 주제의 상충 지시가 남아 있는지 먼저 grep할 것.** 이번에 수급 숫자 재나열을 막으려고 상위 페르소나 규칙만 두 번 고쳤는데 계속 실패했고, 원인은 3번 섹션 규칙 본체에 "1일/5일/20일을 반드시 함께 언급해서 비교 서술할 것"이라는 정반대 지시가 남아 있었던 것이었다. **섹션별 구체 지시가 상위 톤 규칙을 항상 이긴다** — 배포 2회를 낭비했으므로 다음부터는 규칙 추가 전에 반드시 확인
45. (신규, 2026-09-02, **다음 세션 최우선**) 실제 아이폰에서 홈 접속 시 "중간에서 확대된 채로 시작"이 재발하는지 확인 (3장 반영). 재발하면 입력창 16px 외의 원인(세션 복원 배율 등)을 더 파야 함
46. ~~증시 일정 페이지 정리~~ **(2026-09-03 완료)**
47. ~~시세 3종 + 글로벌랭킹 공통 "시장 요약 스트립"~~ **(2026-09-03 완료)**
48. ~~요금제 Premium의 실질 차별점 정의~~ **(2026-09-03 정의 확정 + 1축 구현 완료, 나머지 3축은 아래 52~54번)**
49. (신규, 2026-09-02) 홈 `hxLoadClosing()`의 1순위 경로(`api/repo-data?pick=latest-day`)가 며칠간 안정적으로 도는지 확인. 실패 시 `data/daily/<날짜>.json` 역추적 폴백으로 넘어가며, 그 경우 히어로에 전일 마감시황이 날짜 라벨과 함께 표시된다(오해 없게 라벨은 붙어 있음)
50. (신규, 2026-09-02) 모바일 접이식 기본 펼침 구성 조정 여부 — 특히 마감시황 '오늘의 특징주'를 기본 펼침으로 올릴지 (3장 반영)
51. (신규, 2026-09-02) AI 종목분석 비로그인 랜딩의 전환 효과 관찰 — 가입 전환이 실제로 늘었는지, 랜딩에 "최근 분석된 종목"(Supabase `public_ai_feed` 실데이터) 같은 사회적 증거를 추가할지 검토
52. (신규, 2026-09-03, **Premium 2축**) 과거 데이터 · 조건 백테스트 열람 구현 — 아카이브·스크리너 캐시가 이미 있어 신규 파이프라인 불필요. **지금 무료로 열려 있는 아카이브를 막는 방식은 금지**(기존 기능 회수), "조건검색 과거 시점 재현" 같은 새 기능으로 얹을 것 (3장 반영)
53. (신규, 2026-09-03, **Premium 3축**) 관심종목 데일리 리포트 — DART 이벤트 배치 + 텔레그램 발송을 재활용하고, 사용자별 워치리스트 Supabase 테이블만 추가하면 됨 (3장 반영)
54. (신규, 2026-09-03, **Premium 4축**) AI 종목분석 심화 리포트 — 무엇을 더 줄지 상품 기획이 먼저 (3장 반영)
55. (신규, 2026-09-03) 결제 오픈 전 Pro 감시 슬롯 5개가 적정한지 재판단 — 오픈 후엔 줄일 수 없다 (3장 반영)
56. (신규, 2026-09-03) `D:\vibe`의 백업 브랜치 `backup/local-pre-sync-2026-09-03` 정리 여부 — 2026-08-10 이후 방치돼 있던 로컬 잔재 스냅샷(커밋 `af0a5092`, 로컬 전용·푸시 안 함). 며칠 써보고 아쉬운 게 없으면 `git branch -D`로 삭제. 함께 `archive/local-dead-2026-09-03/`(옛 `api/crypto-data.js`·`crypto-news.js`)와 `_tmp_*` 파일 2개도 정리 (9번 항목과 통합)
57. (신규, 2026-09-03 오후) **이자보상배율 지원 여부 결정** — 사용자가 실제로 요청한 조건인데 유일하게 못 했다. 이자비용이 DART **주요계정**에 없어서 전체 재무제표 API(`fnlttSinglAcntAll.json`)를 종목당 따로 받아야 하는데 상장사 3,988개를 매주 돌리기엔 호출량이 과하다. 대안: ①시가총액 상위 500종목만 전체 재무제표를 받아 이자보상배율을 채우고, 나머지는 "이 종목은 데이터 없음"으로 빈 상태 처리 ②조건에 이자보상배율이 들어온 경우에만 온디맨드로 그 종목들만 조회(검색 지연 발생) ③계속 미지원으로 두고 거절. **①이 현실적**으로 보이나 "일부 종목만 되는 조건"이 오히려 혼란스러울 수 있어 판단 필요
58. (신규, 2026-09-03 오후) **재무 조건검색을 Premium 차별점으로 승격할지 검토** — 지금은 Pro/Premium 공통으로 열려 있다. 부채비율·영업이익률 같은 재무 조건검색은 네이버금융 등 무료 사이트에 거의 없어 **3장 "Premium 미완성 3축" 중 'AI 종목분석 심화'의 실질 내용물**로 쓸 수 있다. 다만 **이미 열어둔 걸 나중에 막는 건 기존 기능 회수**라 하면 안 되므로(52번 항목과 같은 원칙), 승격하려면 "재무 조건 + 백테스트" 같은 **새 조합**으로 얹을 것
59. (신규, 2026-09-03 오후) 재무비율 근거 칩이 **금융주에서 절반이 비는 문제** — 신한지주처럼 영업이익률·순이익률·유동비율이 null인 종목은 칩이 2개만 뜬다. 검색 조건에 걸렸는데 근거가 비면 어색하므로, 금융업은 ROE·부채비율만 쓰도록 안내하거나 업종별로 보여줄 지표를 다르게 할지 검토(3장 반영)
60. (신규, 2026-09-03 오후) **`describeMatch` 근거 칩을 AI 종목분석·마감시황에도 쓸지 검토** — "이 값이라서 이렇게 판정했다"를 보여주는 패턴은 매매시그널 밖에서도 통한다. 특히 AI 종목분석의 "시장 대비 위치" 카드와 궁합이 좋다

---

## 5. 운영 규칙 · 제약사항

### 정시 실행이 필요한 작업은 pg_cron 경로를 쓸 것 (2026-09-08 밤 확장)

**GitHub Actions `schedule:`은 이 저장소에서 상시 1.5~6시간 밀린다 — 시각이 중요한 작업에 절대 쓰지 말 것.** 아래 pg_cron 경로는 실측 **20초 이내**에 워크플로가 시작된다(2026-09-08 확인).

| 작업 | 정시 시각 | pg_cron job | 워크플로 |
|---|---|---|---|
| 장 막판 종가 스캔 (진입 검토) | 평일 **14:30 KST** | `intraday-close-scan-1430` (`30 5 * * 1-5` UTC) | `intraday-close-scan.yml` |
| 장 막판 종가 스캔 (종가 확정) | 평일 **15:31 KST** | `intraday-close-scan-1531` (`31 6 * * 1-5` UTC) | `intraday-close-scan.yml` |

- 경로는 인스타 발행과 동일하다: pg_cron → Edge Function `github-workflow-dispatch` → GitHub `workflow_dispatch`. **새 워크플로를 붙일 때 Edge Function의 `ALLOWED_WORKFLOWS`에 파일명을 반드시 추가할 것** — 없으면 400을 돌려주고 **아무 일도 안 일어난 채 조용히 끝난다**.
- **(2026-09-08 밤 확장) Edge Function이 `inputs` 전달을 지원한다**(버전 9). pg_cron body에 `'inputs', jsonb_build_object('slot','1531')` 형태로 넣으면 `workflow_dispatch`의 inputs로 그대로 넘어간다. 값은 문자열/숫자/불리언만 통과시킨다.
- Edge Function에 쓰이는 GitHub PAT는 **Supabase vault의 `github_pat`**이고, 이건 `actions:write`가 있어 `workflow_dispatch`가 된다. 반면 로컬 `D:\vibe\.cowork-github-pat-token.txt`의 PAT은 `workflow_dispatch`가 항상 403이고 `repository_dispatch`만 된다 — **둘은 다른 토큰이니 헷갈리지 말 것.**
- 워크플로에는 `repository_dispatch` 트리거도 같이 달아두면 Cowork 세션에서 로컬 PAT으로 즉시 수동 실행이 가능하다(`intraday-close-scan.yml`, `kr-screener-cache.yml`이 그렇게 돼 있다).

### 인스타그램 발행 시각·트리거 체계 (2026-09-08 정리)

**정시 발행은 Supabase pg_cron이 담당하고, GitHub Actions schedule은 백업이다.** GitHub의 `schedule:` 트리거는 SLA가 없어 정각·반각대 혼잡 시 크게 밀린다(9/7~9/8 실측: 마감 캐러셀 17:26 예정 → 23:19 실제, 아침 캐러셀 08:03 예정 → 09:59 실제, 랭킹은 아예 실행 안 됨). 그래서 pg_cron이 Supabase Edge Function `github-workflow-dispatch`를 호출하고, 그 함수가 GitHub `workflow_dispatch`를 즉시 쏜다.

| 콘텐츠 | 발행 시각 | 정시 트리거(pg_cron job) | 백업(GitHub cron) | 워크플로 |
|---|---|---|---|---|
| 아침 브리핑 캐러셀 | 평일 **08:30 KST** | job 1 `instagram-morning-precise-dispatch` (`30 23 * * 0-4` UTC) | 08:03 | `instagram-carousel-morning.yml` |
| 마감시황 캐러셀 | 평일 **17:30 KST** | job 2 `instagram-closing-precise-dispatch` (`30 8 * * 1-5` UTC) | 17:26 | `instagram-carousel-v2.yml` |
| 주간 글로벌 랭킹 캐러셀 | 토 **10:30 KST** | job 3 `instagram-ranking-precise-dispatch` (`30 1 * * 6` UTC) | 토 10:30 | `instagram-carousel-ranking.yml` |

- **pg_cron 대상을 바꿀 때는 Edge Function의 `ALLOWED_WORKFLOWS`도 같이 고칠 것.** allowlist에 없는 워크플로를 요청하면 함수가 400을 돌려주고 **아무 일도 안 일어난 채 조용히 끝난다**. 2026-09-08 이관 때 이걸 놓칠 뻔했다. 현재 allowlist에는 캐러셀 3종 + 구 릴스 2종(롤백용)이 들어 있다.
- **가드는 `event_name == 'schedule'`로 걸지 말 것.** pg_cron은 `workflow_dispatch`로 들어오기 때문에, 그렇게 짜면 정시 트리거가 주말·중복·휴장일 방어를 전부 우회한다(2026-09-08까지 실제로 그런 상태였다). 지금은 `date` 입력이나 `force=1` 입력을 명시한 수동 실행만 가드를 건너뛴다.
- **중복 방지 스탬프는 워크플로마다 파일이 다르다** — `generated/carousel-morning|carousel-v2|carousel-ranking/last-published-date.txt`. 구 릴스는 `generated/morning|closing/`을 쓴다. **서로 다른 스탬프를 쓰는 두 워크플로를 동시에 자동 실행시키면 같은 날 2건이 나간다**(9/7~9/8에 실제로 그랬다). 릴스↔캐러셀을 갈아탈 때는 워크플로의 `schedule:`뿐 아니라 **pg_cron 쪽 호출 대상까지 반드시 함께 옮길 것.**
- **마감 카드는 마감시황 AI 분석에 종속된다.** `check-promo-data-ready.mjs --slot=closing`이 그날 `analysis`/`indexes` 유무를 보기 때문에, 마감시황 예약작업이 17:20까지 push하지 못하면 카드도 그날 통째로 스킵된다. 마감시황 쪽 데드라인이 17:20인 이유가 이것이다.

### 인스타그램 카드뉴스 v2 — 지키지 않으면 되돌아오는 규칙 (2026-09-07 신설)

- **타이포 크기를 줄이지 말 것.** 1080px 카드는 아이폰 피드에서 약 390pt로 축소된다(배율 0.36). 본문 24px는 손에서 8.7pt로 보이고 확대해야 읽힌다. 본문 하한은 **34px**(≈12.3pt)다. 빈 공간이 보인다고 글씨를 줄이는 게 아니라 **내용을 줄인다**. 근거는 `scripts/promo-carousel-css.mjs` 최상단 주석에 있다.
- **우상단에 요소를 두지 말 것.** 인스타 캐러셀이 자체 페이지 배지(1/6)를 그 자리에 덮어 그린다. 브랜드·슬롯·날짜는 좌측 블록에 모은다.
- **하단에 자체 페이지 인디케이터를 그리지 말 것.** 인스타가 이미지 아래 중앙에 그려 준다.
- **훅에서 던진 질문의 답은 반드시 2번 카드에 있어야 한다.** 답을 뒤쪽 카드에 작게 숨기면 훅이 아니라 낚시가 되고, 그건 계정 신뢰를 깎는다. 훅 유형과 2번 카드는 짝으로 설계한다.
- **숫자는 코드가 만들고 AI는 문장만 쓴다.** 계산된 실측값을 프롬프트에 통째로 주입해 화면 도표와 본문이 어긋날 여지를 없앤다. AI에게 숫자를 만들게 하지 않는다.
- **모델 준수에만 기대지 말 것.** 길이·중복·문장 수는 코드가 강제한다(`clampCopy`). 경고만 남기면 결국 그대로 발행된다.
- **"무료"라는 단어를 카드·캡션에 쓰지 않는다.** 베타 운영이고 결제가 열리면 그 문구가 그대로 부담이 된다. 유입은 가격이 아니라 **업데이트 주기와 내용**으로 만든다.
- **급등 이유만 쓰지 않는다 — 리스크를 같이 쓴다.** 다른 계정과 구분되는 지점이고 유료 전환의 씨앗이다. 사라고 말하지 않는다.
- **콘텐츠가 카드 높이(1350px)를 넘으면 발행을 실패시킨다.** 글자가 잘린 채 나가는 게 최악이다. 이 가드를 끄지 말 것.
- **repository_dispatch는 push 직후 이전 커밋으로 실행될 수 있다.** 푸시하고 **15~25초 두고** dispatch할 것. 안 그러면 수정 전 코드가 돌아 진단이 꼬인다.
- **발행 워크플로가 동시에 돌면 push 경합이 난다.** 커밋·push 단계는 rebase 후 재시도(3회)를 유지할 것.
- **검수는 dry_run으로.** 각 캐러셀 워크플로의 `dry_run=1`은 렌더·커밋까지만 하고 인스타 발행을 건너뛴다. 새 포맷·프롬프트 변경은 반드시 여기서 실제 이미지를 확인한 뒤 내보낸다.

### 화면 문구와 코드 이름을 분리한다 — "종가시그널" 개명 사례 (2026-09-18 신설)

- 2026-09-18에 '종가베팅'을 **'종가시그널'로 개명**했다(유사투자자문업 규정상 '베팅'이 도박을 연상시킨다). 바꾼 것은 **사용자에게 보이는 것만** — 메뉴·본문 문구(전 페이지 18개 + `site-shell.js`)와 URL(`close-betting.html` → `close-signal.html`, `vercel.json`에 301 리디렉션 추가).
- **의도적으로 안 바꾼 것**: `lib/close-betting-score.js`, `lib/close-betting-catalyst.js`, `scripts/close-betting-review.mjs`, `api/analyze.js?feature=close-betting`, Supabase 테이블 `trade_signal_intraday`, pg_cron job `intraday-close-scan-1520`·`close-betting-review`, edge function `ALLOWED_WORKFLOWS`의 워크플로 이름. 이걸 같이 바꾸면 pg_cron·ALLOWED_WORKFLOWS·GHA 워크플로 파일명까지 연쇄로 맞춰야 하고, 하나만 빠지면 **400으로 조용히 실패**한다(위 pg_cron 항목 참조).
- **읽는 법**: 화면에 보이는 이름은 "종가시그널", 코드·인프라·로그에 나오는 이름은 `close-betting`. 다음에 또 문구를 바꿀 일이 생기면 같은 방식으로 **표시 레이어만** 바꾼다.

### 비용 구조
- **GitHub Actions**: 저장소가 public이라 호스팅 러너 무료
- **AI 분석 비용**: 마감시황/장전브리핑 등 정기 리포트는 Cowork(Claude Desktop) 예약작업이 대화 세션 안에서 직접 작성 → **별도 Anthropic API 토큰 비용 발생 안 함**(기존 Cowork 사용량 안에서 처리). 반대로 GitHub Actions 스크립트가 직접 Claude API를 호출하는 방식(`daily-market-ai.mjs` 등 실행)으로 했다면 월 $10~20 별도 과금이 발생하므로, 이 비용을 피하기 위해 의도적으로 PC 예약작업(Cowork) 방식을 채택한 것
- **인스타 카드/릴스 문구 생성**: Claude Haiku 모델 1회 호출(회당 체감 비용 1원 단위 미만), 실패 시 최대 3회 재시도 후 규칙 기반 폴백
- **Meta Graph API, 텔레그램 Bot API**: 무료
- **Vercel**: Hobby 플랜 기준, free-tier 배포/일일 한도 존재. **배포당 서버리스 함수 12개 제한**을 과거 실제로 초과해 배포가 조용히 `ERROR` 상태로 실패하고 구버전이 계속 서빙된 사고 이력 있음(`api/crypto-data.js`+`api/crypto-news.js`를 `api/crypto.js` 하나로 병합해 해결) — 새 API 파일 추가 시 항상 총 함수 개수 확인할 것
- **(신규, 2026-08-26) Vercel 프로젝트가 GitHub 저장소에 중복 연결되면 배포 한도(Hobby 100개/일, 계정 전체 공유)가 예상보다 훨씬 빨리 소진됨**: 이 저장소가 과거(간단 계산기 프로젝트 시절)부터 실제 서비스 중인 `bori-cal` 외에 도메인 없는 중복 Vercel 프로젝트 "vibe"에도 그대로 연결돼 있어서, push 1번마다 배포가 2번씩 카운트되고 있었음(+자동 데이터 동기화 커밋이 하루 50~58회 발생) — 배포가 유난히 빨리 한도에 걸리면 Vercel 대시보드에서 이 저장소에 연결된 프로젝트가 1개뿐인지부터 확인할 것. 2026-08-26 낮에 사용자가 중복 프로젝트를 직접 삭제했으나(`list_projects`로 확인), **같은 날 저녁 재확인 결과 다시 배포되고 있는 게 발견됨** — 실제 근본원인은 `kr-screener-cache.yml`의 Deploy 스텝에만 `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID`가 빠져 있던 것(다른 4개 vercel-deploy 워크플로우는 정상 보유). `.vercel/`은 `.gitignore` 대상이라 GitHub Actions 체크아웃엔 프로젝트 연결 정보가 없고, 이 상태로 `vercel deploy --prod --token ... --yes`(비대화형)를 실행하면 vercel CLI가 체크아웃 디렉터리명(저장소명 'vibe')으로 새 프로젝트를 자동 생성함 — 평일 16:00 KST(이 워크플로우 스케줄)마다 대시보드에서 지워도 다시 생겨났던 것. 커밋 `27ea7642`로 누락된 두 값 추가해 근본 수정, 사용자가 프로젝트를 최종 삭제 완료(`list_projects`로 재확인). **실제 피해 사례로 당일 저녁 확인됨**: 뒤이어 올린 정렬(sort) 기능 개선 커밋(`2bd7a0d4`)이 GitHub엔 정상 push됐는데도 Vercel 배포 목록에 아예 안 잡히는 현상 발생 — 낮 동안 vibe 중복배포로 한도를 이미 다 썼을 가능성이 높음. 이미 소진된 당일 한도 자체는 삭제해도 즉시 리셋되지 않고 24시간 자연 회복만 되며, `vercel-deploy-catchup.yml`(매일 09:00 KST)이 리셋 후 최신 커밋을 자동 재배포하도록 되어 있어 익일 아침 자동 반영될 것으로 예상. **(2026-08-26 3차 후속 세션에서 갱신) 위 예상이 실제로는 틀렸었음이 드러남** — 다음날이 아니라 같은 날 오후 세션에서 이미 다시 한도가 소진돼있었고(4개 커밋 연속 자동배포 누락), 사용자가 GitHub Actions에서 "Deploy Vercel (catch-up)"을 수동 실행해보니 `Error: Resource is limited - try again in 24 hours (more than 100, code: "api-deployments-free-per-day")`라는 명시적 에러로 한도 소진이 확정됨. 게다가 "매일 09:00 KST에 자동으로 따라잡아준다"고 믿고 있던 `vercel-deploy-catchup.yml`의 cron이 실제로는 `"0 0 21 5 *"`(2026년 5월 21일 단 하루)로 박혀 있어서 **5월 이후 한 번도 자동으로 돈 적이 없었던 별개의 버그**였음이 발견됨 — 이름·주석은 "일일 캐치업"인데 실제 스케줄은 연 1회였던 것. `cfc312cc`로 `"0 0 * * *"`(매일 09:00 KST)로 수정 완료. **앞으로는 Vercel 배포가 갑자기 안 걸리면**: (1) 먼저 GitHub Actions에서 "Deploy Vercel" 또는 "Deploy Vercel (catch-up)"을 수동 Run workflow 해서 로그에 `api-deployments-free-per-day` 에러가 뜨는지로 한도 소진 여부를 확정하고, (2) 한도 소진이 맞으면 이제는 다음날 09:00 KST 캐치업이 정말로 자동 실행되니 기다리면 됨(단, 이 cron 수정이 처음 실전 검증되는 것이므로 다음날 실제로 밀린 커밋이 배포됐는지 한 번은 확인할 것). **(2026-08-26 4차 후속 세션에서 갱신) 배포 상태 확인은 `list_deployments`만으로는 부족함이 다시 확인됨** — 한도 소진으로 거부된 커밋은 CANCELED조차 아니고 `list_deployments` 목록에 아예 나타나지 않는다(정상/취소/진행중 어느 것도 아닌 "생성 자체가 안 됨" 상태). Vercel MCP로 몇 분을 기다려도 해당 커밋이 안 보이면, `curl -s "https://api.github.com/repos/mandoo411/vibe/commits/<sha>/status"`(공개 저장소라 인증 불필요)로 GitHub에 등록된 "Vercel" 커밋 상태(`state`/`description`)를 확인할 것 — 여기서 `"Deployment rate limited — retry in 24 hours"`가 바로 확정 증거로 뜬다. 같은 날 짧은 시간에 코드 수정 커밋을 20회 넘게 연달아 push하는 것만으로도(중복 프로젝트 없이, 데이터 동기화 커밋과 무관하게) 한도가 다시 소진될 수 있음이 확인됨 — 반복 디버깅 세션에서는 로컬 검증(문법체크·합성테스트)을 최대한 마친 뒤 커밋을 모아서 적게 push할 것

### API/인프라 제약
- **Vercel `ignoreCommand`**: `data/` 폴더만 바뀐 커밋은 풀빌드를 스킵함(의도된 최적화, 버그 아님). 순수 데이터 수정을 즉시 반영하려면 비-data 파일에 사소한 변경(주석 등)을 같은 커밋에 끼워야 함. 데이터 전용 워크플로우는 별도 `vercel deploy --prod` 스텝 필요 여부 확인
- **Vercel 함수 12개 제한**(Hobby 플랜) — 위 참고. **(2026-09-02 실측) 현재 정확히 12/12로 꽉 찼다**: `api/` 11개(analyze, crypto, dashboard-data, kis-realtime-data, kis-stock-quote, market-overview, market-ticker, repo-data, stock-analysis, us-market-data, world-market) + `api/payment/complete.js` 1개. **새 API 파일은 더 못 만든다** — 새 엔드포인트가 필요하면 기존 함수에 쿼리 파라미터를 붙여 분기하는 방식으로 해결할 것(`api/repo-data.js`의 `pick=latest-day`가 그 예)
- **KIS API**: 국내·미국 시세/차트 공용. 토큰 자동갱신 워크플로우가 KIS 서버 측 일시 장애로 실패한 이력 있음(수동 재실행으로 해결)
- **CoinMarketCap**: 한글 코인명 검색 미지원 → 정적 별칭 테이블로만 한글 검색 가능
- **Yahoo Finance `meta.chartPreviousClose`**: range(조회기간) 값에 따라 "어제 종가"가 아닌 값을 반환하는 미문서화 동작 있음 — 신뢰 금지, 실제 일별 종가 시계열에서 직접 도출할 것
- **api.github.com 접근 제한**: 일부 세션의 bash 샌드박스 네트워크 정책상 GitHub REST API 직접 호출이 차단됨(반면 `github.com`을 통한 git clone/push는 정상) → GitHub Actions 수동 트리거가 코드로 불가능한 세션에서는 사용자가 GitHub 웹 UI에서 직접 실행하거나, 워크플로우에 임시로 `push` 트리거를 추가하는 방식으로 우회
- **`actions:write` 권한 없는 PAT**: `workflow_dispatch` API 직접 호출이 항상 403 → 임시 push 트리거 우회 또는 사용자의 수동 Run workflow 필요
- **단, `repository_dispatch`는 이 PAT로 정상 동작함(2026-08-14 확인)**: `POST /repos/mandoo411/vibe/dispatches`는 Contents:write 권한만으로 204 성공 — `workflow_dispatch`와 달리 Actions 권한이 필요 없는 별개의 엔드포인트였음. `trigger-daily-market`/`trigger-morning-briefing` 등 저장소가 이미 `repository_dispatch` 트리거를 등록해둔 워크플로우는 Cowork가 코드로 직접 재실행시킬 수 있다 — 앞으로 유사한 "선행 GHA 실패 시 재시도" 설계에 활용할 것(마감시황 SKILL.md에 이미 적용, 아래 6장 참고)
- **(2026-08-18 신규 확인) Cowork "클라우드에서 새 작업 실행" 세션의 git-proxy 저장소 인가 버그**: PAT/GitHub App 권한과 무관하게, 클라우드 샌드박스 세션에서 이 저장소로의 `git push`(및 인증이 필요한 대부분의 GitHub REST API 호출, `workflow_dispatch` 수동 트리거 포함)가 세션 단위로 차단되는 사례 발생. Anthropic 쪽 알려진 이슈(2026-07-09~ 순차 롤아웃 시작, 이 문서 작성 시점 미해결)로 확인됨 — 새 세션 생성, GitHub App(`github.com/apps/claude`) 저장소 접근 권한 부여 둘 다 이미 실행 중인 세션에는 소급 적용 안 됨. **현재 우회책**: Cowork 데스크톱 앱 설정의 "클라우드에서 새 작업 실행" 토글을 끄면 새 세션이 PC 로컬(D:\vibe 마운트, 이미 인증된 사용자 git 자격증명)에서 실행되어 이 버그를 완전히 우회함 — 2026-08-18부터 이 설정으로 운영 중. 읽기 전용 작업(`git clone`, `git ls-remote`)은 클라우드 세션에서도 항상 정상 동작하므로 버그 여부 진단 시 참고할 것

### 콘텐츠/디자인 원칙 (반드시 준수)

- **(2026-09-23) 디자인 최종 덮어쓰기는 `premium.css`에서 한다.** 새 페이지·새 카드 클래스를 만들면 `premium.css`의 "카드 한 겹" 선택자 목록에 넣을 것(안 넣으면 그 카드만 옛 색으로 남는다). 선택자는 `html[lang] body.page-tm-v2 …`로 시작해야 refresh.css를 이긴다.
- **라벨에 `<em>`을 쓰지 말 것** — refresh.css의 `.tm-main em { 초록 !important }`에 먹힌다(일정 라벨·등락률이 초록으로 나온 사고 2회). span으로.
- **카드 안에 또 테두리 박스를 넣지 말 것** — 카드 안 보조 영역은 `var(--p-inset)` 면만, 테두리 없이.
- **다크 초록(#10b981) 버튼의 글씨는 짙은색(#03140e)** — 흰 글씨는 2.5:1로 기준 미달.
- **D:\vibe git: 세션마다 `device_request_delete_permission` 먼저** — 권한 없이 fetch/rebase하면 `.git/index.lock`·`HEAD.lock`·`rebase-merge`가 남아 다음 git 명령이 전부 막힌다(이번 세션에 또 발생). 세션 끝에 lock 잔여물 확인.
- **AI에게 텍스트·숫자·한글을 직접 그리게 하지 않는다**: 이미지 생성 AI(Gemini 등)는 배경/일러스트만 담당하고, 정확한 데이터(가격·등락률·날짜 등)는 항상 코드가 직접 렌더링 — 할루시네이션 방지가 최우선 원칙
- **실데이터가 없으면 지어내지 않고 표시 자체를 생략**한다(순위변동 배지, 특징주 등 — "없으면 빈 칸" 원칙 프로젝트 전체에 일관 적용)
- **내부 사정/disclaimer 문구를 고객에게 노출하지 않는다**: "정확한 수급 데이터 미확인" 같은 문구 대신, 검색을 더 강화해서 실제 데이터를 채워 넣을 것
- **특징주 선정 규칙**: 그룹A(시총100위 이내 +5%↑, 뉴스로 이유 명확) + 그룹B(전종목 +20%↑, 이유 명확) 교차보충으로 총 10종목. 이유 불명확하면 제외. `topGainers` 등 상위 N개로 잘린 배열에 없다고 "후보 없음"으로 단정하지 말고, 종목명을 뉴스 헤드라인 전체에서 별도로 교차 확인할 것. 정확한 수치가 없으면 뉴스의 범위 표현("~10%대")의 하한값으로 보수적으로 표기하고 추정임을 명시(가짜 정밀 소수점 금지)
- **투자 자문 아님 고지**: 인스타 캡션 등에 "투자 참고용 정보이며, 투자 판단 및 결과 책임은 투자자 본인에게 있습니다" 문구 기본 포함. 유료 구독 확장 시 유사투자자문업 신고 필요 여부를 전문가에게 확인 권장(법률 자문 아님)
- **브랜드 컬러/디자인 통일**: SNS 콘텐츠 디자인은 항상 사이트 실제 코드(theme.css 등)에서 값을 추출해 적용 — 하드코딩 금지. 워터마크(우측 하단 TotalMoney.kr), 로고 스타일 고정
- **텔레그램 서식 제약**: Markdown parse_mode는 bold/italic/code/link만 지원(폰트 크기 조절 불가) → "강조"는 볼드+구분선 조합으로 구현
- **(2026-09-02 신설) 긴 서술 문장을 헤드라인 자리에 두지 않는다**: 리포트의 요약 문장(100자 이상)을 큰 볼드로 세우면 헤드라인이 아니라 "볼드 처리된 문단"으로 읽힌다. **데이터 페이지의 헤드라인은 숫자다** — 대표 지표를 큰 모노 수치로 세우고, 문장은 본문 크기(14~15px / 행간 1.8 내외 / regular)에 라벨을 붙여 인용 블록으로 받칠 것. 히어로 좌우 2단 구성에서는 오른쪽 패널에 `align-self: stretch`를 줘 한쪽만 비는 일이 없게 한다
- **(2026-09-02 신설) 모바일 입력창은 반드시 16px 이상**: iOS Safari는 글자 크기 16px 미만 입력창에 포커스가 가면 페이지를 강제 확대하고, 그 배율이 세션에 남아 재방문 시 "화면 중간에서 확대된 채로 시작"하는 것처럼 보인다. `theme.css`에 전역 규칙이 들어가 있으니 새 입력창을 만들 때 별도 조치는 불필요하지만, 페이지 CSS에서 `font-size`를 더 작게 지정하면 `!important`로 덮어써지는 구조임을 알고 있을 것
- **(2026-09-02 신설) 모바일 긴 리포트는 잘라내지 말고 접는다**: 브리핑·마감시황처럼 세로가 긴 리포트는 내용을 줄이는 대신 접이식으로 전환한다(핵심 2~3개만 펼침, 나머지는 제목만, 탭하면 펼침). 데스크톱은 항상 전부 펼친 상태를 유지하고, 수정 후 **데스크톱 전체 높이가 이전과 같은지**로 무영향을 검증할 것

### 보안 규칙 (반드시 준수)
- GitHub PAT는 `D:\vibe\.cowork-github-pat-token.txt`에만 저장, **채팅에 절대 노출 금지**(과거 노출 이력 있어 재발급 권장 상태)
- `.env`, `.kis-token.json` 등 시크릿 파일 내용은 어떤 경우에도 채팅에 출력/노출 금지
- Supabase `service_role` 키는 서버(Vercel 환경변수) 전용, 클라이언트 파일에 절대 포함 금지
- 포트원 `PORTONE_API_SECRET`도 서버 전용

### 배포/작업 워크플로우 원칙 (요약)
- **🔴 (2026-09-18 정정 — 두 번 헛수고했다) Cowork 세션에서 `D:\vibe`에 직접 커밋·푸시할 수 있다. "마운트에선 git 쓰면 안 된다"는 옛 규칙은 폐기한다.**
  - 실측: 마운트 읽기·쓰기 정상(9/8 윈도우 업데이트로 깨졌던 "드라이브 공유 없음" 장애는 해소됨), `git add/commit/push` 모두 동작. origin URL에 자격증명이 박혀 있어 별도 설정이 필요 없다(`git push --dry-run`으로 안전하게 먼저 확인할 것).
  - **⚠️ `-c core.autocrlf=input`을 반드시 붙일 것.** 윈도우 쪽 autocrlf는 전역 설정이라 리눅스 git이 안 읽는다. 그냥 `git add`하면 CRLF가 그대로 커밋돼 파일 전체가 바뀐 것처럼 된다. 같은 이유로 **리눅스에서 본 `git status`/`git diff`는 수십~99개 파일이 변경된 것처럼 보이는데 이건 착시다** — `git -c core.autocrlf=input status`로 다시 보면 0이다. 이걸 실제 변경으로 오해해서 "마운트 git은 쓸 수 없다"고 결론낸 것이 이번 헛수고의 원인이다.
  - **⚠️ 파이썬으로 파일을 고칠 때는 줄바꿈을 보존할 것.** `open(p,encoding=...)`으로 읽고 쓰면 CRLF가 LF로 바뀐다. `newline=''`로 읽어 원래 줄바꿈을 기억하고 쓸 때 되돌린다(이번에 `api/analyze.js` 전체가 LF로 바뀌어 되돌렸다).
  - **파일 삭제는 권한 요청이 필요하다.** 마운트에서 `rm`이 `Operation not permitted`로 막히는 건 정상이고, `device_request_delete_permission`으로 D:\vibe를 요청해 승인받으면 그 세션 동안 풀린다. 승인 없이는 git이 자기 `.git/index.lock`도 못 지워서 **시우의 `git pull`이 "Another git process seems to be running"으로 막힌다** — 세션 중 git을 썼으면 끝나기 전에 lock이 남았는지 확인할 것.
  - **클라우드 컨테이너에서의 push는 여전히 막혀 있다**(git proxy: "not in this session's authorized repository set"). 이건 윈도우와 무관한 Anthropic 쪽 제약이고, 이제 마운트로 푸시하면 되니 우회할 필요가 없다. 컨테이너 clone·검증은 그대로 유효하다.
  - **Downloads 폴더는 연결 폴더가 아니라 손이 닿지 않는다.** 패치 파일 정리를 세션이 하려면 폴더 접근 권한을 따로 받아야 한다.
- **(2026-09-03 갱신) `D:\vibe` 로컬 저장소는 GitHub와 동기화된 상태로 유지할 것 — 이번에 1,514 커밋 뒤처져 있던 것을 바로잡음**: 2026-08-10 이후 모든 실제 작업이 fresh clone에서 이뤄지고 GitHub로 직접 푸시되면서, 로컬 워킹 폴더는 8/10 상태 그대로 방치돼 있었다(`ahead 5, behind 1514`, 미커밋 변경 110개). 사이트는 GitHub 기준으로 배포되므로 프로덕션엔 영향이 없었지만, **로컬을 기준으로 코드를 읽으면 몇 달 전 코드를 보고 잘못된 판단을 하게 된다**(실제로 이번 세션 초반에 `weekly-market.html`을 로컬 기준 1,352줄로 읽어 GitHub의 1,532줄과 100줄 이상 어긋난 채 분석할 뻔했다). 2026-09-03에 백업 브랜치 `backup/local-pre-sync-2026-09-03`(커밋 `af0a5092`, 로컬 전용·푸시 안 함)를 만들어 잔재를 보존한 뒤 `git reset --hard origin/main`으로 동기화 완료. **앞으로의 규칙**: ①fresh clone에서 푸시한 세션은 **끝날 때 `D:\vibe`에서도 `git fetch && git reset --hard origin/main`을 돌려 로컬을 맞춰 둘 것**(로컬에 살릴 변경이 없다는 전제 — 있으면 먼저 백업 브랜치로) ②그래도 **코드 사실 확인의 기준은 여전히 fresh clone**이다(아래 항목의 stale 이슈는 별개로 유효) ③`.env`·`.kis-token.json`·`*token*.txt`는 `.gitignore`에 있어 reset으로 지워지지 않는다(2026-09-03 확인). `TOTALMONEY_STATUS.md`도 GitHub에 없는 untracked 파일이라 안전하지만, **git 작업 전에 항상 존재를 확인할 것**.
  - 부수 정리(2026-09-03): 실제 경로가 사라진 죽은 worktree 등록 2개(`/tmp/vibe-clean`, `/tmp/vibe-push`) prune, 로컬에만 남아 있던 옛 서버리스 함수 `api/crypto-data.js`·`api/crypto-news.js`를 `archive/local-dead-2026-09-03/`로 이동(GitHub에선 이미 `api/crypto.js` + `vercel.json` rewrite로 통합됨 — **로컬에 남아 있다가 실수로 커밋되면 함수가 14개가 되어 Hobby 12개 한도를 넘겨 배포가 실패한다**). 이후 `api/` 함수 수는 GitHub와 동일한 12개.
  - Cowork 마운트에서 `rm`이 `Operation not permitted`로 막힐 때가 있다(이번에 `.git/index.lock` 정리 시 발생). 그때는 포기하지 말고 `mcp__cowork__allow_cowork_file_delete` 툴로 삭제 권한을 요청할 것.
- **D:\vibe는 대용량 파일이나 특정 상황에서 stale한 내용을 보여줄 수 있음 — bash 마운트뿐 아니라 Windows 경로용 Read/Grep 툴도 예외 아님**: 2026-08-26에 `api/analyze.js`(2,936줄)에서 bash 마운트와 `D:\vibe\...` Read/Grep 툴 결과가 둘 다 동일하게 stale한 2,228줄 버전을 보여줘서 "trade-signal 관련 코드 자체가 없다"고 잘못 판단할 뻔한 사례 확인 — 예전엔 "Read/Edit 툴 결과는 항상 신뢰"로 알고 있었지만 대용량 파일엔 예외가 있음. **실제 배포용 수정은 물론, "이 코드가 존재하는지/어떻게 동작하는지" 같은 사실 확인도 접근 경로와 무관하게 항상 fresh clone(`/tmp`, `git clone --depth 1`)을 기준으로 진행·검증할 것**, 특히 대용량이거나 자동 커밋이 잦은 파일일수록 의심할 것
- 세션마다 git push 자격증명 보유 여부가 다름 — 안 되면 GitHub 웹 업로드 UI(Claude-in-Chrome, shadow DOM 대응 필요)로 대체
- 커밋 전 fetch+rebase 필수(자동봇이 수시로 데이터 동기화 커밋을 push하므로 충돌 방지)
- 1MB 초과 JSON(`data/daily-market.json` 등)은 GitHub `contents` API 대신 `git/blobs/{sha}` API 또는 git plumbing(트리 재구성) 사용
- 실제 렌더링 결과(PNG 등)로 직접 검증하는 습관 — 코드만 보고 "맞다"고 판단하지 않기(과거 line-clamp/flex 충돌, 이미지 미반영 등 실제 렌더링에서만 드러난 버그 다수)
- **인스타그램 발행은 더 이상 dry-run이 아니라 실제 무인 자동 발행 중** — 콘텐츠/데이터 수정은 발행 전 검증을 특히 신경 쓸 것(과거 4096자 초과·문장 중간절단·스키마 불일치로 실제 발행 사고/누락 이력 다수)
- **GitHub 웹 편집기(CodeMirror)로 큰 파일을 통째로 입력할 때 주의**: 키 입력을 그대로 시뮬레이션해서 긴 코드를 타이핑하면 자동 들여쓰기가 누적 오작동해 파일이 깨질 수 있다(2026-08-09 `health-check.mjs` 편집 중 실제 발생 — 커밋 직후 `git clone` + `node --check`로 바로 발각해서 재작업함). 안전한 방법: `.cm-content` 엘리먼트에 `ClipboardEvent('paste', {clipboardData})`를 직접 dispatch하는 JS를 실행해 붙여넣기 형태로 주입하면 자동 들여쓰기가 개입하지 않아 원본 그대로 들어간다. 어떤 방식으로 편집하든 **커밋 후 반드시 fresh clone + 문법 검사(`node --check`, `python3 -c "import yaml..."` 등)로 검증할 것.**

---

## 6. 이번 통합 작업 중 발견된 특이사항 (교차확인 결과)

### 2026-09-26 (저녁, 세션 11 — 프로젝트 창) — AI 분석 실패 수정 · 시세 라이선스 조사 (커밋 `45b18a57`·`05336f06`·`b29c193f`)

1. **증상** — SK하이닉스 분석이 3연속 "OpenAI returned non-JSON" → 화면에 내부 문구 빨간 박스 + 관망 50% 빈 리포트. Vercel 로그: web_search 경로 응답 끝에 `}` 하나 더(2건, "Unexpected non-whitespace character after JSON"), 속성 문법 깨짐 1건. 세션 10의 `repairJsonText`는 따옴표·줄바꿈만 고쳐 못 살림.
2. **수정(`45b18a57`)** — 괄호짝 추출 + 로컬 실패 시 OpenAI JSON 모드 교정 1회(평소 비용 0). 실로그 유형 4종 로컬 테스트 통과.
3. **실패 처리(`05336f06`·`b29c193f`)** — 실패한 빈 리포트가 '내 리포트'에 저장되고 무료 횟수가 차감되던 문제 발견·수정. 시우 계정은 premium·active 정상 인식(무료 차감 기록 없음). 배포 후 `r=11` 정상 생성 확인. api 파일 수 12 유지.
4. **시세 라이선스 조사** — data.go.kr 금융위 주식시세정보 = 공공누리 4유형(상업·변경 금지, KRX 마켓플레이스 구매 시 2차적 저작물 허용). KRX 히스토리컬 데이터 상품 안내(2026.9) 가격: 주식 일별 월 8.3만/연 99만, 투자자별 일별 월 5만/연 60만, 지수 일별 월 9천/연 10만, 주식 1분 월 24.8만/연 297.6만, 주식 패키지 연 6,270만. "단순 복제·재분배 수익 목적 구매 불가", 이용목적 심사. 코스콤 FAQ: 개인도 실시간 계약 가능(기본료+변동료, 비공개), 개인 블로그 게시는 일부 발췌·비체계·비상업일 때만. 상세는 `claude/MARKET_DATA_LICENSE.md`.
5. **코스콤 문의** — 시우가 게시판에 접수(사이트명·운영 현황은 빼고 창업 준비 단계로).

### 2026-09-26 (오후, 세션 10 — 프로젝트 창) — AI 종목분석 Opus 재채점(75점) 반영 (커밋 `3f08c490`·`7257d5bc`·`17d6801f`·`efff10a2`·`8e581a8d`·`994fd893`·`07b26f54`)

1. **PER(`3f08c490`)** — `api/analyze.js`: `computeMarketPosition(code6, {perOverride})`를 DART 실적·TTM 계산 **뒤**로 옮기고, PER 항목만 TTM 값으로 순위 계산(`note: "최근 4개 분기 실적 기준"`). `mpHeadline`은 TTM일 때 ROE(PBR÷PER — PBR은 옛 자본 기준이라 섞으면 129% 같은 엉뚱한 값) 대신 "최근 4개 분기 PER은 중앙값보다 낮다" 문장. `marketPositionPromptBlock`에 TTM 표기와 "이 PER이 중앙값보다 낮으면 비싸다고 쓰지 않는다".
2. **확률(`3f08c490`·`7257d5bc`)** — 서버 조정폭 LIM 20→10. `lib/pattern-stats.js` 프롬프트: 기준 숫자로 자기 설명 금지, 20거래일 통계 인용, 표본<30이면 참고용 명시. 코드 안전망: A안 basis 첫 문장을 "과거 N회 중 20거래일 뒤 M회(%) 상승 — A는 +5% 이상만 세므로 더 낮게 — 표본 적어 참고용"으로 고정, "기준 N% …맞/벗어나지 않/자연스럽" 문장 정규식 삭제.
3. **가격(`3f08c490`·`efff10a2`·`8e581a8d`·`994fd893`·`07b26f54`)** — 강세·중립 손절폭 < ATR이면 진입−ATR로 넓히고 "손절 근거" 문장 부착(하이닉스 A 183.6만→179.5만, 손익비 1:1.5). B.entry==A.entry면 B를 현재가 진입으로. 종합의견: AI가 자기 매매가로 쓴 문장에 새 숫자를 끼우던 `swapPlanPricesInText` 대신 `dropStalePlanSentences`(옛 가격이 든 문장 삭제 — "205.8만원은 되돌림을 받아낼 자리" 사례). `fixCurrentPriceMentions`: "현재가가 N원이라/입니다"만 실제 현재가로(“N원선 아래” 같은 기준선 표현은 유지). `normalizeQuick(q, price)`: 현재가 위 가격에 "지키면"→돌파 문구, 아래 가격에 "회복/넘"→이탈 문구, 0.5% 이내 중복 제거.
4. **점수·사실(`3f08c490`·`7257d5bc`)** — `computeScoreCard`: 20일선<60일선이면 추세 최대 +1. `lib/tech-extras.js`: trend.close 추가, 프롬프트에 "[주가 위치] 종가는 20일선 위, 60일선 위…" — "20일선이 60일선 아래"를 "주가가 60일선 아래"로 바꿔 쓰던 오류 방지.
5. **규칙(`3f08c490`·`7257d5bc`·`efff10a2`)** — HONEST_TONE_RULE에 재료·업황 근거(날짜·금액 확인된 것만, 자사주 매입·소각은 materials에 포함, HBM·ASP는 숫자 동반), 가격 표현 정합(괴리율 30%↑면 '가깝다' 금지, 현재가 위 돌파가는 저항), B안 구분, 단정 표현 금지.
6. **법적 표현(`3f08c490`)** — 면책 문구 보강, "상승 확률"→"강세 시나리오 추정", 확률 막대 "AI 추정치·보장 아님". 유사투자자문업자 고지는 신고 뒤.
7. **JSON 복구(`17d6801f`)** — `safeParseJSON` 실패 시 `repairJsonText`(문자열 속 이스케이프 안 된 `"`·줄바꿈 처리, 닫는 따옴표는 뒤에 `, : } ]`가 올 때) 후 재파싱, 실패 로그에 위치 근처 300자.
8. **검증** — Claude in Chrome으로 SK하이닉스 5회 재생성. 확인: PER 8.40배(낮은 쪽 상위 24%), 추세 +1, A 손절 179.5만, 근거 첫 문장 고정, 순환 문장 없음, 자사주 재료 복귀, B 진입 186.3만. 한 번은 non-JSON 실패(복구 배포 전). 발견: 붙여넣은 리포트는 **브라우저에 남은 옛 JS(v≤29)**로 그려져 "+1/8"·"매수"·면책 없음이 보였던 것.
9. **스크리너 캐시** — `trigger-screener-cache` repository_dispatch는 204·성공이지만 KRX 휴장 체크로 스킵. 1년 수익률 수정은 9/28 16:00부터.
10. **문서** — `docs/AI_REVIEW_LOG.md`(=프로젝트 `claude/AI_REVIEW_LOG.md`) 6번 행 "Opus 재채점 75점" 추가.

### 2026-09-26 (점심, 세션 9 — 프로젝트 창) — 종가시그널 카드 정리 · AI 종목분석 외부 리뷰 4종 반영 (커밋 `27375bcd`·`fe80325d`·`32886bfa`·`7e315ed1`·`6bd6f846`·`019bb0c2`·`61961c69`·`3874d5b3`·`e7a9a88a`)

1. **종가시그널**(`27375bcd`) — `assets/wide.css` 1280px 이상 두 칸 그리드에서 `#cbTodayCard`·`#cbYesterday`에 `align-self: stretch`(기존 `align-items:start`라 높이가 달랐다). `close-signal.html`의 `.cb-rec__v`가 `.cb-up/.cb-down`보다 뒤에 선언돼 색을 덮었다 → `.cb-rec__v.cb-up/.cb-down` 추가. 히어로 누적 성적 칸은 premium.css 규칙 때문에 여전히 회색(요청 범위 밖). wide.css v=4.
2. **보조지표**(`fe80325d`) — 새 `lib/tech-extras.js`: 분석 때 이미 받는 4년 일봉(`fetchKisDailyHistory`, `mapWmRow`에 volume 추가)으로 MACD(12,26,9)·볼린저(20,2) %B·밴드폭 120일 백분위·거래량(오늘/20일, 5일/20일, 상승일 비중)·**ATR(14)**·**추세 사실**(20/60/120일선 배열, 1·3개월 수익률). `techExtrasPromptBlock`이 세 프롬프트 경로 모두에 들어간다. 화면에는 `techExtras`로 내려가 ATR 배수 표시에 쓰인다. 차트 분석 새 칸 "보조지표 교차 확인"(`CHART_SECTIONS` aux).
3. **정직성·초보자·계획 일치 규칙**(`fe80325d`·`7e315ed1`·`6bd6f846`·`61961c69`) — `api/analyze.js` 상수 `HONEST_TONE_RULE`(불리한 사실 뒤 위안 문장 금지), `BEGINNER_RULE`(quick 필드 형식, 전문용어 첫 등장 시 괄호 풀이, 지표는 결론 먼저·숫자 하나, 종합의견-기준계획 일치, 재료 강도 시총 대비(0.1% 미만 '하'), 같은 표현 2회까지). 세 경로에 `patternStatsPromptBlock` 뒤로 붙는다.
4. **기준 확률**(`32886bfa`) — `lib/pattern-stats.js` `base`: A=20거래일 뒤 +5% 이상, C=−5% 이하, B=사이. p=(조건 표본 수 + 10×전체 기간 비율)/(표본+10). `normalizeAnalysis`가 AI 확률을 기준 ±20%p로 묶고 합 100 유지(최고 확률 시나리오 결정 전에). 화면 `.ai-probbar__base`·`__def`.
5. **목표가 강제**(`019bb0c2`·`61961c69`) — 강세·중립 시나리오 목표 < 진입 + max(ATR×1.5, 손절거리×1.5)이면 코드가 올리고, basis의 "목표 근거:" 문장을 사실대로 교체. AI가 적은 "ATR 약 N배"도 실제 배수로 고친다. ⚠️ 문장 치환 정규식은 소수점("0.2배")에서 끊기지 않게 `[다요]\.(?=\s|$)`까지.
6. **종합 의견 첫 문장**(`6bd6f846`·`3874d5b3`·`61961c69`) — `planLeadSentence`: "기준 계획은 A안(강세)입니다 — 190만원 돌파를 확인한 뒤 들어갑니다. 돌파 전에는 추격하지 않고, B안처럼 186.3만원 부근에서 버티는지 확인될 때만 대안으로 봅니다." AI가 따로 쓴 "기준 계획은/가장 높은 확률은" 문장은 `stripAiPlanClaims`가 제거. 화면 계획 줄에 돌파형/눌림형(`planModeText`, 채점과 같은 ±1%).
7. **TTM PER**(`019bb0c2`) — `lib/dart-financials.js` `computeTtm`: 직전 연간 + 올해 누적 − 작년 같은 기간(누적 보고서·1분기만). `quote.financials.ttm`(per·perOp·quotePer). 시총은 KIS `hts_avls`(억원). 프롬프트에 "밸류 부담은 TTM PER로, 차이 크면 둘 다". 실적 카드 `ttmNote`. 시장 대비 위치 요약에 ROE(=PBR÷PER) 문장(PBR 비싼 축 + ROE 15% 이상일 때).
8. **초보자용 화면**(`7e315ed1`) — `renderQuickView`(좋은 점·주의할 점·볼 가격 3개, `.aq`), `stanceFromScenarios`(상단 "현재 판단"), `normalizeQuick`(서버, 길이 제한·가격 내림차순). 채점 기준 한 줄 `.ai-opinion-grade`(`fe80325d`). stock-analysis.js v=30, css v=28.
9. **검증** — SK하이닉스로 새 리포트 5회 생성. 최종본: 추세 "단기 반등, 중기 골격 회복 전", TTM PER 8.4배 문구, 목표가 ATR 1.5배(손익비 2.8), 종합의견 첫 문장 계획+대안, 기준 확률 A61·B31·C8. 1회 "OpenAI returned non-JSON"(재시도 정상) — 3장 체크리스트 1번.
10. **리뷰 점수 흐름** — Kimi 91 / GPT 82 / Gemini 84 / Opus 65(구 버전 기준). Opus 지적 대부분이 "숫자로 검증 가능한 모순"(PER 기준 시점, 역배열인데 우상향, 계획-의견 충돌)이었다 → 칸별 확인만으로는 못 잡는다. 수정 후 리포트 전체 모순 점검을 절차로.

### 2026-09-26 (낮, 세션 8 — 프로젝트 창) — 모바일 차트 빈칸 · 암호화폐 상세 · AI 종목분석 4종 보강 · 종가시그널 결과 카드 (커밋 `d2380c03`·`f3d47157`·`34c029a6`·`fa0b443a`)

1. **국내주식 모바일 차트 빈칸**(`d2380c03`) — `realtime-board.css`의 모바일 규칙(230px)이 파일 뒤쪽 기본값(300px)에 덮여 칸은 300, 차트는 230으로 그려져 아래 70px가 비었다. 파일 맨 끝에 모바일 280px 규칙 + JS `LW_CHART_MOBILE_H=280`. css v=10(국내·미국·마감시황).
2. **암호화폐**(`d2380c03`·`f3d47157`) — `api/crypto.js` listings(캐시 키 v4)에 circulatingSupply·totalSupply·infiniteSupply·change30d/90d·volumeChange24h·dominance 추가. 새 `action=info&id=`(CMC `/v2/cryptocurrency/info`, 하루 캐시 + CDN `s-maxage=86400`). ⚠️ CMC info의 aux에 `date_launched`는 안 된다(400) → `urls,date_added`. 뉴스는 `kind=news`·vercel 라우트·화면 전부 삭제(`lib/crypto-news.js`는 `api/analyze.js`가 계속 사용). 왼쪽 폭 `assets/wide.css` `minmax(420px, 30%)`, 상세 차트 높이는 화면 바닥까지(340~640). 트레저리 보유량·CertiK 평점은 CMC 기본 API에 없어 생략.
3. **1년 수익률 모순**(`34c029a6`) — "1년 +512.8%"의 기준가(30.7만)가 52주 저점(33.3만)보다 낮았다. 원인: 한국 증시는 1년에 245~248거래일이라 "252거래일 전"이 52주보다 1~2주 더 과거. `computePeriodReturns(closes, times)`·`trade-condition-eval` periodBase가 52주(364일) 창 첫 거래일을 기준으로(내일 봉 기준은 하루 밀어서). 반영은 다음 스크리너 캐시 재생성부터. **순이익 134조 > 영업이익 98조**는 실제 공시와 일치(2분기 순이익 93.9조 > 영업이익 60.5조) — 오류 아님, 화면에 설명 한 줄(`fin__why`).
4. **진입가 단일화**(`34c029a6`) — `normalizeAnalysis`가 확률 최고 시나리오(약세 C 제외)의 entry/stop/target을 상단 값으로 덮어쓰고 `opinion.planScenario`를 내려준다. 종합 의견 속 옛 숫자는 `swapPlanPricesInText`가 같은 표기("183만5000원"/"1,835,000원"/"183.5만원")로 교체. 프롬프트도 "진입가는 하나만"으로.
5. **실적 발표 이벤트**(`34c029a6`·`fa0b443a`) — `earningsSeasonEvent`: DART 실적이 있는 국내 종목에만. `data/kr-earnings-manual.json`·`data/earnings-calendar.json`에 예상일이 있으면 "잠정 실적 발표 예상일(날짜 바뀔 수 있음)", 없으면 다음 미제출 분기의 정기보고서 법정 기한(분기 +45일, 사업보고서 3/31)을 날짜로 쓰고 문구에 그 뜻을 밝힌다. 이미 IR·잠정실적 이벤트가 있으면 생략.
6. **과거 유사 국면 통계**(`34c029a6`·`fa0b443a`) — 새 `lib/pattern-stats.js`. 분석 요청 때 KIS 일봉 4년치를 140일 창 4개씩 병렬로 받아(`fetchKisDailyHistory`) 오늘 상태(종가 vs 20일선, 20일선 vs 60일선, RSI 5구간)와 같은 날을 찾고 5·20거래일 뒤 수익률을 센다(연속된 날은 5일 간격으로 한 번만). 12회 미만이면 thin. ⚠️ **실제 분석은 `openaiWebSearchAnalyze`(및 RSS 폴백) 경로라 `buildUserPrompt`에 넣은 블록이 안 들어갔다** → 두 경로의 입력 데이터 뒤에도 `patternStatsPromptBlock` 추가(`fa0b443a`). 라이브 확인: SK하이닉스 표본 15회, 확률 근거에 "15회 중 11회 상승" 인용. 참고: `marketPositionPromptBlock`·`financialTrendPromptBlock`도 OpenAI 경로엔 안 들어가 있다(기존 상태, 손대지 않음).
7. **AI 분석 성적표**(`34c029a6`) — Supabase `analysis_reports`에 채점 컬럼 추가(plan_label·outcome·outcome_date·outcome_price·outcome_return_pct·entry_date·ret5_pct·ret20_pct·bars_after·graded_at). 규칙 `lib/ai-report-grade.js`(다음 거래일부터 20거래일, 진입가 ±1%면 시가 진입/낮으면 눌림 체결/높으면 돌파 체결, 같은 날 목표·손절 동시면 손절, 진입한 날은 손절만 판정). 배치 `scripts/ai-report-grade.mjs` + `.github/workflows/ai-report-grade.yml`(평일 17:40 KST, `repository_dispatch: trigger-ai-report-grade`). 화면 `?feature=report-track`(로그인 필요, 같은 날 같은 종목은 마지막 리포트 하나, 국내만, 개인 정보 없음) → `#ai-track` 달력. 새 api 파일 없음(12/12 유지).
8. **종가시그널 결과 카드**(`34c029a6`) — `close-signal.html` `ydCardsHtml`·`ydSummaryHtml`·`ydNoteHtml`. 결과 표(`resultTableHtml`)는 달력 상세·로그인 전 카드에서 그대로 쓴다. 두 머리글은 날짜 줄을 항상 다음 줄(제목 글자 시작점)에 두도록 통일, 오른쪽 설명문을 한 줄 길이로.
9. **QA 메모** — 긴 요청(AI 분석 27초)은 클라우드 컨테이너 프록시가 "upstream request failed"로 끊는다. 실측은 시우 PC 쪽 device_bash에서 node fetch로(세션 토큰은 `Claude outputs/qa/mk-session.js`로 새로 만들어야 함 — 1시간 만료).

### 2026-09-26 (오전, 세션 7 — 프로젝트 창) — 히트맵 균형·라이트 · 읽는 페이지 배치 · 페이지 넘김 선택 유지 · 첫 로딩 속도 (커밋 `72f3baac`·`32c4b886`·`86375a7b`·`691a6b8f`·`c7c6488b`·`c9fa52b1`·`a1479255`)

1. **히트맵 균형**(`72f3baac`) — `assets/home-heatmap.js` `SEC_POW=0.5`(섹터 면적=시총^0.5), `TILE_POW=0.75`(섹터 안 칸), 작은 칸 거르기도 조정된 면적 기준. 코스피 IT 64%→약 33%. 툴팁 시가총액 숫자는 실제값 그대로.
2. **히트맵 라이트 모드**(`32c4b886`) — 바탕·섹터·말풍선이 CSS에 어두운 색으로 박혀 있었다. `[data-theme="light"]` 규칙 추가, `tileColor()`는 라이트에서 보합 회색을 밝게(150,158,172), `data-theme` MutationObserver로 테마 전환 시 즉시 render. 범례 -1%·0%·+1% 색도 라이트용으로.
3. **읽는 페이지 배치**(`86375a7b`→`691a6b8f`) — 새 파일 `assets/read-layout.css`·`assets/read-layout.js`(wide.css 다음). JS가 페이지가 그린 섹션 중 제목이 '간밤 미국시장 마감·환율 · 원자재 · 크립토·간밤 시장 온도'(브리핑), '지수 마감·오늘의 시장 온도'(마감시황)인 것을 `.rd-rail`로 옮기고 자리 표시(`span.rd-ph`)를 남긴다. 페이지가 다시 그리면 MutationObserver가 다시 나눈다. 1200px 미만이면 원위치. 폭·배율은 `.tm-main > .page:has(...)`에 `zoom: var(--rd-z)`, `max-width: calc(1640px / zoom)`.
   - **함정 1**: 마감시황 `.page--daily-market`의 `overflow-x:hidden`이 스크롤 상자를 만들어 왼쪽 칸 sticky가 안 먹었다 → `overflow-x: clip`.
   - **함정 2**: zoom 안에서는 길이가 배율만큼 커진다 → sticky `top`·`max-height`를 `/ var(--rd-z)`로 나눔.
   - **함정 3**: zoom 안의 lightweight-charts는 십자선이 마우스와 약 14% 어긋났다(실측) → 매매시그널·AI종목분석은 zoom 제외, 폭만 1640px, 작은 글씨는 실측 목록(클래스별 11px 이하 +2px, 그 위 +1.5px)으로 개별 확대. 날짜 선택 팝업(브리핑·마감시황)은 zoom 안에서도 정상.
   - **QA 메모**: Playwright는 기본 로캘이 `en-US@posix`라 lightweight-charts가 "Invalid language tag"로 차트를 못 그린다 → 컨텍스트에 `locale:'ko-KR'` 필수. 또 addInitScript로 Supabase 세션을 매 페이지 덮어쓰면 회전된 refresh token 때문에 두 번째 페이지부터 로그아웃 화면이 뜬다 → 비어 있을 때만 넣기.
4. **페이지 넘겨도 선택 유지**(`c7c6488b`) — 원인: PC 분할 화면의 오른쪽 상세는 tbody 안 `tr.rt-detail-row`(절대 위치)인데, 페이지 전환 시 `openChartCode=null` → `scheduleSplitAutoOpen()`이 새 페이지 첫 종목을 클릭. 수정: `realtime-board.js` 페이지 전환 2곳(`isSplitLayout()`이면 유지), `renderTable` 재그리기 때 이 페이지에 없는 열린 종목의 상세 행을 떼었다가 맨 끝에 다시 붙임, `syncDetailDomAfterRows`도 같은 처리. `us-market.js`는 `keepOffPage`로 상세 행을 끝에 추가. `crypto.html`은 `renderDetailPane`이 전체 목록(`state.coins`)에서 찾음 + `refreshAll()`이 폴링마다 `currentPage=1`로 되돌리던 버그 제거.
5. **첫 로딩 속도**(`c7c6488b`·`c9fa52b1`) — 실측: 국내 목록 2.7초인데 오른쪽 차트 6.7초. 병목은 `/api/stock-analysis?quoteOnly=1` 1.1~3.8초(KIS 3개 병렬 + 수급 폴백 순차). 조치: 그 응답과 `kis-stock-quote` 현재가 응답에 `public, max-age=0, s-maxage=10, stale-while-revalidate=20`(CDN HIT 3.5초→0.16초 확인), `realtime-board.js` `fetchQuoteRaw()`로 같은 종목 20초 합치기 + init에서 삼성전자(시총 1위) 상세를 목록과 동시에 요청. 결과(캐시 살아 있을 때): 국내 차트 6.7→2.7초, 미국 3.1초, 암호화폐 3.0초. 정적 파일은 curl 0.18초라 미들웨어는 병목 아님(건드리지 않음).
6. **종가시그널 머리글**(`a1479255`) — `.cb-sec`가 `align-items:center`라 날짜 줄이 줄바꿈된 왼쪽만 높아 좌우 제목 높이가 30px 어긋났다 → 시작점 정렬.
7. **세션 경로 메모** — 프로젝트 밖 채팅(세션 6)에서 한 작업은 이 창의 대화 검색에 안 잡힌다. 이번엔 시우가 그 대화를 붙여 줘서 이어받았고, D:\vibe의 STATUS.md를 프로젝트 문서(`claude/TOTALMONEY_STATUS.md`)로 이 세션에서 직접 동기화했다. git push 중 `index.lock`이 남아 D:\vibe 삭제 권한을 받아 정리(메모리 dvibe 규칙대로).

### 2026-09-26 (새벽, 세션 5·6) — 홈 히트맵 머리줄 · 홈 리포트 줄 개편 (커밋 `eae95a25`·`29d98b81`·`226f73f9`)

1. **히트맵 v3**(`eae95a25`) — 지수 8개(삼성전자·SK하이닉스는 /api/market-ticker의 hub.samsung/skhynix), `.hm__panel` 머리줄 `.hm__bar`에 제목+시장 탭. 1100~1400px 값 15px·'원' 숨김, 1099↓ 4열, 700↓ 2열.
2. **모바일 히트맵**(`29d98b81`) — ≤700에서 지수 3개 한 줄, `fitMobile()`이 innerHeight−지도top−범례로 높이 계산(최소 340, 폭 바뀔 때만).
3. **홈 리포트 줄 개편**(`226f73f9`) — `index.html`: `.hx-report--ai` 삭제 → `.hx-report--sched` "일정"(id `home-schedule-body` 유지, 링크 weekly-market.html). `home.js`: `renderSchedule()`이 오늘 이후 한·미 지표를 시간순 5개("오늘 03:00" / "9/28(월) 23:30"), `hxRenderFeatured()` slice 3→8(데이터에 6~9개라 날마다 다름). `bindAiForm` 등은 요소가 없으면 빠져나가 그대로 둠. CSS는 `premium.css` 끝(`?v=20260926`), 1500px 미만에선 사이드가 한 줄 전체·특징주 2열. 2560·1400 폭 렌더 확인.
4. **작업 경로 메모** — 세션 6은 프로젝트 밖 일반 채팅이라 프로젝트 규칙(device_bash 직접 커밋)을 못 보고 `_apply-home-schedule.ps1`(D:\vibe, 미커밋)로 시우가 푸시. 그 과정에서 device_bash의 `git status` 한 번이 `.git/index.lock`을 남김(스크립트가 삭제) — git 읽기 명령도 잠금 파일을 만들 수 있다.

### 2026-09-25 (낮~저녁, Cowork 세션 4) — 사이트 비공개 · 넓은 화면 전면 정리 · 차트 공용 규칙 · 홈 섹터 히트맵 (커밋 `a45be90d`~`e7ab2210`)

1. **종가시그널 가격 제거**: B안(`a45be90d`)으로 갔다가 시우가 "A를 B로 착각" 정정 → **A안**(`a924476a`, 원 단위 가격 열만 삭제·종목별 % 유지). API `cbPublicDay`도 가격 필드 제거.
2. **로그인 전 화면 통일**(`0ba7ebe4`·`2997d690`) — 종가시그널·AI 종목분석을 매매시그널처럼 소개+가입 카드. 종가시그널은 오늘 순위 숨김(API visible=0) + 가입 카드 안에 전일 성과, 바로 푸터.
3. **국내주식·미국주식 개편**(`c0018f97`·`28662f69`·`5e01fd2f`·`8667b9cb`) — 6탭, 좌목록·우상세 분할, 20개×페이지. 차트 날짜 잘림의 원인은 `.rt-table td` 여백이 Lightweight Charts 내부 table에 먹은 것.
4. **전체 폭·메뉴**(`0e8dd8c0`·`c3a0a3f8`·`5fa8bb88`) — 1200px 제한 해제, 홈 시세 3칸, 메뉴 11개, 제목 배너 제거, 실시간시세→국내주식.
5. **비공개 전환**(`276a2ce0`·`44a89651`·`c86b192f`, `2aef5ad3`) — 2장 요약 참고. 시우: "당분간 나만 볼 수 있게 + 기존 아이디와 승인한 사람만". 인스타·텔레그램 공개 발행도 중지. ⚠ 이 세션 중 로컬에서 KIS tokenP를 호출한 적이 있어 원칙 재확인: **토큰은 워크플로만 발급**(`639faccc`로 repository_dispatch 재발급 경로 추가).
6. **넓은 화면 전면 정리**(`d8ba17d2`) — 20개 HTML에 `assets/wide.css`, 5개 차트 페이지에 `assets/tm-chart-style.js`. 1920/1440에서 모든 페이지(매매시그널 검색 결과·종가시그널 회원화면·AI 리포트 포함) 렌더 확인. 발견·수정: 암호화폐 sticky가 `.crypto-panel{overflow:hidden}` 때문에 안 붙던 것, `worldLogoFail`이 IIFE 안에 갇혀 onerror에서 못 부르던 것, 추석 휴장일에 마감시황이 "준비 중"으로 뜨던 것, 휴장일 티커가 오류 문구로 바뀌던 것, 마이페이지 가입일이 전부 오늘로 나오던 것(site_members.created_at → auth.users.created_at).
7. **차트 빈 구간 사고**: 엔비디아 일봉이 200개인데 2024-10~2025-02 뒤 2026-01~06으로 건너뛰고 최근 3개월이 없었다. 원인 = 과거 구간 8개 창을 동시에 요청하다 KIS 초당 제한으로 일부 창(최신 창 포함)이 조용히 실패 + 그 결과가 CDN에 수시간 캐시. 실패 창 순차 재시도 + 부분 응답은 캐시 20초로 수정 → 500봉·당일까지 확인.
8. **홈 섹터 히트맵**(`7e2944e8`·`3f07e564`·`e7ab2210`) — 시우 참고 이미지 기반. 섹터 분류는 WISEindex WICS 구성종목 API(코스피+코스닥 2,459종목, 우선주는 보통주 섹터 따름). 미국은 Nasdaq 스크리너 섹터가 부정확(TSLA=Industrials 등)해 GICS 수기표 사용. 배치는 squarify 직접 구현. 이름 잘림 없애기: 글자 폭(한글 1em·영문 0.55~0.64em) 추정으로 한 줄 축소 → 두 줄 → 최소 9px에도 안 들어가면 칸 제외 후 재배치, 섹터 머리글도 같은 방식. 자동 점검으로 4탭×3폭에서 넘침·이름 없는 칸 0 확인. 오른쪽 실시간 AI 분석 칸과 모바일 AI 피드 삭제.
9. **AI 자동 분석 출처 추적**: `public_ai_feed`에 매일 09:12~09:14(일요일 포함) 삼성전자·SK하이닉스·현대차가 로그인 없이 쌓이고 있었다 → `api/analyze.js`의 `INTERNAL_SEED_KEY` 경로 + PC 로컬 Cowork 예약작업 `totalmoney-ai-feed-seed`(STATUS 6장 기존 기록에 존재). 서버 경로는 폐기했고, 예약작업 삭제는 시우가 앱에서(세션이 로컬 예약작업 폴더에 접근 불가).

### 2026-09-25 (새벽, Cowork 세션 3) — 시세 라이선스 결론 · 재편 결정 · 개인 대시보드 1단계 (커밋 `48c6962d`·`9cb0714f`)

- **라이선스 조사**: KIS 약관 제5조③(개인 업무 한정·제3자 제공 금지, 위반 시 이용정지), KRX OPEN API 제6조②(비상업)·제11조②(제3자 제공 금지), data.go.kr 금융위 주식시세정보 = 공공누리 4유형(상업 금지). 코스콤 오픈API 시세 라이선스는 **법인만**(개인사업자 불가), 웹 공개표출은 정액제. TradingView 위젯은 주식 **지연**(암호화폐·환율만 실시간), KRX 종목 지원 여부 미확인. 상세 `claude/MARKET_DATA_LICENSE.md`.
- **시우 결정**: 사업자는 개인으로. 등록 전까지 재편하며 가다듬기. 개인 대시보드는 사이트 안에 숨김. AI 종목분석은 버리지 않고 게시형으로 바꿔 핵심 유료 상품으로. 유튜버 운영 사이트 조사(대부분 시세 대신 의견·교육을 팔고 네이버 프리미엄콘텐츠·강의 플랫폼으로 결제) 후 이 방향 합의.
- **구현**: 위 2장 "2026-09-25 새벽" 참고. RLS를 SQL 역할 전환으로 검증(다른 계정 0행·시우 1행·anon 권한 거부). 화면은 컨테이너 Playwright로 비로그인 차단/운영자 표시/관심종목 추가·필터 확인. 배포 후 `/data/kr-all.json`은 SPA 폴백 HTML(파일 없음), `/api/repo-data?path=data/kr-all.json`은 404.
- **함정 메모**: 테스트용으로 `Claude outputs/_tmp`를 만들면 삭제 권한이 필요하다(device_request_delete_permission). 저장소 git 히스토리에는 옛 kr-all.json이 남아 있다(공개 저장소면 과거 스냅샷은 여전히 조회 가능).

### 2026-09-24 (심야, Cowork 세션 2) — 구글 로그인 · GNB 재구성 · 리뉴얼 착수 · 시세 라이선스 이슈 (커밋 `3886271b`~`c4a42d03`)

- **구글 로그인**: GCP에 OAuth 클라이언트가 아예 없었다(브랜딩만 있음). 브랜딩에 홈·약관·개인정보 링크와 승인 도메인(totalmoney.kr, slcplrblmdiydakwzqdc.supabase.co) 추가 → 웹 클라이언트 생성(원본 www/비www, 리디렉션 Supabase 콜백) → 프로덕션 게시. 🔴 **새 콘솔은 클라이언트 생성 창을 닫으면 비밀번호를 다시 못 본다**(시우가 창을 닫아 상세화면 복사 버튼으로 복구). 비밀키 붙여넣기는 시우가 직접. 계정 선택 화면에서 두 번째 계정(authuser=1) 세션 꼬임으로 400 한 번 — 다른 계정으로 성공. 선택 화면에 supabase.co 주소가 보이는 건 Supabase 커스텀 도메인(유료)으로만 해결.
- **GNB**: 우선순위 접힘 JS는 `#home-nav-links > .home-nav__link`만 센다 → "시장" 그룹은 그 밖(형제)에 둬서 항상 보이고 형제 폭으로 계산된다. 아이콘 타일을 넣자 1200px 컨테이너에서 한 개가 계속 더보기로 밀렸다 — 원인은 ①숨은 더보기가 자리를 차지 ②`margin-left:auto`를 JS가 폭으로 셈. 홈 버튼 제거로 해결. `.home-nav__links`는 overflow:hidden이라 드롭다운을 그 안에 두면 잘린다.
- **em 초록 함정 재발**: 배지 안 `<em>`이 전역 em 색 규칙에 칠해져 ▲▼가 초록. `color:inherit !important`로도 안 됐고 span으로 바꿔 해결.
- **kr-realtime.json의 `tvBoard`는 부정확**(cap·tv 탭 전부 KOSPI) — 시장 구분에 쓰지 말 것. 새 `kr-all.json`은 시장별로 받아 정확.
- **네이버 코스피 시가총액 목록에는 ETF·ETN이 섞여 있다**(코스피 2,484 중 상당수) → `kind` 필드(S/F/N).
- 컨테이너 node fetch는 네이버가 egress로 막힘 → 네이버 호출 테스트는 device_bash에서.
- **device_commit_files로 새 파일을 올린 직후 md5가 달랐다**(직전 수정분 누락) — 커밋 전 md5 대조 규칙이 또 사람을 살렸다. device에서 같은 수정을 다시 적용해 재커밋.
- 시세 라이선스 조사 결과는 3장 7번.

### 2026-09-24 (밤, 마무리) — 일정 변경 기록

- 유사투자자문업 교육 **수료 완료**, 사업자등록은 **2027년으로 연기**(시우). 결제 오픈도 그에 맞춰 연기.
- 소셜 로그인은 사업자와 무관하게 진행: 구글은 콘솔 설정만 안 된 상태(사업자 불필요), 카카오는 개인 개발자 비즈 앱 전환 경로로. 다음 세션에 Redirect URLs → 구글 → 카카오 순서로 처리.

### 2026-09-24 (밤, 마지막) — 종가시그널 결과에 선정 순위·점수 vs 수익 순위 배지 (커밋 `00c7c35d`)

- 종목명 옆 초록 "선정 N위 · 점수" 배지 + "종가 수익 N위 ▲/▼" 배지(15:45 전엔 시가 기준). 선정보다 2계단 이상 오르거나 하위권이 수익 1위면 빨간 "역전" 배지. PC 표엔 "선정 → 수익 순위" 열. 9/22 선정분에서 5위 뉴파워프라즈마 → 종가 수익 2위 ▲3 확인.

### 2026-09-24 (밤) — 종가시그널 모바일 결과 목록 · 자기학습(기법 가중치 자동 조정) 신설 (커밋 `4d585c8d`·`21a16e76`·`b30e9dd1`)

- **모바일 결과 목록**(≤560px): 표 가로 스크롤 대신 "종목 | 시가 매도 | 종가 매도"(크게) + 아래 줄 매수→시가→종가·하루 고저. 직전 선정 결과·달력 펼침 공통. 다크/라이트 390px 검증.
- **자기학습**(시우 결정: 자동 반영 + 주간 텔레그램 보고, **사이트엔 노출 안 함**)
  - `close_signal_outcomes`(Supabase, RLS만·정책 없음 = 서비스키 전용): 매일 필터 통과 후보 전원(30~40종목)의 특성 + 익일 시가·종가·고저 수익률. 상위 5종목만 기록하면 선택 편향이라 전원 기록.
  - `scripts/close-signal-learn.mjs` + `close-signal-learn.yml`: collect(거래일 16:05, 휴장일 스킵, 백필은 force=1) / learn(토 09:00). 목적 지표 = (시가+종가 매도)/2. 기법별 평균 대비 우위 → 수축 추정 → 가중치 0.5~1.5, 주당 ±0.2, **표본 30건 + 20거래일 이상일 때만 조정**(그 전엔 관찰만 → 10월 중순부터 실조정 예상). 특성 구간 분석으로 "새 기법 후보"·"피할 구간" 보고.
  - `lib/close-betting-score.js`: 기법 합의도 = 학습 가중 합(초기 `data/close-signal-weights.json` 전부 1.0 = 기존과 동일).
  - 백필: 9/14~9/22 7거래일 232건. 첫 미리보기: 후보 평균 시가 +0.56%/종가 +0.60% vs **실제 선정 5종목 시가 +1.08%/종가 +2.88%**(35건) — 현재 채점이 평균보다 낫다. 후보 구간: 거래량비 150~200%·시총 1~3조·갭 +1~3%. 피할 구간: 종가위치 0.6 미만·5일선 이격 100~103·갭 -1~0%. (7일치라 참고만.)
  - 새 기법 후보를 정식 기법으로 넣는 건 자동이 아니다 — 보고를 보고 시우가 "후보 기법 추가해줘"로 요청.

### 2026-09-24 (저녁) — KRX 휴장일 달력 도입 · 추석 오작동 3종 정리 · 점검봇 재정리 · 아침 브리핑 수치 고정 (커밋 `2eff6736`·`06206632`)

- **사고(9/24 추석)**: ①시세 동기화가 9/23 순위를 복사한 "9/24" 항목 생성 ②점검봇·워치독이 이를 거래일로 오판해 "마감시황 미발행" 반복 알림 ③종가시그널 성과 추적이 KIS가 돌려준 9/23 시세를 "9/24 결과"로 저장(엠케이전자 매수 16,550 → "익일" 종가 16,550 = 0.00%, 실제로는 9/23 봉) ④장중 스캔이 빈 "9/24 랭킹" 저장 → 9/28 채점이 9/23 선정분을 통째로 건너뛸 뻔함. 공통 원인: 각 작업이 "주말만 거르기" 또는 "등락률이 0이 아니면 거래일"로 추측.
- **`lib/krx-calendar.js` 신설** — 2026 KRX 휴장일 17일(확정: 지방선거 6/3·제헌절 7/17 포함) + 2027(잠정). `isTradingDay/nextTradingDay/prevTradingDay/closedReason`. **판단은 여기 한 곳.** `scripts/krx-guard.mjs`로 한국장 워크플로 9개에 가드(daily-market-sync·save-prev-top50·kr-screener-cache·trade-signal-scan·intraday-close-scan·close-betting-review·daily-closing-report·daily-report-watchdog·instagram-carousel-v2). 수동 강제는 `KRX_FORCE=1`.
- **종가시그널**: 리뷰 스크립트가 휴장일 차단 + "선정일 다음 거래일"일 때만 기록 + 비어 있는 랭킹 행 건너뛰기. 화면은 휴장 안내("오늘 휴장 · 직전 거래일 9/23 기준")와 "9/23 선정 5종목 결과는 9/28(월) 09:10부터" 대기 문구(API `market`·`pending` 필드). **9/28 09:10에 9/23 선정분이 자동 채점된다.**
- **데이터 정리**: Supabase `close_signal_results`(9/23→9/24 가짜 결과)·`trade_signal_intraday`(9/24 3행) 삭제, `data/daily-market.json`의 가짜 9/24 항목·`data/daily/2026-09-24.json` 삭제.
- **점검봇 재정리**(`scripts/health-check.mjs`): 항목 12개를 표 하나로 — 매일/평일/거래일/토요일 구분. 워치독과 중복되던 "마감 리포트 누락" 제거(워치독 전담), 마감 카드는 "리포트는 있는데 발행 안 된 경우"만. 종가시그널 결과·스캔 점검 신설. 달력 유지보수 알림(12월·11월). `--list`로 항목 출력. 워치독: 달력 기준 + **거래일인데 시세 동기화 자체가 없으면 알림**(예전엔 조용히 넘어감), "Cowork" 안내 문구 → GitHub Actions 기준으로 정정. 문서: `docs/운영점검_시퀀스.md`.
- **아침 브리핑 수치-분석 불일치 원인**: 08:14 AI 분석 작성 시점엔 숫자가 일치(EWY -3.62%·WTI -2.49%)했는데, 08:21·09:57 수치 재수집이 덮어써서(EWY -1.86%·WTI -3.18%) 어긋났다. → 오늘 분석 발행 후엔 재수집하지 않고 고정(`BRIEFING_FORCE_REFRESH=1`로 강제 가능). 또 Cowork가 실패한 날 **어제 분석이 오늘 숫자 옆에 남던** 문제 → 발행 기록이 오늘이 아니면 분석을 비움. ("나스닥 1%대 하락"은 나스닥종합 기준으로 보이며 수치 파일엔 나스닥100만 있어 확인 불가.)
- 검증: 운영 환경에서 Close Betting Review 실행 → "추석 연휴 — 다음 거래일 9/28" 건너뜀 확인, 라이브 API `market/pending` 확인, 종가시그널 화면 스크린샷 확인, 점검봇 로컬 실행(휴장일 6/12개만 점검, 이상 없음).

### 2026-09-24 — 미해결 3건 정리 · 인스타 카드 로고 교체 (커밋 1개)

- **삼성 실적 숫자 의심 건 종결**: 웹 공시 보도(1Q 133.9조/57.2조, 2Q 171.5조/약 89.5조)와 화면 값(305조 3,729억 / 146조 7,252억)이 일치 — 버그 아님.
- **라이트모드 로그인 버튼**: 시우 결정으로 초록 유지.
- **인스타 카드 로고**: TM 글자 배지 → 스텝 마크(청록 타일), 커밋 `e9d0bc69`. 시안 A/B/C 비교 후 C 선택. 클라우드 clone + Playwright로 4슬롯 21장 전부 로고 1개·TM 텍스트 0개 확인.
- 새로 발견: 아침 카드 1번 수치 칩 우측 잘림(3장 참고).
- **마감 카드뉴스 v3 "이슈 스토리" 전환** (커밋 `863f5081`·`cc405ab9`·`e1600640`) — 시우 피드백: "매일 2번 카드가 삼성·하이닉스, 매일 돈 몰린 곳만 얘기, 그날 이슈가 없다". 원인: v2 훅을 숫자로만 골라 거래대금 1·2위·업종 카드가 고정. 마감 리포트의 원인 사슬(뉴스→반응→결과)은 카드에 안 쓰였다.
  - 새 구성: ①훅(그날 사건을 질문으로) ②③이슈 카드(사건→반응→결과 + 움직인 종목) ④급등 이유 ⑤수급 한 줄+다음에 볼 것 ⑥CTA. 이슈 1개면 5장.
  - 파일: `scripts/promo-build-deck-story.mjs`(문장 생성·검증), `scripts/promo-carousel-story.mjs`(슬라이드), `promo-build-deck.mjs`(v3 먼저 시도 → 실패 시 v2 폴백), `promo-render-carousel.mjs`(layout `closing-story`).
  - 할루시네이션 방지: 종목 등락률은 코드가 붙임(리포트 본문 > 특징주 > 시세목록 — 시세목록은 시간외 가격이 섞여 리포트와 다름, 9/23 두산에너빌리티 -5.55% vs -4.39%). AI 문장 속 숫자는 원문에 **같은 단위로** 있어야 통과(테스트에서 "+3.5%"가 "3.5조" 덕에 새는 것 잡음).
  - **버그 수정**: 렌더가 이전 회차 `slide-N.png`를 안 지워, 장 수가 줄면 지난 카드가 섞여 발행될 뻔했다 → 렌더 전 삭제.
  - 문장 생성 순서 OpenAI → Claude로 변경(Anthropic 잔액 소진으로 매일 실패 호출이 나가던 것 정리 — 마감 덱만).
  - 검증: repository_dispatch dry_run으로 9/23 데이터 실AI(gpt-4o) 3회 → 3회차 이슈 2·급등 3·체크 3, 6장 정상. 실제 첫 발행은 추석 연휴 뒤 첫 거래일 17:30.
- **아침 브리핑 카드도 v3로 전환 + 카드 색 사이트 통일** (커밋 `e90c40d5`·`06b984b0`) — 시우 결정: 카드 색을 사이트(검정 #000 + 에메랄드 #10B981)와 통일(`deck.theme="site"`, CTA 슬라이드에도 적용).
  - 아침 구성: ①훅(간밤 사건 질문) ②③이슈(사건→반응→한국장 영향 + 간밤 움직인 지표) ④오늘 한국장 볼 곳 ⑤원/달러·WTI·나스닥선물 띠+장중 체크포인트 ⑥CTA. 파일 `scripts/promo-build-deck-morning-story.mjs`.
  - ⚠️ **아침 분석 원문(aiAnalysis)이 실측과 어긋난다**: 9/24 원문 "나스닥 1%대 하락"·EWY -3.62%·WTI -2.49% vs 실측 나스닥100 -0.04%·EWY -1.86%·WTI -3.18%. 카드는 "지표 이름 바로 뒤 %는 실측과 대조"로 걸러내지만, **사이트 아침 브리핑 본문 자체는 틀린 숫자가 그대로 나가고 있을 수 있다** → 별도 확인 필요(3장).
  - 이슈 카드 빈 공간: 사슬을 남은 높이에 가운데 정렬, 마지막 단계(결과/한국장 영향)를 박스로 강조. 이슈 2개 미만·사슬 3단계 미만이면 1회 재요청. 3대 지수가 모두 1.5% 미만이면 훅의 "급락·폭락·급등"을 자동으로 낮춤.
  - 버그: 훅 칩 3개일 때 오른쪽 잘림, 날짜 옆 점이 검게 나오던 것, 아침 CTA가 "매일 오후 5시"로 나오던 것 수정.
  - 검증: 아침(9/24)·마감(9/23) 실AI dry_run 각 2회 → 최종 이슈 2·사슬 3단계, 6장.
- git: pull 도중 `index.lock` 잔여 → 삭제 권한 받아 정리, 부분 체크아웃된 untracked 파일 2개(`data/briefing|daily/2026-09-24.json`) 제거 후 ff 성공. 세션 끝 lock 없음.

### 2026-09-23 — 전 페이지 디자인 마감(premium.css) · 로고 재교체 · 일정 페이지 재구성 · 차트 통일 · 버그 5건 (커밋 8개)

1. **종가시그널→AI종목분석 이동 시 종목명 대신 코드("440110")** (`6c250492`) — `resolveForAnalysis()`가 URL에 `code`만 있고 `name`이 없어도 지름길을 타서 코드를 이름으로 썼다. name이 있을 때만 지름길, 없으면 종목 리스트 조회. 종가시그널 링크에도 `&name=` 추가. 이미 저장된 `public_ai_feed` 2건(440110)은 SQL로 "파두"로 정정.
2. **다크모드가 가끔 라이트로 강제 전환** (`6c250492`) — `site-shell.js` `enhanceShell()`의 테마 폴백이 9/22 다크 기본 전환 때 안 바뀐 `"light"`였고, `tmApplyTheme`가 localStorage까지 덮어써 계속 라이트로 남았다. `"dark"`로.
3. **순위표 줄무늬·차트 여러 색 / 상세 열면 행이 밀림** (`9555af28`) — `.rt-table tbody tr:nth-child(even) td`가 lightweight-charts 내부 `<table>`까지 칠해 차트 칸마다 배경이 달랐다. 줄무늬 제거 + 호버를 직계 행으로 한정. 상세가 열린 동안 시세 갱신은 `applyRowToTr()`가 **옛 6칸 구조로 셀 번호를 하드코딩**해 패치하고 있어 로고가 사라지고 한 칸씩 밀렸다 → `stockRowHtml()`로 행을 통째로 다시 만든다.
4. **차트 일원화** (`6676d414`) — 기준은 매매시그널 차트. 실시간시세는 캔들/거래량 차트 2개를 1개로 합쳤다(`createLwDualPanelCharts`가 `chartVol`을 같은 차트로 돌려줘 기존 호출부 무수정). 거래량 칸의 별도 눈금·두 번째 TV 로고·경계선이 사라졌다.
5. **일정 페이지** (`798792e6`·`071ffcd4`·`b2e73e3f`·`2b4d72e3`) — 하루 = 카드 한 장, 그날 일정분석을 카드 안으로, 소제목 제거, 초록 테두리 행 박스 → 헤어라인. 시우 피드백 3회 반영: 다크 검정 바탕+회색 카드 / 모바일에서 맨 밑으로 밀려 쓸모없던 달력·요약을 숨기고 상단 "다음 핵심 지표"+가로 날짜 띠 / 신규상장 앰버 칩+맥박 점 / 라이트 좌우 바탕 흰색(다른 페이지와 통일). **함정**: refresh.css 모바일 블록이 `.tm-main .page`에 흰색 `!important`를 걸어 라이트 바탕이 두 톤이었다.
6. **로고 재교체** (`2b4d72e3`) — 시안 v1(밤티) → 검색으로 핀테크 로고 흐름(워드마크 중심·정확한 기하·장식 제거) 조사 → v2 4종 → 시우가 "니가 골라" → 스텝 마크 확정. 헤더 판 제거, 워드마크는 HTML 글자 그대로 두고 CSS `text-transform: lowercase`. 파비콘 전 세트 재생성, **다크 헤더 하단 초록 줄**(`.home-top` border-bottom)도 제거.
7. **전 페이지 디자인 마감 `premium.css`** (`f7dfa0e6`) — 15페이지 × PC/모바일 × 다크/라이트 60장 전수 캡처로 진단. 원인은 표면 체계: 다크에서 본문 띠 #161616 > 카드 #000(명암 역전), 라이트는 페이지마다 바탕 톤이 다름, 카드 3겹 중첩, 숫자만 JetBrains Mono. 다크에서 `--r-bg`를 카드 면(#111113)으로 재정의하고 바탕 요소(body·tm-main·page·header·ticker·masthead·footer)만 명시적으로 #000. `--gradient-primary`(다크에서 검정→남색→회색이던 것)를 브랜드 초록 단색으로. `--font-mono`를 Pretendard로 바꾸고 stock-analysis.css·weekly-market.css의 JetBrains 하드코딩 16곳 교체. ui-audit 대비 미달 41→7건.
8. **검증 방법(재사용)** — 컨테이너에 저장소 clone + `D:\vibe` 변경 파일을 tar로 묶어 stage → 로컬 서버에서 Playwright가 `/api/**`만 라이브 사이트로 프록시(`route.fetch`)하면 실데이터로 렌더된다. 전 페이지 캡처는 썸네일 접촉 시트로 한 장에 모아 비교.

### 2026-09-22 — 종가시그널 문구 정비(유사투자자문) · 인스타 성적 카드뉴스 · 브랜드 전면 교체(로고·파비콘·다크 기본) (커밋 4개)

1. **유사투자자문 문구 전수 점검** — 시우 지적("'이긴날과 진날' 이런 용어는 잘 써야할듯. 유투업에서 쓸 수 있는 용어인지")에서 시작했다. 금감원 제재 사례(2026-04, 유사투자자문업자 35곳 과태료 4.7억)의 적발 유형 6가지와 대조해 보니, **②좋은 기간만 제시 ③미실현 수익률 ④단순 합산 ⑤손실보전·이익보장은 구조적으로 걸리지 않았다** — 거래일을 연속으로 싣고(손실 난 날 포함), 실현된 기록만 쓰고, 평균이며, 보장 문구가 없기 때문이다. **이 구조를 깨지 말 것.** 실제 문제는 ①필수 기재사항 누락과 ⑥프레이밍 두 가지였다. 승부·적중 표현(이긴 날/진 날·맞힌·성적표·찍은·#단타)을 사실 서술로 바꾸고, **원금 손실 가능성 / 개별 투자상담 불가 / 정식 금융투자업자 아님** 3종을 카드 2·4·5장과 캡션, 웹 하단 고지에 넣었다. 쓰지 말 단어 목록은 `scripts/promo-build-deck-close-signal.mjs` 상단 주석에 못박았다. **시우는 유사투자자문업 교육 수료·신고 전**이라 지금 표기는 "정식 금융투자업자가 아닙니다"다 — 신고 완료 후 신고번호가 나오면 전 페이지·카드를 같이 바꿔야 한다(시우 결정: 그때까지 이대로 유지).

2. **인스타 성적 카드뉴스 신설** — 토 11:00 KST 주 1회(pg_cron jobid 12, edge function v12 allowlist). **이 덱만 AI를 부르지 않는다** — 다른 덱은 "숫자는 코드, 문장은 AI"인데 성과 수치는 한 글자만 틀려도 신뢰가 날아가고 문장 자체가 숫자 서술이라 AI가 낄 이득이 없다. 발행당 토큰 0. 거래일별 막대는 **0선을 가운데 둔 좌우 막대**라 손실 난 날이 반드시 같이 보인다. 기록이 10거래일 미만이면 **덱 파일을 쓰지 않고 정상 종료**한다(0이 아닌 코드로 끝내면 매주 가짜 실패 알림이 울린다). 12줄 케이스가 1389px로 카드 규격(1350px)을 넘겨 행 높이를 54→46px로 조였다.

3. **기본 테마를 다크로** — 저장 설정이 없는 첫 방문자가 라이트로 떨어지고 있었다. `theme.js` 세 곳(폴백 2 + normalizeTheme 반전).

4. **모바일 헤더 위 회색 띠의 정체** — 페이지 안에는 여백이 없었다(실측: header가 y=0, body 배경 #000). 그 띠는 **모바일 브라우저 주소창 영역**이고 `meta theme-color`가 칠한다. 그런데 그 값이 팔레트 교체 때 안 지워진 옛 네이비였다 — HTML 20개 `#07264b`, `theme.js` 런타임값 `#0d172a`, `site.webmanifest`도 `#07264b`. 전부 `#000000`으로 통일했다. **색 팔레트를 또 바꾸면 이 세 곳을 같이 볼 것.**

5. **골드(#ffd84d) 전멸 — 노란 밑줄 재발의 뿌리** — 9/19에 로그인 버튼에서 막았던 증상이 마이페이지에서 다시 나왔다. 원인은 `.home-nav__theme`의 `color`가 여전히 골드였고, **브라우저 기본 밑줄은 텍스트 자식의 색이 아니라 앵커 자신의 색을 따른다**는 것. 하드코딩 `#ffd84d` 11곳(site-shell·site-nav·home.css)을 `var(--accent-icon)`으로 바꾸고 라이트 토큰도 `#059669`로 — **이제 저장소 CSS·JS에 #ffd84d가 없다.** 여기에 더해 `.home-nav__account-link`에 color를 직접 지정해 상속을 끊었다. 부작용으로 테마 토글(해/달) 아이콘이 에메랄드가 됐다(되돌리려면 `--accent-icon`만).

6. **로고·파비콘 재디자인** — 시우: "아마추어 로고 같다는 말 많이 들음". 원인은 40px 박스 안에 **9px 텍스트 'TM'/'AI'를 두 줄로** 넣은 것이라 마크가 아니라 자리표시자로 읽혔다. 시안 3종을 헤더·파비콘 크기 × 다크/라이트 비교 시트로 만들어 시우가 골랐다 — **A 상승 캔들안은 "전화기 수신강도 같다"고 반려**(막대 3개 상승은 신호세기로 읽힌다), **C M 모노그램 확정**. M의 가운데 골에서 위로 솟는 획으로 상승을 글자 안에 넣었고, 판은 **검정 고정 + 에메랄드 헤어라인**이다(테마마다 판 색을 바꾸면 라이트에서 마크가 날아간다). 인라인 SVG로 21개 HTML에 직접 넣어 캐시 문제를 없앴다. 워드마크는 "TotalMoney / powered by AI" 두 줄 → **"TotalMoney AI" 한 줄**. 파비콘은 전 세트 교체하되 **16·32px는 별도 단순화 버전**(상승획 제거, 굵은 M만)을 쓴다 — 작은 크기에서는 획 사이 여백이 먼저 메워져 디테일을 남기면 덩어리로 보인다. 캐시버스터 `?v=6 → ?v=7`.

7. **추적 파이프라인 첫 실전 확인(9/22)** — 신규 pg_cron jobid 11(15:45, phase=close)이 정상 작동해 9/21 선정분이 `source='live'`로 기록됐다. **9/21 선정 5종목 실결과: 익일 시가 +1.96%(4/5 수익) / 종가 +0.27%(3/5).** 9/22 스캔도 새 로직으로 397종목 → 통과 26 → 기법포착 24 → 랭킹 5종목(1위 파두, 합의 6).


### 2026-09-21 — 종가시그널 v2 전면 개편(기법 합의 채점·상위 5종목·성과 추적·달력) + 성적표 카드뉴스 신설 (커밋 2개)

시우 요청은 한 줄이었다: **"종목 5개로 줄이고 적중률을 좀 높여보자. 너무 적중율이 떨어짐."** 여기에 "조건식이나 트레이딩 방법 모두 동원해서 중복되거나 점수 높은 종목 5개", "다음날 결과를 시가·종가까지 추적해서 달력에 매일 기록"이 붙었다.

1. **문제를 감이 아니라 숫자로 먼저 잡았다 — 이게 이번 작업에서 가장 중요한 부분이다.** Supabase `trade_signal_intraday`의 1520 payload에는 **후보 400종목의 스냅샷이 통째로 남아 있고**, 다음 날 payload의 `openCur`/`closeCur`가 그대로 익일 결과가 된다. 즉 **저장된 데이터만으로 과거 임의 날짜를 재채점하고 성과까지 측정할 수 있다.** 9/14~9/18 5거래일을 돌려보니 v1 상위 10종목 익일 시가 +0.67%인데 **상위 3종목은 -0.07%(종가 -1.33%)** — 점수가 높을수록 결과가 나빴다. "적중률이 떨어진다"는 체감의 정체는 낮은 승률이 아니라 **순위 변별력이 0이었다는 것**이었다. → **채점 모델을 고칠 때는 반드시 이 백테스트부터 돌릴 것.**

2. **팩터별로 갈라 보니 v1이 만점을 주던 항목이 전부 반대로 움직이고 있었다** (통과 147종목, 시가/종가 평균): 종가위치 0.9↑ `+0.57 / -1.47`(0.6~0.8 구간은 `+0.58 / +1.01`) · 윗꼬리 20%↓ `+0.09 / -1.02` · 완전 정배열 `+0.26 / +0.28`(아닌 쪽 `+0.71 / +0.78`) · 60일↑ 신고가 `+0.02 / +0.92`(아닌 쪽 `+0.55 / +0.47`) · 5일 누적 10% 미만 `+0.19 / -0.31`(+25%↑는 `+1.01 / +1.40`) · 막판강도 0% 미만이 `+0.64 / +1.57`로 0.5%↑(`+0.43 / +1.02`)보다 좋았다. 반대로 실측과 이론이 함께 가리킨 것: 기관 순매수 `+0.90`(승률 68.3%, 아닌 쪽 -0.04) · 양매수 `+0.82 / +1.54` · 갭 상승 출발 `+0.78`(승률 65.2%, 갭 하락 -0.07) · 시총 3,000억 미만 `+1.18` · 코스닥 `+0.78 / +1.51` vs 코스피 `+0.10 / -0.71` · 1년 수익률 50%↑ `+0.68`(아닌 쪽 +0.18).

3. **그런데 5거래일 표본으로 배점을 뒤집지는 않았다 — 의도적으로 참았다.** 실측만 보고 반대로 주면 그건 그냥 다음 달에 또 뒤집힐 과적합이다. 근거가 약한 팩터(신고가·정배열·윗꼬리·막판강도)는 **반대로 주는 대신 배점에서 아예 뺐고**, 실측과 이론이 같이 가리키는 것만 남겼다. 그리고 단일 점수의 노이즈를 줄이려고 **서로 다른 근거를 쓰는 매매기법 9종을 독립적으로 돌려 합의도를 최대 배점 축(35점)으로** 올렸다. 실측상 합의 0개 `-0.56%` / 3개↑ `+0.87%` / 4개↑ `+0.91%`로 단조 증가한다. **기법 합의는 성능 장치이기 전에 과적합 방어 장치다** — 한 팩터의 오차가 순위를 통째로 결정하지 못하게 만든다.

4. **재료(DART) 20점을 분모에서 빼고 가산점(최대 10)으로 돌렸다.** 같은 날 확인한 사실이 근거다 — 9/21 스캔에서 하드필터 통과 41종목 **전원이 `{present:false, score:0}`**이었다(API 키 정상, 조회 실패 경고 0건 — 그날 호재 공시가 실제로 없었다). 재료 없는 날이 흔한데 만점 20을 분모에 넣으면 **그 날은 전 종목의 점수대만 통째로 내려앉는다.**

5. **성과 추적이 이번 개편의 본체다.** 전용 테이블 `close_signal_results`(as_of_date PK, 하루 한 행)를 만들고, 익일 **09:10 phase=open(시가만) / 15:45 phase=close(시가·고가·저가·종가 확정)** 2단계로 기록한다. **워크플로 파일명과 pg_cron job 이름은 `close-betting` 그대로 뒀다** — 5장 개명 규칙대로다. 덕분에 15:45 신규 job은 **같은 워크플로 파일을 다른 inputs로 부르는 것**이라 edge function `ALLOWED_WORKFLOWS`를 안 건드려도 됐다. → **allowlist는 파일명 단위라는 점을 기억할 것.**

6. **달력이 비어 있는 채로 출시하지 않으려고 백필을 먼저 돌렸다.** `scripts/close-signal-backfill.mjs`가 저장된 1520 스캔으로 그날 상위 5종목을 다시 뽑고 **KIS 일봉에서 실제 익일 시가·고가·저가·종가**를 받아 채운다. 그날 실제로 화면에 나갔던 선정이 아니므로 `source='backfill'`로 남기고 **화면에서도 회색 점 + "사후 검증 기록"으로 구분**한다. 실기록(`source='live'`, `phase='close'`)이 있는 날짜는 덮어쓰지 않는다. **PAT은 workflow_dispatch API에 403이라 `repository_dispatch`로 쏴야 한다**(5장 규칙 그대로).

7. **화면 함정 2가지를 실제로 밟았다.** ①성과 달력 칸 폭이 46px라 `+0.95%`가 잘린다 → 달력만 소수점 한 자리(`signShort`) ②결과 표에 `min-width:600px` + 가로 스크롤을 안 주면 모바일에서 종목명이 "산일\n전기"처럼 **세로로 찢어진다**. 그리고 **가짜 버그로 30분을 날렸다** — Playwright에서 `data-theme`를 `<html>`에만 강제로 넣었더니 `<body>`는 `light`로 남아 `--bg-primary`가 흰색으로 잡혔고, 흰 배경에 흰 글씨처럼 보였다. **테마 테스트는 반드시 `#theme-toggle` 실클릭**(9/19 교훈이 또 나왔다).

8. **인스타 성적표 카드뉴스(수익화) — 이 덱만 AI를 부르지 않는다.** 다른 덱은 "숫자는 코드, 문장은 AI"인데, 성과 수치는 한 글자만 틀려도 신뢰가 통째로 날아가고 문장 자체가 "N거래일 중 M일이 플러스" 같은 숫자 서술이라 AI가 낄 이득이 없다. **발행당 토큰 비용 0.** 훅 문구는 누적 성적의 부호를 따라간다 — 마이너스 구간에서는 "진 날도 그대로 남깁니다"로 바뀐다. 거래일별 막대는 **0선을 가운데 둔 좌우 막대**라 손실 난 날이 반드시 같이 보인다(이긴 구간만 골라 보여주지 않기 위한 구조다). 기록이 `CS_MIN_DAYS`(기본 10거래일) 미만이면 **덱 파일을 쓰지 않고 정상 종료** — 0이 아닌 코드로 끝내면 매주 가짜 실패 알림이 울린다. 12줄 케이스에서 1389px로 카드 규격(1350px)을 넘겨 행 높이를 54→46px로 조였다(렌더러가 초과를 즉시 실패시키므로 컨테이너에서 미리 실측).

9. **검증**: 채점 모델은 저장 스냅샷 백테스트, 화면은 컨테이너 Playwright(라이트/다크 × 390/768/1280, API fixture 주입), 라이브는 배포 후 Claude in Chrome 실브라우저로 5종목·기법칩·성적표·달력 실데이터 렌더 확인. 카드뉴스는 `dry_run=1 + min_days=3`으로 **덱 생성 → 렌더 → 커밋 → 발행 스킵까지 CI 전 구간 실행 확인**(run 35605063330, 발행은 안 나감).

**최종 성적(실제 KIS 일봉 기준, 9/14~9/18 상위 5종목)**: 시가 매도 +0.91%(승률 60%) · 종가 매도 +3.24%(승률 56%) · 플러스 거래일 4/5. (v1 상위 5종목: 시가 +0.56%/60% · 종가 -0.06%/44% · 3/5)


### 2026-09-20 — 다크모드 "Jet Black & Steel" 전환 + 잔존 네이비/녹색 4차 스윕 + 네비 메뉴 인터랙션 개편 (커밋 6개)

- **배경 전환**: 시우가 참고 사이트(four.meme)를 보여주며 "배경은 완전 검정, 박스는 다크그레이"를 요청 → `theme.css`의 `[data-theme="dark"]`를 무채색(zinc) 배경 + 에메랄드 그린 포인트 팔레트로 전환. `--header-bg`/`--body-bg` #000000, `--card-bg` #161616, `--border-color` #262626, 변수명은 하위호환을 위해 그대로 두고 `--accent-gold`의 실제 값만 에메랄드 #10b981로 재정의. **`refresh.css`가 마지막에 `!important`로 body 배경을 덮어쓰는 레이어라 `--r-bg` 계열(구 네이비 #10151d)도 같이 안 고치면 화면에 반영 안 됨** — 로그인 버튼 색 분리 버그(같은 날 별건)와 같은 함정.
- **잔존 색상 스윕 — 스크린샷 제보 기반으로 반복**: ①마감시황 달력 패널 녹색 그라데이션(`color-mix` 제거) ②주간일정 KOSDAQ 배지 네이비 ③매매시그널·AI종목분석·종가시그널 히어로 카드의 녹색 라디얼 글로우(`--ts-soft`/`--sa-soft`/`--cb-soft` 계열, 전부 flatten) ④실시간시세 테이블 패널 녹색 테두리 ⑤미국주식 캔들차트 배경 네이비 — **원인이 `assets/lw-candle-chart.js`의 자체 캔버스 렌더러(CSS 변수와 무관한 하드코딩 `#131722`)였음을 이때 처음 발견** ⑥실시간시세 로딩 스플래시(`#rt-loading`) 녹색기 — 1차는 opacity만 줄였다가 재지적받아, `::before` 레이어의 `opacity:0.55`+`blur(40px)`까지 통째로 무력화(`background:none; opacity:0`)해서 완전히 제거.
- **🔴 핵심 발견 — 같은 컴포넌트의 복붙 사본이 스윕에서 빠져 있었다.** `realtime-board.js`(실시간시세·매매시그널 공용)와 `stock-analysis.js`(AI종목분석)는 `assets/lw-candle-chart.js`를 재사용하지 않고 **차트 테마 코드(`getLwChartTheme`/`getLwTheme`, OHLC 크로스헤어 툴팁 CSS)를 각자 따로 복붙**해 갖고 있었다. ⑤에서 `lw-candle-chart.js`만 고친 게 이 두 파일에는 전혀 반영되지 않아, 실시간시세 종목 상세의 캔들차트(사용자가 직접 스크린샷으로 재지적: "여기 차트도 아직 네이비네")와 AI종목분석 검색창(`rgba(9,17,33,.66)` 네이비)이 계속 남아 있었다. `grep -rl "getLwTheme"`로 실사용처를 전수 확인해 `realtime-board.js`·`stock-analysis.js`·`crypto.html`·`us-market.html`(차트 컨테이너)·`us-market.html`(랭킹 테이블 고정열 폴백)까지 동일 원인 6곳을 한 번에 정리했다. **교훈: 같은 UI 패턴이 페이지마다 복붙돼 있으면 원본 파일 하나만 고치고 끝났다고 판단하면 안 된다 — 함수명으로 `grep -rl`부터 돌려 실사용처를 확인할 것.**
- **네비 메뉴 인터랙션 개편(사용자 요청)**: 호버 시 텍스트 색만 바뀌고 배경·움직임이 없어 "누를 수 있는 요소"로 안 느껴진다는 지적 → `.home-nav__link` 호버에 은은한 배경 틴트(무채색 6~8%) + `translateY(-1px)` 들림 애니메이션 추가(라이트/다크 공통, 로고·홈 버튼 제외). 현재 페이지 메뉴(`aria-current="page"`)에 채워지던 녹색 배경(라이트 `rgba(5,150,105,.12)` / 다크 `rgba(16,185,129,.16)`)도 "메뉴 전체가 녹색 박스가 된다"는 지적으로 제거하고, 무채색 필 배경 + 하단 accent색 밑줄 바(`::after`)로 교체 — "지금 보고 있는 메뉴"라는 표시는 유지하되 색면적을 줄였다. `home.css`에 이미 같은 의도(무채색 배경)의 규칙이 있었는데 `site-shell.css`가 로드 순서상 나중이라 이겨서 실제로는 계속 녹색이 나오고 있었다는 것도 이번에 확인.
- 전부 커밋마다 Vercel 배포(`READY`, `www.totalmoney.kr` 별칭 확인) 후 Claude-in-Chrome으로 `data-theme="dark"` 강제 설정 → 실제 클릭/호버로 재현 + `getComputedStyle` 대조까지 실측 검증 완료. **API 라우트 변경 없음 → 함수 12개 제한 영향 없음**(전부 정적 CSS/JS/HTML).

**커밋**: `2ee7a578`(배경 무채색 전환) → `c6aa4bac`(로딩스플래시·달력 1차) → `e7f0d374`(히어로 글로우·KOSDAQ) → `44275734`(차트배경·패널테두리) → `ac9f58f3`(AI종목분석 검색창 네이비·로딩스플래시 2차·복붙 차트코드 6곳 발견분) → `c36bbad0`(네비 호버·활성메뉴 개편).

### 2026-09-18 (2차, Cowork 세션) — 요약 카드가 수급을 반대로 말한 사고 · "예문 앵커"의 역효과 · 세션 작업경로 대정정 (커밋 2개)

- **🔴 사고: 요약 카드(1번)가 수급 방향을 정반대로 말했다. 같은 리포트의 수급 카드(5번)는 맞았다.** 삼성전자 리포트 요약에 "외국인과 기관이 같은 방향으로 오늘은 팔았지만"이 나왔는데, 실제로는 외국인 순매도 / **기관 순매수**였고 5번 카드는 그걸 정확히 쓰고 있었다.
  - **원인은 프롬프트의 예문이었다.** `ANALYST_PERSONA_RULES`의 `[수급 숫자 인용 상한]` 항목에 `· 정답 예: "오늘은 외국인과 기관이 같은 방향으로 팔았습니다. 5일로 넓혀 봐도 방향이 그대로입니다…"`가 있었고, 모델이 이 문장을 **사실 확인 없이 템플릿으로 복사**했다. 5번 카드는 FACT 표 숫자를 보고 쓰기 때문에 안 틀렸다.
  - **교훈 — 1차 세션의 "실제 예문을 앵커로 박으면 톤이 잘 잡힌다"는 발견에는 뒷면이 있다.** 예문이 *방향·판정이 들어간 완성 문장*이면 톤이 아니라 **내용까지 복사된다.** 예문은 문장의 *모양*만 보여주고 사실 부분은 대괄호 자리로 비워둬야 한다. 4곳의 예문을 `"오늘은 외국인과 기관이 [같은 방향입니다 / 서로 반대 방향입니다]…"` 꼴로 바꿨다.
  - **대책은 프롬프트 강화가 아니라 판정 이전(移轉)이다.** `buildSupplyDirectionFact()`를 신설해 코드가 방향을 확정한 문장을 만들고(예: `오늘(장중 누적 잠정치): 외국인 순매도·기관 순매수(외국인·기관 서로 반대 방향) / 5일 누적: … / 20일 누적: …`), 프롬프트 3경로(Claude / OpenAI 웹서치 / OpenAI 폴백) 입력 데이터에 `supplyDirectionFact`로 주입했다. 규칙도 신설 — 방향을 말하는 모든 문장(요약·스토리·수급·총평·시나리오 근거)이 이 문장과 일치해야 하고, 요약 카드는 "수급은 엇갈립니다" 수준 한 마디까지만 허용한다.
  - **검증 완료(실물 리포트).** 재분석 결과 요약이 "외국인 매도와 기관 매수의 엇갈림 속에서", 수급 카드가 "오늘은 외국인과 기관이 서로 반대 방향입니다"로 **양쪽이 같은 이야기를 한다.**
  - 규칙이 3곳에 중복돼 있으므로(`supplyDemandRule`/`supplyDemandRule2`/OpenAI 블록) 예문 수정은 **4곳을 동시에** 해야 한다 — `ANALYST_PERSONA_RULES`는 세 경로가 공유하므로 새 규칙은 여기 한 곳에만 넣으면 전부 적용된다.

- **어휘 사고 — "정교합니다"가 반복된 것도 예문 때문이었다 (시우 지적).** 리포트가 "추격보다 눌림을 기다리는 쪽이 더 정교합니다"를 계속 썼다. 일상에서 안 쓰는 말이라 어색하다는 지적. **원인은 1차에 넣은 좋은 예문 자체에 "훨씬 정교하고 안전한 전략입니다"가 들어 있던 것** — 모델이 거기서 단어를 물었다. 좋은 예문의 표현을 "훨씬 낫고 안전합니다"로 바꾸고, 나쁜 예문의 같은 단어도 바꿔(나쁜 예라도 어휘는 학습된다) 금지 상투구 목록에 `"정교하다"·"더 정교합니다"`를 추가했다(대체어 명시: "더 낫습니다", "더 바람직합니다", "이쪽이 안전합니다"). **위의 수급 방향 사고와 같은 뿌리다 — 예문에 들어간 것은 문장 구조든 단어든 사실이든 그대로 복사된다.**

- **1차 어투 전환에서 빠져 있던 '-다'체 3곳 — 코드가 만드는 문장은 프롬프트를 고쳐도 안 바뀐다.** 2번 카드(시장 대비 위치) 요약문은 AI가 아니라 `mpHeadline()`이 집계 결과로 조립하고, 하단 각주 2곳(`.mpos__note`, `.fin__note`)은 `stock-analysis.js`의 정적 문자열이다. 전부 정중체로 전환했다. **다음에 어투를 바꿀 땐 프롬프트만 보지 말고 "코드가 만드는 문장"을 먼저 훑을 것.**

- **⚠️ 배포했는데 화면이 안 바뀐 원인 — 캐시 버스터를 안 올렸다.** `stock-analysis.html`이 `./stock-analysis.js?v=20`으로 불러오는데 파일만 고치고 v를 안 올려서 브라우저가 옛 파일을 계속 썼다(각주가 '-다'체로 남아 보였다). v21로 올려 해결. **`stock-analysis.js`·`*.css`를 고치면 `?v=` 번호를 같이 올린다** — 서버 헤더는 `max-age=0, must-revalidate`지만 쿼리스트링이 그대로면 그걸로도 안 잡히는 경우가 있다.

- **🔴 세션 작업경로 대정정 — 이 세션에서 두 번 헛수고했다. 자세한 규칙은 5장에 신설.** 요지: **Cowork 세션에서 `D:\vibe`에 직접 커밋·푸시가 된다.** 9/8 윈도우 업데이트로 깨졌던 마운트는 복구됐고, origin URL에 자격증명이 있어 push도 그냥 된다(실제로 이 세션에서 `91eaef6f`를 직접 푸시했다).
  - **헛수고 1**: 어제 채팅 세션(디바이스 도구 없음)의 제약을 확인 없이 오늘도 그대로라고 가정하고, 시우에게 PowerShell 명령을 넘겼다.
  - **헛수고 2**: 리눅스에서 `git diff`를 돌렸더니 **99개 파일이 전부 바뀐 것처럼** 나와서 "마운트 git은 쓰면 안 된다"고 결론냈다. 실제로는 줄바꿈 표기(윈도우 CRLF ↔ 저장소 LF) 차이로 생기는 **착시**다 — `git -c core.autocrlf=input status`로 보면 0개다. 커밋할 때도 이 옵션을 붙여야 CRLF가 그대로 들어가지 않는다.
  - **부작용 1건**: 상태 확인용 `git diff` 때문에 `.git/index.lock`이 남았고, 마운트는 기본적으로 파일 삭제가 금지돼 있어 git 자신도 그 lock을 못 지웠다 → 시우의 `git pull`이 막히는 상태가 됐다. `device_request_delete_permission`으로 D:\vibe 삭제 권한을 받아 해결.
  - **파이썬으로 파일을 고칠 때 줄바꿈이 통째로 바뀐다**: `open(p, encoding=...)`으로 읽고 쓰면 CRLF가 LF로 변환돼 파일 전체가 변경된 것처럼 된다. 이번에 `api/analyze.js`가 그렇게 됐고 되돌렸다. `newline=''`로 읽어 원래 줄바꿈을 기억하고 쓸 때 복원할 것.

- **잔여물 일괄 정리(삭제 권한 확보 후).** D:\vibe: `.git/index.lock`, `_to_delete\`(18개 — 과거 세션이 "지울 수 없어서 모아둔" 것들), `_incoming-v29\`(205KB zip), `_apply-v24~v29.ps1`, `_apply-design-v12.ps1`. Downloads(`C:\Users\oem\Downloads`, 이번에 폴더 접근 권한 신규 확보): 적용 완료된 패치 5개 + `files.zip`. `_push-and-clean.ps1`·`TradingLog_STATUS.md`·PNG 2개는 남겼다.

- **비용 메모 — 시우가 Opus로 3일 만에 한도를 소진해 브리핑·마감시황 예정 작업을 며칠 못 돌렸다. 다음 세션부터 Sonnet 사용 예정.** 이 세션처럼 "원인 추적 → 코드 수정 → 실물 검증 → 문서화"를 한 번에 가는 작업은 토큰을 크게 먹는다. 절약 포인트: ①대용량 파일은 `grep -n -o`로 필요한 줄만 뽑아 읽기(이 STATUS.md도 한 줄이 8,000자라 통째로 읽으면 실패한다) ②`git log`·Supabase 조회로 과거 세션 내용을 역추적하는 것보다 그 세션에서 STATUS.md를 갱신하는 게 훨씬 싸다 ③검증은 사이트 전체를 훑지 말고 고친 문자열만 `curl | grep`으로 확인.


### 2026-09-18 — 재벌 그룹주 로고 통일 · '종가베팅'→'종가시그널' 개명(문구+URL) · DART 재료 팩터 신설 · AI 종목분석 어투 전면 정중체 전환 · 헬스체크 인스타 감시 현행화 (커밋 7개)

- **재벌 그룹 계열사가 각자 다른 파비콘을 쓰고 있었다 — 그룹 대표 마크로 통일 (커밋 3개, `28c975b4`·`e36c8b91`·`c216acfb`).** 삼성SDS·삼성물산·삼성바이오로직스가 자기 회사 파비콘을 쓰고 있어 표에서 같은 그룹인데 마크가 제각각으로 보였다. 삼성 → 삼성전자 S / SK → SK스퀘어 / HD → HD현대 / 현대차 → 현대자동차 / LG → LG전자 / 카카오 → 카카오 / LS → LS로 통일하고, 삼성전자우 같은 우선주와 에피스홀딩스·지주사까지 채웠다. 손댄 곳은 `assets/logos/kr-domains.json`(도메인 매핑) + `kr-curated.json` + 실제 webp 파일이고, 국내 manifest count는 **484 → 503종목**이 됐다.

- **'종가베팅' → '종가시그널' 개명 — 유사투자자문업 규정 대응, 2단계로 나눠 진행 (커밋 2개, `8d57d2c4`·`b38b0f79`).** 1단계는 사람이 보는 문구(GNB·더보기·모바일 하단바·사이트맵 — HTML 18개 + `site-shell.js`), 2단계는 URL(`close-betting.html` → `close-signal.html`) + `vercel.json` 301 리디렉션 + `site-shell.js`에 남아 있던 `nav-priority`/`page-id` 키 정리. **내부 파일명·함수명·Supabase 테이블·pg_cron job 이름은 일부러 그대로 뒀다** — 같이 바꾸면 pg_cron과 edge function `ALLOWED_WORKFLOWS`까지 연쇄로 손봐야 하고 하나만 빠지면 400으로 조용히 끊긴다. 이 원칙은 5장에 규칙으로 올렸다.

- **비어 있던 재료(D) 팩터 20점을 DART 당일 공시로 채웠다 (`247c5c2f`).** `lib/close-betting-score.js`의 `factorCatalyst`(만점 20)는 처음부터 있었는데 `row.catalyst`를 채워주는 쪽이 없어 **항상 미반영**이었다 — 즉 그동안 80점 만점으로 돌고 있었다. 신설한 `lib/close-betting-catalyst.js`가 그 자리를 채운다. **AI에게 "이 공시가 호재냐"를 묻지 않고 공시 제목을 정규식으로 분류한다** — 호재: 공급계약·무상증자·기술이전(각 15) / 특허·자사주취득(각 10). 주의(0점이지만 화면에 표시): 불성실공시·관리종목·상장폐지·소송·횡령배임·감사의견·유상증자·최대주주변경. 유상증자는 제목만으로 제3자배정/일반공모를 구분할 수 없어(본문을 더 읽어야 한다) 일괄 '주의'로 뒀다 — 잘못된 호재 판정보다 안전한 쪽을 택했다.
  - **corp_code 소스 선택**: `data/dart-corp-map.json`(상장사 전체 약 4천건, `scripts/dart-corp-map.mjs`가 매일 갱신)을 재사용했다. `dart-events.mjs`의 고정 워치리스트 방식은 못 쓴다 — 종가시그널 후보는 매일 종목이 바뀐다.
  - **함정(설계 원칙)**: 일부 종목에만 재료가 붙으면 종목별 채점 분모(max)가 달라져 순위가 왜곡된다. 그래서 **하드필터 통과 후보 전원에게** 붙이고(재료 없으면 `0/20 재료 없음`), `DART_API_KEY`가 아예 없을 때만 전원 스킵해 분모를 균일하게 유지한다.
  - `close-signal.html`은 수정 불필요 — '재료' 그룹 렌더링이 이미 준비돼 있었다. 채점 블록은 try/catch로 격리해 재료 조회가 실패해도 그날 랭킹은 그대로 나간다. mocked DART 응답으로 end-to-end 검증 완료.

- **AI 종목분석 어투 전면 개편 — 2단계로 갔다 (`6544df61` → `733268d7`).** 1단계: '술술 읽히는'→'세련되게 읽히는'으로 문체 규칙 수정(모든 문장을 짧게 끊으면 오히려 딱딱해진다는 지적 반영 — 단문·장문 리듬 혼용 명시), `[쉬운 설명]` 규칙 신설(이격도·PBR·RSI·손익비·종가위치 등 전문용어 첫 등장 시 괄호 풀이 강제, 반복 설명은 금지), 장중 수급 '당일 잠정치' 참고문구를 **AI 프로즈에 맡기지 않고 코드가 고정 문구로 얹는 방식**으로 분리(`buildSupplyFlowFact`가 `supplyFlow.isToday`를 내려주고 `renderSupplyFlowFact`가 FACT 표 위에 삽입).
  - 2단계: **종결어미를 '-다'체에서 정중체 '-습니다'로 전면 전환** + 독자에게 말 걸듯 쓰는 규칙(결론만 던지지 말고 독자가 떠올릴 법한 걱정을 짧게 대신 말해준 뒤 답하기) + 용어를 쉬운 말로 먼저 풀어쓰기("밸류에이션 부담" → "주가가 비싸 보이는 부담"). 문장 길이 상한은 80자 기본/110자 허용으로 조정(받은 예문 중 91자 문장이 있어 기존 90자 상한과 충돌했다).
  - **효과가 가장 컸던 것은 규칙 문장이 아니라 예문이다.** 시우가 준 실제 삼성전자·SK하이닉스 리포트 문장을 **좋은 예/나쁜 예 쌍**으로 프롬프트에 직접 박아넣은 것이 톤을 안정적으로 고정시켰다. 추상적인 "세련되게 써라"만으로는 부족했다.
  - **숨어 있던 함정**: `ANALYST_PERSONA_RULES` 안팎에 '-다'체 예시 문장이 흩어져 있었고, 수급 정답/금지 예시는 **같은 텍스트가 3곳에 중복**돼 있었다. 하나만 고치면 모델이 상충된 어미 예시를 보고 흔들린다 — 전부 정중체로 맞췄다.
  - **영향 범위는 AI 종목분석뿐이다.** 모닝브리핑·마감시황 등 다른 AI 콘텐츠는 별도 스크립트라 그대로다(어투를 맞추려면 각각 따로 손봐야 한다).

- **헬스체크가 없어진 슬롯을 보고 거짓 경보를 내고 있었다 (`1e3fc77d`).** `scripts/health-check.mjs`의 인스타 감시가 이미 폐기된 `morning`/`closing` 슬롯을 기준으로 돌아 9/7~9/8 옛 데이터로 계속 알림을 냈다. 현재 구조인 `carousel-morning`·`carousel-v2`·`carousel-ranking`으로 교체하고, 주간 랭킹용 `saturdayOnly` 플래그를 추가, `generated/health-check/state.json`을 초기화했다.

- **종가시그널 1520 슬롯 실운영 확인 (2026-09-18, Supabase 직접 조회).** 9/14~9/18 5거래일 전부 정상 기록. 스캔 398~400종목 → 하드필터 통과 27~32종목 → 랭킹 10종목, 181~232초. 9/11 캐시 시뮬레이션의 51종목보다 통과 수가 적지만 하한(10개) 위로 안정적이다. 탈락 사유는 F1(134~154)·F3(119~137)이 대부분. 1위 점수는 65~75점대(9/18 대한광통신 74.6). **단 이 시점 랭킹의 `catalyst`는 전부 null** — 재료 팩터 커밋이 18:59 KST에 들어가 15:20 스캔보다 늦었다. 첫 실전 반영은 2026-09-21(월)이다.

- **⚠️ 작업 경로 기록 — 이 작업들은 Cowork가 아니라 일반 채팅 세션에서 했다.** 디바이스 도구가 없어 저장소를 직접 못 고치므로 **패치 파일(`.patch`)을 만들어 시우가 `git am`으로 직접 적용**하는 우회 경로를 썼다. 부작용 2건: ①`git push`가 한 번 rejected — 그 사이 자동 데이터 커밋이 origin을 앞서갔다. `git pull --rebase; git push`로 해결 ②**STATUS.md는 git 비추적 로컬 파일이라 패치로 보낼 수 없어 그 세션에서 갱신을 못 했다** — 이 6장 항목은 뒤이은 Cowork 세션에서 git 로그·Supabase를 역추적해 작성한 것이다. **교훈: 파일을 직접 고쳐야 하는 작업(특히 STATUS.md 갱신)은 Cowork 세션에서 한다.** 다운로드된 패치 파일도 자동으로 못 지우므로 쌓인다 — 채팅 세션에서 패치를 받았으면 `Downloads`의 `000?-feat-*.patch`를 직접 지워야 한다.


### 2026-09-13 — 종목 아이콘 전면 개편 · PC 표 레이아웃 3중 함정 · 배포해도 안 바뀌던 캐시 버그

하루에 v15~v23까지 아홉 번. 시작은 "아이콘 넣었더니 PC 화면이 깨졌다"였고, 끝은 "아이콘이 허접하다"를 파고들어 로고 체계를 다시 세운 것이었다.

**1) PC 표가 깨진 원인은 세 겹이었다 — 하나만 고치면 다른 증상이 남는다**

- **홈 카드**: `.home-rt-col--name`이 PC에서 **2칸 그리드**(`16px minmax(0,1fr)`)였는데 아이콘이 3번째 자식으로 들어가 줄바꿈됐다. 종목명이 16px 칸으로 밀려 "삼 / 005930"으로 세로로 깨졌다. **모바일은 같은 칸이 flex라 멀쩡했고 그래서 PC만 깨졌다.**
  → 교훈: **행에 자식을 하나 추가할 때는 그 행이 grid인지 flex인지 페이지마다 확인한다.** grid면 열 개수를 같이 늘려야 한다.
- **실시간시세 표**: v14에서 "표를 콘텐츠 폭에 맞춰 가운데 정렬"로 막아 뒀더니 카드(1198px) 안에서 좌우 197px씩 비어 표가 떠 보였다. → **표는 width:100%, 열은 비율(%)**로 바꿨다. 그런데 비율을 주는 순간 두 함정이 드러났다:
  - `.rt-col-name`은 **모바일 헤더의 `<span>`이자 PC 표의 `<col>`**이다. 거기 걸려 있던 `display:flex`가 그 열을 표의 열 계산에서 통째로 빼면서 **colgroup 폭이 한 칸씩 밀렸다**(종목명 142px / 시가총액 284px).
  - `realtime.html`의 **colgroup 순서가 JS의 실제 렌더 순서와 달랐다**(…거래대금·시가총액·거래량 vs …거래량·거래대금·시가총액). 전부 105px일 때는 아무도 몰랐던 오래된 버그.
  - 거래대금·시가총액은 tv 탭에서 자리가 서로 바뀌므로 **두 열은 같은 폭**으로 둔다.
- `#us-rank-table`(미국주식)은 자기 CSS 변수와 sticky 열을 쓴다. 전역 % 규칙에서 **제외하려다 오히려 회색 띠가 생겨 되돌렸다** — 지금(전역 규칙 적용) 상태가 정상이다.

**2) 국내 종목 로고 346 → 484 — 구글 파비콘 서비스가 돌파구였다**

FMP 로고는 2,684종목 중 **13%만** 정사각으로 쓸 만하다. 삼성전자·NAVER·셀트리온·KB금융이 전부 가로로 긴 워드마크라 탈락했다. 그래서 대표 종목일수록 이니셜 배지로 떨어지는 역설이 있었다.

- 처음에 "회사 파비콘"을 404·16px 때문에 불가로 판단했는데, 그건 `도메인/favicon.ico`를 **직접** 때린 결과였다. **구글 파비콘 서비스**(`google.com/s2/favicons`)를 쓰면 리다이렉트·봇차단을 대신 풀어 주고 최대 128px 정사각을 준다.
- ⚠️ **질의는 반드시 전체 URL 형태여야 한다.** `domain=samsung.com` → **404**, `domain=https://www.samsung.com` → **200**. 이것 때문에 처음에 184개 중 67개가 실패했다.
- ⚠️ **파비콘은 여백을 잘라내면 안 된다.** 정사각 캔버스에 디자이너가 잡아 둔 여백을 살려야 한다. 트리밍 후 비율로 재면 현대차 H 엠블럼(납작한 타원)이 1.92로 탈락한다. → 캔버스가 정사각이면 원본 그대로 쓰고, 트리밍 비율은 "순수 워드마크"(기아 4.0, KT&G 3.2)만 걸러내는 데 쓴다.
- 자기 파비콘이 16px뿐인 회사 29곳은 **그룹 공통 마크로 폴백**시켰다(삼성 계열 → samsung.com, 현대차 계열 → hyundai.com, 두산 → doosan.com …). 실제로도 같은 심볼을 쓰므로 틀린 표시가 아니다.
- 🔴 **기계 판정으로 못 거르는 게 있다.** 카카오페이 파비콘은 "브랜드사이트" 배너, 한온시스템은 일반 문서 아이콘, 호텔신라는 긴 워드마크였다. 만든 뒤 **몽타주로 전부 눈으로 보고** 8종목을 제외 목록에 넣었다.
- 🔴 **동명이인 함정** — `samyang.com`은 삼양그룹(삼양식품 아님), `poongsan.com`·`ecopro.com`은 외국 동명 회사다. 셋 다 128px 정상 이미지라 자동 검사는 통과한다. **눈으로 안 봤으면 엉뚱한 회사 로고가 붙은 채 배포됐다.**

**3) 주간 재빌드가 조용히 망가뜨리던 것 두 가지 (같이 고침)**

- `--force`가 파비콘본을 FMP 이미지로 덮어썼다 → `kr-curated.json`의 코드는 FMP 빌더가 아예 건드리지 않게 했다.
- **tints가 676개 → 7개로 줄었다.** 이미지를 실제로 분석한 종목만 tint가 생기는데 주간 실행은 대부분 캐시로 건너뛴다. 그대로 두면 배지 색이 전부 해시 팔레트로 후퇴한다 → **이전 manifest의 tints를 바닥에 깔고 새 값만 덮어쓴다.**

**4) 🔴 배포는 됐는데 화면이 그대로였던 이유 — `force-cache`**

로고 127종목을 추가해 배포까지 끝냈는데 시우 화면은 그대로 배지였다. `assets/stock-logo.js`가 manifest를 `fetch(url, { cache: "force-cache" })`로 받고 있었다. **`force-cache`는 브라우저가 서버에 묻지도 않고 캐시본을 쓰게 만든다** — 서버가 `must-revalidate`를 보내도 요청 쪽이 이긴다.

진단 순서를 남겨 둔다: ①커밋이 올라갔나 → ②`curl`로 **실서버가 새 파일을 주고 있나**(여기까지 정상이면 배포 문제가 아니다) → ③그 파일을 읽는 **JS의 fetch 옵션** → ④시우에게는 **Ctrl+Shift+R**로 즉시 확인.

**5) 이니셜 배지는 한 글자로는 안 된다**

"삼성전자·삼성전기·삼성생명이 전부 `삼`, 미래에셋증권이 `미`, 효성중공업이 `효`, KT&G가 `KT`로 나와 KT랑 헷갈린다"(시우). 한국 대기업은 앞 글자가 겹친다. → **한글 2자 / 영문 덩어리 4자**로 바꿨다. `KT&G`가 `KT`로 잘려 **다른 상장사와 완전히 같은 배지**가 되던 게 제일 나쁜 경우였다. 기아는 로고 자체가 KIA 워드마크라 `KIA`로, S-Oil은 하이픈에서 잘려 한 글자가 되던 걸 `S-OIL`로 못 박았다.

> 삼성 계열은 한때 `삼성` 한글 배지로 바꿨다가 **시우가 "기존 S로 가자"고 해서 되돌렸다. 다시 바꾸지 말 것.**
> 판단 기준은 "이 아이콘만 보고 어느 회사인지 알겠는가"인데, **최종 판단은 시우가 한다.** SK텔레콤은 자기 파비콘이 리브랜딩된 `AI` 마크라 통신사인지 알 수 없어 SK 나비로 돌렸다.

**6) 미국·해외는 상황이 정반대였다**

`home.js`가 `financialmodelingprep.com`을 화면에서 직접 링크하고 있었다(폴백은 companiesmarketcap.com). 국내는 남의 CDN을 안 쓴다고 해 놓고 정작 미국은 그러고 있었다. → 받아서 우리 저장소에 두고, **미국주식 페이지에는 로고를 신규 추가**했다.

- FMP의 미국 로고는 대부분 정사각이라 **160종목 중 107개(67%)가 통과**했다(국내는 13%).
- **탈락해도 손해가 작다** — 미국은 티커가 곧 이름이라 `AMZN`·`COST`·`KO` 배지가 오히려 잘 읽힌다. 무리해서 붙일 이유가 없다.
- FMP가 **빈 PNG를 주는 티커**가 있다(V·QQQ·ABBV·UNH). 알파 투명 250x250이라 "빈 이미지"로 탈락하는 게 정상이다.
- 배지 색은 **탈락한 이미지에서도 뽑는다**(순서를 바꾸면 아마존이 팔레트 해시색으로 칠해진다). 국내와 달리 밝은 색을 빼지 않는다 — 아마존 주황·캐터필러 노랑이 "너무 밝다"고 걸러졌었다. 대비는 자동 보정이 잡는다.

**7) 토스·트레이딩뷰 로고 CDN은 안 쓴다 (시우가 세 번 물어봤다)**

공통: 런타임에 남의 CDN을 **핫링크하지 않는다.** Referer 차단 한 번에 전 종목이 동시에 깨지고 우리가 손쓸 방법이 없다.

- **토스**(`static.toss.im/png-icons/securities/…`) — 품질은 최상이지만 토스가 직접 만든 디자인 자산이고, 토스는 같은 시장의 경쟁 서비스다.
- **트레이딩뷰**(`s3-symbol-logo.tradingview.com/{logoid}--big.svg`) — 심볼 검색 API가 공개돼 있고 **배지로 남은 91종목 전부 logoid가 있었다(커버리지 100%)**. 그런데 SVG 안에 `<!-- by TradingView -->` 주석이 있다 — 회사 로고가 아니라 **트레이딩뷰가 다시 그린 벡터**다. 게다가 **매핑이 틀린 게 섞여 있다**: `LS(006260) → ls-marine-solution`(다른 상장사), `일진전기 → iljin-hysolus`, `이수페타시스 → isu-chemical`. 증권 시세 화면에서 엉뚱한 회사 로고는 미관 문제가 아니라 **데이터 신뢰도 문제**다.

**8) 매매시그널 계열 메뉴 스크롤 튐 — 범인은 CSS 한 줄**

"계열을 누르면 스크롤 상태에 따라 화면이 움직인다"(시우). `refresh.css`에 "선택되지 않은 계열은 설명을 숨기고 패딩을 줄인다"는 규칙이 있어 **선택된 줄만 25px 높았다.** 계열을 바꾸면 위쪽 줄들의 높이가 변해 방금 누른 줄이 밀리고, JS(`tsKeepInPlace`)가 그걸 되돌리려고 `scrollBy`를 부르면서 화면이 움직였다. **JS는 정확히 동작하고 있었고, 제자리에 둘 필요가 생긴 것 자체가 문제였다.**

→ 줄 높이를 선택 여부와 무관하게 같게 맞췄다(설명은 오른쪽 패널에 이미 나온다). 8개 계열 × 스크롤 위치 3군데에서 이동량 **0px**.

⚠️ 검증 함정: **Playwright의 `page.click`은 요소를 화면에 넣으려고 먼저 스크롤한다.** 그게 앱의 튐으로 오인돼 한참 헤맸다. 스크롤 검증은 `page.evaluate(()=>el.click())`으로 직접 눌러야 한다. 그리고 매매시그널 구역은 로그인 전 `.ts-hidden`이라 컨테이너에서는 걷어내야 보인다.

**9) 표 밀도 정리 (여러 세션째 밀려 있던 것)**

탭 바가 61px을 먹고 버튼 3개가 화면 폭을 1/4씩 나눠 가져서 표가 한 화면에 14줄밖에 안 들어왔다. → 탭은 내용만큼만 차지하는 알약으로 왼쪽에 모으고(61→49px), 행 39→33px. **한 화면 14줄 → 17줄**, 모바일 7→8줄. 실시간시세·마감시황 종목순위·미국주식 **세 표에 같은 밀도**를 적용했다(페이지를 옮겨 다닐 때 같은 서비스로 읽히게).



### 2026-09-12 — 종가베팅 랭킹 신규 구축(설계→배포→디자인 통일) · 사이트 메뉴가 4곳에 흩어져 있다는 발견

토요일 세션. 커밋 6개(`386cb57a` → `09483723`). 장이 닫힌 날이라 **실데이터 검증은 9/14로 미뤄져 있다**(3장).

1. **조건 설계를 먼저 하고 코드를 나중에 썼다.** 시우가 "최적의 조건을 먼저 찾아달라"고 해서 학술 근거(오버나이트 수익률 편향 — 개인 주도 매수는 밤에 몰린다 / 장 막판 모멘텀 — Gao 외, JFE 2018)와 실전 통설을 합쳐 하드필터 8종 + 12개 팩터를 세웠다. 설계서는 `close-betting-spec.md`로 전달.
2. **저장소에 이미 있던 걸 재사용한 게 핵심.** `intraday-close-scan.mjs`가 이미 400종목을 4~5분에 스캔하고 있었고 이평·정배열·이격도·신고가·수급이 전부 계산돼 있었다. 새로 만든 건 채점 함수 하나(`lib/close-betting-score.js`)와 **1520 슬롯**뿐이다. 기존 1531 슬롯은 이미 정규장이 끝난 뒤라 종가 매수를 못 한다 — 이게 슬롯을 새로 판 이유.
3. **1일치 실데이터로 임계값을 4번 고쳤다**(9/11 캐시 2,765종목). 처음 잡은 값은 전부 너무 헐거웠다. ①회전율(거래대금/시총) 만점 15% → **30%**(실측 p50 2.1 / p75 10.6 / p90 34.8이라 15%면 상위 23%가 만점) ②거래량비율 만점 300% → **500%**(통설의 "2~3배"는 이 후보군에선 하위권, p75가 400%) ③등락률 하한 0% → **+2%**, 배점 8 → **10점**(회전율 101%·당일 +0.69%짜리가 9위로 올라왔다) ④**과열을 배점이 아니라 감점으로 옮김** — 5일 누적 +48.6%·이격도 118인 종목이 수급 만점만으로 1위를 먹었다. 이격도>115 −4, 5일누적>30% −6을 넣으니 6위로 내려갔다. **교훈: 통설의 임계값은 전체 시장 기준이라, 이미 거래대금 상위로 걸러진 후보군 안에서는 변별력이 없다.**
4. **점수 막대를 시각화 원칙대로 만들었다.** 순위별로 색을 바꾸지 않는다(색이 순위를 뜻하면 74.4점과 65.2점의 9점 차이를 훨씬 크게 오독한다 — 상위 3개는 테두리로만 구분), 빈 트랙은 회색이 아니라 같은 색의 옅은 단계(막대 전체가 하나의 자로 읽힌다), 숫자는 tabular 자릿수로 세로 줄맞춤, 모든 막대에 숫자 라벨 병기.
5. **⚠️ 사이트 메뉴가 네 군데에 따로 하드코딩돼 있다 — 이번에 세 번에 걸쳐 하나씩 발견했다.** ①각 HTML의 `.home-nav__links`(데스크톱 GNB, **20개 파일**) ②`site-shell.js`의 `TM_ALL_PAGES`(페이지 식별) ③`NAV_PRIORITY`(더보기 접힘 순서) ④**`NAV_SHEET_GRID`(모바일 전체보기 시트, 4×3 하드코딩)**. 처음에 ②만 고치고 "메뉴에 추가했다"고 보고했다가 시우가 화면에서 안 보인다고 두 번 지적했고, 마지막엔 **모바일에서만 조용히 빠져 있었다** — 모바일엔 상단 GNB가 없고 하단바 "전체보기"가 유일한 이동 수단이라 휴대폰에서는 이 페이지로 갈 방법이 아예 없는 상태였다. **다음에 페이지를 추가할 땐 이 네 곳을 체크리스트로 쓸 것.** GNB 자리가 8개로 꽉 차 있어 미국주식을 더보기로 내리고 종가베팅을 넣었다(시우 결정). 종가베팅에 PRO 배지를 붙이면 폭이 41px 늘어 AI 종목분석까지 밀려나므로 배지는 뺐다.
6. **페이지 골격도 공통 요소가 통째로 빠져 있었다.** 처음엔 `<main>` 하나만 놓고 시작해서 **제목 배너(`.masthead`)·티커(`#home-ticker`)·푸터·모바일 하단바가 전부 없었다.** 다른 페이지와 같은 골격(`header.home-nav` → `#home-ticker` → `main.tm-main > div.page > header.masthead` → 본문 → `footer.home-footer` → `nav.tm-bottom-nav`)으로 맞췄다. `.masthead`의 `h1`은 브라우저 기본 margin(18.76px) 때문에 배너가 다른 페이지보다 31px 높아진다 — 다른 페이지들은 각자 눌러두고 있어서 안 보이던 함정.
7. **pg_cron 2건 등록 + edge function v11.** `intraday-close-scan-1520`(평일 15:18 KST, jobid 9) · `close-betting-review`(평일 09:10, jobid 10). `github-workflow-dispatch`의 ALLOWED_WORKFLOWS에 `close-betting-review.yml` 추가해 v11 재배포. **ALLOWED_WORKFLOWS는 환경변수가 아니라 index.ts에 하드코딩된 Set이라 함수를 통째로 재배포해야 하고, `verify_jwt: false`를 명시하지 않으면 기본값 true로 덮여 cron이 401을 받는다.** 주말에 dispatch를 실제로 한 번 쏴서 `{"ok":true,"githubStatus":204}`까지 확인했다(워크플로의 평일 가드가 즉시 종료시키므로 안전한 스모크 테스트).
8. **PowerShell 스크립트 인코딩 — 14번 항목의 "BOM을 붙여라"보다 확실한 답을 찾았다.** Windows PowerShell 5.1은 `.ps1`을 CP949로 읽어 UTF-8 한글이 깨지고 `TerminatorExpectedAtEndOfString`으로 죽는다(파싱 단계라 스크립트는 한 줄도 실행되지 않으므로 상태는 안전하다). **해법: 스크립트 본문·콘솔 출력을 ASCII 전용으로 쓰고, 한글 커밋 메시지는 별도 `_commit-msg.txt`(UTF-8, BOM 없이)에 넣어 `git -c i18n.commitEncoding=UTF-8 commit -F <파일>`로 넘긴다.** PowerShell 인코더를 아예 안 거치게 된다. 보내기 전 `grep -P '[^\x00-\x7F]'`로 비ASCII 0건, 백틱 줄바꿈 뒤 공백 0건을 확인할 것.
9. **작업 경로**: bash 마운트는 여전히 죽어 있어(KB5124008) 컨테이너 fresh clone에서 구현·검증하고 `device_commit_files`로 D:\vibe에 스테이징 폴더를 만든 뒤 PowerShell 한 줄로 push하는 경로를 그대로 썼다. **화면 검증은 컨테이너 Playwright**(`/opt/pw-browsers/chromium`)로 로컬 정적 서버 + `page.route`로 API 응답을 fixture로 갈아끼워 라이트/다크/무료/Pro/390px를 전부 렌더했다 — 이 방법이 이번 세션의 버그 대부분(점수 막대가 span이라 안 보이던 것, 잠긴 행 정렬 32px 어긋남, 배너 높이 31px 차이)을 잡았다.


### 2026-09-09~11 — PRO 페이지 리디자인 · 차트 속도 · 전역 타이포 · 인스타 훅 다양화 · 마감시황 미발행 원인규명 (커밋 9개) · 마감 리포트 GHA 이관

한 세션에서 이어서 진행. **2026-09-11 중반부터 Cowork bash가 죽어(KB5124008) GitHub 웹 편집기로 직접 커밋**했다 — 큰 파일은 전체를 덮어쓰지 않고 CodeMirror API로 바꿀 지점만 골라 수정했고, 각 앵커가 정확히 1회만 매칭되는지 확인 후 적용했다.

1. **매매시그널·AI 종목분석 레이아웃 재구성**(`babf57d`): 12~13px 텍스트에 같은 테두리 카드만 반복돼 위계가 없던 두 PRO 페이지를 브랜드 accent 토큰(라이트 teal/다크 gold)만으로 다시 짬. 매매시그널에 히어로 배너(3단계 사용 흐름)·섹션 아이콘 타일·결과행 모노폰트 확대·등락률 색 배경 칩 도입. AI 종목분석은 검색창을 히어로로 승격하고 **"리포트에 담기는 것" 8개 카드 신설**(로그인 후 텅 비던 초기 화면을 채움, 리포트가 열리면 `:has()`로 자동 접힘).
2. **매매시그널 검색 로딩 연출**(`f6fe9b7`): 프리셋을 눌러도 화면에 아무 변화가 없어 "종목이 나온 건지 조건에 안 맞은 건지" 스크롤해야 알 수 있던 문제. 결과 자리에 스켈레톤+진행문구를 띄우고 **누르는 즉시 그 자리로 스크롤**, 끝나면 결과(0건 포함)/오류 위치로 다시 스크롤 후 플래시. 0건 빈 상태도 "검색은 끝났고 0개"임이 드러나는 카드로 교체.
3. **매매시그널 401 "로그인이 필요합니다" 버그**(`b1cf41a`): 로그인·Pro인데도 검색이 실패. 탭을 오래 열어두면 Supabase access_token이 만료되는데 게이트는 캐시된 상태로 이미 통과한 뒤라 화면은 로그인처럼 보이고 서버로는 만료 토큰이 나갔다. **`stock-analysis.js`는 2026-07-11에 같은 문제를 이미 고쳤는데 `trade-signal.html`의 `apiFetch`에만 반영이 안 돼 있었다.** 요청 전 세션 갱신(60초 스로틀) + 401이면 강제 갱신 후 1회 재시도.
4. **차트 로딩 7~8초 → 2초대**(`7e9177d`): 실측 `kis-realtime-data?action=candle` 콜드 7.2~8.6초. 원인 ①캔들 100개씩 순차 수집 + 호출 사이 `KIS_GAP_MS`(기본 700ms) 대기 ②`kis-stock-quote`는 전 응답이 `no-store`라 캐시 미작동. 구간을 날짜로 미리 나눠 **병렬 수집**(창 폭은 "그 안의 거래일 수 < 100"으로 잡아 보충 호출이 안 생기게)하고, 차트 응답에 짧은 s-maxage + 긴 stale-while-revalidate를 부여. **검증: 실거래일 분포로 시뮬레이션해 누락 0건, 배포 후 OHLC·거래량까지 수정 전과 완전 동일 확인**(장중 변하는 마지막 봉만 제외).
5. **사이트 전역 가독성 보정**(`44be3dd`·`4aac4ee`·`306c7cb`, `typography.css` 신설): 전 페이지를 라이트/다크 × 데스크톱/모바일로 렌더링해 **실제 화면 픽셀에서** 대비를 측정(CSS 값만 보면 그라디언트·반투명 때문에 오판). 대비 미달이 페이지당 최대 48건이었고 원인은 소수의 색(#e24b4a 3.5, #3b82f6 3.3, #9ca3af 2.3, 푸터 rgba(255,255,255,.3) **1.75**). 10px 424개·11px 297개로 **10~11px 텍스트가 700개 초과**. 색·크기가 11개 CSS에 흩어져 있어 각 파일을 고치면 회귀가 나므로 **마지막에 로드되는 보정 레이어**를 새로 만듦. 결과: 대비 미달 전 페이지 0, 12px 미만 홈 59→6.
6. **인스타 카드뉴스 첫 장 훅 다양화**(`d5583c7`): 실제 계정 확인 결과 마감 카드 첫 장이 연속으로 "오늘 증시의 돈 N조가 딱 두 종목에 몰렸습니다" — 숫자만 바뀌고 문장이 동일. 원인은 `pickHookType`의 첫 조건 `top2Share >= 45`인데, 국내에서 삼성전자·SK하이닉스가 상위 30종목의 절반을 넘는 건 **거의 매일 성립하는 구조적 상수**다(최근 60거래일 최소값 52.1%). **60일 전부 A로 떨어졌고 나머지 3유형은 한 번도 안 걸렸다.** 절대 임계 대신 최근 20거래일 대비 이례성으로 점수를 매기고 후보 중 최고점을 고르게 바꿈(마감 10종·아침 9종·랭킹 7종), `promo-hook-history.mjs`로 최근에 쓴 유형을 감점. **재시뮬레이션: 100% → 최대 18%, 같은 훅 최대 연속 60일 → 2일.** 카드 폰트 71곳 중 38곳이 아이폰 피드에서 7.6~11.2pt로 보이던 것도 함께 상향.
7. **마감시황 9/10 미발행 원인규명 + 워치독 배포**(웹 UI 커밋): 원인은 **Windows KB5124008**(3장 참조). 2026-06-23에 만들어졌으나 **저장소에 푸시되지 않아 한 번도 돌지 않은** 워치독을 발견해 다시 배포하고 실행 검증까지 완료. 거래일 판정은 "순위 데이터에 0%가 아닌 등락이 하나라도 있으면 거래일" — 휴장일 오탐을 막는 게 핵심.
8. **Pro 테스트 계정 발급**: `kwonpro@totalmoney.kr`(표시명 트레이더권). 메일 수신 불가 주소라 `email_confirmed_at`을 관리자가 직접 채우고 `subscriptions`를 pro/active로. **비밀번호는 운영자가 직접 설정**(Claude는 계정 생성·비밀번호를 다루지 않음). 결제 없이 수동 부여라 실제 결제 이력은 없다.
9. **전체 메뉴 시트 로그인 상태 미반영 버그**: 로그인했는데도 시트 헤더에 "로그인" 버튼이 떠 있었다. `ensureNavSheet()`가 시트를 한 번만 만들면서 **그 시점(=인증 확인 전)의 상태를 HTML에 박아넣고** 이후 갱신이 없었다 — 상단바는 `tm-auth-ready`에서 갱신하는데 시트만 빠져 있었다. 슬롯 방식으로 바꿔 상태 확정 시 다시 그리게 하고, **로그아웃 버튼 신설**(시트가 열리면 backdrop이 상단바를 덮어 로그아웃할 방법이 아예 없었다).

10. **마감 리포트 생성을 GitHub Actions로 이관**(`0357f70`): 이번 세션의 본체. PC 의존을 끊은 것이 핵심이다. 데이터(지수 OHLC·수급·환율·뉴스)는 전부 무료 API로 코드가 모으고, 4,500자 본문만 OpenAI가 쓴다. 규칙 기반 템플릿도 검토했지만 오늘자 리포트 수준(시장 흐름 해석 + 특징주 8종목 재료/포인트/위험)은 재현이 불가능해 유료 경로를 택했고 운영자 승인을 받았다. 기존 `daily-market-ai.mjs`의 시스템 프롬프트를 재사용한 것이 설계의 요점 — 할루시네이션 가드가 이미 정교하게 들어 있어 새로 쓰면 손해였다. 프롬프트 빌더 6개에 `export`만 붙였고 로직은 안 건드렸다. **함정 하나**: `buildUserPrompt`는 `indexes`를 `[{name, value, change}]` **배열**로 받는데 JSON 저장 형태는 `{kospi:{close,...}}` **객체**라 그대로 넘기면 터진다 — 드라이런에서 잡았다.

11. **이모지 제거 회귀 복구**(`627aa58`): 3장 참조. 어제 스크립트 버그가 9/11만이 아니라 **아카이브 전체 789개 필드**를 훼손한 것을 발견해 복원했다. 발견 경로는 우연이었다 — GHA 이관 작업 중 9/9 `analysis`가 공백으로 시작하는 걸 보고 전수 조사했다.

12. **pg_cron 3건 등록 + edge function allowlist 확장**(Supabase): `daily-closing-report-dispatch`(평일 16:20 KST) · `daily-report-watchdog-first`(16:40) · `daily-report-watchdog-final`(19:00). `github-workflow-dispatch`를 v10으로 재배포해 두 워크플로를 ALLOWED_WORKFLOWS에 추가했다 — **이 allowlist를 빼먹으면 pg_cron이 400을 받고 조용히 실패한다.** 체인 실측 검증 완료(GitHub 204).

13. **카드뉴스 자동 복구 트리거 + 이틀치 공백 복구**(`e099ee6` → `3f828f6`): `instagram-carousel-v2.yml`에 `data/daily-market.json` push 트리거 추가. 9/11 리포트가 19:07에야 들어와 17:30 게이트를 놓쳤는데, **푸시 그 자체가 새 트리거를 발동시켜 19:52에 자동 발행**됐다. 9/10 카드는 의도적으로 내지 않았다(피드 순서).

14. **작업 경로 메모 — bash가 죽은 상태에서의 우회**: 컨테이너에서 공개 저장소 `git clone`은 되지만 `push`와 GitHub API는 막힌다. `.github/` 이하는 원격 파일툴이 protected로 거부하므로 루트에 `_incoming-*.yml`로 쓰고 PowerShell이 `Move-Item`으로 옮겼다. 커밋·푸시·정리를 한 번에 하는 `_push-and-clean.ps1`을 만들어 운영자가 한 줄만 실행하게 했다. **주의**: Windows PowerShell 5.1은 BOM 없는 UTF-8 `.ps1`을 CP949로 읽어 한글이 깨지고 파싱이 실패한다 — BOM을 붙이거나, **더 확실하게는 스크립트를 ASCII 전용으로 쓰고 한글 커밋 메시지는 별도 파일로 분리할 것**(2026-09-12 항목 8 참조).

15. **⚠️ 공개 저장소인데 `보리트레이딩pass.txt`가 어떤 ignore 패턴에도 안 걸려 있었다**: `git add -A` 한 번이면 그대로 노출되는 상태였다. `.gitignore`에 `*pass*.txt`·`*pat*.txt` 등을 추가했다. 앞으로 이 저장소에서 `git add -A`는 쓰지 말 것 — 경로를 지정해서 add한다.

### 2026-09-08 (밤) — 매매기법 프리셋 3종 + 장 막판 종가 스캔 경로 신설 (커밋 4개)

사용자 요청은 "매매시그널에 매매기법별 종목검색 메뉴를 만들 수 있나"였고, 예시로 수급 기반 종가매매 기법을 줬다. 조건을 하나씩 뜯어 보니 대부분 기존 조건엔진 조합으로 되는데 **수급의 "지속성"(연속 양매수)과 "규모"(순매수 금액)가 표현이 안 됐고**, 더 큰 문제로 **장중에 검색하면 전날 데이터가 나온다는 게 드러났다.**

1. **연속 순매수·순매수 금액 조건 신설 (KIS 추가 호출 0회)** — `inquire-investor`가 원래 최근 여러 영업일치를 배열로 주는데 `fetchInvestorFlow`가 **첫 행만 쓰고 나머지를 통째로 버리고 있었다.** 응답을 더 읽는 것만으로 외국인/기관 연속 순매수 일수, 양매수(둘 다 순매수) 연속 일수, 순매수 금액(억원, `tr_pbmn`은 백만원 단위 → /100)을 얻었다. 응답 행 순서를 신뢰하지 않고 영업일 내림차순으로 직접 정렬한다. 목 응답 단위테스트로 정렬·연속일수·금액환산 전부 검증.

2. **매매기법 프리셋 3종** — 구조화된 condition을 서버로 그대로 보내 **AI 파싱을 건너뛴다**(응답 빠름·토큰 0). 임계값은 실제 캐시(9/7 종가 2,765종목)로 결과 개수를 확인해 정했다 — **처음 짠 조건은 1종목밖에 안 나와서(고점권 조건이 과했다) 완화했고, 눌림목은 0종목이었는데 원인이 아래 버그였다.** 라이브 실측: 눌림목 23종목, 돌파 9종목(로컬 시뮬레이션 결과와 정확히 일치).

3. **버그 3건 — 프리셋을 만들다 드러난 것들**
   - **`volume_ratio`가 lt/lte를 조용히 gte로 취급**하고 있었다. "거래량이 평소보다 줄어든 종목"을 찾으면 **정반대인 급증 종목**이 나오던 상태이고, 눌림목 조건 자체가 불가능했던 원인이다. 네 연산자 전부 지원으로 수정.
   - 조건 요약이 `>`와 `≥`를 뭉뚱그려 "외국인 순매수 ≥ 0주"처럼 **경계값이 포함되는 것처럼** 읽혔다. 네 연산자 구분 표기로 수정.
   - `volume_ratio` 문구가 "이상" 고정이라 감소 조건이 정반대로 표시됐다.

4. **장 막판 종가 스캔 — 15:36 반영 경로 (이번 세션의 본체)**
   - 사용자 요구는 "15:40 스탠바이"였다(NXT 시간외 종가매수). 전종목 캐시는 빌드에 **80분**이 걸려 불가능.
   - **해결의 핵심은 "오늘 봉 하나만 얹기"다.** 밤 캐시 빌드가 `snapshot.next`(20일 평균거래량 / 각 이동평균에서 빠져나갈 종가 / 기간수익률 기준가 / 구간 최고가·최대거래량)를 미리 접어두면, 장중엔 **일봉 재조회가 필요 없어져** 종목당 호출이 시세 1회 + 수급 1회 **총 2회**로 떨어진다. 400종목 4~5분.
   - **정확도 검증**: 난수 300봉 시계열 20회 시뮬레이션에서 "전일 스냅샷 + 오늘 봉"이 "처음부터 전체 재계산"과 **560개 검사 전부 일치**. 근사가 아니라 동일값이다.
   - **한계를 코드에 명시**: RSI·MACD·볼린저·스토캐스틱·ADX·캔들패턴·다이버전스는 시계열 전체가 필요해 갱신 불가 → 전일 값 유지하고 `INTRADAY_UPDATED`/`INTRADAY_STALE`로 구분해 둔다. 프리셋 3종은 전부 갱신되는 지표만 쓴다. **장중 수급은 잠정치**라는 것도 화면에 표기.
   - **저장은 Supabase**(`trade_signal_intraday`). data/ 커밋은 Vercel `ignoreCommand` 때문에 별도 배포가 필요해 2분이 더 들고 배포 한도까지 깎는다.

5. **GitHub `schedule:` 지연이 저장소 전반의 문제였다는 발견** — 인스타 3종은 9/8 낮에 이미 pg_cron으로 옮겼지만, **다른 워크플로의 실제 시작 시각을 재보니 전부 밀리고 있었다**: 장전 브리핑 07:55 예정 → **09:26~09:48**(장 시작 후에 "장전" 브리핑이 돈다), daily-market-sync 15:40 → 20:40~21:58, kr-screener-cache 16:00 → 20:52~23:42. 이 때문에 **즉시검색 결과에 당일 종가가 반영되는 시각이 사실상 밤 22시~자정이었다**(설정상으론 16시). 3장에 후속 항목으로 남김.

6. **pg_cron 경로 실측 검증** — pg_cron이 보낼 요청을 그대로 한 번 쏴서(vault 시크릿은 SQL 안에서만 읽힘) Edge Function 200 → GitHub 204 → **워크플로 20초 이내 시작**까지 확인. Edge Function은 버전 9로 올리며 `inputs` 전달 지원을 추가했다(슬롯 구분용). 워크플로 실행 로그로 KIS 인증·순위 API·휴장일 가드·"기준없음" 처리까지 동작 확인.

7. **순위 API 30건 제한 발견** — 실행 로그에서 거래대금순 후보가 **59건**(코스피 30 + 코스닥 29)밖에 안 나왔다. 거래대금순만 보면 **오늘 처음 거래가 터진 중소형주가 상위 30위 밖이라 통째로 빠지는데, 종가매매가 노리는 자리가 정확히 거기다.** 정렬 기준 3종(거래금액순·거래증가율순·평균거래량순) × 2시장으로 132건까지 넓히고 나머지는 전일 거래대금 상위로 보충.

**미완**: 작업 시점의 배포 캐시에 아직 `snapshot.next`가 없어 실데이터 end-to-end 검증은 못 했다(9/8 밤 캐시 빌드 이후 가능). 검증용 일회성 예약작업 `verify-intraday-close-scan`(9/8 23:40)을 걸어뒀다. 첫 자동 실행은 9/9 14:30.

### 2026-09-08 — 마감시황 예약작업 무음 중단 사고 + 인스타 릴스/캐러셀 중복 발행 발견·정리

**1) 사고: 마감시황이 17시가 넘도록 발행되지 않음 (사용자 제보로 발견)**
- 16:15 예약작업 `totalmoney-daily-closing-report`은 **실행은 됐다**(`lastRunAt` 16:16). 그런데 세션 트랜스크립트를 보면 STEP 2 웹서치를 몇 번 돌리다가 **최종 메시지도 커밋도 없이 idle로 끝났다**. 데이터 수집(GHA `daily-market-sync.yml` 15:40)은 정상이었고 `topGainers` 등 시세는 다 들어와 있었으며, AI 분석 필드(`analysis`/`indexes`/`sectors`/`featured_stocks` 등)만 통째로 비어 있었다.
- **알림이 하나도 가지 않은 이유**: 이 무음 실패를 잡으라고 만들었던 `.github/workflows/daily-report-watchdog.yml`(16:40 점검)이 **어느 시점엔가 저장소에서 삭제돼 있었다**. `scripts/check-daily-report-watchdog.mjs`도 없다. 매시간 도는 `health-check.mjs`는 마감시황에 대해 **지수 등락률 이상치만** 보고 "분석이 아예 없음"은 보지 않았다.
- **연쇄 피해**: 17:30 인스타 마감 카드 워크플로가 `check-promo-data-ready.mjs`(analysis 유무 확인)에서 "데이터 미준비"로 판정해 **그날 카드 발행까지 통째로 스킵**됐다. 마감시황 하나가 비면 인스타도 같이 죽는 구조라는 걸 이번에 확인.
- **복구**: 이 세션에서 실데이터로 직접 작성·발행(`dc975d5`). 코스피 6,954.52(−0.58%)·코스닥 811.88(−1.25%), 특징주 10종목 전부 확인된 재료로 채움. 구조화 필드는 `marketTone`/`indexDetail`(코스피 시가·고가, 코스닥 시가)/`sectors` 6개/`investor_trend.etc`/`featured_stocks[].risk` 충족, `netBuyLeaders`와 코스피 저가·코스닥 고저는 어느 기사에도 없어 규칙대로 **키 생략**. 텔레그램 발송 워크플로 success까지 확인.

**2) 재발방지책 3종**
- **health-check에 무음 실패 감지 추가**(`d82c073`, `checkDailyClosingAnalysis`): 평일 17시 이후 오늘자 `analysis`가 비었거나 `indexes.kospi.close`가 없으면 텔레그램 개인 DM. **휴장일 오탐 방지**로 `topGainers` 등락률이 전부 0이면 건너뛴다(공휴일에도 sync는 돌아 더미가 들어오기 때문). 알림 문구에 "이 상태면 인스타 마감 카드도 스킵된다"를 명시해 연쇄 피해를 바로 알 수 있게 함. 실패 케이스를 실제로 만들어 알림이 뜨는 것까지 검증.
- **재시도 예약작업 `totalmoney-daily-closing-report-retry` 신설**(평일 16:50 KST): 장전 브리핑 retry와 같은 구조. 오늘자 `analysis`가 이미 있으면 즉시 종료(대부분의 날 — 싸게 끝나야 한다). 없으면 대신 전 과정을 수행한다. **데드라인 17:20** — 인스타 카드가 17:26~17:30에 데이터를 확인하기 때문에 그 이후 발행은 카드를 살리지 못한다.
- **원 예약작업 프롬프트 보강**: STEP 2에 검색·페치 **8회 상한**, **16:45까지 STEP 3 착수** 데드라인, "커밋 없이 조용히 끝내지 말 것"(반드시 커밋 또는 명시적 STOP 보고) 규칙 추가. 첫 쿼리를 `"코스피 코스닥 외국인 기관 순매수"` 한 방으로 고정 — 상세한 [마감시황] 기사 하나에 지수 종가·시가·장중고가·양 시장 수급(기타법인 포함)·업종 등락률·환율·상승하락 종목수가 전부 들어 있다는 걸 이번에 확인했다.

**3) 발견: 인스타 릴스와 캐러셀이 하루 2건씩 중복 발행되고 있었다**
- 9/7에 캐러셀로 전환하면서 구 릴스 워크플로 2종의 `schedule:`은 껐는데, **Supabase pg_cron이 여전히 구 릴스를 호출하고 있었다**(job 1: 08:30 KST → `instagram-card-post-morning.yml`, job 2: 17:30 KST → `instagram-card-post.yml`). 캐러셀은 GitHub schedule로만 돌았다.
- **중복 방지 스탬프 파일이 서로 다른 게 결정타**였다 — 릴스는 `generated/{morning,closing}/`, 캐러셀은 `generated/carousel-*/`. 서로를 못 보니 둘 다 발행된다. 실측: 9/7 마감 = 릴스 17:30 + 캐러셀 23:19 둘 다 success, 9/8 아침 = 릴스 08:30 + 캐러셀 09:59 둘 다 success.
- **GitHub Actions schedule 지연 실측**: 캐러셀 마감분이 예정 17:26 → 실제 **23:19**(약 6시간), 아침분이 예정 08:03 → 실제 **09:59**(약 2시간). 워크플로 주석에 이미 적혀 있던 알려진 문제가 그대로 재현됐다. 랭킹 캐러셀(토 10:30)은 **schedule 실행 이력이 아예 없다**.

**4) 조치: pg_cron 정시 트리거를 캐러셀로 이관**(`cacddd0` + Supabase)
- pg_cron 3개를 캐러셀로 교체·신설: job1 08:30 → `instagram-carousel-morning.yml`, job2 17:30 → `instagram-carousel-v2.yml`, **job3 `instagram-ranking-precise-dispatch` 신설**(`30 1 * * 6` UTC = 토 10:30 KST) → `instagram-carousel-ranking.yml`. 구 릴스 2종은 이제 완전 수동 전용(롤백용으로 남겨둠).
- Edge Function `github-workflow-dispatch`의 `ALLOWED_WORKFLOWS`에 캐러셀 3종 추가(v8 배포). 이 allowlist를 안 고치면 pg_cron이 400을 받고 조용히 아무것도 안 한다.
- **가드 구멍 수정이 이 조치의 핵심**: 캐러셀 워크플로들이 주말·중복·데이터준비 체크를 `github.event_name == 'schedule'`일 때만 하고 있었다. pg_cron은 `workflow_dispatch`로 오기 때문에 그대로 뒀으면 **정시 트리거가 방어를 전부 우회**해 휴장일에도, 이미 발행한 날에도 또 나갔을 것이다. 이제 자동 트리거는 전부 가드를 통과해야 하고, 우회는 `date` 입력이나 신설한 `force=1` 입력을 명시한 수동 실행에서만 가능하다.
- 오늘(9/8) 마감 캐러셀은 데이터가 늦게 들어와 스킵됐던 것이라 `repository_dispatch: carousel-v2`로 수동 발행 — 18:01 success, 스탬프 2026-09-08 기록 확인(`c7577b2`, `bf6ede8`).

### 2026-09-07 — 인스타그램 카드뉴스 v2 전면 재편

**커밋 9건**: `8430953`(캐러셀 렌더러·발행 파이프라인 신설) → `572a385`(훅의 답을 2번 카드로) → `0768f34`(마감시황 자동 발행 캐러셀 전환) → `8c58b23`(Claude 호출 안정화) → `cfe725ed`(모델 규칙 위반을 코드가 강제) → 타이포 재설계 → 아침·랭킹 전환 → push 경합 수정 → `a61d4fe`(AI분석 리포트 열기 스크롤)

발단은 "홍보용 카드뉴스가 인기도 없고 내용·디자인도 별로다"라는 운영자 지적이었다. 뜯어보니 디자인 문제가 아니라 **구조 문제**였다.

- **★ 정지 이미지를 릴스로 올리고 있었다 — 도달이 안 나온 가장 큰 원인.** 릴스 추천은 **시청 유지율**로 굴러가는데, 움직이지 않는 1장짜리 영상은 1초 이탈이 찍히고 알고리즘이 그걸 "재미없는 영상"으로 학습한다. 같은 내용을 캐러셀로 올리면 판정 지표가 **체류시간·저장**으로 바뀌고, 정보성 카드뉴스는 이쪽이 압도적으로 유리하다. 캐러셀 발행 함수(`postInstagramCarousel`)는 이미 저장소에 있었다 — 2026-07-28에 캐러셀에서 릴스로 갈아탄 결정이 되돌려진 셈이다.
- **★ 매일 만들어지는 7,000자 AI 분석이 카드에 한 글자도 안 쓰이고 있었다.** 마감시황 데이터에는 핵심 한 줄·시장 흐름·특징주별 `risk` 필드까지 다 들어 있는데, 카드에는 지수와 거래대금 표만 나갔다. 네이버금융에도 있는 숫자만 올리고 있었으니 팔로우할 이유가 없었다. **콘텐츠 축을 "숫자 나열"에서 "AI 한 줄 판단"으로 옮긴 것이 이번 재편의 본질이다.**
- **★ 훅에서 던진 질문의 답이 카드에 없으면 그건 훅이 아니라 낚시다.** 첫 발행분에서 "돈 8조가 딱 두 종목에 몰렸습니다"라고 던져놓고 정작 종목명은 4번 카드 반도체 항목에 작게만 뒀다. 운영자가 "카드 다 뒤져봐도 두 종목이 안 나온다"고 지적. **2번 카드를 '쏠림 공개' 전용으로 신설**하고(거래대금 1·2위를 크게 + 상위 30종목 대비 비중 막대), 1번 카드 하단 칩도 지수에서 두 종목 거래대금으로 교체했다. 이후 **"훅 유형과 2번 카드는 반드시 짝이 맞아야 한다"를 설계 원칙으로 못 박았다.**
- **★ 타이포는 감이 아니라 계산으로 잡아야 했다.** "아이폰에서 확대 안 하고 볼 수 있어야 한다"는 지적을 받고 역산해보니, 1080px 카드가 iPhone 피드에서 약 390pt로 축소돼 **배율이 0.36**이었다. 즉 카드의 24px 글자는 손에서 **8.7pt**다. 읽기 하한을 13pt로 잡아 **본문 34~36px**, 항목 제목 36~42px로 올렸다. 폰트를 키운 만큼 담기는 양을 줄여야 해서 특징주 3→2종목, 업종별 종목 나열 2개 상한을 코드가 강제한다. **이 계산 근거를 CSS 최상단 주석으로 박아뒀다 — 나중에 "빈 공간이 있으니 글씨를 줄이자"는 판단이 나오는 걸 막기 위해서다.**
- **★ 인스타 캐러셀은 이미지 우상단에 자체 페이지 배지(1/6)를 덮어 그린다.** 거기 슬롯·날짜를 두고 있어서 실기기에서 겹쳤다. 브랜드·슬롯·날짜를 전부 좌측 한 블록으로 모으고 우상단을 비웠다. 하단에 직접 그리던 페이지 점 표시도 인스타 자체 인디케이터와 중복이라 제거.
- **★ Anthropic API 크레딧이 비어 있다는 걸 이 작업에서 발견했다.** 첫 GHA 실행이 `400 "credit balance is too low"`로 막혔다. 처음엔 `Connection error.` 한 줄만 남아 진단이 불가능했는데, **실패 사유(name/status/message)를 로그에 남기도록 고친 뒤에야** 원인이 드러났다. 저장소의 다른 AI 작업과 같은 방식으로 OpenAI 폴백을 넣어 발행은 정상화. **같은 키를 쓰는 유료 기능들이 조용히 폴백으로 돌고 있을 수 있어 3장에 최우선 확인 항목으로 올렸다.**
- **모델 준수에만 기대면 안 된다.** `gpt-4o-mini` 결과가 훅에 문장을 덧붙이고, 2번 카드에서 공개한 종목을 4번에서 또 쓰고, 판단 카드를 "강세로 마감했습니다"로 썼다. 경고만 남기면 결국 그대로 발행되므로 **코드가 잘라낸다** — `firstSentence()`로 첫 문장만 남기고, 길이 초과 시 절단하고, flowIn 첫 항목에서 거래대금 1·2위 이름을 제거한다. 모델은 `gpt-4o`로 올리고 프롬프트에 좋은 예/나쁜 예를 넣었다.
- **버그 3건 — 전부 실제 렌더 이미지를 눈으로 봐서 잡았다.**
  - `firstSentence()`가 **"4.60%"의 소수점을 문장 끝으로 오인**해 훅이 "한국 ETF가 ▲ 4." 로 잘렸다. 숫자 사이 마침표는 건너뛰도록 수정.
  - `buildRankingData()`의 `marketCapWon`은 숫자가 아니라 **`"7,454.2조원"` 형태의 문자열**이었다. 숫자로 보고 1e12로 나눠 전 종목 시총이 `0.0조`로 렌더됐다.
  - `featured_stocks`에는 `market` 필드가 없어 **코스닥 종목이 전부 "코스피"로** 찍혔다(스카이랩스). 종목코드로 랭킹 목록에서 시장을 찾아 채우고, 없으면 지어내지 않고 비운다.
  - 재발 방지로 **렌더러가 콘텐츠 높이 1350px 초과 시 발행을 실패시킨다.** 글자가 잘린 채 나가는 게 최악이다.
- **워크플로 동시 실행 시 push 경합이 난다.** 아침·랭킹을 같은 시각에 돌렸더니 뒤에 끝난 쪽이 non-fast-forward로 밀려 발행까지 못 갔다. 이미지·데이터 커밋이라 rebase 후 재시도(3회)로 해결.
- **repository_dispatch는 push 직후 이전 커밋으로 실행될 수 있다.** 푸시하고 바로 dispatch를 쐈더니 `head_sha`가 직전 커밋이라 수정 전 코드가 돌았다. 진단에 시간을 썼다 — **push 후 15~25초 두고 dispatch할 것.**
- **폰트를 CDN이 아니라 저장소에 넣었다.** `templates/fonts/PretendardVariable.woff2`(OFL 1.1, 2MB). CDN을 쓰면 렌더 시점 네트워크 상태에 매일 발행이 걸린다. `templates/`는 Vercel 함수 번들에 들어가지 않아 배포에 영향 없다.
- **AI 종목분석 '리포트 열기'가 클릭한 자리에 머물던 문제도 함께 수정.** 기존 코드는 `window.scrollTo({top:0})`이라 **페이지 최상단(검색창)**으로 가는 동작이었고 — 정작 리포트는 화면 아래 남는다 — 그마저도 안 먹혔다. 패널을 다시 그리는 동안 차트가 비동기로 채워지며 문서 높이가 계속 바뀌는데, **브라우저 스크롤 앵커링이 개입해 진행 중이던 부드러운 스크롤을 되돌린 것**이 원인이었다. 목표를 sticky 헤더 높이를 뺀 **리포트 첫 줄**로 바꾸고, 다음 프레임과 400ms 뒤에 두 번 더 보정하되 **사용자가 직접 스크롤하면 즉시 손을 뗀다**(안 그러면 읽으려는 화면을 도로 끌어올린다). CSS에 `overflow-anchor:none` 추가, 포커스도 패널로 이동, 1회 플래시(`prefers-reduced-motion` 존중), 버튼에 로딩 상태 추가. 헤드리스 브라우저로 아이폰(414×896)·데스크톱(1440×900)에서 재현 검증 — 리포트 첫 줄이 헤더 아래 32px에 오고 800px짜리 차트가 늦게 추가돼도 위치를 유지했다.
- **"무료"라는 단어를 카드·캡션에서 전부 뺐다.** 베타 운영이고 결제가 열리면 이미 무료라고 말해 둔 게 그대로 부담이 된다. 유입 문구는 가격이 아니라 **업데이트 주기와 내용**으로 만든다("매일 오후 5시 업데이트", "전체 리포트 → totalmoney.kr").
- 세션 산출물(기획안·시안·요일별 라인업·캡션/해시태그 전략)은 Cowork 출력 폴더 `카드뉴스_재편안/`에 HTML과 PNG로 정리돼 있다.

### 2026-09-04 — 회원가입·로그인 전면 강화 + 증시 일정 정리

**커밋 10건**: `2b9a24e`(인증 개편 본체) → `448e321`(비번 토글 정렬) → `b27101d`(a11y) → `c3a5660`(셋업 가이드) → `99b934d`(service key 폴백) → `8fa0ab9`(네이버 Client ID) → `eab2ecf`(진단 로그) → `d07330d`(소셜 노출 스위치) → `c305de1`(일정 레이아웃+핵심지표) → `acd3a34`(카드 최소폭)

- **★ 비밀번호 찾기 기능이 아예 없었다 — 착수 전엔 아무도 몰랐던 구멍**: 사용자는 "암호가 너무 취약하다"고만 지적했는데, 코드를 열어보니 **재설정 경로 자체가 존재하지 않았다**(`resetPassword`/`forgot` 문자열이 저장소 전체에 0건). 비밀번호를 잊으면 계정을 영영 못 쓰는 상태였다. 소셜 로그인보다 이게 더 급한 문제였다.
- **★ 텔레그램 "주요 지표" 필터가 사실상 아무것도 거르지 않고 있었다**: `isHighImpact()`가 `impact === "high"`를 보는데 **데이터 408건이 전부 `"high"`**였다(소스가 애초에 high-impact만 수집). 2026-09-03에 웹 배지 문제로 같은 근본원인을 한 번 발견했었는데 **그때 웹만 고치고 텔레그램은 그대로 뒀던 것** — 같은 원인이 두 곳에 있었고 한 곳만 고쳐서 절반이 남아 있었다. 이번에 `assets/key-indicators.js`로 판정을 통합해 재발 여지를 없앴다.
- **Vercel Hobby 함수 한도가 실제로 설계를 바꿨다**: `api/` 폴더가 이미 **12/12**(11개 + `payment/complete.js`)로 꽉 차 있어 네이버용 서버 코드를 Vercel에 만들 수 없었다. Supabase Edge Function으로 뺀 것이 결과적으로 더 나았다 — 배포·시크릿 관리가 Supabase 안에서 끝난다.
- **Supabase가 `SUPABASE_SERVICE_ROLE_KEY`를 deprecated 처리 중**: 대시보드 Default secrets에서 `DEPRECATED` 배지 확인. 신규는 `SUPABASE_SECRET_KEYS`(JSON 사전). Edge Function에 **신규 먼저 → 기존 폴백** 순으로 읽는 `readServiceKey()`를 넣어뒀다. 실제 로그에서 `SERVICE_KEY: true, has_SECRET_KEYS: true, secretKeysShape: "object:default"`로 폴백 동작 확인.
- **★ 진단 로그가 5분 만에 원인을 짚었다**: 네이버 로그인이 "설정되지 않았습니다"만 반복해서, **어느 환경변수가 비었는지만**(값은 절대 남기지 않고 boolean만) 찍는 버전을 배포했다. 로그가 `NAVER_CLIENT_ID: false, NAVER_CLIENT_SECRET: false, SERVICE_KEY: true`를 반환 → 원인이 코드가 아니라 시크릿 미등록임이 확정. 이어서 대시보드 화면을 보니 **Client ID를 Name 칸에 넣어** 시크릿 이름이 `1VUxr1f4dnZWhUt_UYrh`로 저장돼 있었다. 추측으로 헤매지 않고 로그로 좁힌 게 결정적이었다. 이 진단 로그는 정상 동작 시엔 실행되지 않으므로 그대로 남겨뒀다.
- **`.git/index.lock`을 내가 만들어놓고 사용자를 막았다**: 세션 초반 샌드박스에서 `D:\vibe`에 `git fetch`를 돌렸는데 마운트 쓰기 권한이 막혀 중간에 끊기며 lock 파일이 남았고, 이후 사용자의 `git pull`이 계속 실패했다(42 커밋 뒤처진 상태 유지). **5장 규칙대로 `D:\vibe`에는 읽기만 하고 쓰기 작업(fetch 포함)을 하지 말 것.** 실제 배포용 작업은 이번에도 fresh clone에서 진행했고 그건 문제없었다.
- **CSS 클래스에 스타일이 아예 없어도 화면은 "그럭저럭" 보인다 — 그래서 오래 방치됐다**: `event-analysis-*` 6개 클래스에 CSS가 0줄이었는데, 브라우저 기본 `<ul>` 스타일 덕에 깨져 보이진 않아서 문제로 인식되지 않았다. 실제 원인 지목은 `.analysis-grid`의 `repeat(3, 1fr)`(예전 3장 카드 디자인 잔재)이었고, 이건 **CSS 파일을 grep하지 않았으면 못 찾았다** — 같은 클래스에 규칙이 4곳에 흩어져 있었다.
- **레이아웃 수치는 실측해야 한다**: 처음 `minmax(300px, 1fr)`로 잡았는데 3열이 안 나왔다. 브라우저에서 재보니 뷰포트가 2602px여도 **본문 영역은 874px 고정**(사이드바 + 페이지 max-width)이라 300×3 + gap이 안 들어갔다. 268px로 낮춰 해결. `min-width: 1500px` 미디어쿼리는 본문이 908px를 절대 넘지 않아 무의미해서 제거했다.
- **비밀번호 표시 토글이 강도바 높이만큼 밀렸다**: `attachPasswordToggle`이 `input.parentElement`를 래퍼로 썼는데 그 안에 강도바·안내문이 함께 들어 있어 버튼이 아래로 밀렸다. input만 감싸는 전용 래퍼를 만들고 수직 중앙 정렬로 수정. **실제 렌더링을 안 봤으면 못 잡았을 종류의 버그.**
- **접근성 트리에서 소셜 버튼 이름이 비어 있었다**: 라벨이 `<span>` 안에 있어 `read_page`에 `button [ref_28]`로만 잡혔고, 링크가 포함된 동의 체크박스는 "에 동의합니다"로만 읽혔다. 둘 다 `aria-label` 추가.
- **카카오 이메일은 비즈 앱 전환이 필요하다**(2026-09-04 확인) — 사업자등록번호가 없어도 "개인 개발자 비즈 앱 전환"(본인인증 + 데브톡 `account_email` 신청) 경로가 있다. 운영자 판단으로 사업자등록 후 일괄 처리로 보류.
- **네이버 개발자센터 "개발 중" 상태에서도 등록자 본인은 로그인이 된다** — 그래서 검수요청 전에 전체 흐름을 실제로 검증할 수 있었다. 로그 확인 결과 토큰 교환 → 프로필 조회 → 계정 생성 → 세션 발급까지 **에러 없이 302로 완주**.
- **PowerShell 5.1은 `&&`를 지원하지 않는다** — `cd D:\vibe && git pull`이 `ParserError`. 줄을 나누거나 `;`를 써야 한다. 앞으로 사용자에게 명령을 줄 때 유의.

### 2026-09-03 (오후) — 매매시그널: 근거 표시 + 재무 조건검색

**커밋 3건**: `95cb303`(근거 칩 + 데이터 버그 2건) → `b195594`(재무비율 조건검색 + 미지원 조건 거절) → `e24e0a4`("십억" 단위 오독). 전부 Vercel 배포 success 확인, **함수 개수 12개 유지**(새 `api/*.js` 없이 기존 파일에 얹음).

- **★ 근거 표시가 곧바로 숨은 데이터 버그를 드러냈다 — 이게 이번 세션의 가장 큰 수확**: `describeMatch`를 만들고 실캐시로 첫 검증을 돌리자마자 "PBR 0.5 미만" 검색 결과에 **리노공업(실제 PBR 5배대)**이 `PBR 0`으로 떴다. 원인은 **KIS가 "값 없음"을 `null`이 아니라 문자열 `"0"`으로 내려주는 것** — 그대로 저장돼서 `pbr < 0.5` 검색이 데이터 없는 **292종목**을 전부 잡고 있었다(실측: per 0값 292 / pbr 290 / eps 292). 저PBR 검색은 가치투자자가 가장 많이 쓰는 조건인데 그게 통째로 오염돼 있었던 것이다. 수집 단계(`lib/kis-indicators.js`의 `toValuation`)와 판정 단계(`evValuation`) **양쪽에 가드**를 넣었다 — 수집만 고치면 캐시가 다시 만들어지기 전까지(그리고 과거 캐시로 재현할 때) 여전히 틀리기 때문이다. 결과: 15건 → **6건**(한화손해보험 0.49·롯데쇼핑 0.2·한화생명 0.31 등 전부 실제 저PBR). **교훈: 판정 결과만 보면 멀쩡해 보이는 필터도, 그 판정에 쓰인 실제 값을 화면에 꺼내 놓으면 거짓이 바로 드러난다.**
- **갭 판정이 두 기준으로 갈려 있었다(2026-08-26 `within` 기능 추가 때 생긴 것)**: "오늘 갭상승"(`snap.gapUp`)은 `kis.detectGap`이 **시가 > 전일 고가**로 보는데, "최근 N봉 이내 갭상승"용 `daysSince` 시계열은 **저가 > 전일 고가**로 계산하고 있었다. 같은 "갭상승" 조건이 `within` 유무에 따라 **다른 종목을 냈다**(중앙에너비스: 오늘 갭상승으로는 걸리는데 30봉 이내로는 27일 전 것만 잡힘). 캔들에 시가가 있으면 `detectGap`과 같은 기준으로 통일하고 없으면 기존 기준으로 폴백(구버전 캐시 안전). ⚠️ **`daysSince`는 캐시에 저장되는 값이라 다음 캐시 빌드부터 반영된다**(3장 검증 항목).
- **근거 문구가 판정과 어긋나던 케이스도 오프라인 검증에서 잡았다**: `within` 없는 갭 조건은 판정 자체가 "오늘 발생"인데 근거는 `daysSince`를 읽어 "27일 전"이라고 적고 있었다. `within`이 지정된 조건 — 즉 **판정이 실제로 `daysSince`를 본 경우** — 에만 그 값을 쓰도록 수정. **근거는 판정이 실제로 본 값과 반드시 같아야 한다.**
- **적자기업의 음수 PER이 "PER 15배 이하"에 걸렸다**: PER −49가 15보다 작아서 단순 대소비교로 통과한다. 실제로 "시가총액 5,000억 이상 + PER 15배 이하" 검색 상위가 전부 적자기업이었다. PER/PBR은 **배수**라 음수가 "더 싸다"는 뜻이 전혀 아니므로, **저평가 조건(`lt`/`lte`)에서만** 음수를 제외했다(`gt`/`gte`는 원래 음수가 안 걸리므로 손대지 않음). EPS는 배수가 아니라 금액이라 음수(주당 손실)도 의미가 분명해 그대로 뒀다. 검증: 145건 중 음수 PER 0건.
- **★ HTS 조건식의 "십억" 단위를 라이브에서만 잡았다**: 사용자 원문 "시가총액 현재가 기준 500십억 원 이상"(=5,000억)을 모델이 **500억으로 읽어** 시총 1,737억 종목(JW신약)까지 결과에 넣었다 — **307건**. 파싱 가이드 환산표에 `십억=10`을 명시하자 `market_cap value:5000`으로 정확히 파싱되며 **70건**으로 정정됐다(기아·HD한국조선해양·GS·현대글로비스 등 전부 대형주). **교훈: 조건 파싱 정확도는 오프라인 합성 테스트로는 절대 안 잡힌다 — 사용자가 실제로 쓴 표현 그대로 라이브에서 돌려봐야 한다.** HTS 조건식은 십억 단위를 자주 쓰므로 앞으로도 단위 표현을 의심할 것.
- **재무비율 실측 검증(2,858종목)**: 삼성전자 부채비율 29.9%·영업이익률 13.07%·ROE 10.36%, SK하이닉스 영업이익률 48.59%·ROE 35.59%, 현대차 부채비율 189%(금융 계열 포함 연결), NAVER 41.9%·18.35% — 전부 공시 실값과 부합. **신한지주는 부채비율 1201.9%·ROE 8.42%는 나오지만 영업이익률·순이익률·유동비율이 null**이다(금융업은 주요계정 구조가 달라 매출액·유동자산 항목 자체가 없음) — **지어내지 않고 생략하는 게 설계 의도대로 동작한 것**이지만 사용자 눈엔 어색할 수 있어 4장 59번으로 이월.
- **DART 호출량 실측**: 상장사 3,988개 × 1회(데이터 없으면 1회 더) → 약 9분 소요, 수집 2,858종목(FY2025 2,788 + FY2024 70). 일일 한도 20,000회에 여유. 수집률 30% 미만이면 **기존 파일을 덮어쓰지 않고 실패**하는 가드를 넣었다(DART 장애나 파싱 회귀로 멀쩡한 파일을 날리지 않기 위함).
- **`repository_dispatch`가 이번에도 정상 동작**(204) — `workflow_dispatch`는 PAT에 Actions 권한이 없어 여전히 403이다. 5장 규칙대로 신규 워크플로에는 항상 `repository_dispatch` 트리거를 함께 열어 둘 것.
- **Playwright 렌더 검증은 이 샌드박스에서 불가**했다 — 브라우저는 설치되지만 시스템 라이브러리(`libxdamage1` 등) 설치에 root가 필요해 `install-deps`가 실패한다. 대신 **Claude-in-Chrome으로 실제 로그인 세션에서 검증**했다(라이트/다크 칩 색상은 페이지에 요소를 주입해 computed style로 확인). 앞으로 이 세션 유형에서는 처음부터 Claude-in-Chrome을 쓸 것.

### 2026-09-04 (새벽) — 로드맵 D·E·F 일괄 처리 + 라이브 검증

**커밋 8건**: `59473d6`(DART 실적 추이 카드 + 종목 중앙값 표기 + 스냅샷 축적) → `c75b924`(리포트 저장·사후 추적 + materials 진단 로그) → `46d0bb1`·`b96a4d5`(corp-map curl 전환 + Vercel 배포 스텝) → `dbe3a54`(리포트 원문 보관) → `(일목/차트)` → `bfb6390`(재료 규칙 2건 보강). 배포 전 구간 READY, **함수 개수 12개 유지**(한도 내) 매 배포마다 확인.

- **⚠️ `D:\vibe` 로컬이 origin/main보다 22커밋 뒤처져 있었다** — 게다가 지난 세션 작업분이 **커밋되지 않은 워킹트리 변경**으로 남아 있어서, 그대로 push했으면 최신 코드를 되돌릴 뻔했다. 5장 규칙대로 **fresh clone에서 편집 → `git diff`로 검증 → push**로 진행했다. 앞으로도 이 저장소는 항상 이렇게 할 것.

- **★ 과거 "corpCode.xml GitHub Actions 타임아웃"의 진짜 원인 규명 (2026-08-28 이후 미해결이던 것)**: 타임아웃 값을 30초 → **180초**로 늘리고 3회 재시도하게 고쳐 돌렸더니 **3회 모두 정확히 180초를 채우고 0바이트로 실패**했다(수신 크기 로그가 한 번도 안 찍힘). 그런데 같은 시각 다른 환경에서 같은 엔드포인트는 **0.1초에 응답**했다. 즉 파일 크기도, 타임아웃 값도, DART의 IP 차단도 아니었다 — **Node의 `fetch`(undici)가 이 응답에서 멈추는 것**이 원인이다. 워크플로를 **curl 다운로드 + 스크립트는 `--zip`으로 파싱만** 하도록 바꾸자 **7.8초에 3.6MB(압축 해제 27.3MB XML)를 받았다**. **교훈: GitHub Actions에서 큰 파일을 받을 때는 처음부터 Node fetch가 아니라 curl을 쓸 것.** curl에는 `--max-time`(총 제한)과 `--speed-limit/--speed-time`(진짜 멈췄을 때만 끊기)을 따로 줄 수 있어서 "느린 것"과 "멈춘 것"을 구분할 수 있는데, Node fetch는 둘 다 통째 중단으로만 나타나 원인 파악 자체가 안 된다.
  - 결과: **전체 법인 118,831건 중 상장사 3,988건** 매핑(`data/dart-corp-map.json`, 187KB). 삼성전자 `00126380`·SK하이닉스 `00164779`·SK이노베이션 `00631518`·NAVER `00266961`·삼성바이오로직스 `00877059` 실값 확인.
  - **`repository_dispatch` 트리거를 열어 뒀다** — Cowork 세션의 PAT에는 **Actions 실행 권한이 없어서** `workflow_dispatch` API가 403이 난다(이번에 확인). 저장소 쓰기 권한만으로 부를 수 있는 `POST /repos/.../dispatches`는 204로 통과한다. **앞으로 세션에서 배치를 직접 돌려야 하면 이 방법을 쓸 것.**
  - **`data/` 전용 커밋은 Vercel 배포가 안 된다는 함정을 또 밟을 뻔했다** — `vercel.json`의 `ignoreCommand` 때문에 매핑이 커밋만 되고 함수 번들엔 안 들어간다. `kr-screener-cache.yml`과 같은 패턴으로 워크플로에 명시적 Deploy 스텝을 추가했다. 더불어 `lib/dart-financials.js`에는 번들 `require()` 실패 시 GitHub Contents API로 읽는 폴백을 넣어 뒀고, **번들러가 정적 `require`를 해석하지 못해 빌드가 깨지는 것을 막으려고 자리표시자 JSON을 미리 커밋**해 뒀다.

- **유료 게이트 뒤의 출력을 검증하는 방법을 확립했다** — AI 종목분석은 로그인+구독 게이트라 Claude가 화면으로 결과를 볼 수 없어서, 지난 세션에 재료 문제를 고치고도 **결과를 확인할 방법이 없었다**. 이번에 `[analyze] materials(N) 코드: 재료명[반영률%]` 진단 로그를 넣어 **Vercel 함수 로그로 검증하는 경로**를 만들었다. 실제로 이 로그로 3종목을 검증해 통과 판정과 약점 2건을 동시에 찾아냈다(3장 상세). **앞으로 유료 기능 출력을 바꾸면 같은 방식으로 진단 로그를 함께 넣을 것.**

- **일목균형표는 "AI가 게을러서"가 아니라 데이터를 안 줘서 생긴 문제였다** — 사용자 제보("분석할 때마다 수치가 없다고 하거나 보이지도 않는 구름 얘기함")를 받고 확인해 보니, 서버가 프롬프트에 넘기는 지표에 **전환선·기준선·선행스팬이 아예 없었다**(MA 4종과 RSI만). 쓰라고 지시해 놓고 데이터는 안 주니 모델이 할 수 있는 건 얼버무리거나 지어내는 것뿐이었다. **교훈: 프롬프트에 항목을 추가할 때는 그 항목을 쓸 수 있는 입력 데이터가 실제로 가는지 먼저 확인할 것.**

- **보관 리포트에서 드러난 잠재 버그**: 저장된 리포트를 열 때 캔들 데이터를 `null`로 넘겼더니 TradingView 폴백이 떴고, **무료 TradingView 위젯은 KRX 심볼을 못 그려서** "TradingView에서만 제공되는 심볼입니다" 경고창과 빈 차트가 나왔다. 리포트를 열 때 KIS 차트를 새로 받아 자체 캔들차트로 그리도록 고쳤다. **이건 보관 리포트만의 문제가 아니라, 평소 분석에서도 KIS 차트 조회가 실패하면 똑같이 나왔을 잠재 버그였다** — 국내 종목의 TradingView 폴백은 사실상 작동하지 않는다고 봐야 한다.

- **E는 "저장"만으로는 절반이었다** — 처음엔 요약·가격·재료만 저장해서, 목록에서 종목을 눌러도 그때 리포트를 다시 못 읽고 새 분석이 돌아갔다(무료 회원은 월 3회 한도까지 소모). 사용자 질문("내가 본 리포트는 언제든 불러서 볼 수 있나?")으로 드러났다. **`payload` 컬럼에 응답 전체를 저장하고 AI 호출 없이 재렌더**하도록 고쳤다. 목록 조회는 `payload`를 뺀 컬럼만 읽는다(행당 20~30KB × 30건 = 1MB). 보관 리포트를 열면 상단에 **작성일 + "그 이후 주가 ±N%"** 배너를 붙여 오래된 판단을 오늘 판단으로 오해하지 않게 했다.

- **검증 방식**: Playwright는 샌드박스에 chromium 호스트 의존성이 없어 설치 실패 → 실제 렌더 함수를 떼어내 **오프라인 HTML 생성 검증**, 모의 Supabase로 **API 종단 테스트**(저장/목록 payload 제외/원문 복원/구버전 행/용량 상한 5건), 배포 후 **라이브 파일 문자열·API 상태코드 확인**, 마지막으로 **사용자 실화면 확인**으로 마무리했다.

### 2026-09-03 (밤) — 유료 핵심 기능 퀄리티 향상 1차 (AI 종목분석)

- **세션 시작 시 버그 1건 즉시 수정 (`dd8b7b6`)**: 사용자 제보 "로그인 했는데 AI 종목분석에 계속 비로그인 랜딩이 뜬다". **로그인 문제가 아니라 CSS 우선순위 버그**였다 — `hideAnalysisLanding()`이 `hidden` 속성만 세팅하는데 `.ail { display: flex }`(작성자 스타일시트)가 브라우저 기본 `[hidden] { display: none }`(UA 스타일시트)를 이겨서, 로그인·Pro 회원에게도 소개 랜딩이 검색 UI 위에 계속 남아 있었다. 결정적 단서는 스크린샷 맨 아래에 "종목 검색 NEW"가 함께 보였던 것 — 검색 UI는 정상 활성화됐고 랜딩만 안 사라진 상태였다. `.ail[hidden] { display: none !important; }` 추가로 해결. **교훈: `hidden` 속성으로 감추는 요소에 `display`를 지정할 때는 반드시 `[hidden]` 규칙을 같이 넣을 것.** (게이트 요소 `.ai-access-gate`에는 원래 `[hidden]` 규칙이 있었고 `.ai-stock-section`은 display 지정이 없어 둘 다 정상이었다. 신설한 `.ail`만 빠져 있었다.)
- **현황 파악 결과 — 무엇이 부족한지**: 두 유료 기능의 프롬프트는 이미 매우 정교했다(`ANALYST_PERSONA_RULES` 문체 규칙만 60줄). 부족한 건 서술 품질이 아니라 **무료 시세 사이트엔 없는 데이터**였고, 구체적으로 세 구멍을 찾았다. ①AI 종목분석에 재무·상대비교 데이터가 0(PER/PBR을 숫자 하나로만 받아 AI가 말로 판단) ②`data/kr-screener-cache.json`(2,765종목 × PER·PBR·RSI·MACD·ADX·외국인보유율·기간수익률)이 매매시그널에서만 쓰이고 종목분석에선 방치 ③매매시그널에 백테스트·성과 추적 없음. 사용자가 "AI 종목분석 먼저 + 데이터 4축 전부"를 선택.
- **커밋 4건**: `ad67530`(백분위 카드 신설 + 업종 수집) → `7165ff0`(출처 배지 제거 + 카드2 타일형 재설계 + 도표 2종) → `f5ee1df`(차트 카드 기술 스냅샷 도표) → `d63f6f2`(재료 최신 뉴스 누락 수정 + 요약 문구 재작성). 전부 배포 확인 완료(`stock-analysis.css?v=21` / `stock-analysis.js?v=20`, `/api/analyze` 401 정상 응답으로 모듈 로드 확인). 상세 내용은 2장 "AI 종목분석" 항목 참고.
- **사용자 피드백 3건과 그 처리** — 이 프로젝트의 품질 기준을 보여주는 사례라 기록해 둔다.
  1. **"출처나 '사실' 이런 문구는 구독자가 알 필요가 없음. 그럼 사실 안 써있으면 거짓인가?"** → 정확한 지적. 배지가 붙은 블록만 사실처럼 보이게 만드는 역효과였다. `ai-fact-badge`/`ai-interp-badge`를 JS·CSS에서 완전 삭제. **앞으로 "이건 AI가 아니라 코드가 계산했다"를 화면 문구로 증명하려 하지 말 것** — 데이터 자체의 정확도로 증명한다.
  2. **"제일 번잡스러움 / 중구난방 나열 / PPT 느낌이었으면"** → 8행 나열을 3묶음 타일 그리드로 재설계하고 도표 4종을 추가. **텍스트 문단을 늘리는 방향이 아니라, 읽기 전에 그림으로 답을 주는 방향으로 갈 것.**
  3. **"1년 26% 수익률에 상위 20%면 그냥 상위권이지 최상위권은 아님 / 코멘트가 너무 성의 없음 / '주가 성과'가 수익률인지 애매"** → 코드가 조립하는 문장도 **과장·모호 표현을 그대로 검수당한다.** 백분위 → 표현 매핑을 5/20/40/60/80/95% 단계로 정교화하고, 요약을 3문장(밸류에이션·수익률 흐름·시장 관심)으로 확장, 라벨을 "주가 수익률(해당 기간 상승률)"로 명확화.
- **`hidden` + `display` 함정 외에 이번에 확인한 것**: `api/*.js` 함수 개수는 **정확히 12개**로 Vercel Hobby 한도에 이미 닿아 있다(`api/payment/complete.js` 포함). 신규 기능은 반드시 기존 파일에 쿼리 파라미터로 얹어야 한다.
- **검증 환경 제약(다음 세션도 동일)**: 이 샌드박스에는 Playwright 브라우저를 설치할 수 없고(`--with-deps` sudo 실패, 호스트 요구사항 미충족), Artifact 게시도 이 세션에서는 승인 카드를 띄울 수 없어 막혔다. **Claude-in-Chrome 확장도 이번 세션 내내 연결되지 않았다.** 그래서 렌더 검증은 ①실제 캐시 데이터로 렌더 함수를 오프라인 실행해 HTML을 뽑고 ②실제 CSS를 합친 미리보기 HTML을 만들어 `present_files`로 사용자에게 보여주는 방식으로 대체했다. **로그인 게이트 뒤의 실제 리포트 출력은 사용자만 확인할 수 있다** — 다음 세션에서도 "사용자에게 확인 요청"이 필요한 항목을 미리 정리해 둘 것.


- **`docs/README.md`가 TotalMoney와 무관한 내용**입니다 — "간단 계산기(Vercel 배포용)"라는 별개의 정적 계산기 프로젝트 배포 가이드였습니다. 다른 프로젝트에서 섞여 들어온 파일로 추정되며, 실수로 남아있는 것인지 확인이 필요합니다. 이번 통합/아카이브 작업에서는 손대지 않고 `docs`에 그대로 두었습니다.
- **첨부해주신 `totalmoney-ai-핸드오프.md`가 이번 세션에 실제로 업로드되지 않았습니다.** 업로드 폴더를 확인했지만 파일이 없어서 이번 통합본 교차 확인에는 반영하지 못했습니다. 해당 파일을 다시 첨부해주시면 이 문서와 비교해서 누락/차이를 별도로 보고해드리겠습니다.
- 인스타그램 콘텐츠 포맷이 최소 3~4차례 방향 전환(카드뉴스 5장 → 경쟁사 벤치마킹 리디자인 → 세로 릴스)을 거쳤는데, 중간 단계(경쟁사 벤치마킹 리디자인)가 실제 프로덕션에 반영됐는지 문서만으로는 확정할 수 없었습니다. 현재 라이브 상태(릴스 포맷)를 기준으로 정리했습니다.
- 결제 상태는 사용자 확인으로 해소됨: 유사투자자문업 교육 수료 → 사업자등록 완료 후 결제서비스 정식 오픈 예정, 현재는 의도적으로 비활성 상태(3장에 반영 완료).

### 2026-08-09 (오후) — X 자동화 제거, PAT 위험도 재평가, 세션 관리 방식 변경

- **X(트위터) 자동화 완전 제거**: 계정/토큰 없이 평일 09:00~17:30 KST 30분 간격으로 계속 실패만 하고 있던 `twitter-post.yml`/`twitter-post.mjs`/`promo-oauth1.mjs`를 fresh clone에서 삭제·커밋·푸시하고, GitHub 원격 저장소에서 완전히 사라진 것까지 재확인함. `package.json`의 `post:twitter` 스크립트도 제거. 브랜치 `promo/instagram-x-cardnews`는 main에 병합된 적 없는 죽은 브랜치로 남아있음(정리 여부는 4장 참고).
- **GitHub PAT 노출 위험 재평가**: 전체 git 커밋 히스토리(2,665개)를 PAT 전체 문자열로 검색한 결과 일치 없음 확인. 평문 노출은 로컬 전용 파일에만 있었고 GitHub에 push된 적은 없는 것으로 판단됨 — 급한 항목에서 중간 우선순위로 하향.
- **D:\vibe 로컬(bash) 마운트가 실제 GitHub 저장소와 어긋나 있는 사례 실측 확인**: `daily-report-watchdog.yml`은 로컬에만 남아있고 실제로는 push된 적 없는 파일, 반대로 실제 저장소엔 있는 `health-check.yml`·`instagram-globalranking-post.yml`이 로컬 마운트 목록엔 안 보이는 것을 확인함. 5장에 이미 있던 "마운트가 stale할 수 있다"는 경고가 실증된 사례.
- **Anthropic API 크레딧 이슈 종결**: OpenAI 단독 폴백으로 충분하다고 판단, 크레딧 충전/이중화 복구는 불필요로 결론.
- **세션 관리 방식 변경**: 앞으로 미션이 끝나거나 세션이 길어지면(대략 사용자 메시지 8~12개 기준) Claude가 먼저 이 문서 업데이트 + 새 채팅 전환을 제안하기로 함(토큰/사용량 절약 목적). Claude 메모리에 저장해 다음 세션부터 자동 적용.

### 2026-08-09 (저녁) — package.json 문법 오류로 워크플로우 11개 연쇄 실패, 원인 수정 + 재발 방지 알림 2종 추가

- **사고 경위**: 18:28 KST의 X(트위터) 자동화 제거 커밋(`061abe7`)에서 `package.json`의 `scripts` 마지막 항목을 지우면서 바로 위 줄에 trailing comma를 남겨 JSON이 깨졌다. `npm ci`를 쓰는 워크플로우 11개(Refresh Indicator Actuals, Market Data Sync, Daily Market Sync, Instagram Daily Card News 모닝/마감, Instagram Global Ranking Reel, KR Screener Cache Build, Morning Briefing, Trade Signal Scan, Weekly Schedule, World Market Cache)가 약 2시간 동안 `npm error EJSONPARSE`로 조용히 전부 실패했다. 사용자가 Refresh Indicator Actuals·Market Data Sync 실패 메일을 보고 이상을 감지해서 조사를 요청함.
- **수정**: `package.json`의 trailing comma를 제거(커밋 `a523c2a`)하고, `refresh-indicator-actuals.yml`·`market-data-sync.yml`을 수동 재실행해 정상화(Success)까지 확인함.
- **재발 방지(2건 신규 추가)**:
  1. `scripts/health-check.mjs`에 `checkPackageJsonSyntax` 체크 추가(6번째 점검 항목) — 매시간 도는 헬스체크가 package.json이 깨지면 텔레그램 개인 DM으로 알림.
  2. `.github/workflows/validate-package-json.yml` 신규 — package.json이 main에 push될 때마다 몇 초 안에 즉시 문법 검사해서 깨지면 텔레그램으로 바로 알림(헬스체크의 최대 1시간 지연보다 훨씬 빠른 1차 안전망).
  두 워크플로우 모두 수동 실행(`workflow_dispatch`)으로 정상 동작(Success, 오탐 없음) 확인 완료.
- **부수 발견**: `D:\vibe` 로컬(bash) 마운트가 origin과 407커밋 차이로 완전히 stale한 상태임을 재확인(`git status` 기준 diverged). 이번 조사는 전부 fresh clone(`/tmp`)에서 진행함 — 로컬 마운트에서 작업했다면 존재하지도 않는 옛날 코드를 기준으로 잘못된 결론을 냈을 것.

### 2026-08-11 — README/패키지 정리, 워크플로우 안정화, 차트 버그 2건, 인스타 대형주 오라벨링, META 등락폭 오류 수정 (총 6건)

이번 세션에서 사용자가 요청한 순서대로 6건의 실제 문제를 원인 분석 후 근본 수정하고 전부 배포·검증까지 완료함. (개별 상세 내역은 위 2장 각 표 행에도 반영)

1. **README/package.json 정리** (`0b6b282`) — 예전 계산기 프로젝트 템플릿 잔재를 실제 TotalMoney AI 소개로 교체. 기능 영향 없음 확인.
2. **`update-stock-list.yml` KRX 403 간헐 실패** (`1de5b5a`) — 재시도(5회, 지수백오프)+소프트-fail로 보강. 실제 push-트리거 실행(#1639)으로 정상 동작 확인.
3. **실시간시세 캔들/거래량 차트 데스크톱 하단 잘림** (`c832870`) — CSS 높이값이 실제 JS 렌더 높이(210px/90px)와 어긋나 있던 게 원인, 전종목 공통 버그였음. 라이브 스크린샷으로 검증.
4. **차트 열어둔 채로 5초 자동갱신되면 차트·버튼 먹통** (`a46025b`) — 테이블 순위 재정렬 시 `outerHTML` 재조립으로 이벤트 리스너가 유실되던 게 원인. 열려있던 행을 DOM에서 분리해뒀다가 재조립 후 그대로 다시 붙이는 방식으로 수정. 실제 30초+ 대기하며 순위가 바뀌는 상황을 재현해 주봉 전환·차트닫기 정상 동작 확인.
5. **인스타 모닝브리핑 "미국 대형주" 오라벨링**(코인베이스·팔란티어 등) (`effe415`) — `data/world-market-cache.json` 기반 실제 시총 TOP20을 동적으로 계산해 그 안에 든 종목만 대형주로 언급하도록 수정. 실제 시총 데이터로 TOP20 재계산 검증 + 3가지 시나리오 목업 테스트.
6. **미국주식 META 등락폭 "+575.18/0.00%" 모순 표시** (`1c20ee4`) — 잘못된 거래소(NYS↔NAS) 데이터가 섞여 들어오면서 등락률과 모순되는 값이 나오던 버그. `resolveChangePoints`에 교차검증 가드 추가(전종목 공통 방어). 재현 테스트 + 배포 후 실제 API/사이트에서 정상값 확인. 캐시 데이터 3개 파일(`data/us-market-*.json`)도 수동 갱신(`2e23d54`)해 다음 30분 주기를 기다리지 않고 즉시 반영.

그 외 **저장소/폴더명 `vibe`→`TotalMoneyAI` 리네임**을 검토 요청받아 위험도(raw.githubusercontent.com URL 리다이렉트 안 됨 등) 분석해 제시했으나, 사용자가 복잡도를 이유로 보류 결정("너무 복잡해지겠다, 이건 건들지 말자") — 코드 변경 없음, 3장에 기록.

이번 세션 중 사용자가 STATUS.md 업데이트 제안 방식을 한 번 더 교정함: 작업 하나 끝날 때마다 제안하지 말고, 항상 합의된 대화 횟수가 찰 때나 중요한 변경이 있을 때만, 다음 채팅으로 넘어가기 직전에 세션 전체를 한꺼번에 반영하는 방식으로(Claude 메모리에 반영 완료).

### 2026-08-12 — 장전 브리핑 텔레그램 발송 중단 사고 복구 + 재발 방지 + 날짜 캘린더 신규 추가

사용자가 "브리핑이 작동 안 하는 것 같다, 텔레그램이 8/5 이후로 안 온다"고 제보해 조사·복구·재발방지·신기능까지 한 세션에서 완료.

1. **원인 규명**: `totalmoney-morning-briefing` 예약작업(평일 08:03 KST) 세션 트랜스크립트를 직접 열어 확인한 결과, 8/7·8/10·8/11·8/12 실행 모두 `D:\vibe` 폴더가 세션에 마운트되지 않아 GitHub PAT를 읽지 못하고 STEP 1(clone)조차 못 가서 종료된 것으로 확인(8/8·8/9는 주말). git log상 마지막 정상 발행(`AI 분석` 커밋)은 8/6 08:10 KST. 8/7 실패는 별개로 GitHub Actions 자체가 일시 정지됐던 사례도 섞여 있었음.
2. **즉시 복구**: 이번 세션(D:\vibe 마운트 정상)에서 직접 Naver 뉴스 검색으로 8/12 실제 시황을 취재해 `aiAnalysis` 19개 필드를 작성, `data/morning-briefing.json`에 반영 후 커밋(`16e09054`)·푸시. "Morning Briefing Telegram Notify" 워크플로 런(31556328790)이 `success`로 완료된 것까지 API로 직접 확인 — 텔레그램 실발송 검증 완료.
3. **재발 방지 2건**:
   - `totalmoney-morning-briefing` SKILL.md에 `D:\vibe` 마운트 확인 재시도 로직 추가(20초 간격 최대 6회, 약 2분) — 마운트가 지연 부착되는 경우를 흡수.
   - `scripts/health-check.mjs`에 7번째 점검 항목(`checkMorningBriefingPublishStamp`) 추가 — `generated/morning-briefing/last-published-date.txt` 스탬프가 평일 09:00 KST까지 오늘 날짜로 안 찍히면 텔레그램 개인 DM으로 즉시 알림. SKILL.md STEP 5도 이 스탬프 파일을 aiAnalysis와 같은 커밋에 남기도록 갱신.
4. **날짜 캘린더 신규 기능** (사용자 요청, 마감시황과 동일한 UX로 통일): `briefing.html`에 날짜 배지를 클릭 가능한 트리거로 바꾸고 월별 캘린더 드롭다운 + 좌우 화살표(이전/다음 발행일, 공휴일·미발행일 자동 스킵) 내비게이션 추가. 과거 날짜 선택 시 `data/briefing/<YYYY-MM-DD>.json` 아카이브(라이브 API 재호출 없이 그날 스냅샷 그대로)를 불러오고, "지난 브리핑" 배지 표시 + URL 해시(`#YYYY-MM-DD`)로 직접 링크 가능. `data/briefing/index.json`이 선택 가능 날짜 목록 관리. 과거 "AI 분석" 커밋 31개(2026-06-24~08-06)에서 데이터를 추출해 아카이브 백필, 오늘자(08-12)까지 총 32일치.
5. **배포 후 실제 화면 검증 중 버그 1건 발견·즉시 수정**: `.bf-archive-badge`에 `display: inline-flex`를 직접 지정해둬서 `hidden` 속성보다 우선 적용되는 CSS 버그로, 오늘(라이브) 화면에서도 "지난 브리핑" 배지가 계속 보이는 문제가 있었음. `[hidden]{display:none}` 규칙 추가로 수정 후 재배포, Chrome으로 오늘/과거 날짜/화살표 내비게이션 모두 재검증 완료.
6. 관련 커밋: `16e09054`(8/12 분석 발행), `6acccc54`(캘린더+헬스체크+아카이브), `1aa64e70`(배지 CSS 버그 수정), `10e282cc`(좌우 화살표 추가).

### 2026-08-13 — 브리핑 UI 폴리시 + 글로벌랭킹 릴스 TOP10 리디자인·실발행 + 브리핑 재발 사고 복구 + 2단계 안전망 신설

한 세션에서 이어서 진행한 4건.

1. **브리핑 날짜 UI 폴리시 2건**: 날짜 트리거에 있던 아래화살표(chevron) 아이콘이 "지저분해 보인다"는 피드백으로 제거(기능은 유지) → 대신 클릭 가능하다는 걸 알 수 있도록 호버 시 색상 강조 추가. 커밋 `6680c89a`(chevron 제거), `49616fa1`(호버 강조).
2. **증시일정 내부 문구 제거**: `weekly-market.html`의 "데이터가 YYYY-MM-DD 이후 갱신되지 않았습니다" 배너가 내부 운영 상황(자동 수집 지연)을 알리는 용도였는데, 고객에게 노출할 필요 없는 내부 사정 문구라는 피드백으로 제거(같은 상황은 health-check.mjs가 계속 내부적으로 감지). `masthead-notice` 엘리먼트도 함께 정리. 커밋 `752dcea0`.
3. **글로벌랭킹 인스타 릴스 TOP10 리디자인 + 실제 발행**: 사용자가 인스타 글로벌랭킹 릴스(당시 TOP20) 스크린샷을 보며 "TOP10으로 축소하고 더 임팩트 있게, 삼성전자·SK하이닉스는 국내기업으로 따로, 순위변동 화살표는 지저분하니 제거, 시안 먼저 보여달라"고 요청.
   - 요청 파악: `templates/card-globalranking-reel.html`(디자인)과 `scripts/promo-render-globalranking.mjs`(데이터/렌더 로직)에서 문제의 "지저분한 화살표"는 `rankDeltaHTML()`이 만드는 주간 순위변동(▲/▼) 배지임을 확인.
   - **시안 제작·검토**: 로컬 헤드리스 크로미움 렌더가 샌드박스 공유 라이브러리 부재(`libXdamage.so.1` 등, sudo/apt 권한 없음)로 실패해서, 대신 자체 완결형 목업 HTML(`preview-globalranking-v2.html`, TOP10 + 국내기업 섹션 + 골드 타이틀 + 화살표 없음)을 만들어 저장소에 임시(비공개 링크) 커밋·배포한 뒤 Claude-in-Chrome으로 실제 화면을 스크린샷해서 시안으로 제시, 사용자 승인 받음.
   - **프로덕션 반영**: `buildTop20Rows()` → `buildRankingData()`로 재작성해 세계 TOP100 캐시 안에서 "TOP10"과 "TOP10 밖의 한국 기업"(현재는 삼성전자·SK하이닉스, 하드코딩 아니고 국가 필드로 동적 필터링이라 향후 다른 한국 기업이 진입해도 자동 반영됨)을 함께 계산하도록 변경. 국내기업은 국내순위 원형 배지 + "글로벌 N위" 텍스트 배지로 표시, 순위변동 화살표(및 `data/world-ranking-history.json` 기반 delta 계산 로직)는 완전 삭제. 타이틀은 골드 그라데이션 + 1~3위 메달 이모지로 임팩트 강화. 캡션 문구도 TOP20→TOP10으로 수정. 커밋 `2cbf0020`.
   - **검수용 안전장치**: `instagram-globalranking-post.yml`에 `workflow_dispatch` 입력 `publish`(boolean)를 추가해, `false`로 실행하면 렌더링·커밋까지만 하고 실제 Meta Graph API 발행은 스킵하도록 만듦(발행 전 실제 렌더 결과 검증 원칙 준수 목적).
   - **실제 1회 테스트 발행**: 사용자가 GitHub Actions에서 직접 "Run workflow"를 눌렀는데, `publish` 기본값이 `true`였던 탓에 검수 단계 없이 곧바로 실제 발행까지 진행됨(위 3장 "해프닝" 참고). 결과 자체는 정상 — 릴스(id `18133297021619233`)·스토리(id `18444304984130479`) 모두 성공, 실제 렌더 이미지 확인 결과 로고 12/12 정상 로드·레이아웃 정상. 이 실행에서 Gemini 배경 생성이 429(쿼터 초과)로 실패해 OpenAI 폴백으로 자동 전환된 것도 확인(위 3장 참고).
   - **PAT 권한 관련 확인 사항**: Cowork가 쓰는 GitHub PAT는 코드 push(Contents) 권한만 있고 GitHub Actions를 API로 직접 실행(`workflow_dispatch`, Actions 권한)할 수는 없음 — 사용자가 "이전엔 발행이 됐는데 왜 지금은 권한이 없냐"고 질문해서, 과거 발행은 전부 cron/push로 자동 트리거된 워크플로우(저장소 시크릿 사용)였지 이 PAT가 관여한 적이 없었다는 점을 설명함. 워크플로우를 지금 당장 수동으로 실행하려면 사용자가 GitHub 웹 UI에서 직접 누르거나, PAT에 Actions 권한을 추가해야 함(5장에 이미 있던 제약사항과 동일 패턴).
4. **장전 브리핑 재발 사고 복구 + 2단계 안전망**: 같은 날 아침, `totalmoney-morning-briefing`(08:03 KST) 예약작업이 D:\vibe 마운트 실패로 또 발행하지 못함(8/12 재발방지책에도 불구 재발) — health-check.mjs가 09:39경 정상적으로 감지해 텔레그램 알림을 보냄(사용자가 이 알림 내용을 채팅에 붙여넣어 제보). 조사 중 새로운 버그를 발견: 수치 데이터(`morning-briefing.yml`, 07:55 KST 독립 GHA)는 정상적으로 오늘자로 갱신돼 있었지만, `aiAnalysis`(질적 분석 텍스트)는 어제(8/12) 내용이 그대로 남아있어 사이트가 "오늘 날짜에 어제 분석"을 보여주고 있었음(수치·텍스트 불일치 상태로 라이브 노출 중이었던 것 — 이 pipeline은 원래 Cowork가 aiAnalysis를 나중에 덮어쓰는 걸 전제로 설계돼 있어서 Cowork가 실패하면 이 상태로 방치됨). Naver 뉴스 검색으로 8/13 실제 시황(CPI 둔화·코어위브 실적발 반도체 랠리·코스피 4%대 급등 6800선 등)을 취재해 `aiAnalysis` 19개 필드 재작성, 아카이브·발행스탬프 갱신 후 커밋(`888a516`)·푸시 → "Morning Briefing Telegram Notify" 워크플로가 자동 트리거돼 실제 발송까지 로그로 확인. 재발방지책으로 평일 **08:15 KST에 발행 여부를 재확인해 1차가 실패했으면 08:30까지 전체 파이프라인을 대신 완주**하는 신규 Cowork 예약작업 `totalmoney-morning-briefing-retry`를 추가(사용자 요구사항: "8시 실패시 8:30분엔 포스팅 되어 있어야 함" — 30~40분 뒤 재시도로는 늦다는 피드백 반영). 이미 발행된 날엔 즉시 종료해 토큰 낭비 없음. 1차 SKILL.md의 실패 안내 문구에도 이 재시도 작업의 존재를 기록해둠.

### 2026-08-14 — 장전 브리핑 3차 재발 + 재시도 작업의 새로운 "절반만 완료" 실패 패턴 발견 + 수동 복구

사용자가 "모닝브리핑 텔레그램 메세지가 안옴"이라고 제보. 전날(08-13) 만든 2단계 안전망이 있는데도 또 발송되지 않아 조사.

1. **1차(`totalmoney-morning-briefing`, 08:03 KST)**: 어제와 동일하게 D:\vibe 마운트 실패로 6회 재시도(약 2분) 후 포기. 세션 로그로 직접 확인함.
2. **2차 재시도(`totalmoney-morning-briefing-retry`, 08:15 KST)**: 이번엔 **마운트는 성공**했음. git 커밋 이력으로 확인한 결과, STEP 1(수치 데이터 신선도 확인 → stale이라 판단해 `.briefing-trigger` 수동 갱신)까지는 실행해서 08:16:25 KST에 트리거 커밋을 남겼고, 이 트리거로 `morning-briefing.yml`이 08:16·08:20·08:29에 세 차례(수동 트리거 + cron-job.org 백업 + 지연된 정규 schedule) 실행돼 수치 데이터는 정상적으로 갱신됨. **하지만 그 이후 STEP 2~5(뉴스 리서치·`aiAnalysis` 작성·커밋)는 전혀 진행되지 않고 세션이 그대로 끝남** — `mcp__session_info__list_sessions`/`read_transcript`로 해당 세션을 찾아보려 했으나 세션 타이틀이 다른 실행들과 구분되지 않아(전부 "Totalmoney morning briefing") 특정 세션의 상세 로그를 확보하지 못했고, 정확한 중단 원인은 미상으로 남음.
3. **health-check.mjs 알림도 지연됨**: 정상이라면 09:00 KST까지 미발행 시 알림이 가야 하는데, health-check.yml 자체의 매시간(`5 * * * *`) 실행이 08:52 KST와 11:38 KST 사이에 약 2시간 46분 공백이 있었던 것으로 확인됨(정상이면 시간당 1회씩 실행돼야 함) — 그 사이 스케줄된 실행이 GitHub Actions 쪽에서 지연되거나 유실된 것으로 추정, 정확한 원인은 미상. 실제 알림은 11:38 KST에야 발송됨(로그로 "이상 1건 감지" 확인, 13:57 KST 실행에서는 "동일 문제 지속 중 — 리마인더 주기 전이라 알림 생략" 로그로 재알림 억제 로직이 정상 동작하는 것도 확인).
4. **수동 복구**: Naver 뉴스 검색으로 8/14 실제 시황(美 PPI 둔화 안도·S&P500 사상 최고치·반도체 랠리 지속·코스피 5거래일 연속 상승 주간 +11.5%·외국인 3조원대 순매수·현대차 +8.24% 순환매 등)을 취재해 `aiAnalysis` 19개 필드 재작성, 아카이브·발행스탬프 갱신 후 커밋(`f2096fd`)·푸시 → "Morning Briefing Telegram Notify" 워크플로 자동 트리거, 로그로 "Sent morning briefing Telegram message." 확인.
5. **남은 숙제(3장에도 반영)**: (1) 재시도 작업이 어디서 멈췄는지 사후 진단할 수 있도록 SKILL.md에 STEP별 중간 체크포인트(예: 각 STEP 완료 시 상태 파일 커밋)를 추가할지 검토, (2) health-check의 GitHub Actions schedule 트리거 지연이 재발하는지 지켜보고 재발하면 외부 백업 트리거(cron-job.org 등, `morning-briefing.yml`이 이미 쓰고 있는 패턴과 동일)를 health-check에도 붙이는 걸 검토. 이틀 연속 재발이라 마운트 이슈 자체는 당분간 계속 발생할 것으로 가정하고 대응하는 게 안전할 듯.

### 2026-08-14 (저녁) — 마감시황 KIS 동기화 실패 복구 + repository_dispatch 재시도 로직 신설

사용자가 `totalmoney-daily-closing-report` 예약작업의 실패 리포트를 붙여넣으며 "마감시황 발행이 안됨. 원인파악하고 수정하고 재발방지책까지 마련해줘"라고 요청.

1. **원인 규명**: 선행 GHA `daily-market-sync.yml`(KIS시세+뉴스 수집)이 이날 두 번 실행됐음을 GitHub Actions API 로그로 확인 — ① `repository_dispatch`(cron-job.org 외부 백업, 16:00 KST경) 실행이 "Sync KIS top 30 + Naver news" 스텝에서 KIS API `fetch failed`로 3회 재시도(스크립트 전체 재시도, 30초 간격) 모두 실패(run #131). ② 그런데 저장소 자체의 `schedule` 트리거(cron 15:40 KST 지정)가 지연되어 약 1시간 뒤(~17:00 KST)에 자동 재실행됐고 이땐 정상 성공(run #132) — 즉 아무 조치 없이도 스스로 복구는 됐지만, `totalmoney-daily-closing-report`(16:15 KST 실행)가 하필 그 실패~자연복구 사이의 빈틈에서 돌아서 기초데이터 없이 정상적으로 STOP한 것. 최근 10거래일(run #123~132) 중 이 KIS 실패는 이번이 유일 — 반복 패턴이 아니라 일회성 네트워크 이슈로 판단.
2. **수동 복구**: 자연복구로 `data/daily-market.json`에 8/14 기초데이터(topGainers/topDecliners/indexes 등)는 이미 채워져 있었으나 `indexes`/`analysis`/`investor_trend`/`featured_stocks`(AI 질적분석 파트)는 비어 있었음. Naver 뉴스+기사 원문 교차검증으로 8/14 실제 마감시황(코스피 6,977.94 +2.42%, 장중 7,010 터치, 5거래일 연속 상승, 외국인 3조380억 순매수 vs 개인·기관 순매도, 현대차 +8.24% 등 특징주 7종목)을 작성해 반영, 커밋(`59f6dc1`)·푸시. `telegram-notify.yml`(마감시황 전용 텔레그램 발송 워크플로, 커밋 메시지의 정확한 "AI 마감시황 분석" 트리거 문구로 구동) run #46이 `success`로 완료된 것까지 API로 직접 확인.
3. **중요 발견 — PAT 권한 재평가**: 조사 과정에서 Cowork의 GitHub PAT로 `POST /repos/mandoo411/vibe/dispatches`(`repository_dispatch`)를 직접 테스트 호출해보니 **204 성공**함을 확인. 지금까지 "PAT엔 Actions 권한이 없어 GHA를 코드로 재실행시킬 수 없다"고 알고 있었던 건 `workflow_dispatch`(Actions:write 필요, 계속 403)에만 해당하는 얘기였고, `repository_dispatch`는 별개 엔드포인트라 Contents:write만으로도 동작한다는 걸 새로 확인함(5장에 반영 완료).
4. **재발 방지**: 이 발견을 활용해 `totalmoney-daily-closing-report` SKILL.md의 STEP 1을 수정 — 앞으로 오늘자 기초데이터가 없으면 (휴장일이 아닌 한) 즉시 STOP하지 않고, 먼저 `repository_dispatch`로 `daily-market-sync.yml`을 직접 재실행시킨 뒤 최대 3분간 폴링해서 복구를 시도하고, 그래도 안 되면 그때 STOP하도록 변경(`mcp__scheduled-tasks__update_scheduled_task`로 반영). 동시에 STEP 3에 `indexes` 필드 작성 지침(기존엔 누락돼 있었음 — daily-market-sync.yml이 채워주지 않는 필드라는 점을 명시)도 보강.

### 2026-08-18 — Cowork 클라우드 세션 git-proxy 버그 최초 확인, 로컬 실행으로 전환해 우회 + 마감시황 발행 사고 복구

원래 16:15 KST `totalmoney-daily-closing-report` 예약작업이 디스크 공간 부족(`VM_DISK_SPACE_INSUFFICIENT`) 에러로 시작조차 못 하는 것을 계기로 조사가 시작됨.

1. **1차 조사(이번 대화 세션)**: 직접 8/18 마감시황을 취재·작성해 `/tmp` fresh clone에서 커밋까지 마쳤으나(로컬 커밋 `38d75888`), 이 세션(클라우드 샌드박스)에서 `git push`가 계속 403으로 거부됨. curl로 프록시 응답을 직접 뜯어본 결과 GitHub 정상 응답과 다른 패턴(프록시가 생성한 에러 JSON, "저장소가 이 세션에 인가되지 않음")을 확인 — 토큰/권한 문제가 아니라 **세션 단위 git-proxy 인가 버그**로 결론.
2. **시도했지만 효과 없었던 것들**: 재시도, `github.com/apps/claude` GitHub App을 저장소 접근 권한으로 신규 설치(이미 떠 있는 세션엔 소급 적용 안 됨), 완전히 새 Cowork 채팅에서 재시도(동일 에러 재현 — 세션 고유 문제 아님을 확인).
3. **효과 있었던 우회책**: Cowork 데스크톱 앱 설정에서 "클라우드에서 새 작업 실행" 토글을 끄면 새 세션이 PC 로컬(D:\vibe, 이미 인증된 git 자격증명)에서 도는 것을 확인 — 이걸로 push 문제 자체는 해결. 이 버그는 공개 GitHub 이슈로 등록된 Anthropic 쪽 알려진 문제(2026-07-09~ 순차 롤아웃 시작, 이 시점 미해결)임도 확인함(5장에 상세 반영).
4. **로컬 전환 직후 새 장애물**: 로컬 Cowork 워크스페이스가 "디스크 공간 부족" 에러를 냄 — D: 드라이브가 아니라 C: 드라이브(WSL2 가상디스크 추정) 문제로 진단, 사용자가 아이폰 백업 등 대용량 파일을 D:로 옮겨 C: 여유공간을 60GB 수준까지 확보한 뒤 재실행하니 정상 동작.
5. **최종 결과 — 전체 파이프라인 재확인**: 사용자가 "예정작업 다시 실행했더니 발행됨"이라고 확인해줘서, 별도 fresh clone으로 GitHub(`2560877` 커밋, 로컬 실행이 자체적으로 생성한 8/18 분석 — 데이터 손실 없음, diff상 큰 삭제량은 JSON 포맷팅(줄바꿈 제거) 때문이었음), 라이브 사이트(`totalmoney.kr/data/daily-market.json`, 배포 반영 확인), 텔레그램(`t.me/totalmoney_ai` 채널 08-18 마감 리포트 게시) 세 곳 모두 직접 재확인 완료.
6. **남은 확인사항(3·4장에 반영)**: 로컬 실행 방식이 기존에 있던 "D:\vibe 마운트 간헐적 누락" 문제까지 같이 해결하는지는 아직 미확인(다음 예약작업들에서 지켜볼 것), 이번에 로컬이 자체 생성한 특징주 10종목의 뉴스 근거 검증 수준도 다음 발행 몇 건에서 한 번 더 확인 권장.

### 2026-08-26 — IPO 공모가 표시 버그, 종목검색 별칭 확장, Vercel 중복 프로젝트 배포한도 소진 사고, 매매시그널 HTS급 조건검색 확장, period_return 버그 수정, Vercel 중복 프로젝트 재발 근본원인 수정, 즉시검색 정렬(sort) 기능 추가, 거래중지/감자 필터, 종목검색 정렬·스크롤 버그 3건, catch-up cron 버그 (총 12건, 9~12번은 같은 날 3차 후속 세션)

한 세션에서 이어서 진행한 4건 + 같은 날 2번째 후속 세션에서 이어진 4건 + 같은 날 3번째 후속 세션에서 이어진 4건. 4번(매매시그널 확장)이 첫 세션의 핵심 작업, 6·7·8번이 2번째 후속 세션의 핵심 작업, 9~12번이 3번째 후속 세션의 핵심 작업이라 상세히 기록.

1. **증시일정 IPO 공모가 "0원" 표시 버그**(사용자 제보: "가격은 다 0원이라고 써놔서 지저분하기만함") — `weekly-market.html`의 `formatOfferingPrice`가 미확정 공모가를 0으로 렌더링하던 걸 공란 처리로 수정(`2c4ed32`). 근본원인도 함께 발견: `scripts/weekly-schedule.mjs`의 `parse38ConfirmedPrice`가 38.co.kr 상세페이지의 "확정공모가" 라벨과 실제 금액이 서로 다른 `<td>` 셀에 나뉘어 있는 걸 못 읽어서 이미 확정된 공모가도 계속 null로 빠지고 있었음(기도산업·니어스랩 등) — `parseHtmlRows`로 셀 단위 파싱하도록 재작성(`db59460`). `refresh-kr-ipo-only.mjs` 실행으로 기도산업 28,400원·니어스랩 41,200원·해치텍 23,000원 실제 확정가 반영, 라이브 사이트에서 직접 확인 완료.
2. **종목검색 "현대차" 미스매치 버그**(사용자 제보: "종목검색할때 현대차 하면 안나오고 현대자동차 라고 써야 나옴") — `realtime-board.js`에 `STOCK_NAME_ALIASES`(현대차→현대자동차, 기아자동차→기아, 포스코→POSCO홀딩스, 하이닉스→SK하이닉스 등 다수) 테이블을 신규 추가해 검색·자동완성 양쪽에 반영(`e96f98a`). 사용자가 "AI종목분석 페이지는 적용이 안 됐다"고 재지적 — `stock-analysis.js`가 실시간시세와 완전히 별개인 자체 검색 구현이라 누락됐던 것으로 확인, 동일 테이블을 그대로 이식(`0fd079b`). 이 배포가 아래 3번 사고로 지연됐다가 이번 세션에 라이브 반영까지 최종 확인.
3. **Vercel 배포 한도(100개/일) 예상보다 훨씬 빨리 소진된 사고**(사용자가 "전엔 훨씬 더 작업해도 한도 안 걸렸는데"라고 의문 제기해 재조사) — GitHub 커밋 상태 API로 확인한 결과 push마다 Vercel 배포가 2개씩("bori-cal", "vibe") 걸리고 있었음. 원인: 이 저장소가 과거 "간단 계산기" 프로젝트로 쓰이던 시절의 잔재로 실제 서비스 중인 `bori-cal` 외에 도메인 없는 중복 프로젝트 "vibe"에도 계속 연결돼 있어서, 자동 데이터 동기화 커밋(하루 50~58회)까지 겹쳐 한도를 빨리 채운 것. 사용자가 자신의 프로젝트 히스토리 기억("vibe가 원래 계산기 프로젝트였는데 설명 바꾸다 꼬인 듯")과 맞아떨어짐을 확인해줌 → 사용자가 Vercel 대시보드에서 "vibe" 프로젝트 직접 삭제, `list_projects`로 삭제 확인 완료(5장에 재발방지 규칙 반영). 이미 소진된 당일 한도 자체는 리셋되지 않아 2번 항목의 마지막 배포가 다음날까지 대기했었음.
4. **매매시그널 HTS급 조건검색 확장**(사용자 요청: "hts와 같은 규모의 조건을 다 불러올수 있으면 좋겠어", "키움hts에 쓰는 조건검색식을 대화창에 그대로 가져다 붙여도 hts 같은 검색 결과가 나올수 있게") — 스크린샷 2장(① "주가 1,000원 이상" 조건이 스키마에 없어 전체가 `understood=false`로 실패하는 에러, ② 키움 HTS 조건검색식 표+수식 예시)을 근거로 범위를 조사한 뒤, "1단계(보여주신 예시만)" vs "HTS 완전 동급" 중 사용자가 처음 1단계를 선택했다가 곧바로 "1단계에 추가로 HTS랑 동급으로 업데이트 해주면 안되나? 어차피 가야될 방향이라"고 범위 확장을 요청해, 실행 가능한 범위까지 최대로 확장해서 진행함.
   - **조건 스키마**: `{logic:AND, clauses:[...]}` 단순 나열 → `{logic:AND|OR, clauses:[leaf|group]}` 중첩 Boolean 트리로 확장. 기존에 저장된 전략은 `negate`/`group`이 없는 형태 그대로이므로 100% 동일하게 계속 평가됨(하위호환 보장, 회귀 위험 없음). `negate:true`로 NOT, `within:{bars:N}`으로 "최근 N봉 이내 발생"(키움식 조건) 지원.
   - **신규 조건 11종**: `price`(현재가 — 원 리포트 버그의 직접 원인이었음), `per`/`pbr`/`eps`/`foreign_hold_rate`/`volume_turnover_rate`(이미 매일 호출하던 KIS `inquire-price` 응답에서 더 추출하는 것뿐이라 **API 호출 추가 없음**), `foreign_net_buy`/`institution_net_buy`(KIS `inquire-investor` 종목당 1회 신규 호출 필요 — AI종목분석 기능에 이미 검증된 동일 패턴 재사용), `high_breakout_n`/`low_breakdown_n`/`volume_record_high_n`(52주 고정 대신 20/60/120/240봉 커스텀 신고가·신저가·신고거래량). 이격도·이평돌파에 240일선도 추가. ROE/부채비율/BPS/배당수익률은 필드 신뢰도 미확인, VI(변동성완화장치)·프로그램매매는 데이터 소스 자체가 없어 이번 범위에서 의도적으로 제외(다음에 필요하면 별도 조사 필요).
   - **수정 파일**: `lib/trade-condition-eval.js`(핵심 로직 전면 재작성, 신규 clause 타입·재귀 평가·`within` 판정용 `crossDaysSince` 사전계산 추가), `lib/kis-indicators.js`(`fetchMarketSnapshot` 확장 + `fetchInvestorFlow` 신규), `scripts/build-screener-cache.mjs`/`scripts/trade-signal-scan.mjs`(신규 필드를 스냅샷 빌드에 반영, 두 스크립트가 동일 로직 유지 원칙 준수), `api/analyze.js`(TS_PARSE_TOOL/TS_SCREEN_PARSE_TOOL 스키마 확장 + 재귀 정규화 + 입력길이 300→4,000자), `trade-signal.html`(조건 표시 문구가 신규 타입·중첩 그룹·negate·within을 사람이 읽는 문장으로 표시하도록 갱신).
   - **검증**: Node 합성 스냅샷으로 22개 케이스 유닛테스트(신규타입 계산·negate 반전·OR그룹·within 판정·기존 flat 저장전략 하위호환) 작성해 전부 통과 확인. `node --check`로 전 파일 문법 검증. 커밋 `bf5d2df`, Vercel 배포 READY 확인(www.totalmoney.kr 반영).
   - **미검증 남은 부분(3·4장에 반영)**: 로그인+Pro 게이트 때문에 실제 API 종단 테스트는 Claude가 직접 못 함 — 사용자가 사이트에서 직접 확인 필요. 신규 KIS 호출 필드(PER/PBR/외국인순매수 등)는 다음 정기 `build-screener-cache.mjs` 실행 후에야 실제 채워짐(이 세션엔 KIS 자격증명이 없어 API 호출량 증가에 따른 실행시간/성공률을 직접 검증 못함).
5. **작업 중 확인된 인프라 이슈**: `api/analyze.js`처럼 자동 커밋이 잦고 큰 파일은 bash 마운트뿐 아니라 `D:\vibe\...` 경로의 Read/Grep 툴로 봐도 stale할 수 있음을 재확인(5장에 반영) — 항상 fresh clone 기준으로 사실 확인할 것.
6. **매매시그널 `period_return`(기간수익률) 조건 0건 버그**(사용자 제보 스크린샷: "한달동안 상승률 50% 이상" 검색 → "최근 undefined거래일간 등락률..." + 0개 종목) — 원인 조사 결과 AI는 `{type:'period_return', days:21, op:'gte', value:50}`을 정확히 반환했지만, `api/analyze.js`의 `tsNormalizeLeafClause`가 `fast`/`slow`/`period` 등은 복사하면서 `days` 필드만 스키마·정규화 로직 양쪽에서 빠뜨리고 있었음(7/19 `bd22dd68`에서 `period_return`이 lib에 처음 추가된 시점부터 존재하던 별개 버그, 같은 날 HTS 확장 커밋 `bf5d2df0`과는 무관). 진단 과정에서 로컬 `D:\vibe` 마운트가 `api/analyze.js`(2,900줄+) 최신 커밋을 전혀 반영 못 하고 있던 것도 재확인(5장 원칙이 다시 실증된 사례). 스키마+정규화 로직에 `days` 필드 추가(`4f05d5a8`) → 실제 캐시데이터(2,653종목)로 52건 매칭 오프라인 검증 → Vercel 배포 READY 확인 → **Claude-in-Chrome으로 실제 로그인 세션에서 같은 문장 재검색해 52개 종목(우리기술투자·한국화장품제조 등) 정상 표시까지 라이브 검증 완료**.
7. **Vercel 'vibe' 중복 프로젝트 재발 및 근본원인 발견**(사용자 질문: "오늘 낮에 삭제한 vibe 프로젝트가 마감시황·브리핑·인스타발행 같은 자동화 때문에 다시 살아난 거 아니냐") — `list_projects`로 확인한 결과 실제로 여전히 살아있고 당일 내내 계속 배포되고 있었음(방금 올린 6번 수정 커밋도 예외 없이 배포됨). 5개 vercel-deploy 워크플로우를 전수조사해 `kr-screener-cache.yml`(매매시그널 캐시 빌드, 평일 16:00 KST) 하나만 Deploy 스텝에 `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID`가 빠져 있는 걸 확인(`6fde4632`에서 Deploy 스텝이 추가된 시점부터 누락, 나머지 4개는 정상). `.vercel/`이 `.gitignore` 대상이라 CI 체크아웃엔 프로젝트 연결 정보가 없고, 이 상태로 비대화형 `vercel deploy --yes`를 실행하면 vercel CLI가 체크아웃 디렉터리명('vibe')으로 새 프로젝트를 자동 생성해 배포하는 게 매일 재발의 진짜 원인이었음 — 마감시황·브리핑·인스타 발행 자동화 자체는 원인이 아니라, push마다 vibe 프로젝트의 Git 연동이 함께 반응해서 배포 목록에 자주 보인 것뿐(그 자동화 커밋들이 잦다 보니 목록을 많이 채운 것). 누락된 두 값 추가해 커밋(`27ea7642`)·푸시 완료, 사용자가 대시보드에서 'vibe' 프로젝트를 최종 삭제 완료(`list_projects`로 확인).
8. **즉시검색(스크리너)에 정렬/랭킹(sort) 기능 신규 추가**(사용자 제보 스크린샷: "1년동안 상승률 높은 순서대로 20종목 보여줘"라고 검색했는데 SK하이닉스 +0.42%, 삼성전자 0.00%처럼 뒤죽박죽으로 나옴) — 원인은 `tsHandleScreen`이 항상 거래대금 내림차순으로만 정렬하도록 고정돼 있었고, 애초에 condition 스키마 자체가 "만족하냐/아니냐"만 표현하는 boolean 필터라 "이 지표로 순위를 매겨달라"는 랭킹 개념이 전혀 없었음(화면도 항상 당일등락률만 표시해 정렬이 맞았어도 요청한 지표 숫자 자체가 안 보였을 것). `TS_SCREEN_PARSE_TOOL`에 최상위 `sort{by,days?,period?,direction}`/`limit` 필드 신규 추가(스크리닝 전용, 종목별 알림 저장엔 랭킹 개념이 없어 `TS_PARSE_TOOL`은 그대로 둠) — by로 period_return/price_change_pct/price/rsi/volume_ratio/market_cap/trading_value/disparity/per/pbr/eps/foreign_hold_rate/volume_turnover_rate/foreign_net_buy/institution_net_buy/adx 15종 지원. 임계값 조건 없이 순위만 요청한 경우(`clauses:[]`)도 "전체 종목 랭킹"으로 인정하도록 검증 완화(기존엔 무조건 understood=false 처리됐음 — evaluateCondition이 빈 clauses면 항상 false를 반환하는 것과 별개로 처리). `trade-signal.html`은 sort가 있을 때 당일등락률 대신 순위번호(N위)+실제 정렬 기준 값을 표시하도록 갱신(커밋 `27cabcb0`). 실제 캐시데이터(2,653종목)로 1년수익률 내림차순 재현(디에이테크놀로지 2900.0% 1위 등, 정렬 검증 PASS) → Vercel 배포 확인 → Claude-in-Chrome으로 라이브 재검색해 화면에 순위·수익률까지 정확히 표시됨을 스크린샷으로 확인 완료. **다만 검증 중 같은 문장에도 약 20% 확률로 모델이 `understood:false`를 반환하는 비결정성을 발견**(condition.clauses가 빈 배열이어도 되는 케이스를 모델이 생소해하는 것으로 추정) — few-shot 예시를 추가한 완화 커밋(`2bd7a0d4`)을 이어서 푸시했으나, 당일 vibe 중복배포로 Vercel 일일 배포 한도를 이미 소진했을 가능성이 높아 이 커밋의 실제 배포는 이번 세션에서 확인하지 못함(3장·4장·5장에 후속 확인 항목 반영).
9. **거래중지/정리매매 종목 제외 + 감자·액면병합 비정상 급등 필터링(매매시그널)**(같은 날 3번째 후속 세션, 사용자 제보: "검색결과중에 거래중지 종목도 나오는데, 이런종목은 안나오게 해줘. 그리고 감자나 액면병합 같은 이슈로 가격이 높아지는 것도 걸러주고(예: 감자전 100원 감자후 1000원)") — 거래중지·정리매매는 KIS `inquire-price`가 이미 매일 호출 중이던 응답에서 `temp_stop_yn`(임시정지여부)/`sltr_yn`(정리매매여부) 필드를 추가로 읽어와(API 호출 추가 없음) `lib/trade-condition-eval.js`의 `buildSnapshotFromSeries`가 스냅샷에 포함하도록 확장 → `api/analyze.js`의 `tsHandleScreen`이 후보 목록에서 해당 종목을 제외. 감자/액면병합은 KIS 수정주가(`FID_ORG_ADJ_PRC=0`)로도 보정이 안 되는 걸 확인해서, 코스피/코스닥 법정 가격제한폭(±30%, 여유 포함 32%)을 초과하는 일별 급등락을 감자성 이벤트로 간주해 그 구간을 포함하는 기간수익률만 `computePeriodReturns`에서 `null` 처리(표시 생략)하도록 수정. `scripts/build-screener-cache.mjs`/`scripts/trade-signal-scan.mjs` 두 스크립트 모두 동일 필드를 스냅샷에 반영(두 파일이 로직을 동일하게 유지해야 하는 원칙 준수). 합성 데이터로 두 로직 모두 오프라인 검증(급등 구간을 포함하는 기간만 null, 완만한 정상 상승은 필터에 안 걸림 확인) 후 커밋(`3bd00239`, 첫 push 때 `scripts/trade-signal-scan.mjs` 반영이 누락된 걸 발견해 즉시 `git commit --amend`로 정정). **다음 정기 캐시 재생성(16:00 KST) 전까지는 라이브 효과 없음** — 3·4장에 확인 항목 반영.
10. **AI종목분석/실시간시세 종목검색 — 관련성 정렬 누락 + 8개 하드컷으로 선택 불가 버그**(사용자 제보 스크린샷 2장: "삼성전자" 검색해도 실제 삼성전자가 아니라 이름에 "삼성전자"가 포함된 채권혼합형 ETF들이 먼저 뜨고 정작 삼성전자는 목록에서 아예 안 보여 선택도 못 함, "외 8개 더 있습니다"에서 막혀 스크롤도 클릭도 안 됨) — 원인 조사 결과 `stock-analysis.js`(AI종목분석)의 `filterStocksForAutocomplete`가 관련성 순위 없이 `stockList` 원본 순서 그대로 substring 필터만 하고 있었음. `realtime-board.js`(실시간시세)는 이미 2026-08-21 별칭 버그 수정 때 도입한 점수체계(정확일치=100 > 별칭=100 > 시작일치=80/75 > 포함=60/55, `findStockMatches`)를 쓰고 있었는데, AI종목분석은 별도 구현이라 이게 반영이 안 돼 있었던 것 — 실시간시세와 동일한 점수체계로 통일. 두 파일 모두 `renderAutocomplete(matches.slice(0, 8), ...)`로 하드컷하고 CSS(`.rt-ac`)도 `overflow:hidden`이라 8개 넘는 결과는 스크롤 자체가 불가능했던 것도 함께 발견 — 표시상한을 8→50(`AC_DISPLAY_LIMIT`)으로 올리고 `realtime-board.css`의 `.rt-ac`에 `max-height`+`overflow-y:auto` 추가해 스크롤 가능하게 전환, 화살표 키로 활성 항목이 뷰 밖으로 나가면 `scrollIntoView`로 따라가게 보정. 합성 데이터(삼성전자 vs "삼성전자" 포함 ETF 5종)로 "삼성전자" 검색 시 005930이 100점 1위, ETF들은 60점으로 후순위 정렬됨을 오프라인 검증, `node -c`·CSS 중괄호 균형 확인 통과. 커밋 `ec256579`.
11. **미국주식 탭 종목검색도 동일 버그**(위 10번 조사 중 같은 클래스(`.rt-ac-item`)를 쓰는 `us-market.js`의 `filterLocalRows`도 관련성 순위 없이 `allKnownRows()`가 반환하는 우연한 순서 그대로 substring 필터만 하고 있음을 함께 발견 — 예: "AAPL" 검색해도 진짜 애플이 아니라 AAPD/AAPU 같은 레버리지 상품이 먼저 뜰 수 있는 구조) — 10번과 동일한 점수체계(`rankUsMatches` 신규 추가, 티커·이름 정확일치=100/95 > 시작일치=80/75 > 포함=60/55)를 적용해 `filterLocalRows`와 로컬+원격 검색 병합 결과 모두에 반영, 표시상한도 동일하게 50으로 통일. 합성 데이터로 "AAPL"/"Apple" 검색 시 AAPL이 항상 1위로 정렬됨을 오프라인 검증. 커밋 `225781eb`. **9~11번 배포는 세 커밋 모두 Vercel 일일 배포한도(100회) 소진으로 이번 세션에서 확인 못 함**(아래 12번 참고, 3·4장에 확인 항목 반영).
12. **Vercel 일일 배포한도 재소진 확정 + `vercel-deploy-catchup.yml` cron이 사실은 "연 1회"였던 버그 발견·수정** — 9~11번 커밋이 전혀 자동배포가 안 걸려서, 사용자가 GitHub Actions에서 "Deploy Vercel (catch-up)"을 수동 실행 → 로그에 `Error: Resource is limited - try again in 24 hours (more than 100, code: "api-deployments-free-per-day")`가 명시적으로 찍혀 한도 소진이 추정이 아니라 확정됨. "다음날 09:00 KST 캐치업이 자동으로 따라잡아준다"고 안내했던 근거였던 `vercel-deploy-catchup.yml`을 직접 열어보니 cron이 `"0 0 21 5 *"`(2026년 5월 21일 단 하루, 5월에 캐치업이 필요했을 때 한 번 쓰고 남은 값으로 추정)로 박혀 있어서, 파일명·주석은 "일일 캐치업"인데 실제로는 5월 이후 단 한 번도 자동으로 돈 적이 없었던 별개의 버그였음이 드러남 — 오늘 하루에만 이미 4개 커밋(`2bd7a0d4`/`3bd00239`/`ec256579`/`225781eb`)이 이 안전망 없이 방치되고 있었던 것. `cfc312cc`로 `"0 0 * * *"`(매일 09:00 KST)로 수정, YAML 문법 검증 통과. GitHub Actions workflow는 Vercel 배포와 무관하게 저장소 push 시점부터 바로 유효하므로 이 수정 자체는 이미 적용된 상태 — 내일 09:00 KST에 실제로 밀린 커밋들을 자동 배포하는지가 "진짜 매일 도는 캐치업"의 첫 실전 검증이라 다음 세션에서 반드시 확인 필요(3·4·5장에 반영).

### 2026-08-26 (4차 후속 세션) — 우선주 종목검색 버그 근본 수정 + AI종목분석 세로텍스트 버그 2건 + GPT 2차 리포트 반영 + Vercel 배포한도 재소진

앞선 3차 후속 세션과는 별개로 이어진 세션. 총 4건 작업.

1. **우선주(삼성전자우/삼성물산우 등) 종목검색 전면 불가 버그**(사용자 제보: "삼성전자우 삼성물산우 이런건 검색이 안되네. 수정할때 모든 종목검색페이지 다 수정해줘야대. ai종목분석, 실시간시세, 등등") — 조사 결과 4개 층위가 겹친 문제였음: ① `assets/stock-list.json`의 원천인 KRX KIND 상장법인목록이 애초에 "회사" 단위 목록이라 우선주가 구조적으로 통째로 빠져 있었음(자체적으로 별도 발행되는 종목이라). ② 그 없는 데이터를 억지로 다뤄도 되도록 코드 정규화 함수들(`code6Maybe`/`normalizeCode6` 계열, 여러 파일에 중복 존재)이 "숫자 6자리만 유효"로 가정하고 있어서 우선주의 영숫자 코드(예: 삼성물산우B=`02826K`)가 들어오면 숫자만 남기고 깎아 엉뚱한 코드로 뭉개짐. ③ "삼성물산우"처럼 보통주 이름을 포함하는 질의가 양방향 부분일치로 보통주+우선주 후보 2개가 걸려 모호 판정되어 못 찾음. ④ 백엔드 KIS 호출 전 검증 게이트(`/^\d{6}$/`)가 영숫자 코드를 거부해, 설령 검색은 통과해도 시세 조회가 막힘.
   - **수정**: `scripts/update_stock_list.py`에 KIS 종목마스터(.mst) 파서(`parse_kis_mst_preferred`)를 신규 추가해 코스피 111 + 코스닥 3, 총 114개 우선주를 정확한 알파벳 코드 그대로 추출·병합(`assets/stock-list.json` 3,817→3,931건, 기존 항목 삭제 없이 추가만). `.github/workflows/update-stock-list.yml`에 종목마스터 다운로드 스텝을 추가해 매일 자동 갱신 시에도 우선주가 계속 포함되게 함. 코드 정규화 함수 전체(`stock-analysis.js`/`api/stock-analysis.js`/`api/analyze.js`/`api/kis-stock-quote.js`/`api/kis-realtime-data.js`/`lib/kis-domestic-quote.js`/`realtime-board.js`)에 영숫자 6자리 통과 분기를 추가하고, 백엔드 검증 정규식을 `/^\d{6}$/` → `/^[0-9A-Z]{6}$/`로 확장. 모호매칭은 startsWith 우선 매칭 규칙을 추가해 해결.
   - **검증**: 로컬에서 KIS `.mst` 파일 파싱 결과를 별도 pandas 기반 참조 구현과 대조해 114건 전부 일치 확인(삼성전자우=005935, 삼성물산우B=02826K, 현대차우=005385 등). 배포 후 "삼성전자우"/"삼성물산우" 그대로 실시간시세·AI종목분석에서 검색해 실제 KIS 시세가 반환되는 것까지 라이브 curl로 확인 완료. 실시간시세 랭킹탭(상승률/거래대금 등)의 숫자전용 필터와 WebSocket 구독 정규화는 이번 범위에서 의도적으로 제외(4장 34번 참고).
2. **AI종목분석 결과 텍스트 세로 렌더링 버그 — 2차례 발견·수정**(사용자 스크린샷 2장) — 모델이 JSON 문자열 필드를 드물게 글자/짧은단어 단위로 개행해서 반환하는 경우가 있는데, 이걸 그대로 `\n` split → 각 조각을 `<p>`로 감싸 렌더링하면 세로 한 글자씩 나열되는 버그.
   - **1차**: `analysis.supply`(수급 분석)/`story`/재료/총평 등 `formatProseText`를 거치는 필드들 — 조각들의 평균 길이가 비정상적으로 짧으면 문단 구분이 아니라 오분할로 판단해 이어붙이는 `mergeFragmentedParagraphs`를 신규 추가해 해결. 배포·라이브 확인 완료.
   - **2차**: 배포 후 사용자가 "아직도 세로로 나옴"이라고 재제보 — 이번엔 카드①("한눈에 요약")의 `summary.description`이 원인이었음. 이 필드는 `formatProseText`를 아예 거치지 않고 `escapeHtml()` + CSS `white-space: pre-line`(줄바꿈 있으면 그대로 꺾어 보여줌)만 쓰는 완전히 별개의 렌더 경로라 1차 수정이 미치지 못했음. 동일한 "평균 조각 길이가 짧으면 이어붙이기" 로직을 `sanitizeOneLineText`로 별도 구현해 백엔드(`api/analyze.js`의 `normalizeAnalysis`/`mapToolInputToLegacy`)와 프론트(`stock-analysis.js`의 렌더 시점) 양쪽에 방어적으로 적용. **커밋(`3e83e7da`)·푸시까지 완료했으나 Vercel 배포한도 재소진으로 아직 배포 확인 못 함(3장 최우선 항목 참고).** 향후 새 텍스트 필드를 추가할 때는 이런 "모델이 개행을 남발할 수 있다"는 전제로 방어 로직을 기본 적용할 것.
3. **GPT의 AI분석 품질 2차 리포트(18개 항목) 반영**(사용자가 GPT로 받은 리포트 전문을 붙여넣으며 "처음부터 하나하나 꼼꼼하게 읽고 지적한 부분, 수정했으면 좋겠는 부분 수정해줘"라고 요청) — 리포트가 마지막에 제시한 "개발자라면 이 5개만 고치겠다" 우선순위를 전부 반영하고 부가 항목도 추가로 반영, 전부 코드 계산(비-LLM)으로 처리해 할루시네이션 방지 원칙을 지킴.
   - A/B/C 시나리오 상호배타화: 시나리오C(약세)가 "지지 붕괴=매수 기회"로 읽히지 않도록 프롬프트 재구성(신규매수 금지 → 재진입 조건 → 재진입가 순서, 재진입가는 이탈가 대비 최소 3%↓), 시나리오B의 진입가와 총평(`aiComment`)이 모순되지 않게 금지 규칙 추가.
   - 수급 1/5/20일 추세: KIS `inquire-investor` 응답이 이미 여러 영업일치를 내려주는 걸 활용해 API 호출 추가 없이 `buildSupplyFlowFact()`로 5/20일 누적 순매수를 코드에서 직접 계산, "사실(KIS 데이터)" 배지를 달아 AI 서술과 시각적으로 분리.
   - 재료 반영률: `bucketReflectionPct()`로 10% 단위 강제 반올림해 "62%" 같은 존재하지 않는 정밀도 방지.
   - 엘리어트 파동: 파동 번호 단정을 전면 금지, 언급 시 "엘리어트 참고 해석:" 접두사 필수.
   - FACT/AI해석 시각 구분: 수급 분석 카드에 "사실" 파란 배지 / "AI 해석" 노란 배지 CSS 신규 추가.
   - 부가 반영: 종합점수 표기를 `+3` → `+3/8`(분모 명시, 상단 확률%와 다른 척도임을 명확화), target1 대비 15% 이상 먼 target2는 "장기 잠재 목표"로 라벨 구분(`target2IsLongTerm`), story(왜 지금 이 가격인가) 서술을 "기존 재료 → 오늘의 변화" 순서로 구분하도록 프롬프트 추가, 수급 원화환산액 문구 정리, 재료 반영률과 수급 반영을 서로 다른 개념으로 혼용 금지.
   - **검증**: 신규 코드 계산 함수들(`buildSupplyFlowFact`/`bucketReflectionPct`/`target2IsLongTerm`)을 합성 데이터로 오프라인 단위테스트, `node --check` 통과. 커밋 `6a447f41`로 배포·라이브 확인 완료(신규 CSS 배지·`+3/8` 표기 등 코드 마커가 실제 서빙되는 JS/CSS에서 확인됨).
4. **Vercel 배포한도 재소진 — 이번엔 원인이 중복 프로젝트가 아니라 세션 자체의 잦은 push**: 위 2번의 2차 수정 커밋(`3e83e7da`)이 몇 분을 기다려도 `list_deployments`에 전혀 나타나지 않아 조사한 결과, GitHub 커밋 상태 API에서 Vercel이 `"Deployment rate limited — retry in 24 hours"`(2026-08-26 09:56 UTC)로 응답한 걸 확인 — 3차 후속 세션 때의 "vibe 중복 프로젝트" 문제와 달리, 이번엔 순수하게 이 세션이 짧은 시간(~7.5시간) 안에 코드 수정 커밋을 20회 넘게 연달아 push한 것 자체가 원인. `vercel-deploy-catchup.yml`(3차 후속 세션에서 연1회→매일 09:00 KST로 고쳐둔 그 워크플로우)이 다음날 09:00 KST에 이 커밋을 포함해 자동 배포하는지가 다음 세션 최우선 확인사항(3·4·5장에 반영).

### 2026-08-26 (5차 후속 세션) — 세로텍스트 2차 수정 배포 확인 + AI종목분석 UI 전면 리디자인(카드1~7)

앞선 4차 후속 세션과 이어진 세션. 총 2건.

1. **세로텍스트 2차 수정(`3e83e7da` 계열, 실제 배포 sha `c7e3d769`) 배포·라이브 확인**: 세션 시작 시점엔 4차 세션에서 확인 못 했던 배포 대기 상태였으나, 이번 세션에서 다시 확인해보니 Vercel 배포한도가 정상 회복되어 있었고 해당 수정이 이미 `READY`로 배포돼 있었음. Claude-in-Chrome으로 실제 로그인 세션에서 삼성전자 AI분석을 재실행해 "한눈에 요약" 카드의 `summary.description`이 세로로 깨지지 않고 정상 렌더링되는 것을 최종 확인(3·4장 반영, 해당 항목 해결 처리).
2. **AI종목분석("AI 종목분석" 페이지) UI 전면 리디자인**(사용자 요청: "카드1을 좌: 배지+확률 / 중앙: 요약문 / 우: 2x2 지표 필그리드로, 종합점수는 하단으로. 아래 카드들도 같은 행 높이 통일하고 내부 padding·배경 정리" → 시안 제시 후 사용자가 "ㅇㅇ 이대로 진행해줘. 밑에 나머지 분석들도 이 ui 컨셉에 맞도록 수정해주고"로 승인 + 카드4~7까지 범위 확장 승인)
   - **카드1(한눈에 요약)**: 기존 `renderScoreCard()`를 `buildScoreCardParts()`로 재작성해 `pillsHtml`(2x2 지표 그리드, grid 안)과 `footerHtml`(종합점수+설명, 카드 하단 전체 폭)로 분리 반환. `signalBadgeClass()`도 전체 클래스명 대신 `buy`/`hold`/`avoid` 접미사만 반환하도록 단순화. 새 CSS 클래스 체계(`sum2-*`)로 좌(신호배지+상승확률)/중앙(요약문)/우(2x2 필) 3분할 grid 구성.
   - **카드2/3(왜 지금 이 가격인가/수급 분석)**: `.ai-card`에 `display:flex;flex-direction:column`, `.ai-card__body`에 `flex:1` 추가해 같은 행의 두 카드가 내용 길이와 무관하게 자동으로 높이가 맞춰지도록 수정(그리드 `items-stretch` 기본 동작 활용).
   - **카드4(다가오는 이벤트)**: 데스크톱 미디어쿼리의 `grid-column:1/-1`(전체 폭) 선택자 목록에 `.ai-card--events`가 누락돼 있어 반쪽 폭으로 잘리던 기존 버그를 발견해 함께 수정, 이벤트 리스트(`.ai-event`)에 카드형 배경·둥근 모서리·pill 뱃지 스타일 적용.
   - **카드5~7(재료 분석/차트 흐름 분석/AI 주관적 판단)**: `.ai-mat-card`/`.ai-outlook-card`/`.ai-opinion-price`/`.ai-opinion-comment`/`.ai-scenario`/`.ai-chart-box` 등 내부 요소들의 padding·border-radius·배경색을 카드1과 통일된 톤으로 정리.
   - **검증**: 먼저 실제 사이트 디자인 토큰을 그대로 뽑아낸 독립 목업(`stock-analysis-redesign.html`)으로 시안을 만들어 스크린샷으로 사용자 승인을 받은 뒤 프로덕션 코드(`/tmp` fresh clone)에 반영. Playwright 헤드리스 크로미움(로컬 캐시된 브라우저 재사용)으로 인증·API를 모두 스텁 처리한 오프라인 하네스를 구성해 라이트/다크/모바일 3종 뷰포트에서 합성 데이터(카드1~7 전체, materials/events 등 실제 스키마에 맞춘 목데이터)로 렌더링 검증(카드2/3 높이 일치, 카드4 전체 폭 확인). 이어서 커밋(`8e94e90`)·푸시 → Vercel 배포 `READY` 확인(`www.totalmoney.kr` alias 포함) → **Claude-in-Chrome으로 실제 로그인 세션에서 삼성전자 라이브 분석을 실행해 카드1~7 전체를 라이트+다크 모드로 스크롤하며 육안 검증**(디자인 의도대로 렌더링됨, 기존 세로텍스트 버그 재발 없음, 실데이터 기준 카드2/3 높이 일치·카드4 전체 폭 확인). 파일 변경: `stock-analysis.js`(+48/-줄 다수), `stock-analysis.css`(+205/-줄 다수), `stock-analysis.html`(캐시버스팅 `?v=15`→`?v=16`).
   - **남은 확인사항(4장 36번 반영)**: 이번엔 삼성전자(국내주식) 1종목만 라이브 검증 — 미국주식·암호화폐 등 카드 내용 길이가 다른 종목에서도 레이아웃이 정상인지 다음에 한 번 확인 권장.

### 2026-08-28 — AI종목분석 "다가오는 이벤트" AI/웹서치 → DART 공시 기반 완전 전환

사용자 제보(SK하이닉스 분석에서 이미 끝난 NVIDIA 실적을 "예정"으로 오표기, 오늘 날짜 이벤트 미제외, 기준금리 결정 같은 종목분석과 안 맞는 매크로 일정·"미래 일정으로 확인됩니다" 필러 문구 혼입 3건)로 시작해, 세션 중간에 사용자가 "웹서치/뉴스API로 땜질하지 말고 DART API 기반으로 다시 짜라"는 아키텍처 전환을 직접 지시(4가지 요구사항: ①공시검색으로 주총/배당/주요사항보고서 실제 날짜 추출 ②실적발표는 결산월 기준 법정기한으로 "구간" 계산 ③데이터 없으면 뉴스로 채우지 말고 비워둘 것 ④요청 시마다 조회하지 말고 하루 1회 배치).

1. **신규 배치 파이프라인 구축**: `scripts/dart-events.mjs`(DART `list.json`/`document.xml`/`tsstkAqDecsn.json` 등 호출) + `scripts/dart-zip-utils.mjs`(외부 의존성 없이 순수 Node `zlib.inflateRawSync`로 ZIP 직접 파싱 — `adm-zip` npm 설치 시도했다가 `package-lock.json`이 통째로 재포맷되는 부작용 발견해 폐기하고 직접 구현) + `.github/workflows/dart-events.yml`(매일 06:10 KST + 수동실행). corp_code 매핑은 대용량 `corpCode.xml`(87,346개) 다운로드가 GitHub Actions에서 반복 타임아웃돼 포기하고, `list.json`을 종목코드로 스캔하는 방식으로 대체.
2. **`api/analyze.js` 연동**: `normalizeAnalysis`를 비동기로 전환, KR종목이면 AI가 만든 events를 무조건 버리고 GitHub Contents API로 가져온 `data/dart-events.json`(30분 캐시)의 실제 공시 데이터로 교체. `normalizeEventType`이 `"중립"`을 전부 `"호재"`로 오매핑하던 걸 로컬 테스트로 사전 발견해 `"neutral"`로 수정.
3. **실제 DART 문서 파싱 중 발견·수정한 버그 2건**: (1) `git diff --quiet`가 신규 미추적 파일(첫 실행의 `dart-events.json`)을 감지 못해 워크플로우 커밋 스텝이 조용히 스킵되던 문제 → `git status --porcelain`으로 교체. (2) 날짜 추출 정규식이 앵커 문구의 첫 등장에서만 시도하다 보니, 문서 안에 같은 문구가 여러 번 나오고 첫 등장이 날짜 없는 설명절인 경우(SK하이닉스 배당공시에서 실측) 추출 실패 → 모든 등장을 순서대로 시도하도록 수정. 동시에 탐색창을 100~120자에서 40자로 좁혀 인접한 무관 날짜(디코이)를 잘못 집어오는 위험도 재현 테스트로 확인 후 방지.
4. **검증**: GitHub PAT에 `workflow` 스코프가 없어 워크플로우를 코드로 직접 실행 못 시키는 제약이 있어, 사용자가 GitHub Actions에서 수동 Run workflow → 콘솔 로그를 붙여넣어주는 방식으로 3차례 반복 디버깅(코드 수정은 항상 로컬 Node 목 테스트로 먼저 검증한 뒤에만 라이브 실행 요청). 최종적으로 `data/dart-events.json`이 정상 생성됨을 확인(SK하이닉스·현대차 배당기준일 2026-08-31, 신한지주 2026-11-03), Claude-in-Chrome으로 실제 사이트에서 SK하이닉스(다가오는 이벤트에 배당 공시 단일 항목만 표시, 재료분석과 중복 없음)·삼성전자(DART 이벤트 없어 "현재 확인된 예정 이벤트 없음" 빈 상태 정상 표시)까지 라이브로 최종 확인.
5. **변경 파일**: 신규 `scripts/dart-events.mjs`, `scripts/dart-zip-utils.mjs`, `.github/workflows/dart-events.yml`. 수정 `api/analyze.js`(KR 이벤트 오버라이드), `scripts/earnings-calendar.mjs`(`KR_CODES`를 export해 재사용). 새 API 라우트를 추가한 게 아니라 Vercel 함수 12개 제한에는 영향 없음.
6. **남은 확인사항(3·4장 37~39번 반영)**: 신규 배치라 며칠간 연속 정상 실행되는지, 정기 주총 시즌 등 이벤트가 몰릴 때도 잘 버티는지 관찰 필요. KR종목은 AI가 만든 events를 항상 버리므로 프롬프트에서 아예 생성을 스킵시켜 토큰을 아낄 여지도 있음(후속 검토 항목).

### 2026-09-01 — AI종목분석 문체 전면 개편 + 마감시황 "브리핑 덱" 리디자인

한 세션에서 두 건을 처리. 두 건 모두 "정보는 맞는데 상품성이 없다"는 같은 문제의식에서 출발했다.

**(1) AI종목분석 리포트 문체·내용 규칙 개편** (커밋 `d3f9ec6` → `c2a9e6b`, 4개)

사용자 제보: SK하이닉스 분석 결과가 "어투가 너무 딱딱하다, 읽을 때 질린다". 실제로 확인해보니 문장마다 절이 3~4개씩 붙은 장문에 "~라고 보는 게 맞다"류 상투구가 반복되고, 같은 재료(40조 자사주 소각)가 4개 섹션에 중복 등장하고 있었다.

1. **문체 규칙 신설**(`api/analyze.js`의 `ANALYST_PERSONA_RULES`): 기존 "어조 통일" 1줄짜리 규칙을 구체적 규칙 블록으로 교체 — 문장 40~70자(90자 상한), 절 3개 이상 장문 금지, 2~4문장마다 문단 분리(`\n\n`), 두괄식, 종결어미 연속 반복 금지, 상투구 9종 명시 금지("~라고 보는 게 맞다"/"~에 무게가 실린다"/"전형적인 모습이다" 등), 접속사 남발 금지, 카드당 짧은 단문 1회로 강약. **어투는 사용자 선택으로 '~다'체 유지**(존댓말·구어체 옵션 중 선택).
2. **섹션 역할 분담 + 중복 금지**: 같은 재료를 3개 이상 섹션에 반복 금지(최대 2곳), 카드별 담당 범위 명시(summary=결론만, story=오늘 바뀐 것, supply=수급 방향, materials=재료 강도, chart=가격 구조, opinion=대응).
3. **이미 끝난 이벤트를 재료 슬롯에 넣지 못하게 금지** — 반영도 90% 이상으로 매겨질 건(예: 지난달 끝난 실적발표)은 재료 항목으로 만들지 말고 story 배경 한 문장으로만.
4. **내부 필드명 노출 차단**: 라이브 검증 중 종합 의견에 `scenarioB.entry를 160만5000원으로 둔 이유는…`이 그대로 출력되는 걸 발견. 프롬프트 금지 규칙 + `stripCitations()` 안에 `stripInternalFieldNames()` 안전망(정규식 26종 → 한국어 라벨 강제 치환) 이중으로 방어.
5. **수급 숫자 재나열 차단 — 세 번 만에 해결**: supplyDemand 본문이 바로 위 FACT 표의 1일/5일/20일 수치 6개를 그대로 전사하고 있었다. ①페르소나 규칙에 "재나열 금지"(실패) → ②"본문 금액 표기 최대 2개" 정량 상한(여전히 실패) → ③원인 발견: **3번 섹션 규칙 본체에 "1일/5일/20일 값을 반드시 함께 언급해서 비교 서술할 것"이라는 정반대 지시가 남아 있어 상위 페르소나 규칙을 이기고 있었음.** 해당 지시를 "방향 비교만 서술"로 교체하고 숫자 상한·금지/정답 예시를 섹션 규칙 안에 직접 삽입 → 라이브 재검증에서 금액 6개 → 2개로 확인. **교훈: 섹션별 구체 지시가 상위 톤 규칙보다 항상 우선한다. 프롬프트 수정 시 같은 주제의 다른 지시가 남아 있는지 먼저 grep할 것.**
6. **디자인 — 진입/목표/손절 색상 시각화**(`stock-analysis.js`/`.css`): 한국 관습대로 목표=빨강/손절=파랑/진입=앰버 색상 토큰 신설(라이트·다크 별도 팔레트). 종합의견 가격 블록에 상단 3px 액센트 바 + 그라데이션 + 그림자/인셋 하이라이트로 입체감, 2×2 그리드 확정(flex `calc(50% - 5px)`가 반올림 때문에 1열로 무너지던 문제 → grid로 교체). 가격 순서를 매매 흐름대로 진입→목표→손절로 재배치(시나리오 카드와 통일). 손익비는 같은 크기 블록에서 빼서 "손절까지의 위험 1 대비 1차 목표까지의 기대수익 N" 캡션 한 줄로 분리. 시나리오 카드는 가격 행(라벨 좌/숫자 우 + 점선 구분 + 등폭 숫자)과 서술 행(라벨을 위로 올린 블록)을 분리. 수급 FACT 표는 순매수=빨강/순매도=파랑 색 구분 + flex-wrap을 그리드로 교체(숫자가 자릿수 중간에서 끊기던 문제 해결), 모바일은 기간별 세로 스택.
7. **DART 이벤트 문구 정리**(`scripts/dart-events.mjs`): "배당 관련 공시가 나왔다 (배당기준일 …)" 같은 서술형을 명사구로 — `배당기준일 2026-08-31 — 이 날까지 보유해야 배당 대상`. **다음 06:10 KST 배치가 `data/dart-events.json`을 재생성해야 화면에 반영됨.**
8. **검증**: Playwright 오프라인 렌더(라이트/다크/모바일 390px) → 배포 → Claude-in-Chrome 실제 로그인 세션에서 SK하이닉스·삼성전자 분석을 3회 실행해 문체·필드명·숫자 개수를 직접 확인.

**(2) 마감시황 브리핑 덱 리디자인** (커밋 `82fe96b`/`735ad67`)

사용자 지적: 8/31 마감시황이 "메모장에 대충 갈겨 쓴 느낌", "PPT처럼 색감·표·도표를 써서 어디 가서 화면 띄워놓고 브리핑할 수준으로". 원인은 `analysis`(이모지 소제목이 붙은 4,600자짜리 한 덩어리 텍스트)를 `<div>` 하나에 `white-space: pre-wrap`으로 그대로 쏟아붓고 있었던 것.

1. **섹션 파서 신설**(`daily-market.js`의 `dmxParseAnalysis`): 이모지·기호를 떼고 남은 제목으로 7개 섹션(핵심 한 줄/지수/시장 흐름/투자자별/특징주/향후 전략/내일 변수)을 분해. 헤더를 2개 미만 찾으면 `null`을 반환해 **기존 줄글 렌더링으로 폴백**(섹션 헤더가 없던 5~6월 과거 데이터도 안 깨지게 이중 경로 유지). 특징주·전략·내일변수 렌더도 각각 try/catch로 기존 함수 폴백.
2. **신규 컴포넌트 8종**: ①히어로 배너(핵심 한 줄 + 날짜 + 장 성격 태그, 지수 방향에 따라 색) ②지수 카드 + **장중 저점→시가→종가 레인지 바** ③오늘의 시장 온도 ④투자자 수급 **다이버징 막대차트** ⑤업종별 등락 랭킹 바 ⑥기관·외국인 순매수 상위 ⑦시장 흐름(문단 번호 배지 + 애널리스트 인용문 blockquote 분리) ⑧특징주(급등/급락 그룹 분리 + 등락률 바 + 종목코드 + 리스크 슬롯) + 내일 변수 번호 카드 그리드.
3. **차트 설계에서 실제로 중요했던 판단 3가지**:
   - **시장 온도는 AI가 아니라 코드가 집계**한다 — 상한가/+20% 이상/하한가/거래대금 TOP10 중 상승/거래대금 1위를 `topGainers`·`topDecliners`·`topTradingValue` 실데이터에서 직접 센다. 할루시네이션 여지가 원천적으로 없고 과거 모든 날짜에 소급 적용된다. TOP30을 넘는 구간은 "30+"로 정직하게 표기.
   - **수급 막대는 코스피·코스닥을 같은 스케일로 정규화**했다. 처음엔 그룹별로 따로 정규화했는데, 그러면 코스닥 개인 +2,535억이 코스피 기타법인 +1조5,000억과 같은 길이로 보여서 브리핑용으로 위험했다.
   - **장중 고가가 확인 안 되는 날이 많다** — 없으면 지어내지 않고 시가·종가 중 큰 값을 오른쪽 끝으로 삼되 라벨을 "고"가 아니라 "종"으로 바꿔 표기.
4. **버그 수정**: `dmxNum()`이 빈 값/`undefined`에 `Number("")=0`을 돌려주고 있었다 — 이것 때문에 고가가 없는 날 레인지 바가 통째로 안 그려졌다(첫 렌더 검증에서 발견).
5. **생성 규칙 확장**(`scripts/daily-market-ai.mjs`): 스키마에 `marketTone`(장 성격), `indexDetail`(시/고/저), `investor_trend.etc`(기타법인), `sectors`(업종 등락), `netBuyLeaders`(순매수 상위), `featured_stocks[].risk`(리스크 한 줄) 추가. **전 항목에 "확인 안 되면 키 자체를 생략(0이나 추정치 절대 금지)"을 명시.** 향후 전략은 "관망이 유효하다"류 일반론 금지 + 방향 전환 트리거 조건·피해야 할 구간을 오늘 데이터의 숫자로 쓰도록 요구. 내일 변수는 `1.`/`2.`/`3.`을 각각 별개 줄로 강제.
6. **검증자 관점 내용 감사 결과**(사용자 요청): 숫자 정합성은 문제 없었다(장중 -3.55%, 저점 대비 +272.26p, 전일 종가 역산 6,788.88 전부 일치). 상품성 결함 5가지를 발견해 4가지는 이번에 해결 — ①"내일 주목할 변수"에 네 가지가 `1.` 하나에 뭉쳐 한 카드로 렌더링됨(백필로 4장 분리 + 프롬프트 강제) ②특징주에 리스크 항목 부재(스키마 추가, **8/31은 백필 안 함 — 새로 써야 하는 분석이라 내일 리포트부터 적용**) ③전략이 일반론 수준(프롬프트 강화) ④업종 등락률·기타법인 수급이 본문 텍스트에만 있어 차트화 불가(스키마 추가) ⑤외국인·기관 순매수 상위 종목 부재(스키마 추가).
7. **8/31 백필**(`data/daily-market.json`): `analysis` 본문에 **문자 그대로 존재하는 숫자만** 구조화 필드로 옮겼다 — 지수 시가·저가(6,613.58/6,547.76, 821.67/805.14), 업종 9개 등락률, 기타법인 순매수, 장 성격("전약후강"), 내일 변수 4건 분리. 백필 스크립트에 원문 포함 여부 assert를 걸어 검증. 본문에 없는 장중 고가·순매수 상위 종목은 채우지 않았다.
8. **변경 파일**: `daily-market.js`(+594), `daily-market.css`(+869), `daily-market.html`(패널 구조), `scripts/daily-market-ai.mjs`(스키마·규칙), `data/daily-market.json`(8/31 백필). API 라우트 추가 없음 → Vercel 함수 12개 제한 영향 없음.
9. **검증**: 로컬 정적 서버 + Playwright로 라이트/다크/모바일(390px) 풀페이지 렌더 확인(다크 테마는 `localStorage.theme` 선주입 필요), 3회 반복 수정 후 배포 → 실제 사이트에서 8/31 선택해 덱 6블록·레인지바 2개·막대 15개·특징주 13장·내일변수 4장 렌더 확인.

### 2026-09-02 — 장전 브리핑 덱 전환 → 사이트 전체 감사 → 홈 재설계 · 비로그인 전환 퍼널 · 모바일 대응 (커밋 7개)

한 세션에서 세 덩어리를 처리했다. 출발점은 "마감시황처럼 브리핑도 돈 받고 파는 퀄리티로"였고, 이어서 "사이트 전체를 유료 사용자 눈으로 평가하고 고쳐라", 마지막으로 "아이폰에서 이상하고 모바일 스크롤이 너무 길다"였다.

**(1) 장전 브리핑 → 브리핑 덱** (커밋 `b22c17d`, 후속 `6a7ed72`)

`briefing.html`(+1,379/-348). 데이터(`data/morning-briefing.json`의 `aiAnalysis`)는 원래도 아주 구조적이고 풍부했다 — 문제는 100% 표시 계층이었다. **3열 표 8개**(글로벌해석/국내체크/환율원자재/미국뉴스/국내뉴스/업종전략/포트폴리오/리스크)가 똑같은 모양으로 반복되고, 긴 문단은 그대로 쏟아져 있었다.

1. **표를 하나도 남기지 않고** 카드·막대·배지로 전면 교체(`bfx-` 컴포넌트 17종).
2. **히어로 배너** — 핵심 한 줄 + 장 성격 태그(리스크오프/리스크온/혼조). 태그는 AI가 아니라 **코드가 간밤 3대 지수 등락에서 직접 계산**.
3. **중복 제거로 카드 2개 삭제** — "간밤 미국시장" 목록과 "원자재/환율 현황" 목록을 수치 카드로 통합하고, 없앤 '글로벌 시장 해석' 표의 AI 코멘트를 각 카드 하단에 흡수(라벨 정규화 후 정확일치 → 부분일치 2단계 매칭).
4. **'간밤 시장 온도' 스코어보드 신설** — 주요 지수/섹터/대형주 상승 개수, 최강·최약 섹터를 전부 실데이터에서 코드가 집계(AI 해석 아님을 화면에 명시).
5. **섹터·대형주 등락을 다이버징 막대차트로** — 기존엔 값이 항상 "—"인 죽은 컬럼이었다.
6. 포트폴리오는 실제 비율 막대("30~40%"의 중간값으로 절대 폭 — 합이 100에 가까우므로 절대 표기가 의미 있음), 업종 의견은 색상 배지, 리스크는 경고 카드.
7. **버그 2건**: ①문장 분리 정규식이 `2.80%`의 소수점에서 끊어 "야간 EWY가 2." / "80% 밀리고…"로 문장이 반토막 났다 → 종결부호 뒤가 공백이거나 문장 끝일 때만 자르는 분리기를 새로 작성 ②`.bfx-alloc__track`이 inline span이라 포트폴리오 막대가 아예 안 보였다.
8. **후속 `6a7ed72`**: 실시간 지수 API(`/api/us-market-data?action=indices`)가 4개만 내려주면 정적 데이터에 있던 **다우지수 카드가 통째로 사라지고** '주요 지수 상승' 분모도 3→2로 줄던 문제. 실시간 배열로 통째 교체하던 것을 id 기준 병합으로 바꾸고, '실시간' 배지는 실제로 실시간 값이 들어온 카드에만 붙게 수정.
9. 검증: 라이트/다크/모바일/태블릿 4뷰 + **6월 아카이브 레코드**까지 렌더 확인(폴백 정상), 배포 후 실사이트 재확인.

**(2) 사이트 전체 감사 (유료 사용자 관점)**

전 페이지를 Playwright로 실제 렌더링해 훑었다. 가장 심각한 4가지:

1. **AI 종목분석 = 벽.** 비로그인 방문자에게 화면 전체를 덮는 모달만 띄워, 제품을 한 줄도 못 보고 이탈. 전환 퍼널의 가장 큰 구멍.
2. **홈에 하드코딩된 가짜 지표.** 모바일 히어로에만 있어서 데스크톱 감사에서는 안 보였다 — `분석 종목수 2,847개 / 오늘 분석 1,203건 / 평균 정확도 73.2% / 실시간 업데이트 30초`. 근거 없는 수치이고 **"평균 정확도"는 투자정보 서비스가 표기하면 안 되는 항목**이다.
3. **홈이 무료 시세 사이트와 구분 안 됨.** 첫 화면이 공허한 카피 + 시세표 3개. 매일 발행되는 브리핑·마감시황이라는 진짜 자산이 첫 화면에 하나도 없었다.
4. **요금제가 내부 사정을 고객에게 노출.** "포트원 테스트 결제 모듈", "결제 시스템 설정이 완료되지 않았습니다 — 관리자에게 문의해주세요".

그 외 채점: 마감시황/브리핑 상 · 증시 일정 하(중복 섹션 + 템플릿 문형) · 시세 3종 중하(원시 표만) · 매매시그널 중(기능은 강한데 비로그인 화면이 안내문 한 줄).

**(3) 홈 재설계** (커밋 `e961ef3`, 위계 교정 `44aeccd`)

`index.html`/`home.js`/`home.css`/`api/repo-data.js`. 2장 홈 항목에 상세. 특기할 것 두 가지:

- **인프라 우회**: 홈이 "오늘 마감시황 핵심 한 줄"을 쓰려면 3.3MB `daily-market.json`이 필요한데, `data/daily/<날짜>.json` 아카이브는 당일 분석이 비어 있고(3장 참고) **Vercel Hobby 함수 12개 한도가 이미 꽉 차 있어**(api 11개 + api/payment/complete.js 1개) 새 API를 만들 수 없었다. 기존 `api/repo-data.js`에 `pick=latest-day` 파라미터를 붙여 **서버에서 최신 하루치만 잘라 내려주는** 방식으로 해결 — 함수 개수 변화 없음.
- **히어로 위계 교정(`44aeccd`)**: 1차 결과물에 대해 사용자가 "글씨체랑 크기가 이상하다, 디자이너가 저렇게는 안 만든다"고 지적. 실제로 **130자짜리 서술형 문장을 21px 볼드 헤드라인으로 세워** 네 줄짜리 글덩어리가 됐고(본문을 헤드라인 크기로 키운 셈), 주인공이어야 할 지수 수치는 작은 카드로 밀려 있었다. 또 오른쪽 AI 패널이 `align-self:start`라 상단 절반만 채우고 아래가 텅 비어 좌우 균형이 무너져 있었다. → **히어로의 주인공을 숫자로**(코스피 42px 모노 + 등락 포인트, 나머지는 보조 칩), **문장은 본문으로 강등**(15px/1.85/regular + 라벨 + 세로 규칙선의 인용 블록), 그리드를 `1.12fr : 0.88fr` + `align-items:stretch`로 바꾸고 AI 패널을 좌측 높이에 맞춰 늘림(피드 항목 균등 배분 + 헤어라인 구분선). **교훈: 긴 서술 문장은 절대 헤드라인 자리에 두지 말 것. 데이터 페이지의 헤드라인은 숫자다.**
- 이 과정에서 히어로를 뜯다가 `hxRenderReports()`가 함께 지워져 리포트 3카드가 "불러오는 중"에서 멈추는 회귀가 발생 → 렌더 검증에서 발견·복구. **큰 블록을 통째로 치환할 때 그 범위에 다른 함수가 끼어 있는지 확인할 것.**

**(4) 비로그인 전환 퍼널 + 요금제 문구** (커밋 `4c2f2fd`)

`stock-analysis.html/.css/.js`, `trade-signal.html`, `pricing.html`. 2장 각 항목에 상세. AI 종목분석 랜딩은 처음에 "리포트 첫 화면 구성" 목업(막대·게이지)을 넣었는데 **렌더 결과가 오히려 깨진 레이아웃처럼 보여서 제거**했다 — 숫자를 지어낼 수 없는 상황에서 형태만 흉내 내는 목업은 역효과.

**(5) 모바일 대응** (커밋 `91c9c7b`, 마감시황 `615443c`)

사용자 제보: "아이폰 사파리에서 접속하면 스크롤 가운데 AI 종목분석 부분에서 확대돼서 시작한다", "모바일 홈/브리핑 스크롤이 너무 길다".

1. **iOS 자동 확대** — 원인은 입력창 글자 크기. iOS Safari는 **16px 미만 입력창에 포커스가 가면 페이지를 강제 확대**하고 그 배율·위치가 세션에 남아, 다시 들어오면 "검색창 자리에서 확대된 채로" 열린다(새 홈 검색창이 13px이었다). `theme.css`에 모바일 입력 요소 16px 고정 규칙 추가(`!important` 필요 — 각 페이지 CSS가 더 작은 값을 지정 중). 홈은 새 방문일 때만 `scrollTo(0,0)`, 뒤로가기는 브라우저 복원 존중. **Playwright/Chromium으로는 재현이 안 되는 Safari 전용 동작이라 실기기 확인이 필요하다(3·4장 반영).**
2. **모바일 분량 축소** — 홈 2,525 → 1,841px, 브리핑 14,728(17.5화면) → 3,526px(4.2화면), 마감시황 6,318 → 1,822px. 홈은 리포트 3장을 옆으로 넘기는 스냅 카드(87% 폭으로 다음 장이 살짝 보이게)로, 브리핑·마감시황은 **접이식**으로 전환. 접이식은 내용을 자르지 않고 제목만 남기며, 탭하면 그대로 펼쳐진다. **데스크톱은 완전 무변화**(검증: 접힘 0개, 전체 높이 브리핑 6,957px·마감시황 3,428px로 이전과 동일).
3. 마감시황은 덱 내부 `.dmx-block`과 페이지 레벨 `.dm-section` 두 종류의 컨테이너가 섞여 있어 둘 다 처리하고, 접힌 `.dm-section`에도 카드 테두리를 줘 접힌 항목들이 하나의 목록으로 읽히게 맞췄다.
4. 검증: 모바일에서 접힘 상태 확인 → 실제 탭해서 펼쳐지는 것까지(높이 증가로 확인) → 데스크톱 무영향 확인 → 배포 후 실사이트 3개 페이지 콘솔 에러 0·시작 스크롤 0 확인.

**커밋 목록**: `b22c17d`(브리핑 덱) → `6a7ed72`(브리핑 지수 병합) → `e961ef3`(홈 재설계 + repo-data pick) → `4c2f2fd`(전환 퍼널 + 요금제 문구) → `44aeccd`(히어로 위계·균형) → `91c9c7b`(iOS 확대 + 홈·브리핑 모바일) → `615443c`(마감시황 모바일 접이식). **7회 push 모두 Vercel 배포 성공**(배포한도 문제 없었음). API 라우트 추가 없음 → 함수 12개 제한 영향 없음.

### 2026-09-02 (밤) — 밀린 검증 9건 일괄 처리 + 근본원인 2건 발견·수정

사용자 요청: "미해결 부분 해결해야지" → 3장에 20개 넘게 쌓여 있던 항목 중 **"다음 세션에서 확인"으로 미뤄져 있던 검증 항목을 한 번에 처리**하는 쪽으로 방향을 잡았다(선택지 4개 중 사용자가 이 방향 선택).

**작업 전제 — bash 마운트가 7월 상태로 stale했다.** `D:\vibe` 마운트에서 `git log`를 찍으니 8/10, `kr-screener-cache.json`은 7/21자였다. 5장 경고가 또 실증된 셈이라 **GitHub에서 fresh clone(`git clone --depth 40`)을 떠서 그걸 기준으로 전부 확인**했다. 이번 세션의 모든 데이터 확인 수치는 fresh clone 기준이다.

**(1) 마감시황 구조화 필드가 이틀째 전혀 안 채워지고 있었다 — 이번 세션 최대 수확**

9/1·9/2 리포트를 열어보니 `marketTone`·`indexDetail`·`sectors`·`netBuyLeaders`가 전부 `null`, `featured_stocks[].risk`는 8종목 중 0건. 라이브 화면에서도 덱 블록이 5개(지수 마감/시장 온도/시장 흐름/투자자별 매매 동향/향후 전략)뿐이고 **업종별 등락·기관/외국인 순매수·장중 레인지 바가 통째로 안 그려지고 있었다.**

원인 추적: 9/1 세션은 규칙을 `scripts/daily-market-ai.mjs`에 넣었는데, `grep -rl analyzeDailyClosingReport`로 확인해보니 **그 함수는 export만 되고 저장소 어디서도 호출되지 않았다**(워크플로우·npm 스크립트에도 없음). 실제 리포트를 쓰는 주체는 `git log`상 16:23에 `mandoo411`로 커밋하는 Cowork 예약작업 `totalmoney-daily-closing-report`이고, 그 SKILL.md(`D:\Claude\Scheduled\...`)에는 새 필드 이야기가 한 줄도 없었다. **규칙을 실행되지 않는 파일에 써놓은 것**이 원인.

조치: SKILL.md는 세션에서 읽기 전용이라 파일 도구로는 못 고치고, `update_scheduled_task`로 프롬프트를 갱신했다. **STEP 3b**를 신설해 필드 5종의 정확한 형태(렌더러가 읽는 키 이름·자료형 그대로)와 "확인 안 되면 **키 자체를 생략**, 0이나 추정치 절대 금지" 원칙을 박고, STEP 2 리서치 항목에 장중 고저·업종 등락률·순매수 상위 검색을 추가, STEP 4 검증과 최종 보고에 "어떤 필드를 채웠고 어떤 필드는 왜 생략했는지"를 남기도록 연결했다. **9/3 발행분이 첫 실검증**(3장 최상단).

**(2) 과거 리포트 본문이 화면에서 통째로 사라지던 버그 — 수정·배포·라이브 검증 완료 (`8bd8221`)**

3장에 "5~6월 구형 레코드에서 폴백이 실제로 동작하는지 미확인"으로 남아 있던 항목을 실사이트에서 5/12·6/2·7/1로 확인한 결과, 화면이 깨지지는 않았지만 **7/1은 2,001자 본문이 한 글자도 안 나오고 있었다**(페이지 전체 텍스트가 1,170자).

원인은 폴백 판정 기준. `renderAiPanels()`는 "덱이 비었으면 줄글로 폴백"인데, `dmxRenderDeck()`은 파싱 실패와 무관하게 지수·수급 같은 **구조화 블록을 그리므로 항상 비어있지 않다.** 그래서 줄글 폴백 경로가 영영 실행되지 않았다. `dmxRenderProseFallback()`을 만들어 파싱 실패 시 원문을 덱 안에 "AI 종합분석" 블록으로 넣고(`.dmx-prose`, `white-space: pre-wrap`), 실사이트 `#2026-07-01`에서 2,001자 정상 렌더 확인. 영향 범위는 7/1 하루(analysis가 있는 53일 중 이모지 헤더가 없는 유일한 날).

**(3) 모바일 접이식이 스크롤 중 튕기듯 닫히던 버그 — 사용자 제보 → 수정·배포·검증 (`c10c3cf`)**

제보: "마감시황 특징주·주목할 변수 아래로 스크롤 시 튕기듯 자동으로 닫힘". **iOS 사파리는 스크롤로 주소창이 접혔다 펴질 때마다 `resize`를 쏜다**(폭 그대로, 높이만 변함). `daily-market.js`의 resize 핸들러가 그때마다 `renderAiPanels()`로 AI 패널을 다시 그렸고, 다시 그릴 때 접이식이 기본값으로 리셋됐다. 2겹 수정: ①레이아웃 모드는 폭으로만 갈리므로 **뷰포트 폭이 실제로 바뀐 경우에만** 재렌더 ②사용자가 직접 펼치거나 접은 섹션은 `dmxUserFold` Map에 기억. 라이브 검증: 특징주 펼침 → iOS형 resize 3연발에도 유지, 폭을 바꿔 강제 재렌더해도 특징주=펼침·내일변수=접힘 유지. 브리핑은 resize 리스너가 없어 같은 증상 없음.

**(4) 정상 확인만 하고 닫은 항목 (6건)**

| 항목 | 확인 내용 |
|---|---|
| 매매시그널 캐시 신규 필드 | 2,765종목 중 PER/PBR/EPS/외국인지분/거래회전율 전부, 외국인·기관 순매수 2,641건. 빌드 실패 0 |
| 거래정지·정리매매 필터 | 플래그 전 종목 채워짐(정리매매 알에프세미 1건), `api/analyze.js`에서 실제 제외 |
| 감자·액면병합 이상감지 | 107종목의 해당 구간 기간수익률만 null. 1년 상승률 상위가 실제 급등주로 정리됨 |
| 종목검색 정렬 | 실시간시세 "삼성전자"→005930 1위, 미국주식 "AAPL"→애플 1위, 드롭다운 스크롤 정상 |
| DART 이벤트 배치 | 9/2 08:18 정상 실행, 새 명사구 문구 반영 |
| Vercel 인프라 | 'vibe' 중복 프로젝트 재발 없음, 배포한도 정상(9/2 커밋 전부 즉시 배포), `data/briefing/` 9/2까지 매일 적재 |

**(5) 사용자 확인으로 종결** — 아이폰 사파리 자동 확대: 실기기에서 해결 확인.

**이번 세션이 남긴 교훈 3가지**
1. **리포트 생성 규칙을 바꿀 때는 예약작업 SKILL.md를 고쳐야 한다.** 저장소의 `scripts/*-ai.mjs`는 실행 경로가 아닐 수 있다 — 바꾸기 전에 호출자가 있는지부터 `grep`할 것.
2. **"폴백을 짰다"와 "폴백이 동작한다"는 다르다.** 이번 것도 폴백 코드는 정확히 의도대로 짜여 있었지만 폴백 조건이 영영 참이 되지 않았다. 폴백은 반드시 실제 데이터로 한 번은 밟아볼 것.
3. **모바일 버그는 "재렌더가 언제 도는가"를 먼저 의심할 것.** iOS의 주소창 resize는 스크롤할 때마다 온다.

**커밋**: `8bd8221`(구형 리포트 본문 유실) → `c10c3cf`(접이식 스크롤 튕김). 둘 다 Vercel 배포 후 실사이트에서 검증 완료. API 라우트 추가 없음 → 함수 12개 제한 영향 없음.

### 2026-09-03 — 사이트 감사 지적 3건 일괄 해결 + D:\vibe 로컬 재동기화

9/2 사이트 전체 감사에서 남았던 3건("증시 일정 페이지가 가장 값싸 보임" / "시세 3종+글로벌랭킹이 원시 표뿐" / "Premium에 실질 차별점 없음")을 한 세션에 처리했다. Premium은 상품 정의가 먼저 필요해 사용자에게 4축(매매시그널 알림 / AI분석 심화 / 관심종목 데일리 리포트 / 과거 데이터·백테스트)을 확인받고 진행했다.

**(1) 증시 일정 페이지 — 지적 4건 전부 근본원인까지 추적해 수정**

| 지적 | 근본원인 | 수정 |
|---|---|---|
| 오늘 일정 ↔ 다가오는 주요일정 중복 | `renderUpcoming(today)`가 항상 '오늘'부터 그려서 첫 그룹이 위 섹션과 100% 겹침 | 선택 날짜 **다음날**부터로 변경 + 그룹 6개 상한 + D-day 칩. 호출부가 제각각이던 것도 `renderSelectedDate()` 단일 진입점으로 통일 |
| 중요도 배지가 전부 "높음" | **데이터 소스가 high-impact만 수집**해 `row.impact`가 403건 전부 `"high"`. 그 값을 배지로 그대로 쓰고 있었음 | 지표 성격 기준 3단계 재설계(`SECONDARY_INDICATOR_RE` 신설). 라벨도 핵심·주목·참고로 변경. **실사이트 실측 핵심 11 / 주목 16 / 참고 11**. 중복이 된 좌측 '핵심' 배지 제거 |
| 일정 분석 4건이 전부 같은 문형 | **프롬프트가 `"예상 상회 시 [영향]. 예상 하회 시 [영향]."`를 스키마에 직접 못 박아둠** | `why`/`bull`/`bear`/`watch`로 분해, 상회·하회는 화면 라벨이 담당. 작성 규칙 6개 추가. 구버전 `analysis` 레코드는 원문 폴백 |
| 캘린더 아래 거대한 빈 공간 | — | '다음 핵심 지표'(D-day·시각·이전/예측)와 '이번 달 일정'(4셀 집계) 카드 신설, 둘 다 실데이터 |

**(2) 시세 4종 공통 요약 스트립 신설** — `assets/market-strip.js` + `assets/market-strip.css`. 컴포넌트 1개로 실시간시세·미국주식·암호화폐·글로벌랭킹을 동시에 개선. 실사이트 실측값:

| 페이지 | 실측 |
|---|---|
| 실시간시세 | 코스피 6,562.72 −3.99% / 코스닥 803.98 −2.10% · 시총 상위 100 중 상승 6·하락 94 · 한진칼 +7.16% / 한화솔루션 −8.46% |
| 미국주식 | 시총 상위 50 중 상승 29·하락 21 · 메타 +3.14% / 팔란티어 −3.14% |
| 암호화폐 | TOP400 24H 상승 124·보합 4·하락 272 · BTC −1.49% |
| 글로벌랭킹 | TOP100 상승 32·하락 66 · 노바티스 +6.04% / CrowdStrike −6.90% · 국내 기업 2개 진입 |

설계상 지킨 것: **새 API 안 만듦**(함수 12개 한도), **표본을 라벨로 명시**(상승률 탭으로 브레드스를 세면 "100종목 전부 상승"이라 거짓말이 된다), **실데이터 없으면 블록 통째로 감춤**.

**(3) Premium 상품 정의** — 코드를 열어보니 Pro와 Premium이 `hasProAccess` 하나로만 갈려 **권한이 완전히 동일**했다(감시 슬롯도 등급 무관 20개). `hasPremiumAccess`를 신설하고, 지금 코드로 바로 되는 **감시 슬롯 Pro 5 / Premium 30**을 서버·화면 양쪽에 반영. 나머지 3축은 `{ soon: true }`로 "준비 중" 배지를 붙여 **없는 기능을 파는 문구를 만들지 않았다**.

**(4) 검증 방법** — 샌드박스에 Playwright(chromium)를 설치해 정적 서버 + `/api/repo-data` 스텁으로 오프라인 렌더 검증(데스크톱/모바일 × 라이트/다크)을 먼저 돌리고, 배포 후 Claude-in-Chrome으로 실사이트 4개 페이지를 실측했다. 오프라인 단계에서 "다크모드인데 스트립만 흰 카드"로 보인 건 **테스트 아티팩트**였다 — `theme.js`의 `applyTheme()`이 `documentElement`와 `body` 양쪽에 `data-theme`을 찍는데, 테스트가 `documentElement`만 직접 바꿔 `body`에 라이트 값이 남았던 것. `localStorage`의 `theme` 키를 미리 세팅하는 방식으로 바꾸니 정상(`#111d35`). **다음에 오프라인 테마 테스트를 할 때는 `localStorage.setItem("theme", ...)`로 세팅할 것.**

**(5) D:\vibe 로컬 재동기화** — 세션 초반에 로컬과 GitHub의 `weekly-market.html`이 100줄 이상 어긋난 걸 발견해 조사한 결과, 로컬이 **`ahead 5, behind 1514`**에 미커밋 변경 110개 상태로 8/10 이후 방치돼 있었다. 마운트가 stale한 게 아니라 **로컬 저장소 자체가 뒤처진 것**이었다(bash↔Read 툴 양방향 프로브로 마운트 정상 동작 확인). 백업 브랜치 `backup/local-pre-sync-2026-09-03`(커밋 `af0a5092`)에 잔재를 보존하고 `git reset --hard origin/main`으로 동기화, 죽은 worktree 2개 prune, 로컬 전용 옛 서버리스 함수 2개를 `archive/`로 이동. 상세와 앞으로의 규칙은 5장에 반영.

**이번 세션이 남긴 교훈 3가지**
1. **"AI가 이상한 글을 쓴다"는 지적은 프롬프트 본문부터 볼 것.** 일정 분석의 템플릿 문형은 모델 문제가 아니라 프롬프트가 그 문형을 스키마 예시로 직접 지시하고 있었던 것이다. 9/1 마감시황 때의 "상충 지시가 남아 있었다"와 같은 계열의 실수다.
2. **데이터 소스가 이미 필터링해서 주는 값을 그대로 UI 신호로 쓰지 말 것.** 중요도 배지가 전부 "높음"이었던 건 코드 버그가 아니라 "high-impact만 수집한 피드의 impact 필드를 그대로 배지로 옮긴" 설계 문제였다. **필드 값의 분포를 한 번 세어보면 5초 만에 드러난다.**
3. **로컬 작업 폴더도 주기적으로 동기화해야 한다.** fresh clone에서만 작업하는 습관이 프로덕션은 지켜줬지만 로컬은 몇 달치 방치됐고, 그 로컬을 기준으로 코드를 읽으면 틀린 판단을 하게 된다.

**커밋**: `c5f3090`(감사 3건 본체) → `59b5e57`(스트립 특징 칩 폴백). 둘 다 Vercel 배포 완료 후 실사이트 검증. **API 라우트 추가 없음 → 함수 12개 제한 영향 없음**(`assets/*.js`는 정적 파일이라 함수로 안 잡힘).

---

## 7. 프로젝트 관리 체계 (2026-08-09 신설)

지금까지 인수인계 md 파일이 세션마다 따로 쌓이면서 정보 유실·중복·불일치가 반복된 것이 이번 통합 작업의 출발점이었습니다. 앞으로는 아래 구조로 관리합니다.

- **단일 진실 소스(SSOT)**: `D:\vibe\TOTALMONEY_STATUS.md` 하나만 "지금 상태"를 대표합니다. 새 채팅에서는 이 파일 하나만 있으면 전체 맥락 파악이 가능해야 합니다.
- **원본 로그 보관**: 과거 세션별 상세 기록은 `D:\vibe\archive`에 그대로 남아있습니다. 특정 버그의 상세 조사 과정 등 디테일이 필요할 때만 참조합니다.
- **가이드/설정 문서는 `D:\vibe\docs`에 유지**: README(단, 현재 내용은 무관한 계산기 프로젝트 문서라 확인 필요), CRON_SETUP, 회원가입·결제 셋업 가이드, 홍보 기초설계 등 — 세션 로그가 아니라 "어떻게 설정하는가"를 설명하는 문서들
- **업데이트 규칙**: 앞으로 매 채팅 세션 종료 시 "TOTALMONEY_STATUS.md 업데이트해줘"라고 요청하면, 해당 세션에서 한 작업을 2장(기능현황)·3장(미해결)·4장(할 일)에 반영하고 6장에 그날 날짜로 변경 이력을 짧게 남기는 방식으로 갱신합니다.

### 2026-08-09 — Cowork 예약작업 5개 점검 결과

프로젝트(TotalMoneyAI) 생성을 계기로 예약작업(`mcp__scheduled-tasks__list_scheduled_tasks`) 5개의 SKILL.md를 전수 점검했다. 이 예약작업들은 프로젝트의 컨텍스트/지침을 자동으로 물려받지 않고 매번 완전히 새로운 세션으로 독립 실행되므로, 자체적으로 안전해야 한다.

- **`totalmoney-morning-briefing` — 보안 취약점 발견 및 수정**: SKILL.md 파일 안에 GitHub PAT 전체 값이 두 군데(CREDENTIALS 섹션, git clone 명령어)에 평문으로 하드코딩되어 있었다. `D:\vibe\.cowork-github-pat-token.txt`에서 읽어와 셸 변수로만 다루도록 수정하고 파일에서 평문 토큰을 완전히 제거했다. **이 프롬프트 파일의 과거 버전을 어딘가에 복사해둔 적이 있다면 그 PAT도 노출된 것이니, GitHub PAT 재발급(로테이션)을 특히 더 우선순위 높게 고려할 것.**
- **`totalmoney-daily-closing-report` — 잠재 버그 발견 및 수정**: 프롬프트 안에 `/sessions/cool-eager-edison/mnt/...` 형태의 샌드박스 세션 경로가 하드코딩되어 있었다. 이 세션 ID는 실행할 때마다 매번 무작위로 달라지므로, 다음 실행에서 그 경로가 존재하지 않아 실패할 위험이 있었다. 매 실행마다 실제 세션 경로를 동적으로 찾도록 수정했다.
- **`totalmoney-earnings-update`, `kr-earnings-manual-json-healthcheck`, `totalmoney-ai-feed-seed`**: 점검 결과 특별한 문제 없음, 수정하지 않음. `totalmoney-ai-feed-seed`는 `INTERNAL_SEED_KEY` 값이 프롬프트에 평문으로 남아있으나, 이 키는 GitHub 쓰기 권한이 없는 저위험 API 키라 이번엔 그대로 둠(원하면 다음에 파일 참조 방식으로 바꿀 수 있음).
- 5개 모두 프로젝트(TotalMoneyAI)의 지침을 자동으로 받지 않는다는 점을 확인했고, "7. 프로젝트 관리 체계"에도 이 사실을 남겨둔다: **예약작업의 동작을 바꾸려면 항상 각 작업의 SKILL.md를 직접 수정해야 하며, 프로젝트 커스텀 지침을 바꾸는 것만으로는 예약작업에 반영되지 않는다.**

### 참고 — "Claude 프로젝트(앱 상단 폴더형 워크스페이스)"에 대해

이 폴더(D:\vibe)와는 별개로, Claude 앱 자체에 채팅들을 묶어서 관리하는 "프로젝트" 기능(커스텀 지침 + 지식 파일을 모든 채팅에 자동으로 공유)이 있습니다. 이건 앱 UI에서 직접 만드는 기능이라 제가 도구로 대신 생성해드릴 수는 없지만, 만드실 경우 이 `TOTALMONEY_STATUS.md`를 프로젝트 지식(Knowledge) 파일로 등록해두면 매 채팅마다 파일을 다시 첨부할 필요 없이 자동으로 맥락이 공유됩니다. 원하시면 프로젝트 커스텀 지침에 바로 붙여넣으실 수 있는 문구를 만들어드리겠습니다.
